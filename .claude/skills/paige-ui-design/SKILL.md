---
name: paige-ui-design
description: Mandatory router for every visible Paige interface change.
---

# Paige UI Design router

Before any visible-interface design or implementation, read `../../../.agents/skills/paige-ui-design/SKILL.md` completely and then read every file it routes.

The `.agents` entry is the canonical project-owned skill. This wrapper contains no alternative rules and must not be used as a summary or substitute.
