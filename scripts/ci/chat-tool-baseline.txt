# The tools declared INLINE in supabase/functions/paige-ai-chat/index.ts on 2026-09-01,
# the day the registry rule was adopted. This list may SHRINK as tools migrate to the
# registry. It must never GROW: a new tool is registered by its domain, not hand-wired
# into the handler. See docs/doctrine/one-approval-gate.md and scripts/ci/chat-tool-registry-lint.mjs.
#
# GROWN ONCE, 2026-09-02, and the reason is recorded here rather than in a commit message
# nobody will read again. The five `team_*` entries below are the Solo Team capability, and
# they are declared inline because the Spine registry the ruling points at does not exist
# yet — the adapter has to live somewhere, and the ruling's own words put the final bounded
# adapter with the Chat workstream. This is that workstream doing that job, not a domain
# team routing around the rule.
#
# It is written down because "the guard fired and I raised the number" is exactly how a
# ratchet stops being one. When the registry lands, these five migrate to it and this list
# shrinks by five — which is the direction it is allowed to move.
#
action_advance
action_file
action_get
action_list
ask_choices
author_event_kind
automation_draft
automation_list
automation_set_grant
automation_set_state
automation_triggers_list
calendar_book_meeting
comms_buy_number
comms_connection_summary
comms_draft_registration
comms_list_numbers
comms_name_number
comms_registration_status
comms_search_numbers
comms_set_primary_number
content_save
crm_add_note
crm_assign_coach
crm_assign_contact
crm_create_contact
crm_create_task
crm_file_document
crm_get_contact_summary
crm_list_deals
crm_list_documents
crm_list_tasks
crm_list_team
crm_log_activity
crm_pipeline_summary
crm_search_contacts
crm_update_contact
crm_update_pipeline_stage
deal_create
deal_move_stage
deep_research
delegate_to_subagent
document_generate
document_pending_reviews
document_resume_review
draft_marketing_content
forge_subagent
generate_image
get_client_rail
get_current_rates
growth_funnel_build
growth_funnel_generate
growth_funnel_publish
growth_list
growth_page_generate
growth_page_publish
growth_page_save
# GROWN A SECOND TIME, 2026-09-23, and again the reason is recorded here rather than in a commit
# message nobody will read again. These five shipped into the handler without this list being
# updated in the same PR, so the guard has been RED ON MAIN ever since — which is how a ratchet
# stops being read at all. They are recorded, not migrated, and the difference matters:
#
#   integrations_list   READ  → supabaseClient.rpc("list_integration_surface")   (measured)
#   inbox_list          READ  → supabaseClient.rpc("list_inbox_messages", {...}) (measured)
#   improvement_list    READ  → .from("paige_improvement_proposals").select      (measured)
#   improvement_propose WRITE → .from("paige_improvement_proposals").insert      (measured)
#   improvement_decide  WRITE → .from("paige_improvement_proposals").update      (measured)
#
# The first two are RPC-backed and ARE already registered in the Spine
# (`integrations.list`, `comms.messages_read`), both at `chatBinding: "PARTIAL"`. They are the two
# that can actually migrate: move them into their domain `*_TOOLS` spread and flip the binding to
# LIVE. That is a Chat/Spine-lane change with a runtime assertion attached, not a trunk-health
# edit, so it is left to that lane rather than asserted here on inference (§13).
#
# The three `improvement_*` cannot migrate yet and this list is NOT pretending otherwise. The Spine
# validator requires `action.executor` to match /^public\.[a-z][a-z0-9_]*$/, and no such RPC
# exists: `20270114000000_improvement_proposals.sql` creates the table and its only function is
# `evaluator_invocation_health`. So they reach the table straight from the handler.
#
# What that IS and IS NOT, measured rather than assumed, because an overstated note here would send
# the next reader hunting a security hole that is not there. They are NOT unauthorized: the handler
# builds `supabaseClient` from SUPABASE_ANON_KEY plus the caller's forwarded JWT (index.ts:703-705),
# so RLS applies as the caller, and the table enables RLS with `pip_owner_read`/`pip_owner_write`
# (tenant_id = current_user_tenant_id() AND admin/super_admin/coach) and `pip_owner_update`
# (tenant + admin/super_admin), with a further inline admin-only gate on `improvement_decide`.
# What they LACK is a Spine capability declaration, the Chat adapter seam, and a canonical RPC —
# architecture debt (§10/§14), not an authorization hole. Recording them here is a sequencing
# decision, and it governs nothing on its own.
#
# The direction this list is allowed to move is still DOWN. It shrinks by two when the reads
# migrate, and by three more when the improvement RPCs are authored.
improvement_decide
improvement_list
improvement_propose
inbox_list
integrations_list
list_event_kinds
list_subagents
marketplace_browse
member_grant_role
member_revoke_role
pipeline_archive_preview
pipeline_catalogue
pipeline_configure
pipeline_folder_archive_preview
plan_add_milestone
plan_assign_task
plan_create
plan_list
plan_remove_item
plan_set_reminder
plan_update_item
presence_is_online
presence_who_online
program_enroll
program_list
propose_action
propose_business_brief_update
save_to_knowledge_base
search_funding_marketplace
search_regional_lenders
search_sba_lenders
team_invite_member
team_invite_resend
team_invite_revoke
team_set_permission
team_set_work_profile
update_business_profile
update_client_data
web_fetch
web_search
zapier_list_actions
zapier_run_action
