/**
 * Compatibility export for callers that still use the historical Solo route name.
 * The shell-owned SoloPaigeWorkspace is the sole PAIGE workspace implementation.
 */
export { SoloPaigeWorkspace as PaigeHub } from "./SoloPaigeWorkspace";
