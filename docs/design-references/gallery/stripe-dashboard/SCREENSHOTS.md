# Stripe — screenshots to capture (Chrome MCP, interactive session only)

**Not yet captured.** The rendered-screenshot loop needs the Chrome MCP
(`mcp__claude-in-chrome__*`), unavailable headless; the real Stripe Dashboard is
auth-gated. Capture ONLY the public marketing/docs pages below — never a logged-in
dashboard with real account data. `README.md` is the durable deliverable.

Public URLs to capture:
- https://stripe.com — homepage (signature animated gradient hero, tight display type)
- https://stripe.com/payments — product page, gradient + code-panel craft
- https://stripe.com/billing — closest public analog to Paige's billing surfaces
- https://stripe.com/docs — docs layout, tables, mono/code treatment
- https://docs.stripe.com/api — the API reference three-column density + mono
- https://stripe.com/customers — restrained card/testimonial layout

Capture at 1440px and 390px. Do NOT log into the dashboard or screenshot any account,
customer, or payment data. The homepage + /payments are the highest-value for the
gradient-motion craft; /billing + /docs/api for the data-density patterns.
