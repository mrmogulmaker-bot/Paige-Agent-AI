# Attio — screenshots to capture (Chrome MCP, interactive session only)

**Not yet captured.** The rendered-screenshot loop needs the Chrome MCP
(`mcp__claude-in-chrome__*`), unavailable headless; the real Attio workspace is
auth-gated. Capture ONLY the public marketing pages below — never a logged-in workspace
with real contact data. `README.md` is the durable deliverable, and Attio is our most
directly relevant CRM reference (§7).

Public URLs to capture:
- https://attio.com — homepage (product screenshots of the record table + panel)
- https://attio.com/product — the object/record model shown in marketing renders
- https://attio.com/crm — CRM positioning + list/table/kanban view imagery
- https://attio.com/reports — data/metric surface treatment
- https://attio.com/customers — layout restraint

Capture at 1440px and 390px. Do NOT log into Attio or screenshot any real workspace.
Attio's marketing pages embed high-fidelity product screenshots — those renders are the
highest-value capture for the record-table + right-panel patterns.
