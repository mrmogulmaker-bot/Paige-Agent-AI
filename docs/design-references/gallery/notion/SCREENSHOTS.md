# Notion — screenshots to capture (Chrome MCP, interactive session only)

These are **not yet captured**. The rendered-screenshot loop requires the Chrome MCP
(`mcp__claude-in-chrome__*`), which is unavailable in a headless env, and Notion's actual
workspace is auth-gated. Capture ONLY the public marketing/product pages below — never
anything behind a login. The annotations in `README.md` are the durable deliverable;
screenshots are a supplement for the pixel-level pass.

Public URLs to capture:
- https://www.notion.so — homepage (softened ink, one warm accent, restrained heading scale)
- https://www.notion.so/product — product surface renders (block canvas, centered column)
- https://www.notion.so/product/docs — reading-column measure + prose type treatment
- https://www.notion.so/product/wikis — nested/collapsible structure pattern
- https://www.notion.so/templates — card-grid restraint over a monochrome ground

Capture at 1440px wide (desktop) and 390px wide (mobile) where layout shifts.
Do NOT log into notion.so or capture any authenticated workspace view.
