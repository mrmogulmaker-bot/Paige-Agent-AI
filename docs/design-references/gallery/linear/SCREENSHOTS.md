# Linear — screenshots to capture (Chrome MCP, interactive session only)

These are **not yet captured**. The rendered-screenshot loop requires the Chrome MCP
(`mcp__claude-in-chrome__*`), which is unavailable in a headless env, and Linear's actual
app is auth-gated. Capture ONLY the public marketing/product pages below — never anything
behind a login. The annotations in `README.md` are the durable deliverable; screenshots
are a supplement for the pixel-level pass.

Public URLs to capture:
- https://linear.app — homepage (display type, negative tracking, dark ground, one accent)
- https://linear.app/features — product surface renders (list rows, density, hairlines)
- https://linear.app/method — long-form typographic reading treatment
- https://linear.app/customers — card/testimonial layout restraint
- https://linear.app/changelog — dense chronological list pattern

Capture at 1440px wide (desktop) and 390px wide (mobile) where layout shifts.
Do NOT log into linear.app or capture any authenticated workspace view.
