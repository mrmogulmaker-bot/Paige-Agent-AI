# Vercel — screenshots to capture (Chrome MCP, interactive session only)

**Not yet captured.** The rendered-screenshot loop needs the Chrome MCP
(`mcp__claude-in-chrome__*`), unavailable headless; the real Vercel dashboard is
auth-gated. Capture ONLY the public marketing/docs pages below — never a logged-in
dashboard. `README.md` is the durable deliverable.

Public URLs to capture:
- https://vercel.com — homepage (Geist type, neutral scale, black/white primary)
- https://vercel.com/geist/introduction — the Geist design-system reference itself
- https://vercel.com/geist/colors — the numbered gray + semantic status scales
- https://vercel.com/geist/text — the type scale and weights
- https://vercel.com/templates — card-grid density + real preview thumbnails
- https://vercel.com/docs — docs layout, tables, code/mono treatment

Capture at 1440px and 390px. Do NOT log in or screenshot any real project dashboard.
The Geist pages are the highest-value capture — they publish the exact tokens.
