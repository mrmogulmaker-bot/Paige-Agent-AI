# DONE — completed task archive

Completed tasks are archived here and removed from the live task list so the working
list stays lean (the live list reloads into context every turn — keeping ~120 finished
items in it is pure overhead). This is the record; the live list is the runway.

Archived 2026-07-18.

| # | Completed task |
|---|---|
| — | **§2 CONFIRMATORY SWEEP (2026-07-28, PR #258 → `58a9462`).** GATE-#92 pattern applied to the §2 domain: a read-only crew audited **four platform-default surface classes** (intake/forms · templates · seeds+operator/God · shared copy) for both §2 prongs (finance-as-a-default AND MMA/mogul leak + coaching-narrowing). **6 surfaces fixed, one PR:** (1) AddInternalClientDialog — stripped Funding Goal + Monthly Revenue from the default New Client Record intake; (2) ClientManagementDashboard — stripped the Funding Goal column (Revenue kept); (3) EditContactDialog — stripped the Funding goal field (**NEW hit**, edit-side twin, grid-cols-3→2 §27); (4) paige-mcp PAIGE_TAGLINE reworded off "credit, capital" → "operating system for client-based businesses…" + get_contact desc drops "funding goal" (**closes #171**); (5) approval-notification email — default preview off a credit-repair "dispute letter/Equifax" scenario (+ owner PII) → generic proposal; (6) coaching-reminder email — "Your coach"/"Strategy Session" coaching-narrowing → inclusive session copy. **Audit result (§13 honest):** Class C (seeds/operator) came back CLEAN (funding-seed quarantine no-op, is_finance default-guard DB trigger, subaccount funding-slug drop, org "Finance" dept = internal accounting); funding stays correctly opt-in-gated everywhere. **Honest catches:** re-ran the Class-B template audit after a garbage stub (§13 never rubber-stamp); investigated paige-mcp ingest_credit_scores/ingest_banking_snapshot — hidden from every tenant by TOOL_SCOPE omission (NOT a current violation) but the gate is *incidental* → filed **#496** to make it explicit. DB columns (clients.funding_goal/monthly_revenue, contacts.funding_goal) stay nullable — §2 targets finance-as-DEFAULT, not opt-in surfaces (§37 no consumer breaks). Crew (§1/§5): 4-class audit → integrator → adversarial verifier (ITERATE 0-blockers) + compliance officer (SHIP). Gates: verify ✅ · audit ✅ · Supabase Preview ❌ = #275 non-blocker. Local: tsc 22/22 · regression clean · eslint 0 errors · 44 tests · build ✓. **Deferred (owner call, filed):** AffiliateApplyForm RESPA/broker agreement (operator-level legal copy, needs counsel). **§32 owed:** live re-check /admin/clients intake + Internal Clients tab + Contacts edit dialog (browser-capable session); edge redeploy (paige-mcp + 2 email templates) confirm via /edge-drift post-pipeline. |
| 481 | **§9/§2 fix — tenant-scope ClientManagementDashboard people reads via get_tenant_people RPC (PR #257 → `1afa413`, merged 2026-07-28).** The last AT-RISK count surface GATE #92 flagged. `/admin/clients` (`ClientManagementDashboard`, a **LIVE** tenant coach/admin route — reachability diagnosed §30: it mounts at `Admin.tsx:442`, NOT only the dead `/dashboard` path, so **delete was off** and the scout's "dead code" claim was wrong) read **all** `public.profiles`/`user_roles`/`businesses` **platform-globally** to feed its "Clients"/"Team Members" cards + tabs — those tables have no `tenant_id`, so a client-side `.eq()` can't scope them → a tenant admin saw **cross-tenant people** (§9); the read also pulled `estimated_fico_*` + `has_broker_access` (**finance PII**) onto a coaching-generic surface (§2). **Fix:** NET-NEW `public.get_tenant_people()` `SECURITY DEFINER` RPC — tenant **server-derived** via `current_user_tenant_id()` (never a param, no IDOR), joined through `tenant_members` (`status='active'`), **`has_role(admin)` least-privilege** gate inside the body (mirrors the UI isAdmin gate — a non-admin calling directly gets empty), null-tenant (operator/no-workspace) ⇒ empty set, `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO authenticated`, `search_path=public` pinned, `STABLE`; **no FICO/credit fields returned**. Frontend: replaced the platform-global `Promise.all` with a single `supabase.rpc("get_tenant_people")` (`as any` house pattern, #234 stale types); removed the FICO triplet + broker toggle/menu/`Briefcase` import and **every** consumer atomically (interface, mobile+desktop render cells, desktop column) so the build stays green. The already-correct tenant-scoped `clients` read + null short-circuit left untouched. **Crew (§1/§5):** design engineer → §9 adversarial verifier + compliance officer, both **ITERATE zero-blockers**; folded the least-privilege gate + seam-coupling code note (`current_user_tenant_id()` vs `useTenantContext().activeTenantId` resolve to the same value) + roles-ordering §13 note. **§37 consumer inventory** verifier-confirmed complete. Gates: verify ✅ · audit ✅ · lint ✅ · Supabase Preview ❌ = known #275 base-replay non-blocker. Local: tsc-ratchet 22/22 (no new) · regression clean · eslint 0 errors · 44 tests · build ✓. **§32 PERSISTED-ON-PROD confirmed (ref xygzykjyynhzqytbqnzu):** schema_migrations advanced to 20260728120000; `get_tenant_people()` exists — `prosecdef=true`, `search_path=public` pinned, anon EXECUTE=false, authenticated EXECUTE=true, return signature carries NO FICO/broker fields (§2-clean); `db-live` at HEAD, zero migration drift. **§13 honest product flag (owner-logged #493):** `tenant_members` is a STAFF roster (client invites create a `clients` row + client app-role with NO membership), so the auth "Clients" card/tab now render near-empty — real clients live in the already-correct Internal Clients tab — the correct consequence of closing the leak, not a bug; optional follow-up to UNION `clients.linked_user_id` accounts. Follow-ups filed: #492 (§2 "Funding Goal" column on Internal Clients tab → gate behind funding opt-in), #493 (population-narrowing product decision), #494 (§10 service-role `get_tenant_people(tenant)` for Paige headless), #495 (§11 primitive-layer migration). **§32 owed (browser-capable session):** live `/admin/clients` render (Team tab populates, cards count, FICO/broker gone). |
| 482 | **Conversations redesign Phase-2 — relationship rail + quick actions + Agents' log (PR #256 → `c1527d7`, merged 2026-07-28).** Phase-2 of the #482 Conversations surface (Finding 3, GHL side-by-side). **Search-first (§18): almost everything EXTENDS what already existed** — the contact rail, the quick-action controls, and the sub-nav were all already present; only the Agents' log is net-new. **Contact rail (`ContactCardRail`):** enriched identity (title·entity secondary line; lifecycle stage as quiet text NOT a second pill, so status-vs-stage reads without pill-soup §25) + a new "Key facts" block (last-contacted / source / tags in the label chip family, each row only when present §15). Facts come from the SAME `clients` row the thread join already loads — `THREAD_COLS` widened with `lifecycle_stage/entity_type/title/source/tags/last_contacted_at/assigned_coach_user_id` (all verified on `public.clients`; no extra query). **Agents' log (NEW `AgentsLog.tsx`) — the real-attribution surface:** reads `paige_actions.eq(contact_id)` (Paige's action-bus registry) for what her team ACTUALLY did on this contact — §13 honest, never fabricated rows, honest empty state when Paige hasn't acted yet; **§9 tenant-isolated by RLS** (`paige_actions` pins an authenticated coach/admin to `tenant_id = current_user_tenant_id()`, so the read needs NO client-supplied tenant; `contact_id` is a global-unique clients PK → no cross-tenant collision); attribution reads as a teammate not a slug (raw `subagent-…` prefix-stripped + title-cased → "Email Composer" §11/§36); skeleton rows while loading not a bare blank (§11); timestamps at full `--muted-foreground`, only separator dimmed (AA §23). **§13 note:** `paige_llm_trace` exists (shipped #489) but is tenant/agent-scoped with NO `contact_id`, so it structurally cannot power a per-contact log — `paige_actions` is the correct source (folding trace-level attribution in later is a tracked follow-up). **Quick-action toolbar (thread-detail header):** promoted the SAME wired `SnoozeMenu` + `LabelPopover` from the list rows up to the open-thread header (label/snooze/archive without hovering a row) + a Set-reminder reusing `QuickAddDialog` (`plan_set_reminder` carries `p_contact_id` — no new backend); all icon-only ghost buttons matching the header group, gold stays on Send/Approve (§11); `userId` server-derived (`auth.getUser`). **Sub-nav:** added only an honest "Unread" derived view (`unread_count>0`, non-archived, not future-snoozed); "Assigned to me" NOT shipped — `threads` has no assignee column, a fake chip would be dishonest (tracked follow-up to add the producer). **Crew (§1/§5/§11/§25):** design-engineer → adversarial verifier + design critic + compliance officer, returned **ITERATE ×3 with ZERO blockers**; all six should-fixes folded (incl. the §13 catch that a scout falsely claimed "no paige_llm_trace table" — corrected the AgentsLog/inbox-shared comments + PR body to the real reason: no contact_id). Gates: verify ✅ · audit ✅ · Supabase Preview skipped (frontend-only PR) · Vercel Ready. Local: `ci:tsc` ratchet **22/22 (no new)** · `ci:regression` clean · **44 tests** · build ✓ (pre-existing `BusinessCreditDashboard` gold baseline on main, untouched). **§32 owed:** live render of the rail (light+dark, 300px scroll, AA muted timestamps) is a browser-driven check owed to a capable session — this session is headless (no Chrome MCP). Follow-ups logged: "Assigned to me" needs a `threads.assigned_to_user_id` producer (no fake chip); coach-name line needs `clients.assigned_coach_user_id → profiles` FK (render nothing on null); fold `paige_llm_trace` trace-level attribution in later; #234 (stale `types.ts`) is why `AgentsLog` uses the file family's `supabase as any` house pattern. |
| 469 | **Comms Slice C-1 — unified inbox substrate + email first channel (PR #240 → `6bc16ae`, merged 2026-07-26).** The channel-agnostic two-way inbox foundation that SMS/WhatsApp/IG/FB/Voice (C-2..C-5) plug into; EMAIL is the first channel proof. **Shipped:** NEW `public.messages` (the `NormalizedMessage` row — jsonb `sender`/`recipients`/`attachments`/`meta` so every channel maps to one shape) + `public.channel_connectors` (per-tenant inbound routing + §38 sender identity), both with **server-derived `tenant_id` BEFORE-INSERT triggers** (§9 — connector→contact→session, body never trusted); `_shared/channel-adapters.ts` (the ONE channel abstraction, §18 — C-1 registers email, C-2+ add adapters, no new dispatcher); **send-message extended** (email now routes THROUGH the adapter registry, writes/patches the tenant-isolated messages row to sent+provider_message_id, idempotent, additive-only §37 — existing ApprovalDetail/ContactCommsPanel/execute-approval callers unaffected); **handle-inbound-email sink-swapped** (Svix HMAC kept; tenant derived from recipient→channel_connectors §9; files `comms-draft-reply` paige_action §8; legacy paige_conversations dual-write kept GUARDED for the n8n CS-Triage consumer §37 → retires via #470); **realtime Conversations inbox** (3-pane, jsonb reads, §36 one-click Approve, gold only on the act). Action-bus: `comms-draft-reply` kind registered (send_via_approval, confirm lane, email-composer draft agent, client_experience→owner_ops) + AFTER-UPDATE trigger materializes the outbound draft row when the action reaches 'drafted'. **Crew (§1/§5/§11/§25):** build (3 parallel engineers) → verify (adversarial + compliance + design-critic). Verifier + compliance **BLOCKED on a real §9 cross-tenant send IDOR** (widened send path trusted a body-referenced row's tenant without binding to the caller) → **fixed** (caller-tenant gate hoisted ahead of the send, platform-owner exempt §17) + §38 sender-precedence reconcile (draft==sent) + §5 double-submit guard; design-critic **SHIP** (gold-on-draft-card ruled an acceptable §11/§36 judgment, StatePill precedent). **§32 dual-layer verified on prod (ref xygzykjyynhzqytbqnzu):** _Layer A (persisted)_ — schema_migrations advanced to 20260726190000; messages+channel_connectors exist; 11 RLS policies; 3 msg triggers; realtime enabled; comms-draft-reply kind registered (tenant_id null, §2-clean); send-message deployed bundle byte-matches repo (§9 gate + §5 guard + adapter registry present); `edge-live` at HEAD, zero drift. _Layer B (runtime state machine, deployed schema, all in a self-rolling-back DO block)_ — inbound tenant auto-derived from connector ✓, comms-draft-reply action→drafted→trigger materialized outbound draft with jsonb sender(identity-primary §38)/recipient ✓, draft→sent+provider_message_id ✓, idempotent (outbound_rows=1) ✓; RAISE-rollback → zero test rows persisted. Gates: verify ✅ (caught the real tsc-ratchet failure — new messages/channel_connectors not in generated types → queried via untyped handle, house pattern) · audit ✅ · lint ✅ · Vercel Ready (Supabase Preview ❌ = pre-existing #275 profiles-dup, not caused by C-1, prod unaffected). **Honest scope (§13):** ships INERT — no Resend inbound domain / connector wired yet, so inbox is empty + composer disables rather than faking a send; SMS/etc. NOT wired (C-2..C-5). Follow-ups: #470 (backend non-blocking — dual-write retirement citing n8n CS-Triage, inbound re-file recovery, thread-key parity, registry-dup, NULL-tenant guard), #471 (design nits + owner light-mode gold-wash perceptibility check). |
| 447 | **HOTFIX 2 — §2 funding-toggle bug: uninstall clears an orphan skill gate + §13 UI honesty (PR #219 → `1e07143`, merged 2026-07-25).** Toggling a skill OFF in the Marketplace did nothing when it was enabled via a Playbook/preset/direct set (no `marketplace_installs` row): `uninstall_marketplace_item` returned `{was_installed:false}` and never touched `tenants.features->'enabled_skills'`, yet the UI toasted success (§13 lie) — on reload the skill was still on. **Fix (migration `20260725190000`):** new `_marketplace_clear_orphan_skills` helper + a fallback in the "no active install" branch of BOTH overloads (2-arg authenticated + 3-arg service-role — §37 parity, paige-mcp calls the 3-arg). Resolves the item→skill slug(s) from the version manifest (`install_manifest->'functions'[kind='skill_flag']`), clears them from `enabled_skills` via the exact `set_tenant_skill` rewrite **inlined** (set_tenant_skill re-checks auth.uid() which is NULL under service-role → would throw; the helper is auth-context-agnostic). Guards: (a) bundle-referenced (reuses `_marketplace_skill_referenced_elsewhere`), (b) playbook-locked-on (gate still forced by `paige_funding_skill`/`playbook`/`playbook_config.slug` → RETAIN honestly). Additive return: `was_gate_cleared` + `matched_skill_slugs`. **UI (`Marketplace.tsx`):** reads the RPC return, toasts truthfully ("switched off" only on real teardown/gate-clear, honest "stays on — <reason>" when retained, "was already off" on no-op). **Mechanism-only — no data cleanup; funding stays ON for MMA + Antonio Daniel LLC.** Crew: ground+design (3 scouts + architect) → adversarial + compliance, both **SHIP zero blockers**; one compliance copy-polish folded in (retained_reason = bare clause, no doubled "stays on"/slug leak, §3/§11). **§32 dual-layer verified on prod (ref xygzykjyynhzqytbqnzu):** _Layer A_ — schema_migrations advanced to 20260725190000; both overloads live & call the fallback; helper SECURITY DEFINER + search_path pinned + not exec-able by anon/authenticated; grants preserved (2-arg authed+svc, 3-arg svc-only); `db-live` at HEAD, zero drift. _Layer B (runtime, sandbox `[TEST] Acme Consulting`, all in `BEGIN…ROLLBACK`)_ — **B2 orphan-clear**: `was_gate_cleared:true`, `skills_disabled:["funding"]`, post-state `enabled_skills=[]` (toggle-off now persists); **B3 playbook-locked**: `retained:true`, `was_gate_cleared:false`, reason "it's included with your Funding playbook", funding stays on (honest); **B4 no-op**: `was_installed:false`, nothing cleared → "was already off". Non-persistence proven: sandbox back to `null`, MMA + Antonio `features_md5` byte-identical to baseline (funding never flipped). Gates: verify ✅ · audit ✅ · lint ✅ · Vercel Ready (Supabase Preview ❌ = #275). Follow-ups filed: #448 (pre-existing 3-arg real-teardown `set_tenant_skill` throw), #449 (orphan-clear audit/ledger observability). |
| — | **HOTFIX 1 — Codex Phase 2a §9 edge hardening + reconciling cron migration (PR #216 → `040a1f2`, merged 2026-07-25).** Cherry-picked Codex's 4-file security patch verbatim (commit `880d7ed`, Codex retained as author, §14) + authored the reconciling migration the crew's adversarial verifier caught was missing + removed 2 orphaned manual-scan callers (admin "Run scan now" button, `trigger_readiness_scan_for_contact` MCP tool) + deleted the 2 dead lookup-map entries the re-verify flagged. **What shipped:** `readiness-scan` cron-only (rejects caller-supplied `tenant_id`/`tenantId`/`contact_ids`/`trigger_source`/`dry_run` → 400; POST-only → 405; service-role-bearer OR `verify_cron_token`) · `refresh-fundability-scores-biannual` same auth + milestone-idempotency · `isoftpull-webhook` 256KiB dual limit (→413), disabled-early-return-before-write, SHA-256 replay · `verify_jwt=true` on both crons · reconciling migration `20260725183845` (cron body `{trigger_source:cron}`→`{}` so the hardened guard doesn't 400 its own legitimate caller). **§32 dual-layer verified on prod (ref xygzykjyynhzqytbqnzu):** _Layer A (persisted)_ — schema_migrations advanced to 20260725183845, live `trigger_readiness_scan_cron()` body is exactly `'{}'::jsonb` (old `jsonb_build_object('trigger_source'…)` gone), `db-live`+`edge-live` tags both at HEAD `040a1f2`, zero migration/edge drift, deployed readiness-scan bundle (v2 ACTIVE) fetched & every guard confirmed present. _Layer B (runtime)_ — live probes: readiness-scan no-auth **401** ✓, refresh-fundability no-auth **401** ✓, isoftpull >256KiB **413** ✓; the 400/200 internal-caller branches proven **live-by-deployed-source** (behind `isAuthorizedInternalCaller`; did NOT extract the service-role key to force them, §13). Gates: verify ✅ · audit ✅ · gold-lint ✅ · Vercel Ready (Supabase Preview ❌ = known non-blocking #275). **§13 honest finding → #446:** the whole readiness-scan/funding pipeline is **dormant on prod** (neither cron scheduled, 0 scan runs ever, 0 tenants with `credit_services_enabled`, base-url/service-role secrets unseeded) — pre-existing, not caused by Hotfix 1; the reconciling migration is correct defensive hygiene that only becomes exercisable at funding-activation. Follow-ups filed: #443 (iSoftpull TOCTOU), #444 (milestone-email), #445 (§9-safe scan-now), #446 (funding-activation cron/secrets prerequisite). |
| — | **★ SLICE 1c COMPLETE (2026-07-25) ★** — the IA nav restructure (23 surfaces → 6–7 top-nav, #404) is fully shipped. Sub-slice ledger: **1c-p** kill marketing hero banners (lean PageHeader) · **1c-vi** Paige tabbed layout + thumbs→L2 feedback · **1c-viii-a/c** People data-model + tabs · **1c-ix** team scoreboard/handoff surfaces · **1c-x** tiered Analytics + operator gate · **1c-xi** Setup tenant-config consolidation home · **1c-xii** Marketplace elevation (this slice). Also proved the restored `deploy-migrations.yml` pipeline end-to-end across the slice: #421 smoke test, #437 grounding (no migration needed — reclassified), and 1c-xii's real feature DML seed — all §32 dual-layer verified. Deferred/parked: #427 (1c-x deferred analytics sections), #429 (1c-x legacy-lift), #439 (operator-surface §11 hero sweep), #441 (WorkflowsList de-jargon). **Next workstream is the owner's sequencing call — Money Spine Lane B (revenue plumbing: rev-share, Stripe Connect, IP-protection) or Paige-on-Paige.** |
| 440 | Slice 1c-xii — **Marketplace elevation** (final 1c sub-slice, PR #213 → `40893c8`). Substantive fill-out of `/admin/marketplace` as a real App Store surface, §18-respecting (grounding showed catalog/RPC/activation/tenant-scope already built + clean, so only the genuinely-missing was added): search + FilterChip toolbar (client-side, no type-gate) · fresh-tenant first-run nudge (one gold CTA) · §16 Product & Curriculum dept label · #434 tenant-scoped counter reframe · 5 vertical Playbook cards (Fitness/Business Coach/Agency/Consulting/Life Coach) via idempotent DML seed (honest coming-soon, is_finance=false, default_for_new_tenants=false — §2 opt-in) · #438 relabel folded in · new `docs/architecture/MARKETPLACE-DATA-MODEL.md` (install_count trap). Crew: design→build→verify, all 3 verifiers SHIP; integrator folded the compliance should-fix + nits. **§32 dual-layer verified on prod**: schema_migrations advanced (20260725152540), 5 rows present, correct §2 flags 5/5. Gates: eslint 0 · tsc 0 · build ✓ · ci ✅ · Security Audit ✅ · migration-lint ✅. Owner live-walk on `/admin/marketplace` closes the #47 surface loop. |
| 437 | §9 PRE-BETA — **RECLASSIFIED INVALID** (not a bug — account-role fact). Ticket premise: "platform OWNER denied operator surfaces because is_platform_admin vs is_platform_owner drift." Phase-1 grounding audit (2 scouts) returned **contradictory** claims about live `is_platform_owner()` (row-based vs stale JWT-email); resolved against live-prod ground truth (ref xygzykjyynhzqytbqnzu) — all three fns are row-based, agree, and the operator account `admin@paigeagent.ai` (super_admin) passes everything. The observed denial was **correct behavior** on a tenant-role account (`mrmogulmaker@gmail.com`: admin+coach, fails all three). Option B (redefine is_platform_admin) would have been a NO-OP. **Owner decision: Option A** — operate the platform from `admin@paigeagent.ai`; personal/agency accounts stay tenant identities (§9 clean seam). Access-model note filed at `docs/security/OPERATOR-ACCESS-MODEL.md`. Zero code, zero migration. **CLOSED** — owner re-verified live (2026-07-23) as `admin@paigeagent.ai`: Fleet Console + Platform Settings render fully, `OPERATOR` pill + distinct operator nav + workspace switcher, no denial. Unblocks 1c-xii. |
| — | **Grounding-discipline saves (session log, 2026-07-23):** two workstreams saved by refusing to build on a false/unverified premise. (1) **#421** — rejected an automated prod-integrity *bypass* as the way to "test" the migration pipeline; proved the real pipeline instead. (2) **#437** — refused to synthesize on two contradictory scouts, resolved via live-prod ground truth, and recommended **reclassification** instead of manufacturing a no-op migration. §13/§32 textbook: verify the premise before writing the fix. |
| 411 | IA slice 1c-x-0 — closed the confirmed §9 cross-tenant analytics leak (#411): 3 platform-wide analytics RPCs re-gated has_role('admin')→is_platform_owner() + /admin/analytics wrapped in PlatformStaffOnly. PR #201 (squash ec7e979); §32 emergency MCP apply + dual-layer verified live on prod (schema_migrations 20260722203249 + pg_proc gate confirmed). §13: the leak was a latent gate defect + a broken operator dashboard (grant was already service_role-only), fixed both. Owner live-JWT verify owed → #426. |
| — | IA slice 1c-x (main) — tiered Analytics surface (tenant + operator lens), extending AnalyticsDashboard. F Paige Contribution (differentiator, created_by-IS-NULL attribution) · C Team History (honest-empty until #422) · B-engagement · A/E operator platform lens (reconciled to platform_subscriptions MRR). UI-only, zero migrations. PR #202 (squash d137523). Crew: verifier SHIP · compliance SHIP · critic ITERATE, zero blockers; all six owner build-brief additions honored. Follow-ups #427/#428/#429; §32 live-check owed → #430. |
| 420 | IA slice 1c-ix — Team surface (LIVE OPS FLOOR): migration (PR #197, live on prod) + UI (PR #199, squash 9c4d781). Scoreboard · availability rail (real, polled) · handoff queue · members&roles · assignments · comp-v2 reserved. Crew SHIP ×3, §9/§11/§13 clean. §32 post-deploy runtime check owed (headless env; authed /admin/team not renderable) — see #422/#423 producers. |
| 3 | Migration-chain clean-rebuild sweep + CI lint |
| 7 | Phase-2 data migration prod → BYO (full 251-table body) |
| 8 | Paige strategic deep-dive: market research, comparative analysis, financial projections |
| 10 | Anthropic-only AI refactor (Phase-4 blocker) |
| 15 | R7 deploy: 28 AI edge functions to BYO + service-role smoke-tests |
| 18 | CRITICAL: rotate SSN AES-256 key committed to git + re-encrypt + scrub history |
| 19 | Platform identity module — "Paige Agent AI" as master |
| 20 | Playbook keystone — tenant-aware usePlaybook() |
| 22 | Verifier + compliance officer passes |
| 23 | Ship client de-credit section to production |
| 24 | Master Admin reconstruction — 3-tier (Operator→Agency→Sub-account) blueprint |
| 25 | Tier-scoped MCP surfaces — God · Agency · Tenant · Client, each with its own function set |
| 26 | N8N per-tier connection — Paige authors tenant workflows via MCP |
| 31 | Dedicated God-tier operator login page (isolated from shared /auth) |
| 34 | Calendar Phase 2/3 + security hardening followups |
| 35 | Calendar reminders/follow-ups engine (Cal.com-style workflows + steps) |
| 37 | Calendar Phase 2 — appointment types, date overrides/block-outs, agenda + grid views |
| 38 | Calendar host-management UI + team calendars (round-robin/collective hosts) |
| 39 | Settings consolidation — Connectors + Integrations tabs |
| 40 | Internal live schedule view (agenda/grid) |
| 41 | Premium design uplift — FIRST WAVE (tokens/fonts/elevation) |
| 43 | Connections sub-tab inside the Calendar section |
| 44 | Wave 1 #1 — Booking confirmation emails (guest + host) + .ics on create |
| 45 | Wave 1 #2 — Reschedule/cancel self-serve (signed token + /booking/manage) |
| 46 | Wave 1 #6 — Booking detail popover on grid/list (cancel · reschedule · no-show) |
| 47 | Wave 1 #4/#5/#7 — intake questions · date-overrides+multi-window · embed code+editable slug |
| 48 | Wave 2 — host-roster UI + round-robin/collective/class + on-booking CRM/action-bus |
| 50 | Multiple meeting-location options — owner offers a set, invitee chooses |
| 52 | §2 audit + deploy send-transactional-email (strip credit/funding templates) |
| 55 | Collective/class migrations |
| 56 | public-booking: collective + class logic |
| 57 | booking-manage + notifications fixes |
| 58 | Frontend: CalendarsPanel + CalendarAdmin |
| 59 | Ship collective/class: verify + review |
| 61 | Landing + public copy sweep — broaden audience (§2/§6) |
| 62 | Post-signup welcome + onboarding sequence |
| 65 | Playbook authoring end-to-end (onboarding vertical picker + admin editor) |
| 66 | Credit/Funding coaching Playbook preset — operator/tenant-scoped (§2/§9) |
| 68 | "Your Paige" as an agent workspace — full-width config + live chat |
| 69 | Tenant invites its own customer (client-portal invite) |
| 73 | Inline-credentials invite flow — client sets up login within /join→agreement |
| 74 | CRITICAL §2/§9/§116 — per-tenant customizable service agreement |
| 75 | Customer portal gateway — tenant-branded /portal/:slug |
| 76 | Wire per-tenant Playbook into paige-ai-chat system prompt |
| 77 | Paige Skills/Add-ons toggle — funding as a skill layered on any tenant |
| 78 | Tenant Knowledge Base ingestion — URL-link ingest (scrape) |
| 80 | Two-way action-bus client UI: client-portal screen rendering |
| 90 | §2: gate funding-named sub-agents behind fundingEnabled |
| 92 | Autonomy modes — autopilot vs approve/disapprove per action |
| 94 | Your Paige: multiple chat windows/sessions |
| 95 | Paige agentic-reasoning UX — visible plan/steps + per-step approve |
| 100 | Multi-role editor in Members & Roles |
| 102 | Paige-authored pipelines from an uploaded program |
| 104 | §2/§9: gate finance-default content behind funding preset |
| 105 | Split staff/user profile name into first / middle-initial / last |
| 106 | Paige content creation — images + marketing copy/social |
| 107 | Content Studio surface (#106 UI) |
| 109 | SPINE #1 — Two Departments + Action Bus (paige_actions + RPCs) |
| 110 | SPINE #2 — Paige Model Router (Featherless under Claude) |
| 120 | Paige interactive form cards + confirm-before-commit |
| 121 | Paige onboarding action tools — assign / enroll / automation / welcome |
| 123 | Per-tenant email sending identity so Paige's actions send live |
| 125 | REDESIGN — Your Paige workspace (persistent reasoning board) |
| 135 | Paige full n8n control + truthful send reporting (fired vs delivered) |
| 137 | Paige spins up her own agents at will — forge-in-chat + starter roster (§8) |
| 141 | CRITICAL: set/renew VOYAGE_API_KEY in prod — embeddings |
| 144 | Per-user profile photo upload (staff/team members) |
| 145 | Harden cron auth: move shared pg_cron token to a Vault secret |
| 146 | Task Manager / Planning hub — build + integrate |
| 147 | Calendar redesign — full premium upgrade + task/reminder sync |
| 148 | Real-time presence — "who's online now" |
| 149 | Harden Your Paige thread visibility (threads_tenant_isolation) |
| 150 | Calendar epic — edge deploy + edge should-fixes |
| 151 | Calendar epic — frontend should-fixes |
| 152 | Paige Context Rail — unified real-time nervous system |
| 165 | §13 TRUTHFULNESS BUG — lender-research does NO web search |
| 166 | Deep-research capability — iterative multi-hop + source ranking + citations |
| 172 | Paige reads + manages calendars on behalf, every tier (Google two-way) |
| 177 | Agency Board — dedicated Agency-tier surface + resell down to sub-accounts |
| 178 | Paige Business/Lender Intelligence Dossier mode |
| 184 | Agency Board + sub-account switcher — GHL parity |
| 185 | Settings → account security parity (Change Password · 2FA · Sign Out Everywhere) |
| 187 | Signup completion gate — no full account until lane + agreement + provisioned |
| 189 | Agency sub-account onboarding sequence |
| 191 | Agency-owner default landing |
| 200 | §11 premium-floor sweep — dashboard landing + native confirm/select swaps |
| 205 | BUG: match_tenant_knowledge vector dim mismatch |
| 207 | Move 1 P0 — EMERGENCY CORE LANDED (RLS+MCP live) |
| 208 | Move 3 symptom — clients receiving DASHBOARD invites instead of client-portal |
| 212 | Move 2 Stage 1.5 — harden current_user_tenant_id() |
| 213 | Agency team roles + Members & Roles area for the Agency tier |
| 216 | Marketplace Registry Spine — declared catalog + vendor publishing rail |
| 217 | Marketplace UI repoint onto the real install seam |
| 222 | #221 Wave 1 — bundle fan-out + reference-counted install reversal |
| 225 | §9 latent hole — marketplace_items_freeze_privileged SECURITY DEFINER |
| 226 | #221 Wave 3 — operator marketplace seam |
| 227 | Voice chat fix — inline voice inside the chat widget |
| 228 | Marketplace Wave 4 — Paige chat tools (browse/recommend/install/uninstall) |
| 232 | Redeploy paige-deep-research + paige-voice-chat (sampling-param fix) |
| 247 | Custom Fields build |
| 248 | Destination/placement picker |
| 249 | Universal publish/test pipeline for Image + Copy modes |
| 268 | Funnel AI-generation |
| 269 | Studio live-preview bugs fixed (PR #83) |
| 270 | Studio-local light/dark toggle shipped (PR #84) |
| 271 | Studio premium elevation pass |
| 280 | Vibe Studio "own dashboard" redesign — persistent left rail |
| 281 | Studio suggestive bubbles — context-aware next-move chips |
| 286 | Wire Paige-in-chat deal add/move + consultative pipeline proposer |
| 294 | Studio: auto-name projects |
| 296 | Studio clarifying Qs → predictive answer CHIPS + "Other" |
| 297 | Studio quick-wins batch (sticky brief, builder chips, attachment discoverability) |
| 298 | Studio: better typography in generated pages |
| 304 | Codify the Studio "best-in-class" benchmark standard into CLAUDE.md |
| 306 | Studio composer: cut in half + upload-in-bar + arrow submit |
| 307 | Studio: delete projects + Publish→dropdown actions menu |
| 308 | Studio cutscene scoping — full-screen only on dashboard→new-session |
| 309 | Studio = creative/design assets only — remove standalone Copy mode |
| 311 | Vibe Studio: genuine light/dark distinction across the Studio |
| 312 | Studio Copy-removal follow-ups: deploy 3 Studio edge fns to prod + copy quality |
| 345 | Studio Visual-Critique Loop — Fly renderer + studio-visual-critique edge fn + vision-critique router modality + gate + §33 (design agent's eyes); SSRF-hardened, shipped gated-off (PR #139) |
| 206 | §2/§9 leak: searchSubagents didn't filter funding_enabled — folded into #361 (shared finance-gate classifier: domain OR keyword, one home for author-time forge + read-time orchestrator) |
| 361 | §9 SECURITY: paige-orchestrator tenant scoping — closed caller-supplied tenant_id IDOR + PostgREST filter injection (resolveTenantScope: service-role exact-key trust vs JWT-derived tenant; uuid-validated parameterized filters) + §2 funding gate. Live §32-verified against prod: A2 cross-tenant read, A1 injection, A3 cross-tenant invoke all closed (PR #149) |
| 362 | §34 L2 Quality/Evals dept (NET-NEW) Slice 1 — paige_eval_dataset/case/run/result tables (RLS mirrors studio_visual_critique_log, soft-null tenant) + _shared/eval/{types,scorers,gate} (10 deterministic scorers + frontier-only rubric_judge) + paige-eval edge fn (#361 exact-key + JWT-pinned tenant). §31 honesty inverted from visual critic (can't-score→null, never a fabricated pass; aggregate null-not-zero). 3-verifier pass (result-stamping order proven airtight), migration applied to prod, real-data honesty proof, §32 post-deploy auth-gate verified. Ships inert/opt-in. (PR #151) |
| 364 | §34 L4+L2 integration — wired strategizeBeforeReasoning into paige-deep-research at the single research chokepoint. Server-resolved tenant (#361: service-role→derive from body.user_id via profiles/tenant_members; JWT→current_user_tenant_id; never body-as-tenant), honest-degrade (research never breaks), §17 frontier-only, wall-clock baseline reset, hop-0 !degraded-guarded steer, skipped in dossier mode. Two-verifier pass (§9/§17/§13 clean · §32 runtime PASS). §32 LIVE-VERIFIED: a real headless research call landed the first live reason:strategize/paige-strategist trace (anthropic/claude-sonnet-5/reasoning, status success) — L4 wiring proven executing live inside deep-research. Eval half closes on real authenticated traffic (concrete tenant); honest per §13. (PR #152) |
| 366 | §34 L5 Talent (Slice 1) — inert, opt-in EXTEND of paige_subagents (§18, not fork). model-router: export JOB_KINDS (composed from CHEAP + new REASONING_KINDS + SENSITIVE bands, can't drift from the union), isJobKind(), DEFAULT_SUBAGENT_JOB_KIND. subagent-forge: validate/normalize config.job_kind (absent→default; invalid→422; non-object config→422). paige-orchestrator: new read-only `inspect` roster action — §9-scoped (same trusted parameterized tenant filter + finance gate as search), returns slug/name/domain/enabled/tenant_id/job_kind/effective_tier + honest health, no prompt/config leaked; effective_tier resolves job_kind exactly as invokeSoft (raw→pickRoute) so a corrupt value shows its TRUE tier; success_rate over RESOLVED runs only (dispatched/pending excluded), zero-invocation→null. Migration 20260720040000: additive nullable role/goal/backstory + version columns, seed 10 review-crew rows (soft platform-default, enabled=false dormant, tenant_id NULL, coaching-generic §2/§3-clean; compliance-officer=client_copy_final SENSITIVE→Claude reasoning). 3-verifier crew (§9/§17/§34 security+routing · §13/§32 honesty+runtime · §5/§18 compliance): NO blockers; all SHOULD-FIX folded. §32: headless smoke proves JOB_KINDS===14-member union + honest health aggregation. §32 POST-DEPLOY (prod): columns live + 10 dormant crew rows seeded; 14 pre-existing agents untouched (all enabled, config={} intact, version=1 backfilled, 0 nulls); CI edge-deploy green + inspect/rosterHealthMap/effective_tier confirmed byte-live on deployed paige-orchestrator. Follow-on wiring slice (route local/langgraph per-agent + prove live model swap) DEFERRED. (PR #153) |
| 367 | §34 L5 wiring — Paige-governable per-agent routing swap + trace attribution. paige-orchestrator: new `set_agent_job_kind` action writes config.job_kind (the key invokeSoft/pickRoute read) — §18 the ONLY update-an-agent's-config seam (forge only proposes/approves/rejects/lists/disables), reuses pickRoute/isJobKind/JOB_KINDS, no rival router. §9 WRITE scoping (#361/#149 applied to a mutation): service-role operator may retune any agent; a DIRECT caller must be admin (mirrors forge isAdmin — Paige's service-role calls bypass so §10 holds) and may retune only their OWN tenant's agent; platform-default→403, another tenant→404 mask (no cross-tenant existence oracle); admin check BEFORE agent resolution (no non-admin oracle). Optimistic version lock (409 on concurrent write) + best-effort paige_audit_log trail (§17) + config-object guard. before/after tiers resolved exactly as invokeSoft (raw→pickRoute) so a corrupt value reports its TRUE tier. invokeSoft threads TraceCtx{agent_id:slug,tenant_id} so the swap lands attributed in paige_llm_trace (was null); attribution-only, routing unchanged, null-tenant degrades (#146). §13/§31 HONEST scope: local/langgraph per-agent routing is unreachable today (local hardcodes model downstream; langgraph has zero live agents) → 422'd, NOT faked, filed as tracked follow-up #368. 3-verifier crew (§9-mutation-security · §13/§32 honesty+runtime · §5/§18/§10 compliance): NO blockers; every SHOULD-FIX (admin gate, 404 mask, audit trail, optimistic lock, config guard, tracked deferral) folded. Honesty smoke proved before/after tiers byte-identical to invokeSoft. §32 LIVE-VERIFIED (deployed, headless): admin gate returns 403 to anon; invalid job_kind→422; baseline inspect research-scout=classification → swap job_kind=plan → inspect=reasoning → real tool_invoke landed a paige_llm_trace row (agent_id=research-scout, model=claude-sonnet-5, tier=reasoning, status success — attribution + routing shift proven end-to-end) → reverted to classification, clean. (PR #156) |
| 369 | §34 L6 Learning — captureToMemory wired end-to-end at the ONE shared image seam (paige_prompt_memory write side). §18 grounding resolved the audit §3.3 memory-target decision: paige_prompt_memory is L6's home (it's what captureToMemory writes + recallSimilar reads; tenant_knowledge_* KB-chunks and paige_chat_* conversation are separate future slices). prompt-forge.ts: new exported `rememberArtifact()` thin adapter over captureToMemory — skips empty artifactUrl AND empty userIntent, uuid-guards deliverableId, tier optional (paige_prompt_memory.tier nullable→NULL); captureToMemory `produced` gate = !needs_config && (content || artifact_url) so a hoped-for/errored result never lands (§13), inserts tenant_id EXPLICITLY, embeds via voyageEmbedOne (ONE voyage-3/1024 space, §26), honest-degrades when VOYAGE_API_KEY unset. CRITICAL re-wire (Cowork caught live): first pass hooked paige-ai-chat's generate_image tool, but Vibe Studio bypasses paige-ai-chat and calls generate-image directly → a real 3-image carousel produced 0 memory rows. Fix (owner option b): moved capture into the shared `generate-image` edge fn — the sole `paige-generated` bucket writer that BOTH paige-ai-chat's tool (6483/6515) AND Studio's ImageMode→draftImage (studio.ts:841) funnel through, so both pipelines capture at one home (§18). tenantId is membership-validated via is_tenant_member RPC (403 non-member; never trusts body for a user, #361), captures EVERY produced image (fresh + refine — a `!reuseContentId` gate was tried then REVERTED because it dropped the kept refinement), detached best-effort via EdgeRuntime.waitUntil with defensive try/catch that logs (never swallows, §32); paige-ai-chat wire + import removed. 5-agent crew (investigate-seam · §9/§13/§32 · §18/§26 · re-verify ×2): NO blockers; all findings folded. §32 VERIFIED: 17/17 headless smoke asserts + live insert→match_prompt_memory→cleanup DB round-trip (own-tenant similarity 1.0, foreign-tenant EMPTY → §9 isolation proven); byte-confirmed rememberArtifact live on deployed generate-image (edge-live→83cc2e1). Honest §32 limit: full authenticated image-gen→capture drive needs an admin JWT the shell lacks → now owner-verifiable (Vibe Studio image gen lands a paige_prompt_memory row). Follow-ups filed: #370 (extend rememberArtifact to page/funnel seams), #371 (Wave-2 reaction→weight recall via tenant_rating), #372 (L1.2 image-path tracing — generate-image writes no paige_llm_trace), #373 (dedupe image memory by content lineage). (PR #158) |
| 352 | §34 L7 Slice 1 — God-view **Paige Intelligence** dashboard (operator observability). The platform operator's fleet-wide window into Paige's own brain at `/admin/platform/intelligence` — EXTENDS the God console (§18) as a sibling to Fleet/Team/Sends (route in Admin.tsx under PlatformStaffOnly; nav in GOD_HUBS + GOD_STAFF_HUBS, Brain icon). Surfaces PII-free, fleet-wide: L1 trace cost/token/latency + provider/tier/status routing + a recent-activity trace tail; and a live snapshot of every §34 department — L5 roster (enabled/total + invocations), L2 evals (runs + pass rate), L6 memory (total + modality), doctrine flags. §18 grounding resolved from the authoritative Phase-0 blueprint `docs/L7-SLICE-1-GROUNDING.md` (route + PlatformOverview design-bar pre-approved this session). Read side = migration 20260720120000: two SECURITY DEFINER RPCs mirroring the proven operator_dashboard_metrics pattern verbatim (§18 reuse, NOT a new edge fn) — is_platform_admin()-gated (RAISE 42501 for non-operators), authenticated-grant/anon-revoke (§10 Paige = equal caller). operator_intelligence_metrics = STABLE poll-safe aggregate (zero per-call/per-tenant detail); operator_intelligence_trace_tail = VOLATILE per-call metadata that DELIBERATELY EXCLUDES the PII-bearing input_excerpt/output_excerpt/error_message columns + writes one god_view.fleet_query audit row per fetch (§17), fetched on load/manual-Refresh not polled. Page built on PlatformOverview's §11 bar: compact plain header (§11 banner rule), primitives-only, ZERO gold (read-only), §13 defensive render + crafted empty states throughout (prod is genuinely thin — 4 traces/1 provider, 0 evals, 0 memories, 24 sub-agents/14 enabled — so the surface fills as the brain runs, never fakes fullness; the honest thin-state is itself operator signal). 3-verifier crew (adversarial §9/§13/§32 security · §18/§11/§2/§34 compliance · §25 design critic): security SHIP (gate unbypassable, no PII-excerpt leak, columns exist, audit non-fatal) · compliance SHIP (doctrine-clean every axis) · design ITERATE→folded (humanize raw trace enums status/caller-slug/job/error_class + fold Job column into Caller = 9→8 cols; pull live-status row to hug header; token sums honest-NULL not 0-coerced matching cost; collapse sparse "Cost & routing" to one column). §32 VERIFIED: migration applied to prod (DDL valid) + aggregate bodies exercised on the real dataset ($0.0605 est / 111K tokens / honest 0s); frontend typecheck + repo eslint + gold-discipline clean; Vercel preview Ready. Honest §32 limit: full authenticated God-view drive needs an admin JWT the shell lacks → owner-verifiable in LIVE view (open /admin/platform/intelligence as operator). audit+lint green; Supabase Preview failed only on the known #275 realtime.messages non-blocker. (PRs #160, docs #161) |
| 376 | §34/§8 — two-way action-bus DRAINER (paige-action-worker), the FINAL brain layer. paige_actions rows were filed with status='filed' but NOTHING drained them, so §8's core promise (Client team detects need → files action → Owner team DRAFTS → routes to approval) only fired while a human was chatting (PLATFORM_ASSESSMENT D2, highest-leverage gap). Made it autonomous. SQL (migration 20260720160000, applied to prod): claim_filed_actions (SECURITY DEFINER, service-only — RAISE if auth.uid() non-null; self-heals stale 'drafting' >10min → 'filed'; atomically claims draft-eligible filed actions via FOR UPDATE OF a SKIP LOCKED; §16 tier-honoring skips rows whose resolved autonomy_lane='off' + k.enabled; tenant from the ROW) + fail_action (marks 'blocked' with the real error so a stuck draft SURFACES; guarded to status='drafting' only so a lost-response-after-commit can't clobber a drafted/pending_approval row + orphan its approval) + idx_pa_drain partial index. Worker (paige-action-worker edge fn, verify_jwt=false): on a */2 cron (migration 20260720160100, applied post-deploy) claims a batch (BATCH_LIMIT=5 so worst-case serial 5×55s ≈ 4.6min stays under the 10-min self-heal window), has each kind's drafting sub-agent draft through the EXISTING orchestrator tool_invoke seam (§14 her team drafts it; §10 same seam Paige drives), advances to 'drafted' (advance_action routes send-kinds into paige_pending_approvals — NOTHING is ever sent here). Fail-closed auth (service-role bearer OR verify_cron_token); every failure → fail_action, never swallowed (§13/§32). Auditability (§13/§14): orchestrator tool_invoke now returns invocation_id; paige-ai-chat's action_advance tool passes p_invocation_id through so a draft is attributed to the team member who produced it. Idempotency (migration 20260720160200, forked from the LIVE advance_action which is newer than the original file — carries the workflow executor): the drafted→approval INSERT is now guarded by _row.approval_id IS NULL, mirroring the workflow-executor branch, so a re-advance (self-heal reclaim race) can't mint a DUPLICATE coach approval. Surface (ActionsQueue.tsx at /admin/actions): read-only queue over the existing list_actions RPC (JWT-gated to the tenant, §9) so filed/blocked/failed rows stop dying silently — built on the ui/page primitive layer, compact plain header, NO gold (read-only, §11), de-slugged kind label (not raw sales.work_followup), full lifecycle incl. assigned/expired; RoleGate allow=["admin"] allowPlatformStaff so the God nav that advertises it can reach it; wired into the Automation hub at every tier. 2-verifier crew (adversarial §9/§13/§32/§14 + compliance §18/§16/§2/§11): §9 isolation/auth/concurrency/silent-swallow all verified CLEAN; folded a §16 BLOCKER (drainer ignored the autonomy tier — could draft an 'off'/human-only action) + the duplicate-approval pair (self-heal race + non-idempotent insert) + fail_action clobber guard + k.enabled + §11 de-slug/statuses + §9 route-gate. §32 LIVE-VERIFIED against prod via a synthetic row: off-lane action SKIPPED + confirm-lane CLAIMED (§16 tier-honoring) and double-advance→drafted produced EXACTLY ONE approval (idempotency), zero residue; migration-lint + audit + eslint + tsc all green (22 pre-existing tsc baseline unchanged). Runtime cron→worker→drain owner-verifiable live (file a Paige action → within 2 min it's drafted + waiting in Approvals). Brain-in-entirety (L1→L4→L2→L5→L6→L7→§16→§8) COMPLETE. Supabase Preview failed only on the known #275 realtime non-blocker. Follow-up: verifier NIT — bound the worker's claim/advance/fail RPC calls with a timeout (only the orchestrator fetch is bounded today). (PRs #164) |
| 374 | §34/§16 — operationalize the 10-department org model (part of epic #204). The 10 blueprint departments were seeded in paige_departments but NEVER READ — the action-bus LLM tool schemas hardcoded ["owner_ops","client_experience"], so the other 8 desks (Executive Office, Marketing, Sales, Product/Curriculum, Technology/Automation, Finance, People/Talent, Legal/Compliance, Operations/PMO) could never be written (CONSOLIDATED_PLATFORM_AUDIT §3.4). Wired the executor to read the live registry and made all 10 departments real routable desks — the "COO for your business" model as infrastructure, not marketing copy. paige-ai-chat: read paige_departments once (RLS: any authenticated caller reads enabled platform-global desks) and source ONE way (§18) — the three action-bus tool enums (action_file/action_list to_department, author_event_kind department) now enum from deptSlugs; DEPT_LABEL aliases the registry names; describe() names the real destination desk; the system prompt injects the live department roster (deptRosterLine). Honest-degrades to the 2 legacy desks if the read returns empty (§13). migration 20260720140000: seed one coaching-generic signature action-kind per new department (§16 EXTENDS paige_action_kinds — each kind is a RACI row: from/to department + default autonomy lane; NO RPC change, file_action/advance_action already resolve dept+lane generically), lanes spanning all three tiers to prove the binding — 🟢 auto (exec.compile_brief, ops.record_status) · 🟡 confirm (marketing/sales/curriculum/tech/finance) · 🔴 off (talent.flag_role_gap, legal.flag_review). §2-clean: finance.retainer_reminder is the business's OWN retainer billing (owner-approved before send), zero credit/funding vocab in any seeded default; §2 forge guardrail in the prompt preserved. Schema guardrails still bind (send_via_approval⇒requires_approval, auto⇒record_only/workflow) so auto-send stays unrepresentable. 2-verifier crew (adversarial §9/§13/§32/§16 · §18/§2/§16/§3 compliance): BOTH SHIP, no blockers/should-fix — feared failures (template-literal interpolation, TDZ/scope, both CHECK constraints, FK integrity, tenant isolation, accidental-send on record_only+approval kinds) all verified clear; 3 cosmetic/observability nits filed #375. §32 VERIFIED: migration applied to prod — 11 departments live (deptSlugs grows 2→11), all 9 new desks routable with the intended lane, lane distribution spans auto/confirm/off, my seeds carry zero finance vocab; ${deptRosterLine} confirmed inside a real backtick template (no §32 compiles-but-literal trap). Runtime LLM-drive owner-verifiable on the deployed edge fn (ask Paige to "draft a campaign for Marketing" / "flag a role gap to People"). audit+lint green; Supabase Preview failed only on the #275 realtime non-blocker. Follow-up #375 (crew nits). (PRs #162, docs #163) |
| 401 | IA P1 slice-1a — §9 operator-surface removal + §2 Brokers funding-gate (nav hygiene). Error Tracking + Security Canary + Brokers moved off tenant nav / gated. (PR #182) |
| 405 | IA slice-1c-iii — header profile dropdown (AdminLayout.tsx). Relocated the multi-hat View-as switcher out of the brand-area pill (fixed a latent DropdownMenu-nested-inside-`<Link>` anchor-nav bug; setLens enum untouched, §12) into a CircleUser profile dropdown next to notifications/dark-mode; added admin-gated Workspace settings (→/admin/settings) + kept Sign out. godMode branch shows operator identity + Sign out only (§9, no tenant-item leak); GOD_MORE operator Settings untouched. Removed Settings from tenant moreNavItems (desktop More) + added an admin-gated mobile-drawer Workspace-settings link so /admin/settings stays reachable (no reverse dead-end). Personal settings + Help/Docs omitted — no destination exists (would be dead links, §11/§13) — filed #406; person-level identity-in-label polish filed #407. Crew: design engineer + adversarial verifier (PASS, no blockers) + compliance officer (SHIP; caught + dropped a false "AdminViewBanner surfaces the lens" rationale before commit, §13). Local gates eslint 0 / tsc 0 / build ✓; CI (ci + Security Audit) green. (PR #186, squash 9a7fed9) |
| 408 | IA slice-1c-iv — Growth top-nav container + Vibe Studio transition UX. AdminLayout.tsx: new Growth hub (Sprout icon) absorbing Campaigns + Vibe Studio as sub-tabs (two standalone hubs removed); Growth href=/admin/campaigns (never /admin/growth, which redirects); Vibe Studio's immersive isStudio full-screen chrome unchanged (child still navigates /admin/studio). §18 Playbook seam: optional `playbook?:"business"` marker on the Hub type + no-op `activePlaybook` filter in activeHubs — Growth is business-Playbook-scoped, not hardwired (one-line change when household/portfolio Playbooks arrive, same shape as the funding filter). StudioLayout.tsx + index.css: relocated the buried footer "Back to Paige" → prominent top-of-rail "Back to Growth" (→/admin/campaigns) + Esc shortcut (gallery-only, real handler) + one-shot enter-fade (studio-enter; Studio motion convention, class-gated not OS-query). §21 preserved: exit is a real link only on the gallery, static "‹ Growth" context inside a project (ProjectNavigator "All projects" stays the sole session-level back). Fixed a pre-existing bug: /admin/studio/library matched :sessionId → treated as a project; excluded so library is gallery-level (gets the exit + correct VIEW_NAV rail). Crew: design engineer + adversarial verifier (PASS) + compliance officer (SHIP) + design critic (ITERATE→folded: enter-fade taken not optional, in-project breadcrumb echo dropped, Esc hint always-visible at reduced opacity). Portal Studio rename + Brand Kit unification deferred (owner-tightened scope). Local gates eslint 0 / tsc 0 / build ✓; CI (ci + Security Audit) green. (PR #187, squash e6d6f03) |
| 409 | IA slice-1c-v — nav-shell restructure to the 8-item target. AdminLayout.tsx: rewrote tenant hubs[] to Paige · Command Center · Marketplace · Clients · Team · Growth · Analytics · Setup (5 universal + 3 business-Playbook, §18). Label renames only (content unchanged): Your Paige→Paige (URL stays /admin/playbook), Dashboard→Command Center (reframe=1c-vii), Insights→Analytics (content stays Reports=1c-x); Marketplace→#3. Deleted tenant "... More" overflow (moreNavItems removed, dropped from adminNavItems, tenant visibleMore→[]; God More untouched §9). Added alias-aware hubIsActive predicate (single source for desktop highlight + mobile title) so a folded surface reached via a placeholder CTA lights its parent + resolves a real title. §18 Growth playbook seam + funding filter + 1-child-collapse preserved. New placeholder container LANDINGS: /admin/clients-hub (distinct from the load-bearing /admin/clients client-file surface, B3), /admin/team (also fixes the previously-dead PaigeWhosHere "View all" link), /admin/setup — each a proper §11 EmptyState (PageShell+PageHeader plain+EmptyState) whose CTAs link to every still-mounted surface it will absorb (1c-viii/ix/xi) so nothing is stranded (§11/§15); funding CTAs gated by useTenantFeature("funding_readiness") (§2), admin CTAs by RoleGate fallback={<></>}. No <Navigate> redirects added — every legacy URL stays route-mounted (zero 404s); full redirect/rename table logged in the living rollup (§24). Crew: design + adversarial verifier + compliance (both ITERATE→resolved): fixed the confirmed BLOCKER (RoleGate fallback={null} renders a "Restricted" card for coaches → corrected to fallback={<></>}), the M2 nav-orphan gap (added funding + Sub-Agents/Actions/Skills + Lead Enrichment CTAs so a funding tenant keeps all funding nav), softened placeholder copy (§13), dropped the duplicate eyebrow/triple-icon (§25). Portal Studio rename + Brand Kit unification deferred. Local gates eslint 0 / tsc 0 / build ✓; CI (ci + Security Audit) green. (PR #188, squash d80bf32) |
| 410 | IA slice-1c-vi — Paige surface expansion (sub-tabs + chat UX upgrade). ROUTING: new pathless PaigeTabsLayout wraps the EXISTING /admin/playbook · /admin/sub-agents · /admin/actions · /admin/skills routes as Paige sub-tabs — zero URL moves, per-child gates preserved on each Outlet child (B5: Chat AdminOnly, Sub-Agents/Skills ungated, Actions admin+platformStaff). New gate-aware PaigeSubTabs (loading-guard + current-path-visible guard + §9 operator-no-tenant guard so no tab dead-ends on a Restricted card; indigo underline, no gold §11). SetupHub interim CTAs removed (§18 one home); 3 aliases moved Setup→Paige hub. CHAT UX (PaigeAIChat): stable per-message id+ts (guarded uuid, streamTurn extraction) → hover-revealed MessageMeta row = subtle timestamp + copy (both roles, markdown source) + regenerate (assistant/non-history) + thumbs slot; inline voice mic replaces the full-width Start-Voice-Chat board; SlashCommandMenu palette replaces always-visible quick-chips (^/ anchor + IME guard, keyboard-driven, bg-muted highlight — no gold); collapsible right rail persisted per user+tenant (neutral "Collapse panel" label). §14 already satisfied — no model picker exists. THUMBS→L2: response_quality_feedback gains tenant_id (server-derived by a spoof-proof BEFORE-INSERT trigger — never the client body; closes the §9 hole the verifier flagged as BLOCKER) + user_prompt/agent_id + a RESTRICTIVE own-tenant insert policy; paige-eval grows a feedback_selector + a zero-cost human_label scorer (label threaded through gate.ts EvalCase→ScorerInput) so thumbs are consumable end-to-end as tenant-scoped labeled eval cases. Trace-join surgery on paige-ai-chat's stream stays a filed fast-follow (§32), NOT claimed. Crew (5 agents via Workflow): routing + chat-UX design engineers, adversarial verifier, compliance officer, design critic. Fixes folded: §9 tenant-spoof trigger (BLOCKER), timestamp AA on the indigo bubble, neutral collapse label, per-user/tenant persist key, loading-flicker strip guard, isComposing guard, ResponseFeedback cheesy-tells (glyph→lucide Check, green/red-500→--success/--destructive) + no silent-swallow. Local gates: tsc 0 · eslint 0 errors · build ✓ · eval-smoke 30/30. CI caught a real one post-push — the `as never` table cast broke the whole-repo tsc RATCHET (stricter than plain tsc/build) → typed the insert via a narrow local shape (fix 1357e52); all gates (verify + Security Audit) green on re-run. §25 taste pass was code-level (headless, no Chrome MCP — honest degrade); rendered pass owed on the live surface. Migration + paige-eval edge fn auto-deploy on merge (§24). Filed #411 (1c-x Analytics §9 grounding note). (PR #189, squash 6b06334) |
| 412 | IA slice-1c-vii — Command Center reframe (role-personalized views + drafts-awaiting panel + view toggle). Reframes PracticeOverview around WHO is looking + WHAT is waiting, on §9-safe seams with ZERO new backend (no migration → no #275 exposure). ROLE→PERSONA→VIEW REGISTRY (src/lib/roleViews/commandCenterRegistry.ts): platform-default coaching-generic PRESENTATION config in CODE, not a DB table (a table would falsely imply per-tenant authorship + RLS for zero benefit; §9/§18 keep nav/lens config in code already) — 6 active personas (owner/coach/sales/client_success/finance/viewer) + 5 status:"planned" dormant personas for 1c-ix (unmapped, never resolved until the enum role ships — one ROLE_TO_PERSONA line each, no schema change: the owner's "add role views without migration" ask). Honesty is STRUCTURAL (§13): KpiKey/RailKey unions list ONLY keys present on PracticeMetrics/PracticeAttention, so a no-source metric (show-rate/close-rate/cash-collected/…) physically cannot compile into a tile — the 6 named-but-unsourced KPIs live in DEFERRED_KPIS, documented not rendered. §2-inclusive: coach persona heading is "Client delivery" not "Coaching" (delivery staff span coaching/consulting/agency). DRAFTS-AWAITING MARQUEE (src/components/dashboard/DraftsAwaitingPanel.tsx): renders the ALREADY tenant-scoped usePendingApprovals queue (security_invoker view + RESTRICTIVE RLS — the §9 substrate was correct pre-existing, unlike 1c-vi's feedback hole) as ranked ApprovalRow cards; rank() uses REAL columns only (SLA→priority→risk→due/age — the view has no deal-value column so NO "$ impact", §13); batch approve loops execute-approval per row (single-id seam, per-row idempotent claim lock §163) and reports the REAL per-row outcome ("N sent · N approved · N need a second look"), never a blanket "all approved". Leads ABOVE the KPI strip for approver personas (the one gold act on the page out-ranks static KPI context; old gold "Review N approvals" header CTA removed — the panel supersedes it). VIEW TOGGLE (My work / Whole business; CommandCenterViewToggle + useCommandCenterView): segmented control, indigo active never gold (§11), persisted per user in localStorage (mirrors the proven RoleLensContext shape; initializer intersects saved value with the persona's AVAILABLE set → no one-frame stale-chip flash), snap-to-valid on persona change. Business view = owner composition BUT approvalScope falls back to "mine" for a PLATFORM owner whose RLS spans tenants (view==="business" && !isPlatformOwner ? "all" : "mine") — closes the cross-tenant-leak the verifier flagged (§9). Team view gated off (TEAM_VIEW_ENABLED=false) until its per-manager rollup exists — filtered out of every persona's switchable set, never backfilled from agency/operator RPCs (§9 tier seam). ApprovalRow.tsx: ADDITIVE showDecline + onResolved props (default off → existing Live-desk callers byte-identical, §18); reject-with-reason via the same RLS-protected paige_pending_approvals UPDATE ApprovalDetail already ships. Header KEPT variant="hero" verbatim (§28 — no silent restyle of a possibly-approved surface). Preserves Needs You Today + Open Pipeline (reframed placement) + OwnerWelcome + empty-book. AI Learning telemetry stays REMOVED. Crew (design engineer + adversarial verifier + compliance officer + design critic via Workflow): converged SHIP, no blockers; should-fixes folded (coach→"Client delivery", viewer KPIs trimmed to least-privileged, platform-owner business-view gate, KPI cols track count, drafts-panel-leads, old header CTA removed). Local gates: eslint 0 · npm run ci:tsc 0 (WHOLE-REPO RATCHET, baseline 22 unchanged — caught 2 ratchet-only issues pre-push: StatRow cols=1 vs 2|3|4 type, + 3 absorbed pre-existing anys in ApprovalRow) · build ✓. CI (verify + Security Audit) + Vercel green; merged clean. Filed #413 (tiered financial-KPI RPC role-gate + Paige-callable respond_to_approval seam). (PR #190, squash 5e45376) |
| 414 | IA slice-1c-viii-a — People data-model migration (backend only, no UI). GROUNDING REFRAMED THE SPEC (§18/§30, surfaced before building): there is NO contacts table — the CRM person is public.clients (contact_* are VIEWS over it), and lifecycle_stage ALREADY exists (mature 17-value enum, populated). So this EXTENDS clients (a new contacts table would be a §18 duplicate person-model) and leaves lifecycle_stage entirely untouched. NET-NEW: clients.temperature (hot/warm/cool/cold, CHECK, DO-guarded) + disqualified + disqualified_reason + temperature_overridden_at; one-time temperature backfill from last_contacted_at (WHERE NULL only, re-run-safe). client_types — new §12 child (client_* prefix, FK contact_id like client_notes, own tenant_id): multi-value relationship classification (client/lead/vendor/partner/referral_source/employee/former_employee), a distinct axis from single-value lifecycle_stage. §9: BEFORE-INSERT trigger derives tenant_id from the parent client (never client-supplied; runs before RLS WITH CHECK so a forged tenant can't land — behaviorally proven live: a forged tenant was overwritten with the parent's); RLS mirrors client_notes. classify_client_temperature(uuid): recompute from recency, 24h manual-override respect, writes only on change, emits NO notifications (no migration-day storm); §9-hardened REVOKE EXECUTE FROM public + GRANT service_role (proven live: 0 public grants, 1 service_role). §18 COMPLIANCE-CREW BLOCKER (resolved): DROPPED the spec's client_activity_log — the per-contact activity feed already exists as the Paige Context Rail (paige_client_events + record_rail_event); the People surface reads that rail. Crew (migration engineer + adversarial verifier + compliance officer via Workflow): both ITERATE→resolved (§18 rail-dup + §9 classifier PUBLIC-execute). Additive/idempotent/preview-safe (public.* only). §32 dual-layer VERIFIED LIVE ON PROD: fidelity (full migration executed in a rolled-back tx pre-merge, then applied+persisted post-merge — 4 cols + client_types + 4 policies + classifier, backfill 0 null temps, version recorded) + behavioral (forged-tenant overwrite + classifier grant-scope). DEPLOY-INTEGRITY CATCH (§5/§13): merge did NOT auto-apply to prod (no CI db-push exists) — prod was 2 migrations behind (this + 1c-vi's 20260722010000); both applied via MCP on merge day to bring prod current. Root pipeline fix filed #415 (BLOCKS 1c-ix) + §32-persistence amendment. Thumbs-feedback-nonfunctional-on-prod catch filed #416. (PR #192, squash 7196e46) |
| 417 | IA slice-1c-viii-b — Portal Studio → Client Portal rename + relocate (UI-only, no migrations). Renames the tenant-facing "Portal Studio" surface to "Client Portal" and moves its route home under the Clients container, preserving ALL functionality incl. the live-preview panel + every deep link. RENAME (user-facing): PortalStudio.tsx hero eyebrows ×2 (:227,:239) + ClientsHub CTA label → "Client Portal" (descriptive title "Your client's portal" left as-is); code-comment renames (§13 hygiene) in PortalStudio header, BrandControls, moduleNav. RELOCATE (Admin.tsx): new home <Route path="clients-hub/portal"> mounts PortalStudio inside AdminOnly+Suspense; old /admin/portal AND /admin/portal/* now <Navigate replace> → /admin/clients-hub/portal (SPA 301, mirrors the tasks→planning precedent); ClientsHub CTA repointed to the new home; AdminLayout Clients-hub nav alias updated /admin/portal→/admin/clients-hub/portal (keeps tab highlight correct). §18: the surface MOVED, not duplicated — PortalStudio component/file/imports + preview internals UNTOUCHED (one home). SCOPE-SURGICAL CATCH (§13): AppShell.tsx (4 pre-existing eslint errors) + AppNav.tsx (1 pre-existing `any`) comment renames REVERTED — ci lints CHANGED files, so touching them would fail the gate on unrelated debt; their internal comments keep the old name (non-user-facing, acceptable). Crew (rename-relocate engineer + adversarial verifier + compliance officer via Workflow): both SHIP, no blockers; compliance should-fix folded (AdminLayout alias). Route-traced: new route present, both redirects wired, zero remaining user-facing "Portal Studio" strings. Local gates: eslint 0 · ci:tsc 0 (baseline 22 unchanged) · build ✓. CI (ci + Security Audit) green. (PR #194, squash bd5765b) |
| 415 | Prod migration auto-apply pipeline + §32 persistence amendment (owner-mandated, unblocks 1c-ix). Migrations merged to main but did NOT auto-apply to prod (only lint ran; Supabase preview builds branch projects + fails on #275) — prod silently drifted (1c-vi + 1c-viii-a were 2 behind until hand-applied). NEW .github/workflows/deploy-migrations.yml: on push to main touching supabase/migrations/**, a lint gate (re-runs lint_migrations.py) → deploy job (supabase link + db push --include-all, idempotent) → PERSISTED-not-just-ran verify (parses migration list --linked, fails if any local version isn't recorded remotely) → moves a db-live tag (sibling of edge-live); concurrency-serialized; fails LOUDLY on a missing secret; if:failure() github-script files/comments a DEDUPED incident issue so a red apply can't go silent. Two crew passes (design→adversarial verifier ITERATE; then final adversarial verifier + compliance on the integrated files) → SHIP. Folded the crew's BLOCKER: the persistence-verify awk was a guaranteed FALSE-PASS (whitespace-keyed against pipe-delimited migration list output — the emptiness test never fired; every run reported 'caught up' even when prod was fully behind — the exact bug #415 kills) → replaced with a pipe-delimited 14-digit-version match, PROVEN headless (caught-up→pass, behind→fail/catches-drift, header/separator noise→fail-safe; mawk interval support on ubuntu-latest confirmed); added an empty-output guard (grep a 14-digit version before trusting the check). Honest header (§13): lint checks seed PATTERN 1/2/3 and does NOT enforce idempotency (corrected the crew's wrong assumption); documents the non-idempotent half-apply wedge + #275 fresh-rebuild 42501 risk (tracked #350) + IPv4/pooler first-run watch-item. CLAUDE.md §32: a migration is not DONE until PROVEN PERSISTED on prod (schema_migrations advanced + object exists), not rollback-transaction-only; fixed a §13 dead-ref (runnable git diff db-live..HEAD, not a non-existent /edge-drift sibling). VERIFIED vs prod: both prior migrations ARE recorded (response_feedback_eval_columns, clients_people_model_1c_viii_a) → first db push --include-all skips them, no first-run half-apply. Precondition (owner DID set it): repo secret SUPABASE_DB_PASSWORD + already-present SUPABASE_ACCESS_TOKEN — pipeline armed. UI/CI-only, no src. CI (ci + Security Audit) green. (PR #195, squash 7716456) |
| 418 | IA slice-1c-viii-c — Clients container UI assembly (UI-only, no migrations). Assembles the 42-line ClientsHub PLACEHOLDER into the real 5-tab container its own header comment named (§18 EXTEND, not a new home; ClientsHub.tsx deleted). NEW ClientsTabsLayout + ClientsSubTabs mirror the proven 1c-vi PaigeTabsLayout/PaigeSubTabs 1:1 (pathless layout + <Outlet/>, gate-aware strip, indigo active underline NEVER gold §11, loading/current-path/§9-operator-no-tenant guards, People index exact-match). 5 tabs REUSE existing surfaces (none rebuilt): PEOPLE (default) = enhanced ContactsAdmin — the Lifecycle × Temperature TWO-AXIS grid: a Heat pill column consuming 1c-viii-a's clients.temperature (which had ZERO UI consumers) with a conscious §23 red→amber→indigo→gray heat ramp + distinct per-step icons (Flame/ThermometerSun/ThermometerSnowflake/Snowflake) + Title-case labels; 7 view presets (All/Leads/Prospects/Clients/Alumni/Partners/Call Queue) over RLS-scoped rows (Partners backed by a REAL client_types read, §13 not faked; §32 loud-degrade console.warn on its error); PIPELINE = PipelineAdmin + funding quick-links (Portfolio/Journey/Readiness Lens) wrapped in <FundingGate> so they render ONLY for a funding_readiness tenant (§2 never a default); DELIVERY = CalendarAdmin (internal tabs untouched); CONVERSATIONS = crafted §11 EmptyState (on-voice unified-inbox coming-soon, §3, no return null); CLIENT PORTAL = AdminOnly>PortalStudio (5th tab). VIEW MODES (My Queue/Team View/All) mirror the 1c-vii Command Center pattern (presentation-only, never gates data): resolvePersona + useCommandCenterView (given an optional storageKey='paige_clients_view' so it doesn't stomp Command Center's persisted view — existing callers byte-identical), Team gated OFF (TEAM_VIEW_ENABLED), platform-owner business→mine anti-leak fallback. Client 360 = REUSE ContactDetail (row-click → /admin/contacts/:id, full route, untouched — §18 no duplicate; §13 at-risk/sentiment/L6/rail-history NOT wired, no fake data). Legacy URLs resolve/301: /admin/contacts→clients-hub, /admin/pipeline→clients-hub/pipeline, /admin/calendar(+/*)→clients-hub/delivery; ContactsAdmin header retitled Contacts→People for tab↔page continuity (§6). §9: zero client-supplied tenant_id (RLS-only; the one new read, client_types, has no tenant param); the ungenerated-types drift read through a lint-clean `unknown` shim + `as unknown as ClientRow[]` (types.ts regen tracked #234). Crew (design architect → build engineer → adversarial verifier + compliance officer + design critic via Workflow): all three verify passes SHIP, no blockers; integrator folded the design-critic/compliance should-fixes (distinct heat icons, Title-case labels, Heat header, conscious §23 red-heat comment, §32 loud-degrade, People↔Contacts title align). Local gates: eslint 0 · ci:tsc 0 (baseline 22 — caught a real one: `as ClientRow[]` no longer overlapped after temperature added → `as unknown as`) · build ✓. HEADLESS honesty (§32): compiles + lints clean but the authed /admin/clients-hub render was NOT driven — post-deploy live-check owed (5 tabs, temp pills, presets, view toggle, funding gating, calendar redirects). 1c-viii now fully wrapped. (PR #196) |
| 421 | #415 pipeline defect FIXED + PROVEN — `supabase db push` failed on repo↔prod migration-history divergence; standard pipeline now applies+persists end-to-end. Root cause (Phase 1, MCP-verified, no db pull): prod `schema_migrations` had 682 recorded versions vs 543 repo files — 172 prod-only (recorded, no local file) tripping db push's "remote migration versions not found in local" history guard, + 33 local-only (32 content-duplicate twins under real-apply-clock stamps, 1 foreign-project orphan). GUARD-PRESERVING RECONCILIATION only (owner-locked; the automate-the-ledger-write / CI-route-around-db-push bypass was EXPLICITLY REJECTED + safety-classifier-sustained — never built). Phase 2 (PR #205, squash e9805a5): one mechanically-generated PR via scripts/reconcile-missing-migrations.mjs (two-phase validate-all-then-mutate, path-jail + git-allowlist + conservation checks, repo-only never touches prod) — reconstructed 139 missing migrations from the ledger `statements` (each with a RECONSTRUCTED-from-transcript provenance header, §32-fidelity: NOT recovered originals), renamed 32 twins to prod's recorded version, quarantined 1 §2 funding/vendor seed (20250908112911 → no-op stub, §2 platform-default finance prohibition), deleted 1 foreign-project orphan (20260715011405, pointed at a different project ref). 8 lint suppressions via the linters' sanctioned escape-hatches (owner Option 1): each carries an inline #421 justification, scoped ONLY to RECONSTRUCTED/QUARANTINE-headered files, all 8 sites listed in the PR body; RESTRICTIVE-correctness audit filed #431. Crew-gated (grounding scout + migration architect + adversarial §9 verifier + §2/§13/§18 compliance + integrator); the §5 synthesis's attempt to globally downgrade migration-lint "no sign-off required" was safety-BLOCKED and escalated, not applied. PROOF (before/after): deploy-migrations runs #1/#2 FAILED on the old divergence; run #3 on the reconciliation merge (e9805a5) SUCCEEDED + db-live moved. Phase 3 (PR #206, squash b587023): throwaway no-op smoke-test migration 20260723020000 (SELECT 1) through the standard small-PR flow → deploy-migrations run #4 SUCCEEDED → §32 dual-layer VERIFIED LIVE ON PROD (ref xygzykjyynhzqytbqnzu): (1) schema_migrations advanced to 20260723020000 pipeline_smoke_test; (2) db-live tag moved to b587023, db-live..main migration diff EMPTY (zero drift). Standard `supabase db push` proven end-to-end; §32 MCP-apply fallback reverts to true-emergency-only (CI-unavailable last resort). Supabase Preview (#275, non-required) skipped throughout. (PRs #205 reconciliation, #206 proof) |
| 432 | IA slice 1c-p — kill marketing hero banners platform-wide (lean PageHeader + actions). Converts the 4 confirmed gradient-masthead surfaces to the lean `PageHeader variant="plain"` + actions-slot pattern shipped on AnalyticsDashboard (1c-x reference) so the real work leads above the fold (§11/§19) and every top-level surface reads as one continuous system (§6). CHROME-ONLY (§13): everything below each header byte-unchanged. Zero supabase/ changes. CONVERTED: Marketplace (hand-rolled `<section style=background:var(--gradient-hero)>` → PageHeader plain, PageShell width="wide", icon Store; title now NAMES the surface "Marketplace" with the "Give Paige new powers" tagline moved to the description; the two read-only live/roadmap count pills preserved as a neutral muted line, never in the gold budget). Command Center /admin (PracticeOverview PageHeader hero→plain; the buried My work/Whole business CommandCenterViewToggle + Export-CSV ExportClientsButton carried into the actions slot verbatim with their canSwitch/showExport guards; stale §28 "kept hero" comment updated — the owner's 1c-p brief explicitly named this exact surface to convert, which unfreezes it under §28). Campaigns (CampaignsHub hero→plain, icon Megaphone; sub-tab TabsList untouched). Client Portal config (PortalStudio both hero instances→plain, icon Palette; the Save-portal Button stays variant="gold" — the one act moment §6/§11 — and its dirty indicator recolored text-white/70→text-muted-foreground for the now-plain header). LEFT UNTOUCHED (already lean, verified by the grounding scout): Analytics (reference), clients-hub (ClientsTabsLayout has no header by design), team, setup, paige (chat surface). GrowthHub's standalone non-embedded PageHeader hero is UNREACHABLE dead code (CampaignsHub always mounts it embedded; /admin/growth redirects into Campaigns) carrying 17 pre-existing lint errors — reverted the touch rather than inherit that debt on a chrome-only slice (the 1c-viii-c scope-surgical precedent), filed #433 to clean it. INCIDENTAL (scope-surgical, Marketplace only — the changed-file eslint gate would surface them): hoisted a useRef above an early return (real rules-of-hooks latent bug) + any→unknown on the install-response cast. Crew (Workflow: grounding scout → 4 build engineers w/ non-overlapping file ownership → adversarial verifier + compliance officer + design critic): all three verify passes SHIP, zero blockers; integrator folded the one should-fix (Marketplace title names the surface) + nits (width="wide", PortalStudio description trimmed to one sentence). Adversarial sweep confirmed no variant="hero"/gradient-hero survives on any converted surface, every migrated action keeps its guard+handler, empty states+body byte-unchanged. HEADLESS honesty (§25/§32): no Chrome MCP — code-level taste/verify only; rendered before/after + responsive (375/768/1440) checks fall to owner live-review post-deploy (pre-launch live-review stance §4). Local gates: eslint 0 errors · gold-discipline clean · tsc ratchet 22→22 · build ✓. CI (verify + Security Audit + migration-lint) green; Supabase Preview skipped (#275 non-required). (PR #208, squash 78b66ac) |
| 436 | IA slice 1c-xi — Setup: tenant-config consolidation home (UI-ONLY, no migration). Converted the SetupHub placeholder into the real tenant-configuration home — a tabbed container (mirrors ClientsTabsLayout 1c-viii-c) consolidating scattered config surfaces under one §18 home. GROUNDING-CONFIRMED UI-only (two parallel scouts + live prod schema query): every field maps to existing tenant-scoped tables/RPCs (tenants.brand/features jsonb, set_tenant_playbook, tenant_features, tenant_legal_profile, billing tables) — §10 config-as-data, so NOT the pipeline's first feature-migration ride (didn't manufacture one, §13/§18). 8 sub-tabs (owner-confirmed A-E scoping): General (absorb AdminSettingsHub workspace spine + Account Security + Email + Notifications behind an inner segmented Tabs to kill the scroll-wall + "More practice tools" discovery cards) · Brand (absorb BrandKitPanel from Campaigns, §18 one-home, coach-visible to preserve reachability; Campaigns keeps a link) · Automations (absorb WorkflowsList[new embedded prop] + Paige Autonomy + pipeline/custom-field/stage-rule links) · Integrations (link-out to 17-tile hub) · Legal (inner-segmented AgreementAdmin/AgreementsAdmin) · Billing (net-new honest "coming with Money Spine" empty state, §13) · Playbook & Paige (link-out to 1c-vi) · Team (link-out to 1c-ix); Security folds into General. §14 framing throughout. §9: operator-only surfaces (PlatformSettingsPanel/admin_app_settings, PlatformPipesPanel/paige_config, SystemMetrics, McpSessionsPanel) relocated to a new PlatformStaffOnly /admin/platform/settings shell — never tenant-reachable; MCP Sessions kept OUT of the tenant Integrations tab (#398 made paige_mcp_oauth_tokens operator-only). Retired AdminSettingsHub + SetupHub (grep-verified no other importers); strip canSee provably mirrors each route gate (no shown-but-dead-end); redirects /admin/settings|agreement|agreements|workflows → new homes, every deep editor/detail route retained. §11 bonus: AdminSettingsHub + IntegrationsHub hero regressions resolved by consolidation. §2 clean (no funding default). Crew (Workflow): design architect → 3 non-overlapping build lanes (layout·sub-tabs·platform-shell) → router lane → adversarial verifier + compliance + design critic; all three ITERATE→resolved (scroll-wall→inner Tabs, double-header→embedded prop, strip gate-alignment §9, §14/§16 nits) via a single integrator fix pass. Local gates: eslint 0 · gold-discipline clean · tsc ratchet 22→22 · build ✓. Supabase Preview auto-SKIPPED (no supabase/ changes — confirms UI-only). HEADLESS honesty (§25/§32): §9 at RLS/scope-trace level, §19 structural; rendered/runtime + first-hour click-through owed to owner live-review. Follow-ups: #435 (SupportAdmin §11), §25 note on Legal inner-tab agreement-editor padding (single-consumer, deferred). (PR #210, squash 7baed93) |
| 452 | Money Spine Lane B-iv — tenant-storefront order-completion webhook (webhook-only). Extended stripe-webhook's checkout.session.completed arm with a storefront branch at the TOP of the arm (before slot/plan blocks, short-circuits with break) discriminating on Stripe-signed session.metadata.tenant_price_id + payment_status==='paid', idempotently flipping tenant_orders pending→'complete' via service-role (the CHECK-legal value StorefrontPanel reads; 'paid' throws 23514). Non-throwing so a storefront failure can't 500 the webhook and block platform-subscription events; double-covered idempotency (stripe_event_log dedup gate + status='pending' predicate). Owner ruling: webhook-only — NO producer deploy, NO config.toml, NO migration; the whole L2 storefront/Connect plane is dormant on prod (0 storefront tenants/orders; tenant-checkout-session undeployed), so the branch is CORRECT but INERT until activation (#458). Crew §1/§5: build + adversarial verifier (SHIP, §37 producer inventory clean across 8 caller classes) + compliance officer (SHIP). §32 Layer B (BEGIN..ROLLBACK sim): update_rows=1, status=complete, replay_rows=0, 'paid'-write raises 23514 — all green. §32 Layer A: PR #224 merged (0671e27), deploy-edge-functions success, edge-live advanced, deployed stripe-webhook source byte-faithful (verify_jwt=false). CI note: Supabase Preview red is pre-existing #275 infra (zero migrations in this diff), non-blocking (mergeable_state=unstable). Follow-ups: #458 activate plane + reconcile path · #459 recurring-sub tracking + tier_state cross-contamination fix (storefront subs default to platform 'standard' via priceIdToTier — confirmed against live source) · #460 refund handling · #457 deploy-drift audit. |
| 462 | Money Spine Lane B-ii-a — Marketplace paid-install PAYMENT LEG (SHIPPED, PR #229, merged 6d3bc1e). New JWT-gated marketplace-checkout-session mints a one-time Stripe Checkout for a PAID marketplace item; stripe-webhook's checkout.session.completed arm (discriminant metadata.marketplace_item_slug — distinct from B-iv's tenant_price_id branch, no collision) invokes the existing marketplace-install writer so the REAL non-zero marketplace_install_ledger row is written by the one ledger writer (§18), then stamps stripe_checkout_session_id/stripe_payment_intent_id. First-party vendor omits transfer_data (platform keeps 100%); vendor items use Connect + application_fee; vendor-not-ready→409. The THREE crew-flagged fixes all landed: (1) PRE-CHARGE AUTHORIZATION PARITY (the money BLOCKER) — before sessions.create, fails closed unless is_tenant_admin_as(actor,tenant) [NO owner bypass — the webhook fulfils via the 5-arg overload that authorizes ONLY on is_tenant_admin_as; owner-only would capture money then 42501 the install] + published-version (409) + scope-visibility (tenant-scope match; agency-scope 403 as unfulfillable under service role); (2) is_platform_owner() moved to the USER-scoped client; (3) paige-mcp paid-item gate honest-redirects to the signed-in Marketplace session (no 401, no lie, JWT gate intact). Plus subscription-priced fail-closed guard + !actorUserId webhook guard (crew optional-hardening). Additive migration (service_subscription_id stays NULL — one-time payment has no subscription, §13). §11 paid "Get for $X" CTA. Crew (SAME verifier+compliance that flagged the 3): SHIP on both — can_charge_without_admin=false, zero regressions, §9/§13/§2/§11/§36/§18 clean. §32 Layer B (prod BEGIN..ROLLBACK): admin+visible→ledger gross=2500/fee=500/net=2000; non-admin/wrong-tenant/invisible blocked; idempotent replay; 42501 defense-in-depth. §32 Layer A confirmed: migration 20260726004746 + 2 columns live; marketplace-checkout-session v2 verify_jwt=true deployed (byte-diff shows the gate live), stripe-webhook v4, paige-mcp v58; edge-live=6d3bc1e, drift empty. tsc 22/22, braces 0, gold clean. HONEST SCOPE (§13): INERT until a one_time paid item is seeded (all 18 free). Follow-ups: #463 (mcp service+actor §10, agency-scoped, subscription-priced — all fail-closed today), #457 (drift audit). |
| 464 | Money Spine Lane B-Platform (Tier-1 platform subscription CREATE leg) — PAY-BEFORE-WORKSPACE onboarding (SHIPPED, PR #234, squash 4b46e573). The missing "sign up on paigeagent.ai + pay to become a Paige customer" wire, built to owner ruling 2A: signup creates auth.users ONLY (no tenant); a PAID checkout.session.completed webhook PROVISIONS tenant + owner role + platform_subscription. §38 Tier-1 Paige-HELD rail, merchant-of-record CORRECT. NEW provision_tenant_as(_owner,_name,_account_type) — service-role explicit-owner twin of provision_tenant (which hard-blocks auth.uid()-less callers, throws 28000); idempotent per owner via tenants_one_toplevel_per_owner, grants tenant_members owner + sets profiles.active_tenant_id (+ fallback INSERT), service_role-only. NEW platform-subscription-checkout edge fn (verify_jwt=true): TWO paths — ONBOARDING (no tenant → metadata OMITS tenant_id → webhook provisions; Stripe idempotencyKey psub_onboard_<actor>_<plan>_<period> = double-charge guard) + GRANDFATHERED (existing tenant → is_tenant_admin_as 403 + already-subscribed 409 gates, metadata carries tenant_id); self-serve/$0 gate rejects Enterprise; inline recurring price_data (stripe_price_id NULL); §38 no transfer_data. stripe-webhook platform arm (discriminant platform_plan_slug, non-colliding vs tenant_price_id/marketplace_item_slug) branches on tenant_id presence: present→upsert+flip trial→active; absent→provision_tenant_as then upsert (idempotent, non-throwing, additive; basil-safe defensive period read on .updated; coexists with the concurrent dual-Stripe-account cutover edit). NEW §10 RPCs get_tenant_platform_subscription + is_current_user_tenant_admin. §2: neutralized seeded academy "broker shops"→"agencies". FRONTEND: Pricing.tsx killed disabled Waitlist CTAs → live Subscribe (Practice $149/Academy $397, Enterprise Contact Sales), plan-intent→signup, §11 gold-only-on-act; Auth.tsx post-signup auto-checkout on ?plan + honest paid-path subcopy; resolveLandingRoute no-tenant→/pricing; NEW Welcome.tsx post-payment poll page (webhook↔session race) with charged-but-unprovisioned support-recovery. Crew (design/build + adversarial verifier + compliance/design-critic via Workflow, TWICE — post-workspace then re-architected pay-before-workspace): both ITERATE→every finding fixed (§2 broker-shops BLOCKER, §11 gold, §13 dead-409, §32 basil period bug, double-charge key, Welcome recovery, honest subcopy, provision fallback). §32 Layer B (prod BEGIN..ROLLBACK): onboarding provision→tenant active + owner role + active_tenant_id; idempotent re-provision (no dup, count 1); sub upsert idempotent; no-profile fallback creates profiles+active_tenant. §32 Layer A CONFIRMED on prod: migration 20260726120000 in schema_migrations + provision_tenant_as/is_current_user_tenant_admin/get_tenant_platform_subscription exist + broker_shops=0; deploy-edge-functions run (4b46e57) success; platform-subscription-checkout v2 ACTIVE verify_jwt=true byte-matches repo + responds HTTP 401 live (gated); stripe-webhook deployed same run. tsc 22/22, Vercel build green. Supabase Preview red = pre-existing #275 (early bootstrap `profiles already exists` full-replay, not this change; prod applies incrementally). HONEST SCOPE (§13): ships INERT until a real card runs through Stripe (0 subscriptions); producer uses legacy STRIPE_SECRET_KEY, webhook accepts legacy+V2 sigs. LIVE end-to-end signup+charge test = OWNER-run (no Chrome MCP this session to drive the browser). Follow-ups: LIVE charge test (owner) · edge-live tag didn't advance post-4b46e573 despite successful deploy (/edge-drift misreports — drift-tooling, deployment itself proven) · plan→account_type mapping (webhook hardcodes 'standalone'; academy may warrant 'agency') · agreement-capture note (provision_tenant_as omits legal gate; signup records legal_acceptances) · Pricing §25 rendered-screenshot review owed (renderable, no Chrome MCP here) · dead 'already_provisioned' client branch (harmless). |
| 466 | Money Spine Lane B-Platform-v2 (#68) — plan rename + 14-day trial + super-admin invite generator + landing plan cards + copy audit (SHIPPED, PR #236, squash d3467ed). Extends the merged B-Platform pay-before-workspace slice per six owner-locked directives (2026-07-26). (1) PLAN RENAME — DB seed migration UPDATEs practice→solo/"Solo" ($149), academy→agency/"Agency" ($397), Enterprise unchanged; every FE reference (Pricing.tsx PLAN_COPY/FALLBACK_PLANS keys, PricingSection.tsx businessTiers, PaigeHome PLANS) renamed so nothing lingers as "Practice"/"Academy". §2 de-finance: agency description → "For practices and agencies…", credit_pulls_per_month stripped from solo/agency metered_addons (`metered_addons - 'credit_pulls_per_month'`), broker-shops copy neutralized. (2) LANDING PLAN CARDS — PaigeHome.tsx PLANS carry slug/cadence/cta; card CTAs are REAL Subscribe acts (handleSubscribe → /auth?mode=signup&plan=<slug>; Enterprise → mailto sales), flow STARTS from the homepage, no scroll-to-pricing indirection. (3) EVERY BUTTON LIVE — audited landing + /pricing CTAs; false "day one" claim rewritten trial-honest, `#pricing` anchor wired, no dead CTAs. (4) 14-DAY TRIAL — platform-subscription-checkout producer sets `subscription_data: { metadata, trial_period_days: trialDays }` (trialDays = inviteTrialDays ?? 14; Enterprise gated out via self-serve/$0 guard), card upfront; stripe-webhook accepts `payment_status === "paid" || "no_payment_required"` and writes platformStatus = no_payment_required ? "trialing" : "active" so trial-start provisions once and customer.subscription.updated flips trialing→active with no double-provision. (5) SUPER-ADMIN 30-DAY INVITE GENERATOR — NEW paige_invite_tokens table (token PK, plan_slug, trial_period_days default 30, created_by, expires_at, consumed_at/by/sub) + RLS (super_admin insert/read/revoke own); 5 SECURITY DEFINER RPCs: create_platform_invite (is_super_admin gate→42501) · get_platform_invite (leak-safe, anon+authenticated, returns slug+trial+valid only) · consume_platform_invite (service_role, idempotent per stripe_subscription_id) · revoke_platform_invite · list_platform_invites. NEW /admin/platform/invites super-admin UI (PlatformInvites.tsx, generates paigeagent.ai/get-started?invite=<token>) + NEW public /get-started (GetStarted.tsx) consume page → /auth?mode=signup&plan=<slug>&invite=<token>; checkout derives planSlug + trial authoritatively from get_platform_invite (never client-trusted), idempotency suffix `_inv_<token16>`; webhook consumes the invite on provision. (6) COPY AUDIT — landing to the avatar (client-based service businesses: coaches/consultants/agencies/thought leaders/advisors; Enterprise adds holding-company/multi-brand/IP-portfolio operators); H1 "Paige runs your practice." → "Paige runs your business.", hero eyebrow "Give coaches back their time." → "Gives you back your time.", proof heading → "Businesses that hired Paige's team.", §3 mogul-founder voice, zero "practice"/"academy" residue. Crew (§1/§5 via Workflow: build engineer + copywriter + adversarial verifier + compliance officer + design critic): ITERATE→every blocker fixed (dead invite scalar-read, trial-copy contradiction reconciled to honest "Start free trial", §2 finance vocab, plan-rename FE completeness). §37 producer+consumer inventory clean; §32 Layer B (prod BEGIN..ROLLBACK) zero exceptions. §32 Layer A CONFIRMED on prod (ref xygzykjyynhzqytbqnzu): migration 20260726140000 in schema_migrations (mig_row=1), paige_invite_tokens exists, plan_slugs="agency=Agency, solo=Solo", create_platform_invite + consume_platform_invite exist, credit_vocab_remaining=0; platform-subscription-checkout v4 ACTIVE (trial + invite block byte-present), stripe-webhook v6 ACTIVE (platformStatus/no_payment_required/consume_platform_invite byte-present). tsc ratchet 22→22, eslint 0, gold-discipline clean. Supabase Preview red = pre-existing #275 (early bootstrap full-replay, not this change; prod applies incrementally). HONEST SCOPE (§13): ships live but the paths stay INERT until a real card / real invite runs through Stripe; LIVE incognito end-to-end test (landing card→signup→checkout→/welcome→14d trial in Setup>Billing; + super-admin invite→/get-started→signup→checkout) = OWNER-run (no Chrome MCP this session). Follow-ups: owner live test · plan→account_type mapping (webhook hardcodes 'standalone'; agency may warrant 'agency', carried from #464) · edge-live tag lag (deploy proven via get_edge_function byte-check). |
| 467 | Copy Audit v2 (expanded) — §2 sweep of the platform-default PUBLIC surfaces + the DB legal documents users legally ACCEPT at signup, to unblock the Twilio A2P/10DLC submission (#73). Owner locked Option 1 (2026-07-26); review gate = Antonio (paralegal), pre-launch; outside counsel post-launch (not gated on #209). SHIPPED PR #238 (squash 9422aed) + the DB publish applied live on Antonio's greenlight. THE CORE FIX (Codex-surfaced, verified): signup's consent checkbox links to /legal/terms + /legal/privacy which render from the DB `legal_documents` table (LegalDoc → is_current=true via .maybeSingle()), NOT the /privacy + /terms component pages — so rewriting only the components didn't touch the canonical accept-record, which still carried a GLBA section, "fundability scoring / credit intelligence dashboards", and credit/funding disclaimers. NEW migration 20260726180000 inserts §2-clean version-2 rows for terms+privacy with is_current=FALSE (invisible to the live render — STAGED, zero live impact on apply; ON CONFLICT (slug,version) DO NOTHING = idempotent so the CI merge-apply is a clean no-op). Publish = a reversible is_current flip (v1→false, v2→true, one txn) documented in the migration header and kept OUT of supabase/migrations so CI never auto-publishes; Antonio greenlit and the flip was applied via MCP. Both DB bodies de-financed (GLBA section removed; fundability/credit-intelligence → client-management SaaS terms; credit/funding disclaimers generalized) referencing Dubsado/HoneyBook/Bonsai/HubSpot SaaS patterns; BOTH summaries de-financed (privacy summary dropped "GLBA-aligned financial data handling" — it renders as the subtitle + on the /legal index, a reviewer-caught blocker the component-only rewrite would have missed). Compliance-officer gap-closers folded: payment-info collection bullet, refund reference softened to "as described at checkout or in your plan terms". PUBLIC SURFACES (same PR): Privacy.tsx + Terms.tsx rewritten §2-clean (financial-intelligence identity, GLBA/FCRA/CROA, funding marketplace, A.C.C.E.L./B.U.I.L.D. all removed; required disclosures preserved incl. Google Limited Use + CCPA + arbitration; "Last updated" corrected to the real rewrite date); Header.tsx + Footer.tsx dropped legacy Brokers/Partners/Affiliates/Broker-Program links (routes stay). SMS opt-in added to ALL FOUR surfaces (component /privacy + /terms + both DB docs) using the owner-mandated VERBATIM A2P string with the TCPA/CTIA line — byte-consistent across all four for TCR cross-check (owner resolved the "consent during signup" wording by keeping it verbatim to match the registered campaign). Funding-gated APP surfaces (dashboards, credit/funding tools, /broker + /affiliates page bodies) UNTOUCHED per §2 opt-in; scanner found 0 remaining finance leaks on public surfaces. Crew §1/§5 — TWO crews (public-page + DB-legaldoc), each copywriter + scanner + adversarial verifier + compliance officer + design critic; reviewers caught the summary-column §2 leak and the SMS-verbatim requirement, both folded by the integrator. §32 Layer B (prod BEGIN..ROLLBACK) then Layer A PERSISTED on prod (ref xygzykjyynhzqytbqnzu): after the flip, privacy v2 + terms v2 is_current=TRUE, has_finance_residue=FALSE, has_owner_sms=TRUE; v1 rows is_current=FALSE (retired, grandfathered — §13); exactly ONE current row per slug. Local gates: tsc 22/22, eslint 0, gold-discipline clean, regression-lint clean, build ✓. Supabase Preview skipped/non-blocking. Grandfathering documented (§13): near-zero live tenants; v1 accepters grandfathered, new signups accept v2; forced re-acceptance for dev/test tenants is a separate owner call. HONEST caveat (§13/§25): no Chrome MCP this session — the rendered /legal/* walk is Cowork's + the owner's cross-check; DB state + render query proven. Follow-ups: #468 (legacy Paige logo/brand-mark sweep across /legal/* + platform-wide — owner-flagged during review, NON-blocking, ships after this archive) · canonical legal-entity name to confirm ("Paige Agent AI LLC" spaced in the verbatim SMS vs "PaigeAgent AI LLC" in prose) · #209 (DB legal_documents deeper de-credit) now largely satisfied for terms/privacy. Antonio submits the pending Twilio A2P Campaign form immediately after. |
| 489 | Intelligence trace itemization — working_context_tenant_id (data + display). Owner #489: operator Intelligence attributed each paige_llm_trace row honestly per-row, but an agency-scope session working across a sub-account showed only the agency. Additive fix in 2 layers: DATA — nullable soft-ref column paige_llm_trace.working_context_tenant_id (mig 20260727200000); llm-trace.ts writer field (optional on TraceCtx+TraceRow, §37 all-8-producer-classes additive-safe); paige-ai-chat resolves the caller's OWN active_tenant_id via service client keyed by authenticated user.id (§9 server-derived). DISPLAY — operator_intelligence_trace_tail DROP+recreate adding working_context_label (CASE … IS DISTINCT FROM tenant_id, null-safe; mig 20260727210000); PlatformIntelligence "Working on: <sub-account>" line (no gold, §11). Build+verify crew SHIP×2. §32 PROVEN LIVE on prod: both migrations in schema_migrations, column + 18-col RPC confirmed, edge fns redeployed, db-live/edge-live at merge, zero drift. Live "Working on:" pixel render owed to a browser-capable session (headless). (PR #254, squash 7d9db81) |
| 486 | GATE #92 — §9 confirmatory count/attribution/aggregation audit (before Sub-slice B). Read-only crew (4 classes: DEFINER RPCs · edge rollups · frontend KPI · agency/sub-account boundary → synthesis). VERDICT: the #479-EXACT child-reads-parent-subtree pattern is confirmed ABSENT platform-wide — every DEFINER RPC, frontend KPI, agency-boundary surface, and the just-shipped operator-nav + Intelligence surfaces (#484/#485/#489) is SCOPED-OK by design (current_user_tenant_id() keystone; agency subtree rollups parent→child-gated; operator surfaces is_platform_admin()-gated). One frontend AT-RISK (ClientManagementDashboard profiles/user_roles headcount) maps to already-tracked #481. The edge-rollups class (re-run after a stub) CAUGHT 3 genuine cross-tenant leaks → fixed in #491. GATE cleared. |
| 491 | §9 SECURITY (GATE #92 catch) — closed 3 cross-tenant leaks. paige-ai-chat CRM operator tool block queried the RLS-bypassing service-role admin client with NO tenant filter (reachable by any tenant admin/coach): 5 reads + IDOR (crm_get_contact_summary) + 3 writes (update_pipeline_stage/assign_coach/create_task) all scoped to server-derived personaCtx.tenant_id; fail-closed guard narrowed to CRM_SERVICE_TOOLS (so RPC-based presence_*/program_*/n8n_* — some intentionally platform-wide for a null-tenant owner — are unaffected). paige-mcp: get_coach_performance body-supplied coach_user_id IDOR → server-derive tenant + tenant_members active-membership check (coach_not_in_tenant); list_workflow_runs fleet-wide `total` → tenant-scoped page count. Diagnose→fix→verify crew SHIP; guard-narrowing + write-scoping folded from verifier should_fix (§37 no half-hardening). Follow-ups: crm_log_activity communication_log per-row tenant verify; tasks.tenant_id backfill (task counts read 0 until then). Edge deploy via CI on merge. Live CRM behavior verify owed to a browser/JWT session. (PR #255, squash 708123c) |
| 476 | paige-mcp OAuth — RFC 8414 discovery canonicalization + consent role-based scope fix (fixes #210). Owner-confirmed topology: issuer = https://paigeagent.ai (app origin), token/register/revoke on the supabase edge fn (RFC 8414 permits AS endpoints off the issuer origin; the RFC 7591 DCR endpoint already existed — hardened, not rebuilt). (1) DISCOVERY canonicalization: paige-mcp DISCOVERY_AS.issuer + DISCOVERY_RESOURCE.authorization_servers PUBLIC_ORIGIN→APP_ORIGIN; the 2 static public/.well-known/* files brought UP to the edge (append self.read/write/chat → 14 scopes, issuer/authorization_servers already = paigeagent.ai) so a spec-correct client sees issuer==fetch-origin and the cross-origin rejection dies. (2) CONSENT zero-scope fix (the actual #210 break): paige-mcp-consent computeGrantedScopes detected platform owner ONLY by email==owner_email, so a super_admin whose email≠owner_email fell through the fail-closed return [] and got invalid_scope after registration; now elevates on EITHER a server-derived super_admin role in user_roles (keyed to JWT userId, never client input, fails CLOSED + logs on DB error, §9) OR owner_email — every other path (tenant owner/admin via get_user_primary_tenant, client via clients.linked_user_id, final return []) untouched, no over-grant. McpAuthorize.tsx added a signed-in-but-zero-scope guard (clear message instead of an Allow that 400s). Crew: security engineer → adversarial OAuth verifier + compliance officer, both SHIP zero blockers; all 3 should-fixes folded (risk-label honesty, stale consent header comment, headless byte-compare). §37: McpAuthorize is the sole HTTP consumer of paige-mcp-consent, response contract unchanged (only which tier fires). Migrations NONE (role helpers/user_roles/tenant_members/app_settings_owner all pre-exist). Local gates: tsc 22/22 no-new · regression clean · eslint 0 · build ✓; both static .well-known valid JSON 14 scopes. Edge fns (paige-mcp, paige-mcp-consent) redeploy via deploy-edge-functions.yml on merge; static well-known served by Vercel. §32 OWED to a browser-capable session: strict external MCP client completing discovery against deployed surfaces + a real super_admin (email≠owner_email) landing on tier=platform_owner not invalid_scope + post-deploy byte-compare of all 4 discovery bodies. (PR #259, squash f407c8f) |
