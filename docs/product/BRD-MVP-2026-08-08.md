# Business Requirements Document — Paige Agent AI MVP

**Date:** 2026-08-08 · **Status:** Draft v1 for owner review · **Owner:** Antonio

> Synthesized by a research → draft → verify crew (§1/§14), grounded in CLAUDE.md doctrine, the architecture/roadmap docs, and the **owner-ruled MVP definition (2026-08-08)**. Every non-owner claim cites its source. Blocks marked **OWNER INPUT REQUIRED** are owner-owed decisions the crew did not invent (§13). Build-status caveats (§32) mark where an owner-locked MVP *intent* is not yet shipped in code.

---

## 1. Executive Summary & MVP Definition

Paige Agent AI is launching an agent-run operating system for client-based service businesses — an intelligent, two-way client portal that reasons, proposes, and acts on both sides of the relationship: it works for the business owner (pipeline, follow-ups, content, at-risk triage) and for each of their clients (onboarding, answers, nurture, a hyper-personalized portal under the owner's own brand), with one orchestrating brain — Paige — coordinating a standing team of specialist agents (CLAUDE.md §7, §8, §14). At MVP the platform ships for three tenant types at once — **Coach, Consultant, and Digital Marketing Agency** (owner ruling 2026-08-08) — across three subscription tiers (Solo, Agency, Enterprise-as-placeholder), monetized from public launch but gated behind an **invite-only beta first** to catch bugs against a general-public quality bar (owner ruling 2026-08-08).

**MVP scope statement.** The MVP delivers a single-setup, then-autonomous core loop: Paige and her six named VP agents handle client communications (appointment setting, follow-ups, reminders, planning-document creation) over email and SMS, produce initial content (social + ads), coordinate the work across the agentic team, present a Paige-driven customer portal to each tenant's clients (the owner ruling's wording was "AI-driven"; per §3 the platform brands this as Paige-/agent-driven, not "AI-X"), and expose the Marketplace of installable capabilities (owner ruling 2026-08-08). The launch-defining differentiator is the **Owner Trilogy** (Systems Check, Business Vault, Twin Capabilities, Live Competitive Analysis, Newswire, and audit-trail — all pillars at MVP), whose "feeling" must carry through to the tenant's own client portal (owner ruling 2026-08-08; docs/strategy/owner-trilogy-2026-07-26.md). The entire experience is held to a **non-technical UX bar** — a solopreneur through an enterprise operator should feel like they are talking to Paige and managing a team, never operating a developer tool (CLAUDE.md §36; owner ruling 2026-08-08). Super Admin (Paige-on-Paige, Wave S3 operator-scope Trilogy) must be fully functional so the owner runs the platform through the same Paige experience tenants get (owner ruling 2026-08-08).

> **HONEST BUILD CAVEAT (§13, §32).** The word "autonomous" in the core use case is a stated MVP intent, not fully shipped runtime behavior today. The action bus exists as rails (`paige_actions` state machine with `autonomy_lane` = auto/confirm/off) but **has no autonomous worker consuming the queue** — every transition is currently a human-in-chat tool call (docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §3, Ranked priority ②). Building the cron-gated autonomous worker, and giving client-facing Paige a send-free tool surface, are named prerequisites for the "portal that acts" claim. These are MUST-HAVE, launch-blocking work (see §4).

---

## 2. Target Personas

All three personas launch at MVP (owner ruling 2026-08-08). Paige's platform layer stays coaching-generic and inclusive — the audience is coaches, consultants, agencies, thought leaders, and advisors, and shared copy prefers *practice · business · clients · work · team* over narrow "coaching" language (CLAUDE.md §2). No persona below is served a finance/credit default; funding/credit is only ever a per-tenant opt-in Blueprint a tenant chooses (CLAUDE.md §2; docs/doctrine/paige-practice-blueprints-2026-07-29.md).

### Persona A — The Coach

A solo or small-team coach who sells outcomes and manages a book of clients through onboarding, sessions, follow-ups, and retention.

- **Pain: client comms eat the day.** Reminders, follow-ups, and appointment-setting are manual and get dropped. Paige runs client communications and scheduling from her standing team, giving the coach back their time (CLAUDE.md §3 voice, §7, §8; core use case, owner ruling 2026-08-08). *Grounded capability:* email (Resend), SMS (Twilio, STOP-compliant), and calendar/booking (`paige_bookings`, public `/book/:slug`, Google + Cal.com) are all built (docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §1).
- **Pain: onboarding a new client is repetitive paperwork.** Client signs up, gets a confirmation, signs an agreement — every time. Paige carries the signup + agreement flow: signature capture, PDF generation, and a signed-agreement record already ship (`paige_signed_agreements`, `finalize-agreement` edge function; supabase/functions/finalize-agreement/index.ts).
- **Pain: no time to create content or plan.** Paige drafts initial social + ads content and creates planning documents so the coach reviews finished work rather than facing a blank page (draft-first, CLAUDE.md §36; core use case). *See §4 status note — social channel posting is an ungrounded gap.*
- **Pain: the client experience looks generic.** The coach wants a portal that feels like *their* app, not ours. The tenant's logo and brand override the Paige brand in the client's view (`get_client_portal_brand`, `useClientPortalBrand.ts`; CLAUDE.md §6).

### Persona B — The Consultant

An advisor or independent professional (or a thought leader running a practice) who manages retainer clients, deliverables, and a pipeline of prospects.

- **Pain: pipeline and follow-up discipline.** Deals slip because nobody chases them. CRM/contacts is the platform's most mature domain (`clients`, ~25-panel ContactDetail, `create_contact`/`update_contact`) and sales pipeline is built (`deals`, `pipeline_stages`, stage-based automation via `stage_automation_rules`) (docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §1). Paige surfaces what each client needs and drafts the next move (CLAUDE.md §7).
- **Pain: professional client-facing communication under their own identity.** Consultants need mail that comes from *their* domain, not a shared sender. Per-tenant sending identities (`resolve_tenant_sender`) and email templates ship (docs/architecture/tenant-domain-and-communications-spine.md).
- **Pain: proving value and staying ahead.** The Owner Trilogy's Business Vault (obligation tracking — insurance, LLC filings, domains, renewals) and Owner Analytics + Competitive Intelligence give a consultant an operator's-eye view of their own practice (docs/strategy/owner-trilogy-2026-07-26.md). *Status: Trilogy pillar builds are PLANNED, not yet on main — see §4.*
- **Pain: too technical to bother.** Consultants are not prompt engineers. The non-technical UX mandate means any capability is discoverable within 5 minutes and Paige actively helps set things up rather than only diagnosing gaps (CLAUDE.md §36; owner ruling 2026-08-08).

### Persona C — The Digital Marketing Agency

A parent tenant running a book of business — multiple sub-accounts (their own clients/brands) under one roof.

- **Pain: managing many client accounts at once.** The Agency account is the platform's compounding unit; it owns sub-accounts with structural isolation — a sub-account is never an agency, enforced at the database layer (CLAUDE.md §51 ABSOLUTE INVARIANT; §17 growth atom). Sub-account provisioning ships (`create_subaccount`, `switch_into_subaccount`, `list_subaccounts` MCP tools).
- **Pain: producing content and campaigns at volume.** Agencies live on landing pages, funnels, forms, and creative. Vibe Studio ships one conversational entry that classifies a brief and drafts pages/forms/funnels/images (docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §2). *Gap: funnels are v1, the in-session campaign engine is a stub, and the visual-critique loop is code-complete but gated off in prod.*
- **Pain: brand consistency across every client's portal.** An agency needs each client's portal to carry that client's brand. Brand cascades to sub-accounts via `resolve_tenant_brand`'s recursive parent walk, so a sub-account inherits the agency brand (docs/doctrine/tier-matrix.md).
- **Pain: power-user reach without technical overhead.** Agencies may be more savvy, but the baseline UX stays non-technical; automations are wired through the n8n bridge behind the scenes (`paige_workflow_registry`; owner ruling 2026-08-08; CLAUDE.md §36).

> **OWNER INPUT REQUIRED** — Whether any of the three personas' MVP journeys assume **native social-channel connection/posting** or **native Zapier**. Neither is grounded as built: social connectors/posting appear in no inventory, callable-seams, or architecture doc; Zapier appears only as an external MMA-ecosystem rail, while **n8n** is the grounded native automation bridge (docs/architecture/ECOSYSTEM_DATA_OWNERSHIP_MAP.md). Confirm scope before a persona story promises either.

---

## 3. Core End-to-End Use Case

The stories below express the owner-ruled core loop (owner ruling 2026-08-08). Each is written as the persona would experience it. Acceptance criteria are gated by §13 honesty: where a capability is rails-only or unbuilt today, the criterion states the required end-state and is cross-referenced to the §4 build status.

### Story 3.1 — Single setup, then it runs

**As a** coach/consultant/agency owner, **I want** to complete one guided setup and then have Paige run the operation, **so that** I stop hand-running my client operations.

Acceptance criteria:
- On first login, Paige runs a **Systems Check** that surfaces setup gaps **one at a time, prioritized by importance, and actively helps configure each** (not just instructions) — comms senders, website, social accounts, automation wiring, company info, CRM contacts, pipeline, revenue tracking, payment processors, and payment methods (owner-locked Systems Check MVP, owner ruling 2026-08-08).
- Systems Check runs at agency, solo, and sub-account levels (owner ruling 2026-08-08; §51 per-tier requirement).
- Setup writes through callable seams (RPC/edge/config-as-data) so Paige can drive it from chat, not only a human clicking (CLAUDE.md §10).
- *Status honesty:* Systems Check is defined as a 30-check catalog (docs/strategy/owner-trilogy-2026-07-26.md) but is **PLANNED (#80), not yet built on main** (docs/doctrine/canonical-build-order.md). MUST-HAVE, launch-blocking.

### Story 3.2 — Autonomous client communications

**As a** tenant owner, **I want** Paige to set appointments, send follow-ups and reminders, and create planning documents without me driving each step, **so that** clients are cared for while I focus on delivery.

Acceptance criteria:
- Paige books/reschedules against the tenant's calendar (`paige_bookings`, Google + Cal.com — **built**, docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §1).
- Follow-ups and reminders fire on schedule via stage-based automation (`stage_automation_rules` — **built**).
- Actions honor the autonomy tier per action kind: 🟢 auto performed-and-logged, 🟡 draft-to-approval, 🔴 human-briefed (CLAUDE.md §16; `autonomy_lane` enum).
- Every autonomous action is reported honestly — a queued or drafted action is never reported as delivered (CLAUDE.md §13).
- *Status honesty:* true autonomy requires the **cron-gated action-bus worker**, which is **not built** (docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §3, Ranked priority ②). MUST-HAVE, launch-blocking.

### Story 3.3 — Email and SMS

**As a** tenant owner, **I want** Paige to communicate with clients over email and SMS under my identity, **so that** outreach looks like it comes from my business.

Acceptance criteria:
- Email sends via the tenant's own sending identity/domain (`resolve_tenant_sender`, `send_composed_email`, email templates — **built**).
- SMS sends via Twilio with STOP compliance (`send_sms` — **built**), *with the honest limit that only one shared Twilio number + one global A2P flag exist today; per-tenant telephony and phone/calling are unbuilt/deferred* (docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §1; docs/architecture/platform-operator-tenant-200.md).
- Message content passes the §3 voice gate at the model-router boundary (banned: "AI-powered", "seamless", "streamline", "empower"; docs/PAIGE-CALLABLE-SEAMS.md).

### Story 3.4 — Initial content creation (social + ads)

**As a** tenant owner, **I want** Paige to produce initial social posts and ad creative I can review and approve, **so that** I show up consistently without staffing a content team.

Acceptance criteria:
- Paige drafts copy and generates images through the model-router seams (`generate-image`, `callModel`; Vibe Studio generation routes — **built** for pages/forms/funnels/images, docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §2).
- Drafts are presented draft-first for one-click approval (CLAUDE.md §36).
- Content stays coaching-generic unless the tenant opted into a vertical Blueprint (CLAUDE.md §2).

> **OWNER INPUT REQUIRED** — **Social-channel connection and posting/scheduling is not grounded as built** in any inventory, seams, or architecture doc. "Initial content creation (social)" can be delivered today as *drafted creative/copy* handed to the tenant, but **publishing to social channels has no shipped connector**. Confirm whether MVP scope is draft-only or includes native posting (which would be net-new build).

### Story 3.5 — The agentic team coordinating autonomously

**As a** tenant owner, **I want** Paige to run a team of specialist agents that coordinate with each other, **so that** I am managing a team, not operating a tool.

Acceptance criteria:
- Paige (orchestrator) plus **six VPs — VERA, NEXUS, CURA, MENTOR, MERIT, ZION** are present and attributed in the product (docs/doctrine/paige-c-suite-roster.md; shipped VP surfacing #243–#247 on main). Dynamic specialist spin-up per CLAUDE.md §14.
- Cross-team coordination follows the router pattern: classify task → route to the owning department/VP → apply autonomy tier → execute/draft/brief (CLAUDE.md §16).
- Team dispatch and status are a **chat act, streamed in the transcript** — never a separate "agent manager" tab (CLAUDE.md §20).
- Agents are distributed across UI surfaces (not bunched in one chat), with Paige reachable from chat to all of them (owner ruling 2026-08-08).

> **OWNER INPUT REQUIRED (a)** — **Agent placement across UI surfaces:** which surface hosts each of the six VPs. Owner-owed; the crew must not invent it.
> **VP remit conflict (must resolve):** the shipped code (`PaigeAttribution.tsx` VP_ROSTER, `vpDepartments.ts`) and the roster doctrine doc assign *different* remits to VERA/NEXUS/CURA/MENTOR/MERIT/ZION. Code is the current shipped source of truth. Any BRD line naming a VP's responsibility must resolve which is authoritative — **OWNER INPUT REQUIRED**.

### Story 3.6 — The Paige-driven customer portal

**As a** tenant owner, **I want** to give each of my clients a portal that feels like my own app and carries the Owner-Trilogy feeling, **so that** my clients experience my brand, not Paige's.

Acceptance criteria:
- The portal is **mobile-first** for clients (the operator platform stays desktop-first) (owner ruling 2026-08-08).
- The coach can skin/snapshot the portal per client: company colors (brand tokens), **their logo overriding the Paige logo everywhere in the client's view**, custom tabs, custom menu, pre-set (unlocked) templates, knowledge base + tabs + references, and client document uploads (owner ruling 2026-08-08).
- Logo/brand override is enforced: `get_client_portal_brand` returns the tenant logo/color; chrome renders a neutral placeholder while loading to avoid flashing the Paige brand (src/hooks/useClientPortalBrand.ts). The portal-config overlay (`set_tenant_portal_config` / `get_client_portal_config`) controls which tabs a client sees and their order (supabase/migrations/20260713020000_portal_config.sql).
- Client signup flow: coach onboards client → client signs up → confirmation → agreement signed. Legal scaffolding **exists** (`paige_signed_agreements`, `agreement_templates`, `finalize-agreement`).

> **HONEST GAPS (§13).** (1) The **shipped** portal taxonomy (`moduleNav.ts` overlay) is *subtractive/reorder over a fixed Playbook catalog only* — it cannot yet add net-new custom tabs or a blocks/widgets canvas; the richer "unlimited custom tabs + blocks" model is the CX-2 **plan**, not built (docs/strategy/client-experience-workstream-2026-07-21.md §3). (2) Deeper portal **intelligence** (at-risk, sentiment, memory-driven history, CURA client-facing Paige) is **explicitly not wired**; client-facing Paige is currently tool-less/one-way (docs/DONE.md; docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md §3). (3) The funding/BTF-flavored agreement wording and `plan_selected` labels must be confirmed exposed coaching-generically, not as a platform default (§2).
> **OWNER INPUT REQUIRED (d)** — the **customer-portal Owner-Trilogy taxonomy/matrix**: owner states it already exists; the crew has located the shipped portal-config + brand-override substrate and the CX-2 plan, but must reference the owner's canonical taxonomy rather than invent one. Also owner-owed: confirm the **holding-company + subsidiary structure inside a client's portal** (no distinct primitive found beyond the agency→sub-account model).

### Story 3.7 — Marketplace

**As a** tenant owner, **I want** to browse and install capabilities from a Marketplace, **so that** I extend my Paige without a developer.

Acceptance criteria:
- Marketplace is a first-class OS surface (CLAUDE.md §35), tenant-scoped via `marketplace_catalog_for_tenant`, with browse/install/uninstall/recommend seams (`marketplace_*` MCP tools; docs/architecture/MARKETPLACE-DATA-MODEL.md).
- Visibility cascades by tier (Enterprise ≥ Agency ≥ Solo; fail-closed to Solo) (src/lib/__tests__/marketplaceTierSubstrate.test.ts). *Code-lag note (§13): the cascade element is stored today as `Academy` in code (`available_to_tiers`, `current_tenant_tier()`, `_mp_tier_cascade_keys()`); the taxonomy rename to `Agency` is an owed follow-up — see §5.*
- Slices 0–2 (tier/role/publish substrate; agency-curation allowlist; tenant-side curation visibility) are **shipped to main** (#277, PRs #382/#391/#392). **Persona L1** (voice-first default prompt) is **shipped** (#384).
- **Per-sub-account curation is MVP scope (owner ruling 2026-08-08).** Today Slice 1 curation is all-or-nothing — an item is either "available to sub-accounts" or not. MVP requires the agency to pick **WHICH sub-accounts get WHICH marketplace items** (per-sub-account targeting, not a single agency-wide allowlist). This ships as **its own PR after the tier-rename reversal**, but it **is** MVP scope, not post-launch (§4 MUST-HAVE). Tier posture (§51): the agency curates only within its own book of sub-accounts; a sub-account never sees another sub-account's or the parent's private curation.

> **OWNER INPUT REQUIRED (c)** — the **specific Marketplace items available at launch**. The substrate ships, but no doc enumerates the launch catalog. Also confirm whether a Marketplace "Slice 3" is scoped (Slice-3 commits found belong to Practice Blueprints, not #277).

### Story 3.8 — Paige AI Chat: durable memory + real tasking (MVP)

**As a** tenant owner (or their client), **I want** my conversations with Paige to persist and stay coherent over time, and for the things I ask her to track to become real tasks, **so that** Paige feels like a continuous team member, not a chat that forgets.

Three owner-ruled MVP requirements (owner ruling 2026-08-08; §7 intelligent portal, §9 tenant isolation, §13 honest, §46). *Verified current state on prod before asserting (§13):* both `paige_conversations` and `paige_chat_threads` **exist**.

Acceptance criteria:
- **(a) Conversation COMPACTION.** When a thread grows past the model context/cost window, Paige compacts it into an **intelligent summary that preserves the relevant context** (not a hard truncation) — the same discipline as Claude Code's session-summary hook — and **BOTH the compaction and the full transcript persist**. *Status (§13/§32):* a **rolling-summary watermark** (`paige_chat_threads.summary_through_seq`) + a session-summarizer prompt exist in `paige-ai-chat` over a most-recent-50-turn window; but the summarizer prompt is currently **credit/funding-flavored** (a §2 flag — must be made coaching-generic) and the "relevance-preserving + both-persist" bar is **not verified end-to-end**. MVP work: generalize + §2-clean the summarizer, and prove both artifacts persist.
- **(b) PERSISTENT conversation history.** History is **durable and keyed to user + tenant, never to a session id** — the user returns to a prior thread and continues it. *Status:* multi-chat persistence **EXISTS** for the "Your Paige" owner workspace (`paige_chat_threads` + `paige_chat_turns`, RLS + create/append RPCs, owner-list index, sliding retention window; migrations `20260711201150`, tenant isolation `20260713024606`). MVP work: verify user+tenant keying (not session) across **all** chat surfaces including the client portal (§9/§51).
- **(c) PERSISTENT TASKING.** When the user asks Paige to track something, she creates a **REAL durable task in the tenant task system**, **auto-linked back to the source conversation** — not a chat mention that evaporates. *Status (§13):* **not confirmed wired** this pass — needs a durable task row + a conversation→task link, verified end-to-end (§32). MVP build.

> **HONEST NOTE (§13).** (a) and (b) have real substrate on prod today (summary watermark, persistent threads); (c) is unverified and likely net-new. All three are MUST-HAVE MVP (see §4). §46 is cited by the owner ruling as the governing chat/memory doctrine; the crew did **not** locate a numbered §46 in `CLAUDE.md` this pass — **OWNER-OWED** doctrine-file reconciliation (the ruling stands; the §-paste is owed).

---

## 4. Feature List

Status legend (from roadmap research, grounded to git main / docs as of 2026-08-08): **Shipped** = on main; **In-flight** = actively building; **Planned** = scoped, no build commits; **Rails-only** = substrate exists but no runtime driver; **Gated-off** = code-complete but disabled in prod. MUST-HAVE = launch-blocking (each row tagged beta-blocking vs public-launch-blocking — e.g. **SOC 2 and live revenue reconciliation are PUBLIC-launch gates, not beta**, per §10/§11); SHOULD-HAVE = post-launch. Enterprise-depth features are SHOULD-HAVE because Enterprise is a placeholder at MVP (owner ruling 2026-08-08).

### MUST-HAVE (launch-blocking)

| Feature | Status | Source |
|---|---|---|
| Email send under per-tenant identity (Resend, templates, sender resolution) | **Shipped** | PLATFORM-FUNCTION-INVENTORY §1 |
| SMS send (Twilio, STOP compliance) — shared number only | **Shipped (limited)** | PLATFORM-FUNCTION-INVENTORY §1 |
| Calendar / booking (`paige_bookings`, Google + Cal.com, `/book/:slug`) | **Shipped** | PLATFORM-FUNCTION-INVENTORY §1 |
| CRM / contacts (clients, ContactDetail, contact seams) | **Shipped** | PLATFORM-FUNCTION-INVENTORY §1 |
| Sales pipeline + stage automation (`deals`, `pipeline_stages`, `stage_automation_rules`) | **Shipped** | PLATFORM-FUNCTION-INVENTORY §1 |
| Vibe Studio generation (pages/forms/funnels/images, one conversational entry) | **Shipped** (funnels v1) | PLATFORM-FUNCTION-INVENTORY §2 |
| Client signup + agreement-signing legal scaffold (`paige_signed_agreements`, `finalize-agreement`) | **Shipped** | supabase/functions/finalize-agreement |
| Customer portal gateway + branded entry + logo/brand override | **Shipped (chrome/config)** | docs/DONE.md; useClientPortalBrand.ts |
| Portal tab config overlay (subtractive/reorder) | **Shipped** | migration 20260713020000_portal_config.sql |
| Portal full customization — unlimited custom tabs + custom menu + editable preset templates + client document uploads | **Planned (CX-2), owner-locked MVP** — the shipped overlay is subtractive-only; the "unlimited/editable" model is net-new build (§32) | owner ruling 2026-08-08; client-experience-workstream-2026-07-21.md §3 |
| Marketplace substrate + tier cascade (Slices 0–2) | **Shipped** | git main #382/#391/#392 |
| **Per-sub-account Marketplace curation** — agency picks WHICH sub-accounts get WHICH marketplace items (today Slice 1 is all-or-nothing "available to sub-accounts"; MVP requires per-sub-account targeting) | **Planned — own PR after this reversal; IS MVP scope** (owner ruling 2026-08-08) | git main #391 (Slice 1, all-or-nothing today) |
| Persona L1 (voice-first default prompt) | **Shipped** | git main #384 |
| **Promotional account type** — Solo + Agency variants only (no Enterprise promo); issuable ONLY from the Super Admin surface; free duration 30–60 days configurable per issuance; **excluded** from the 100-sub graduation metric | **Planned — NEW MVP capability** (owner ruling 2026-08-08) | owner ruling 2026-08-08; §9.1 |
| **Paige AI Chat — conversation COMPACTION** (intelligent summary + relevant context preserved, not hard-truncated; both the compaction AND full transcript persist — same discipline as Claude Code's session-summary hook) | **Partial → MVP** — a rolling-summary watermark (`paige_chat_threads.summary_through_seq`) + a session-summarizer prompt exist in `paige-ai-chat` over a 50-turn window, but the summarizer prompt is credit/funding-flavored (§2 flag) and "both-persist + relevance-preserving" is unverified end-to-end (§32) | prod (`paige_chat_threads`/`paige_chat_turns`, migration `20260711201150`); `supabase/functions/paige-ai-chat/index.ts` |
| **Paige AI Chat — PERSISTENT conversation history** (durable, keyed to **user + tenant**, not session id; user returns to a prior thread and continues) | **Partial → MVP** — multi-chat persistence EXISTS for the "Your Paige" owner workspace (`paige_chat_threads` + `paige_chat_turns`, RLS + create/append RPCs, owner-list index, retention window); must verify user+tenant keying (not session) across ALL chat surfaces incl. the client portal (§9/§51) | prod (migration `20260711201150`, `20260713024606` tenant isolation) |
| **Paige AI Chat — PERSISTENT TASKING** (when the user asks Paige to track something, she creates a REAL durable task in the tenant task system, auto-linked back to the source conversation — not a chat mention that evaporates) | **Planned/Unverified → MVP** — needs a durable task row + a conversation→task link verified end-to-end (§13/§32); not confirmed wired in this pass | owner ruling 2026-08-08; §7/§9/§13/§46 |
| **Brain / Second Brain — L8 Memory Fabric** (deeper semantic memory) — **moved Wave 5 → MVP** | **Planned — MVP (was Wave 5)** | owner ruling 2026-08-08; canonical-build-order.md |
| **Robust interactive analytics with impressive UI** — **moved Wave S2 → MVP** (best-in-class §11/§25 interactive analytics surface, not a static dashboard) | **Planned — MVP (was Wave S2)** | owner ruling 2026-08-08 |
| **Web browsing (Playwright)** — agentic web browse/research for **Paige + her agents** (incl. inside the customer portal) AND for **Claude Code's §32 live verification** | **Planned — MUST-HAVE MVP** (owner ruling 2026-08-08; upgraded from the earlier SHOULD-HAVE / OWNER-INPUT entry) | owner ruling 2026-08-08; CLAUDE.md §32 |
| Paige + 6 VP agents present and attributed | **Shipped** | git main #243–#247 |
| Agentic team dispatch as a chat act (no separate manager tab) | **Shipped (chrome)** / intelligence Planned | CLAUDE.md §20; PLATFORM-FUNCTION-INVENTORY §3 |
| **Systems Check MVP** (10 owner-locked checks, one-by-one, active setup help; the 30-check catalog is the broader superset) | **Planned (#80)** | canonical-build-order.md; owner ruling 2026-08-08 |
| **Action-bus autonomous worker** (makes "autonomous comms" real) | **Rails-only / not built** | PLATFORM-FUNCTION-INVENTORY §3 ② |
| **Client-facing Paige send-free tool surface** (two-way portal) | **Rails-only / not built** | PLATFORM-FUNCTION-INVENTORY §3 |
| Owner Trilogy — Systems Check pillar | **Planned (#80)** | canonical-build-order.md |
| Owner Trilogy — Business Vault (#81) | **Planned** | canonical-build-order.md |
| Owner Trilogy — Twin Capabilities (A/B/C) | **Planned (Twin A, #203)** | owner-trilogy-2026-07-26.md |
| Owner Trilogy — Live Competitive Analysis + Newswire | **Planned** | owner-trilogy-2026-07-26.md |
| Owner Trilogy — Owner Analytics + audit-trail | **Planned (#100/#97)** | owner-trilogy-2026-07-26.md |
| Super Admin (Paige-on-Paige) fully functional — Wave S3 operator Trilogy | **Planned (Wave S3)** | canonical-build-order.md; owner ruling 2026-08-08 |
| Platform-subscription checkout create-wire (sign up → Stripe → activate) | **Not built (B-Platform #56)** | money-spine-architecture.md |
| Revenue-tracking reconciliation to live Stripe (3 stores / 2 webhooks) | **In-flight (go-live blocker)** | PLATFORM-FUNCTION-INVENTORY §4 |
| SOC 2 (required before *public* launch; beta may precede) | **Planned (Wave 9, #136)** | canonical-build-order.md; owner ruling 2026-08-08 |
| Security/hygiene track S (SSN key, IDOR, RLS, finance-default gating, de-brand) before real tenants | **Open, gating onboarding** | paige-master-implementation-order.md |

> **HONEST NOTE (§13, §32).** Several MUST-HAVE rows carry the "autonomous," "Owner Trilogy," and "Systems Check" promises central to the owner's MVP definition and are **Planned, not shipped**. Marking them MUST-HAVE reflects owner scope; it does not imply build status. Any date/commitment attached to these is **OWNER INPUT REQUIRED** — no single owner "must-have checklist" doc exists; the launch-blocking set is inferred from the Wave-8 beta gate (§196) and requires owner confirmation.

### SHOULD-HAVE (post-launch)

| Feature | Status | Source |
|---|---|---|
| Per-tenant telephony + phone/calling/IVR/dialer | **Deferred roadmap** | PLATFORM-FUNCTION-INVENTORY §1; platform-operator-tenant-200.md |
| Native social-channel connection + posting/scheduling | **Ungrounded / OWNER INPUT REQUIRED** | absence across inventory/seams/architecture |
| Native Zapier integration (n8n is the native bridge) | **Not native / OWNER INPUT REQUIRED** | ECOSYSTEM_DATA_OWNERSHIP_MAP.md |
| Vibe Studio visual-critique loop | **Gated-off in prod (renderer not deployed)** | PLATFORM-FUNCTION-INVENTORY §2 |
| Vibe Studio in-session campaign engine (`tenant-campaigns`) | **Stub** | PLATFORM-FUNCTION-INVENTORY §2 |
| App-level "build a real app" (#293) | **Not built** | PLATFORM-FUNCTION-INVENTORY §2 |
| Practice Blueprints (one-click vertical bundles; §2 opt-in mechanism) | **Post-launch v2 (substrate Slices 0–3 on main)** | paige-practice-blueprints-2026-07-29.md |
| L8 Memory Fabric (deeper semantic memory) | **MOVED to MUST-HAVE (MVP)** — see MUST-HAVE table (owner ruling 2026-08-08; was Wave 5) | canonical-build-order.md |
| Portal **Blocks/widgets canvas** (freeform drag-and-drop composition beyond tabs — genuinely beyond the owner's MVP list) | **Planned (CX-2)** | client-experience-workstream-2026-07-21.md §3 |
| Deeper portal intelligence (at-risk, sentiment, client-facing CURA) | **Not wired** | docs/DONE.md |
| **Web browsing for Paige + her agents** (agentic web browse/research; and browse **inside the customer portal**) — owner-requested 2026-08-08 | **MOVED to MUST-HAVE (MVP)** — CONFIRMED MVP via Playwright, for Paige + agents AND Claude Code §32 live verification (owner ruling 2026-08-08); see MUST-HAVE table | owner ruling 2026-08-08 |
| Marketplace recurring skill subscriptions (B-v) + affiliate 3-tier (B-iii) | **Scoped, not built** | money-spine-architecture.md |
| Metered usage billing (tokens/storage/seats, B-Meter #57) | **Not built** | money-spine-architecture.md |
| Tenant BYO-processor lane (Stripe Connect direct-charge, then Square/PayPal) | **Deferred (B-Connect)** | money-spine-architecture.md; CLAUDE.md §38 |
| Enterprise / white-label depth (custom tab styling, franchise/multi-brand) | **SHOULD-HAVE (Enterprise = placeholder at MVP)** | owner ruling 2026-08-08; 1B-growth-map.md |
| L4 consumer-direct billing | **2027 roadmap, not shipped** | 1B-growth-map.md |

---

## 5. Tier Definitions

Three subscription tiers are available at launch — **Solo, Agency, Enterprise** (tier taxonomy FINAL, owner ruling 2026-08-08 v2; this corrects the earlier "Academy" label to **Agency**, aligning the tier name with the doctrine docs) — mapped to the canonical platform-subscription plans (Engine 1 / L1) (docs/doctrine/1B-growth-map.md; docs/doctrine/money-spine-architecture.md Tier 1). In code this maps to the marketplace-visibility set, fail-closed to the most-restrictive Solo, cumulative Enterprise ≥ Agency ≥ Solo (src/lib/__tests__/marketplaceTierSubstrate.test.ts). Parent-capability (owning sub-accounts) is governed by `tenants.account_type` where parent-capable = `account_type IN ('agency','enterprise')` (src/lib/agency/accountCapabilities.ts), and a sub-account is structurally never an agency (CLAUDE.md §51 ABSOLUTE INVARIANT).

> **CODE-LAG HONESTY (§13, verified on prod 2026-08-08).** The plan/tier *taxonomy* is renamed to **Agency** in this document, but the **code layer still stores the string `Academy`**: `marketplace_items.available_to_tiers` holds the element `"Academy"`, and both `current_tenant_tier()` and `_mp_tier_cascade_keys()` contain `'academy'`. No live subscriber is affected (all `user_subscriptions` are `'free'`; invite/stripe/tier tables are empty). The code rename (plan slug `academy` → `agency`; UI/label `Academy` → `Agency`; jsonb tier arrays `['Solo','Academy','Enterprise']` → `['Solo','Agency','Enterprise']`) is an **owed follow-up migration** — it does NOT rename the tenant `account_type='agency'` architectural role, which is a different concept. Until that migration lands, any code reading `available_to_tiers`/`current_tenant_tier()` still sees `academy`; the BRD taxonomy is the target, the code value is the current ground truth.

| | **Solo** | **Agency** | **Enterprise** |
|---|---|---|---|
| **Who it's for** | Solo operator — a coach/consultant with no sub-accounts | Agency running a book of business — owns sub-accounts (the compounding NRR unit) | Franchises / associations / multi-brand — placeholder at MVP | 
| **Sub-accounts** | **None** — Solo is single-tenant, no sub-accounts (owner ruling 2026-08-08) | Yes (parent-capable `account_type='agency'`) | Yes (parent-capable `account_type='enterprise'`) |
| **Marketplace visibility** | Solo shelf | Agency + Solo (cumulative) | Enterprise + Agency + Solo (cumulative) |
| **Enterprise-depth features** | — | — | **Placeholder only at MVP** — deep white-label/franchise features defer post-launch (owner ruling 2026-08-08; §4 SHOULD-HAVE) |
| **Price (public launch)** | **$149/mo** | **$397/mo** | **Custom** |
| **Price (beta)** | **$74.50/mo** (50% off) | **$198.50/mo** (50% off) | Custom (beta discount N/A per owner ruling) |

Pricing is per **owner ruling 2026-08-08**: Solo $149/mo, Agency $397/mo, Enterprise custom; Beta = 50% off Solo + Agency; public launch = full pricing. These numbers **supersede** the ranges in the doctrine docs (which list Solo $97–$297, Agency $497–$997, Enterprise $50K–$500K/yr — docs/doctrine/1B-growth-map.md Engine 1). The launch tier name is **Agency** (owner ruling 2026-08-08 v2, correcting the earlier "Academy" label) — which now **matches** the doctrine docs' "Agency" naming; the "Academy" label is retired at the taxonomy level (code-string rename still owed, see §5 code-lag note).

**Sub-account architecture (owner ruling 2026-08-08).** Sub-accounts belong to **Agency and Enterprise only** — both are parent-capable (`account_type IN ('agency','enterprise')`). **Solo is single-tenant and has NO sub-accounts**, and a sub-account is **never** created under a Solo tenant. This is consistent with the §51 ABSOLUTE INVARIANT (a sub-account is a non-manager child; an agency/enterprise is a top-level parent) and is enforced structurally at the DB layer (CLAUDE.md §51; migration `20260807230000`).

**Billing seam (grounded).** Platform subscriptions flow tenant's card → Paige's Stripe → Paige's bank, with Paige as merchant of record — Paige's own rail (CLAUDE.md §38; money-spine Tier 1). The schema exists (`platform_subscriptions`, `platform_subscription_plans`). **Honest gap (§13, §32):** the **create side is not built** — there is no working "sign up on paigeagent.ai → Stripe checkout → subscription creation → tenant activation" wire; it is build slice **B-Platform (#56), status ❌ new** (money-spine-architecture.md Tier 1). Live self-serve tier checkout therefore does not yet exist and is MUST-HAVE, launch-blocking (see §4). Beta = 50% off is a pricing rule; whether it is implemented as a Stripe coupon vs. a distinct plan is **OWNER INPUT REQUIRED** (not grounded).

> **OWNER INPUT REQUIRED** — Per-tier **seat counts / seat entitlements** and the **specific feature-entitlement matrix** (which MUST-HAVE features each tier includes, sub-account caps, custom-tab caps) are not grounded to a shipped spec. The CX-2 plan proposes portal custom-tab caps (Solo 3 / "Practice" 8 / unlimited) but is unbuilt and uses different tier labels (client-experience-workstream-2026-07-21.md). Confirm the authoritative entitlement matrix before publishing tier feature lists.
>
> **VOICE + §2 NOTE.** All tier and portal copy stays coaching-generic and §2-clean (no finance/credit default), inclusive beyond "coaching," and free of banned voice terms — "AI-powered", "seamless", "streamline", "empower" (CLAUDE.md §2, §3). Per the owner ruling (2026-08-08, NON-NEGOTIABLES), **"Practice" is PROHIBITED** as a tier, plan, portal, or product label until HIPAA + SOC 2 + related certs land (§3.b) — no tier in this document is named "Practice." Reconciliation, owner-resolved (2026-08-08): **§3.b WINS over §2** — the ban is expanded to **all shipped/Marketplace copy** (not just tier labels), and the **§2 doctrine wording is being edited to "business"/"company"** in place of its "practice" recommendation (see §11.4). No "practice" in Marketplace or any shipped copy until the HIPAA + cert research is complete. Still owed (doctrine file edits, not decisions): paste §3.b into CLAUDE.md, edit §2's wording, and confirm the full cert list beyond HIPAA + SOC 2 (**OWNER INPUT REQUIRED** on the cert list only).

## 6. Systems Check MVP

### 6.1 Behavior model

The Systems Check is the first Owner-Trilogy pillar and the launch face of the platform's differentiation (owner ruling 2026-08-08; Pillar 1 of the Owner Trilogy, `docs/strategy/owner-trilogy-2026-07-26.md`). Paige inspects the tenant's actual setup state, then works the gaps rather than merely listing them. Three behaviors are locked:

- **One at a time, not a wall.** Paige surfaces the single highest-priority missing piece, resolves it with the tenant, and only then moves to the next. This is the §36 draft-first / proactive-surfacing pattern — the tenant opens a queue Paige has already reasoned over, not a settings maze they must learn to navigate (CLAUDE.md §36).
- **Priority-ordered by importance.** Gaps are ranked so the tenant fixes what unblocks the core end-to-end use case first (comms before, say, payment-method labels) (owner ruling 2026-08-08).
- **Hands-on assist, not instructions.** Paige does not hand over a checklist and walk away. She actively helps set the missing piece up — connecting a sender, drafting company info, proposing a website in Vibe Studio — consistent with the §14 "Paige never works solo, she does the work through her team" mandate and the §36 "diagnose AND help set up" constraint (owner ruling 2026-08-08; CLAUDE.md §14, §36).

**Runs at all three operating tiers.** The Systems Check fires at **agency**, **solo (standalone tenant)**, and **sub-account** levels, each scoped to its own book per the §51 tier matrix and the ABSOLUTE INVARIANT (a sub-account is never an agency; it checks its own setup, never the parent's) (owner ruling 2026-08-08; CLAUDE.md §51 + `docs/doctrine/tier-matrix.md`).

**Scope note (§13 honesty).** The strategy source defines the Systems Check MVP as a **30-check catalog** (`docs/strategy/owner-trilogy-2026-07-26.md` L226); the ten checks the owner locked for this BRD (below) are the launch-blocking core within that catalog. The pillar's *documentation* has shipped, but the pillar **build** (#80) is PLANNED with no build commits on `main` as of 2026-08-08 (`docs/doctrine/canonical-build-order.md`; git status per roadmap research). The ten checks below are the required MVP set; the underlying capability each check reports on is graded EXISTS / PARTIAL / GAP from the architecture research.

### 6.2 The ten checks (MVP-locked)

| # | Check | Underlying capability status | Grounding |
|---|-------|------------------------------|-----------|
| 1 | **Comms configured across the board** — email senders, SMS senders, business phone number registered | **Email: EXISTS** (Resend, per-tenant sending identities, `resolve_tenant_sender`, `add_email_domain`/`set_default_email_domain`). **SMS: EXISTS but shared** (Twilio + STOP compliance, `send_sms`; only one shared number + one global A2P flag — no per-tenant telephony). **Business phone registration: GAP** — per-tenant telephony is unbuilt and the master Twilio number is deliberately dormant; a phone row reads RED/roadmap, not green. | `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md` §1; `docs/architecture/platform-operator-tenant-200.md` |
| 2 | **Website connected/detected** — if not, suggest creating one in Vibe Studio | **PARTIAL.** Vibe Studio website/landing-page generation EXISTS (`growth-page-draft`, one conversational entry, one-session-per-project). No documented capability for *detecting/connecting an existing external website* was found in the research — the "detect" half is a GAP; the "create one in Vibe Studio" half is built. | `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md` §2 |
| 3 | **Social accounts connected** | **GAP / UNGROUNDED.** No native social-channel connector, posting, or account-linking capability appears in any grounding doc. This check is currently ungrounded. **> OWNER INPUT REQUIRED** — confirm whether social connection is in MVP scope and, if so, what integration is intended; the core end-to-end use case names "initial content creation (social + ads)," but no social-linking substrate is documented as built. | Absence across `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md`, `docs/PAIGE-CALLABLE-SEAMS.md`, `docs/architecture/*` |
| 4 | **n8n or Zapier wired** | **n8n: EXISTS** (workflow bridge behind automations; `paige_workflow_registry`; §118 provider gate restricts n8n to the platform-owner tenant today; seams `register_workflow`/`run_workflow`/`list_workflows`). **Zapier: GAP for native.** Zapier appears only as an external MMA-ecosystem rail, not a native Paige integration. **> OWNER INPUT REQUIRED** — confirm whether Systems Check treats Zapier as in-scope native vs external-only. Note also the §118 gate: n8n is currently owner-tenant-restricted, so a generic tenant "n8n wired" check needs the gate widened or a per-tenant path. | `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md` §1; `docs/architecture/platform-operator-tenant-200.md` (§118); `docs/architecture/ECOSYSTEM_DATA_OWNERSHIP_MAP.md` |
| 5 | **Company info populated** | **EXISTS.** Tenant/business data is authoritatively owned by Paige (`tenants`, business/contact records); `verify_business` and tenant feature seams ship. | `docs/architecture/ECOSYSTEM_DATA_OWNERSHIP_MAP.md`; Paige MCP tool list |
| 6 | **Any customers in CRM** | **EXISTS.** CRM/contacts is the most mature domain (`clients` table, `create_contact`/`search_contacts`/`get_contact`, ~25-panel ContactDetail). A "do you have customers yet?" check reads directly off owned tables. | `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md` §1; `docs/architecture/ECOSYSTEM_DATA_OWNERSHIP_MAP.md` |
| 7 | **Sales pipeline scoped + configured** | **EXISTS.** Pipelines/deals ship (Paige owns `deals`, `pipeline_stages`; `create_deal`/`move_deal_stage`/`list_deals`; stage automation via `stage_automation_rules`). | `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md` §1 |
| 8 | **Sales revenue tracking configured** | **PARTIAL — tables exist, wiring disconnected.** The billing taxonomy tables ship, but the God/revenue dashboards read near-empty/unreconciled tables (three subscription stores, two webhooks unreconciled); reconciling to live Stripe is a named go-live blocker. A revenue check must not present current dashboard numbers as real (§13). | `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md` §4 |
| 9 | **Payment processors set up** | **PARTIAL.** Tenant Stripe Connect Express onboarding is built on the operator/platform side; §38 money boundary holds (Paige facilitator-only for tenant→client money). The B-iv storefront Connect posture (direct-charge vs destination-charge) is UNVERIFIED (spike #58 pending) — do not claim the tenant-side boundary is proven in code. | `docs/PLATFORM-FUNCTION-INVENTORY-2026-07-19.md` §4; CLAUDE.md §38; `docs/doctrine/money-spine-architecture.md` |
| 10 | **Payment method options configured** — cash / Zelle / cards / etc. | **GAP / UNGROUNDED as a configurable option-set.** No grounding source documents a tenant-facing "payment method options" (cash/Zelle/card selection) configuration surface distinct from the Stripe Connect processor wiring in check 9. **> OWNER INPUT REQUIRED** — confirm the intended shape of this check and whether the option-set surface must be built. | Absence across the platform inventory and money-spine docs |

**Cross-cutting §2 guard.** Any check that touches funding/credit (e.g. a payment or readiness surface) must treat that content as a per-tenant opt-in, never a default the Systems Check surfaces to every tenant (CLAUDE.md §2).

---

## 7. Customer Portal Spec

The Customer Portal is a first-class, major MVP scope item and must carry the same Owner-Trilogy *feeling* Paige gives the tenant — reasoning, surfacing, acting — for the tenant's **own clients** (owner ruling 2026-08-08). It is the client-facing expression of the §7 north star: the portal that reasons, suggests, and acts, tenant-authored per Playbook (CLAUDE.md §7).

### 7.1 Form factor

**Mobile-first.** The operator platform is desktop-first; the client portal is **mobile-first** (owner ruling 2026-08-08). This is a stated design axis, not a documented shipped constraint — treat it as a requirement the portal build must satisfy.

### 7.2 Skins / snapshots

The coach gives each customer a specific **skin / snapshot** so the portal feels like an app built for that client (owner ruling 2026-08-08). The shipped equivalent of "skin" is the **portal presentation overlay** (`tenants.features.portal_config`) plus the **brand override**, merged by the single source-of-truth resolver `src/lib/portal/moduleNav.ts`. **Honest caveat (§13):** there is no primitive literally named "skin" or a portal-taxonomy "snapshot" in code — "snapshot" in the repo refers to credit/financial/profile snapshots and `agreement_text_snapshot`, which are unrelated (research: portal-taxonomy-search, missing_sources). If the owner means a named, savable per-client "skin/snapshot" object, that is net-new build on top of the existing overlay + brand mechanism.

### 7.3 Full coach customization

- **Brand colors (brand tokens): EXISTS.** `set_tenant_brand` + `useBrandKit`; `LogoUploader` in `BrandControls.tsx`; brand cascades to sub-accounts via `resolve_tenant_brand`'s recursive parent walk (depth 10) (`src/pages/admin/PortalStudio.tsx`; `docs/doctrine/tier-matrix.md`).
- **LOGO OVERRIDE of the Paige logo: MECHANISM EXISTS; treat residual plastering as an MVP gap to close.** The owner-locked framing is that "currently our logo is plastered everywhere with no override — a §6/§7 MVP gap." The research shows an override mechanism **does** exist for the client-facing portal: `get_client_portal_brand()` (SECURITY DEFINER, keyed on `clients.linked_user_id = auth.uid()`) returns the tenant's `logo_url`/`primary_color`; `useClientPortalBrand.ts` renders a neutral placeholder while loading and falls back to platform (Paige) brand only once resolved-empty — so the client portal is **not** hardcoded to the Paige logo (`src/hooks/useClientPortalBrand.ts`; `supabase/migrations/20260710000358_get_client_portal_brand.sql`). **MVP action:** verify override coverage across *every* client-facing surface and close any remaining spot where the PaigeMark shows without a tenant override, per §6 (one continuous brand system) and §7. The owner OBSERVED the PaigeMark showing live; per §32, a mechanism existing in code does NOT mean it is visible/complete at runtime — so this stays a **launch-blocking MVP gap**. The owner's live observation is authoritative evidence of residual leaks or incomplete surface coverage, not something the shipped `get_client_portal_brand` path waives. **> OWNER INPUT REQUIRED** — confirm which specific surfaces still show the un-overridable Paige logo.
- **Unlimited custom tabs + custom menu: PARTIAL — NET-NEW BUILD for the "unlimited" ambition.** The shipped `portal_config` overlay is **subtractive/reordering only** over a fixed Playbook module catalog (`moduleNav.ts`): it can hide and reorder tabs but **cannot add net-new tabs**. The richer "unlimited custom tabs (with tier caps: Solo 3 / Practice 8 / Studio unlimited / Enterprise unlimited), custom menu, Blocks/widgets" model is **doctrine/plan** (CX-2 composable-portal spec), not built (`src/lib/portal/moduleNav.ts`; `docs/strategy/client-experience-workstream-2026-07-21.md` §3). **Naming note (§3.b):** the CX-2 spec uses the label "Practice" for a tier cap; this BRD should avoid presenting "Practice" as shippable copy until the compliance gate the owner named lands — see §7.7. Any "unlimited custom tabs" claim at MVP is unsupported by current code and must be scoped as build.
- **Pre-set templates (starting points, not locked): PARTIAL.** Per-Playbook default portal templates are part of the CX-2 doctrine/plan; the shipped catalog gives fixed per-Playbook defaults but not an editable template library. Net-new for the full "templates as editable starting points" ambition (`docs/strategy/client-experience-workstream-2026-07-21.md` §3).
- **Portal composition = knowledge base + tabs + references:** matches the CX-2 composable-portal intent (default tabs + resources/KB + custom tabs). Shipped today only as the fixed catalog; the composed/assembled model is plan, not build.
- **Client document uploads: PARTIAL.** Upload components exist (`DocumentUpload.tsx`, `DocumentsManager.tsx`, `useChatDocumentUpload.ts`), but several are funding/BTF-flavored (`btf-onboarding` bucket, financial docs). **> OWNER INPUT REQUIRED** — confirm whether a coaching-generic, §2-clean client "Documents" tab exists as a default vs a funding-preset surface.
- **Holding company + subsidiary structure inside a client's portal: GAP.** No doc or code was found for a holding-company/subsidiary portal concept distinct from the existing agency → sub-account (`parent_tenant_id`) model (research: portal-taxonomy-search, missing_sources). This is net-new; **> OWNER INPUT REQUIRED** to define the intended structure.

### 7.4 Client signup + agreement flow (onboard → signup → confirmation → agreement signed)

**Scaffolding EXISTS — verify, do not rebuild.** The signup + agreement-signing legal scaffolding is already in code:

- **Tables:** `paige_signed_agreements` (client_id, template key/version, signed PDF path, `signature_data`, text snapshot, IP, user agent, signed_at; RLS `client_insert_own_agreement` / `client_or_staff_read`); `agreement_templates` (slug, layer `platform|tenant`, body markdown, merge fields, version, `is_forkable`, `required_at_signup`); `tenant_agreement_versions` (`paige_template|tenant_fork|tenant_upload`) (`supabase/migrations/…9e340148…`, `…ed956ebc…`).
- **Runtime:** the `finalize-agreement` edge function receives the signed agreement from the `/onboard` wizard, generates a PDF (pdf-lib), stores it in the private `btf-onboarding` bucket, writes the signed-agreement row, and advances `clients.onboarding_stage` (`supabase/functions/finalize-agreement/index.ts`).
- **UI:** the `/onboard` multi-step wizard with `Step2Agreement.tsx` (SignaturePad) + operator-side `AgreementsAdmin.tsx` + MCP `list_signed_agreements`.

**Two verification gaps before this can be called MVP-ready (§32 end-to-end verify owed):**
1. The shipped path is **funding/BTF-flavored** (the `btf-onboarding` bucket, "BTF service agreement" wording, and `paige_payment_authorizations.plan_selected` enum `pay_in_full|split|get_started`). Per §2 this must be an opt-in tenant preset, not a coaching-generic default. **> OWNER INPUT REQUIRED** — confirm the signup + agreement scaffold is exposed coaching-generically, not hardcoded to the funding vertical.
2. `agreement_templates.layer='tenant'` and `tenant_agreement_versions` support tenant fork/upload, but a **coaching-generic tenant authoring + requiring + collecting signatures on their own agreement** has not been verified end-to-end. This needs a §32 proof before "any coach can run signup+agreement" is claimed as shipped.

### 7.5 Existing portal taxonomy/matrix (owner says it already exists — verified)

The owner states the customer-portal taxonomy/matrix already exists; the research **confirms it, in code, not as a doctrine doc** (research: portal-taxonomy-search). It is a two-layer model:

- **Module CATALOG + default order** — authored in "Your Paige," stored at `tenants.features.playbook_config`.
- **Presentation OVERLAY** — subtractive/reordering, stored at `tenants.features.portal_config`; new module keys can never be introduced by the overlay.
- **Single resolver:** `src/lib/portal/moduleNav.ts` (`catalogRoutableItems` + `applyPortalOverlay`); concrete tab taxonomy in `MODULE_ROUTES` (home, learn/resources, approvals, actions, plus funding-vertical keys routable **only** for opt-in funding Playbooks — §2-clean).
- **Seams:** `set_tenant_portal_config` (authority `can_manage_tenant_brand`), `get_client_portal_config` (keyed on `clients.linked_user_id = auth.uid()`); fail-open to the default portal on absent/malformed config.
- **Tenant surface:** `PortalStudio.tsx` (brand + tabs/order + welcome + live "View as Client" preview computed from the same merge, so preview cannot drift).

**Reference this taxonomy; do not invent a parallel one (§18).** The richer "assemble your own tabs / Blocks canvas" ambition is the CX-2 spec on top of this, and is net-new build. No doctrine `.md` titled "portal taxonomy" was found — the shipped source of truth is the code above (`moduleNav.ts`). **Owner-owed item 13d is NOT resolved:** the crew located a shipped portal-config taxonomy but did **not** find the customer-portal Owner-Trilogy matrix the owner referenced — see the OWNER INPUT REQUIRED in §13(d).

### 7.6 Tier posture (§51)

Client (Tier 5) and Anonymous (Tier 6) rows are defined in `docs/doctrine/tier-matrix.md`: a client resolves via `get_paige_persona_context()` (`clients.linked_user_id` → `clients.tenant_id`), own-portal-only RLS, client-portal RPCs only; anonymous has no `tenant_id` resolution and every anonymous producer must be §37-inventoried because public surfaces are the easiest place to leak tenant data. **Live hazard to inherit-check:** `get_paige_persona_context()` carries the #588/#589 resolver defects (profile-branch key drift, unordered `LIMIT 1`); a client-signup build relying on this resolver inherits those hazards and must confirm the fix has landed (`docs/doctrine/tier-matrix.md`; CLAUDE.md §51 #588).

### 7.7 Compliance / voice guards on the portal

- §6/§7: tenant brand overrides platform brand in every client-facing surface; the portal must read as one continuous system with the tenant's own brand.
- §2: no funding/credit tab, agreement, or payment-authorization label shown to a client except under the tenant's opt-in Playbook.
- §3 voice everywhere in client-facing copy (never "AI-powered," "seamless," "empower," "streamline").
- **§3.b "Practice" ban — EXPANDED, owner-resolved (2026-08-08):** the ban on the word "Practice" pre-HIPAA/cert now applies to **all shipped/Marketplace/portal copy**, and **§3.b WINS over §2** (whose "practice" recommendation is being edited to "business"/"company" — see §11.4). No "practice" in any client-facing portal copy, and the CX-2 "Practice" tier-cap label must be renamed before it can ship. **Honesty note (§13):** §3.b is not yet a pasted numbered section in CLAUDE.md and §2's text still recommends "practice" — those are owed doctrine-file edits, not open decisions; the ruling is authoritative now. Only the full cert list beyond HIPAA + SOC 2 remains **OWNER INPUT REQUIRED**.

---

## 8. Non-Technical UX Principles (design constraints)

The whole platform must feel non-technical — from solopreneur to enterprise, nobody should have to *think* to use it (owner ruling 2026-08-08). This is the client-facing complement to §36 (intuitiveness is the moat) and binds every surface as a hard design constraint, testable by the adversarial verifier and design critic (§11, §25, §36).

**Testable constraints:**

1. **Feels like talking to Claude / ChatGPT.** The primary interaction is conversational, not a form maze or a prompt window the user must learn to drive. Paige is an agent-driven surface, not a user-driven prompt window (CLAUDE.md §36; owner ruling 2026-08-08). **Test:** a first-time tenant accomplishes a real task by describing it in plain language, with no field-by-field configuration required.
2. **Diagnose AND set up — never just diagnose.** Paige and her team do not stop at surfacing a gap; they actively help set it up (hands-on), per §14 (Paige does the work through her team) and the Systems Check assist behavior in §6 (owner ruling 2026-08-08). **Test:** every diagnosed gap ends in Paige offering to perform or draft the fix, not in an instruction the user must execute alone.
3. **5-minute discoverability (§36).** A non-technical coach with zero prompt-engineering skill must discover and use any capability within their first five minutes, without documentation or a tutorial (CLAUDE.md §36). **Test:** the design-critic/verifier runs this exact question on every user-facing surface; failing it blocks ship.
4. **Draft-first, one-click approval, proactive surfacing, team framing.** Use at least one (ideally several) of the five §36 mechanical patterns on every user-facing build: proactive surfacing (Paige tells the user what needs doing), draft-first (Paige produces finished work, human approves), domain-expert framing (Paige speaks the tenant's business language, never "let me query the database"), one-click approval (Approve is a button, not a reformulated prompt), and §14 team framing (the user manages a team, not drives a tool) (CLAUDE.md §36).
5. **Agencies may be more power-user; baseline stays non-technical.** Agencies can be more savvy and get denser, more powerful affordances, but the baseline UX contract stays non-technical across every tier — the power-user path is additive, never a prerequisite (owner ruling 2026-08-08). **Test:** no capability is reachable *only* through a technical/power-user path; there is always a conversational, non-technical route.
6. **No dev-tool language leaks (§36, §3).** Never surface backend table/function names, "invoke the tool," "run this skill," or agent jargon in tenant- or client-facing copy (CLAUDE.md §11 "no amateur tells," §36 domain-expert framing).

---

## 9. Beta Plan

**Structure (owner ruling 2026-08-08):**

- **Invite-only, closed group.** Beta is a closed, invite-only cohort whose purpose is to catch bugs before the platform is opened to the general public. This is the pre-launch stance: BETA first, then monetize from public launch (owner ruling 2026-08-08). It aligns with §4's pre-launch shipping posture (finished, verified work ships live; owner reviews in live view) (CLAUDE.md §4).
- **Pricing during beta: 50% discount on Solo and Agency.** Solo $149/mo and Agency $397/mo are each offered at 50% off during beta; Enterprise is custom (owner ruling 2026-08-08). Public launch reverts to full pricing. **Honesty note (§13):** the money-spine and growth-map docs name the plan tiers but do **not** contain these exact dollar amounts; the price points are owner-supplied and authoritative here. The middle tier is **Agency** — the launch tier name (owner ruling 2026-08-08 v2) now matches `money-spine-architecture.md`'s "Agency" label (the earlier "Academy" label is retired).
- **Graduation number: first 100 subscribers + their teams.** Beta graduates to public launch at the first 100 paying subscribers plus their teams (owner ruling 2026-08-08).
- **Tiers available at launch:** Solo, Agency, and Enterprise are all available; **Enterprise is a PLACEHOLDER** — it must exist as a selectable option, with deep Enterprise features deferred post-launch (owner ruling 2026-08-08). In code the canonical marketplace-visibility set gates on the same tier element (stored today as `Academy`; taxonomy renamed to `Agency`, code-string rename owed — see §5) with fail-closed cascade (`src/lib/__tests__/marketplaceTierSubstrate.test.ts`), and Marketplace tier substrate Slices 0–2 have shipped to `main` (#277; git `main`), so the tier scaffolding to gate a placeholder Enterprise exists.
- **PROMOTIONAL accounts at beta (NEW MVP capability, owner ruling 2026-08-08).** Beyond the three paid tiers, a **Promotional account type** is issuable from the Super Admin surface as a free comp — see §4 (MUST-HAVE) and §9.1. Promotional accounts are **excluded from the 100-subscriber graduation metric** (they are comps, not paying subscribers).

### 9.1 Promotional accounts (NEW MVP capability — owner ruling 2026-08-08)

A **Promotional account type** is a free comp the platform operator issues to seed the beta. Owner-locked parameters:

- **Variants: Solo + Agency only.** There is **no Enterprise promotional** account (Enterprise is a custom/placeholder tier at MVP).
- **Issuable ONLY from the Super Admin surface.** No tenant-self-serve path; the operator (God tier, Paige-on-Paige) issues it. This makes **Super Admin / Paige-on-Paige top-tier functionality launch-critical** — the operator must be able to issue, configure, and revoke promos through the same Paige experience tenants get (owner ruling 2026-08-08; §10.1 B-Q2).
- **Free duration 30–60 days, configurable per issuance.** The operator sets the comp length within the 30–60-day band at the moment of issuance (not a single global constant).
- **Excluded from the 100-subscriber beta graduation metric.** Promotional accounts are comps, not paying subscribers, so they do **not** count toward the graduation trigger (§9 graduation number; §12.4) and are excluded from Beta MRR (§12.3).
- **§13/§32 status:** this is **NEW MVP scope, not yet on `main`** — the promo account type, its Super-Admin issuance surface, and the graduation-metric exclusion are launch-blocking build, not shipped capability. **> OWNER INPUT REQUIRED** — confirm whether a promo converts to a paid subscription at expiry (auto-prompt vs auto-downgrade vs hard stop), which is unspecified.

**Compliance sequence (owner ruling 2026-08-08):**

- Beta invites may start **before** SOC 2.
- SOC 2 is **required before** public launch. In the build order this is Wave 9 (#136), gated after the beta-launch wave W8 (`docs/doctrine/canonical-build-order.md`; §196 chain).
- HIPAA and other certifications are **post-launch**.
- **§3.b caveat (see §7.7):** the owner's "Practice" copy ban is tied to HIPAA + SOC 2 + related certs landing; enforce per the owner ruling, but note the doctrine text does not yet carry a numbered §3.b — confirm scope (§13).

**Launch-blocking dependency note (§13).** The pre-launch must-have chain (`docs/doctrine/canonical-build-order.md` §196) puts BETA LAUNCH at Wave 8, after Blueprints → Owner Trilogy → L8 Memory → Quality → Playwright. Several beta-relevant items are PLANNED, not built, as of 2026-08-08: the Owner-Trilogy pillar **builds** (#80 Systems Check, #81 Vault, Twins) have no commits on `main`; the platform-subscription **CREATE** wire (sign-up → Stripe checkout → activation, slice B-Platform #56) is explicitly missing; and revenue dashboards read unreconciled tables. Any claim that beta can begin charging the 50%-discount Solo/Agency price depends on B-Platform shipping first (`docs/doctrine/money-spine-architecture.md`; roadmap research).

> **OWNER INPUT REQUIRED** — **Beta duration and timing.** No source specifies how long the beta runs or its calendar start/end. The graduation *number* is locked (100 subscribers + teams), but the duration/timing is owner-owed and must not be invented (owner-owed item 13b).


---

## 10. Launch Criteria

Launch is gated in two stages — **Beta** (invite-only, closed group, bug-catching) and **Public** (general-availability, full monetization) — per owner ruling 2026-08-08. The quality bar is *general-public-ready* from the start (owner ruling 2026-08-08); Beta exists to catch bugs against real users before the doors open, not to ship a lesser product. Each gate below is bucketed **FEATURES**, **QUALITY**, or **COMPLIANCE**. COMPLIANCE detail lives in §11.

> **Honesty note (§13):** Several must-have builds below are documented and scoped but have **no build commits on `main`** as of 2026-08-08 (Owner-Trilogy Wave 4 pillars, Wave S3 operator-scope Trilogy, the action-bus autonomous worker, tool-equipped client-facing Paige). They are launch-blocking *targets*, not shipped capabilities. Where a criterion rests on unbuilt work, it is marked **[PLANNED — not yet on main]**. A green criterion means the capability is shipped and verified, never hoped-for (§13, §32).

### 10.1 BETA-launch gates (invite-only)

Beta may open before SOC 2 lands (owner ruling 2026-08-08). Target cohort: the first 100 subscribers + their teams (the graduation number, owner ruling 2026-08-08).

**FEATURES — the autonomous end-to-end core (owner ruling 2026-08-08, "CORE END-TO-END USE CASE"):**

| # | Criterion | Grounding / status |
|---|---|---|
| B-F1 | Three target-tenant types provisionable: Coach, Consultant, Digital Marketing Agency | owner ruling 2026-08-08 |
| B-F2 | Three plan tiers selectable at signup: Solo, Agency, Enterprise (Enterprise = placeholder option; deep features deferred). Sub-accounts on Agency + Enterprise only; Solo is single-tenant | owner ruling 2026-08-08 |
| B-F2b | **Promotional account type** issuable from Super Admin (Solo + Agency variants; 30–60-day free duration configurable per issuance; excluded from the 100-sub graduation metric) | owner ruling 2026-08-08; **[PLANNED — not yet on main]** |
| B-F3 | Client comms automated: appointment setting, follow-ups, reminders, planning-document creation | owner ruling 2026-08-08; calendar/booking BUILT (research: PLATFORM-FUNCTION-INVENTORY §1); autonomous execution **[PLANNED — not yet on main]** — the action bus is rails with "no locomotive" (research: inventory §3) |
| B-F4 | Client emailing (Resend) + SMS (Twilio) live per-tenant | Email + SMS BUILT (research: inventory §1); SMS caveat: one shared Twilio number, no per-tenant telephony (research: inventory §1 gap b) |
| B-F5 | Initial content creation — social + ads | **OWNER INPUT REQUIRED** — social connectors are **not documented as built** in any grounded source (research: absence across inventory/seams/architecture). Ads/social generation scope must be confirmed before this gate can be marked green |
| B-F6 | Agentic team runs the above autonomously and coordinated | **[PLANNED — not yet on main]** — action-bus autonomous worker is the #2 ranked build (research: inventory §3, Ranked priority ②) |
| B-F7 | Paige-driven customer portal live for the tenant's clients | Portal gateway `/portal/:slug` DONE (research: DONE.md); deeper two-way intelligence (at-risk/sentiment/CURA client-Paige) **[PLANNED — not yet on main]** (research: DONE.md, roster L24) |
| B-F8 | Marketplace: everything shipped in Slice 0–2 + Persona L1 | Marketplace Slice 0/1/2 SHIPPED (research: PRs #382/#391/#392); Persona L1 SHIPPED (research: PR #384) |
| B-F9 | Systems Check MVP (all 10 checks, runs at agency/solo/sub-account) | owner ruling 2026-08-08; **[PLANNED — not yet on main]** — Systems Check build #80 has no commits (research: canonical-build-order L28; strategy owner-trilogy L226 defines 30-check catalog) |
| B-F10 | Paige C-Suite: PAIGE + 6 VPs (VERA, NEXUS, CURA, MENTOR, MERIT, ZION) shipping, plus dynamic spin-up (§14) | Roster locked (research: paige-c-suite-roster §42; VP_ROSTER in code); VP-surfacing cluster #243–247 SHIPPED (research: git main) |
| B-F11 | Owner Trilogy — all **six** owner-named components present: Systems Check · Business Vault · Twin Capabilities · Live Competitive Analysis · Newswire · Audit-trail | owner ruling 2026-08-08 (non-negotiable). **[PLANNED — not yet on main]** — Wave 4 pillar builds have no commits (research: canonical-build-order L28–29) |
| B-F12 | Customer portal carries the same Owner-Trilogy feeling for the tenant's own clients | owner ruling 2026-08-08; taxonomy owner-owed (see §13d) |

**QUALITY gates:**

| # | Criterion | Grounding |
|---|---|---|
| B-Q1 | Non-technical UX end-to-end — a non-technical user discovers and uses any capability within 5 minutes; feels like talking to Claude/ChatGPT; Paige + team actively *help set up*, not just diagnose | §36; owner ruling 2026-08-08 |
| B-Q2 | Super Admin (Paige-on-Paige) fully functional; owner runs the platform through the same Paige experience tenants get | owner ruling 2026-08-08; operator seam BUILT (research: platform-operator-tenant-200) |
| B-Q3 | Wave S3 (operator-scope Owner Trilogy) present | owner ruling 2026-08-08; **[PLANNED — not yet on main]** (research: canonical-build-order L61–66) |
| B-Q4 | Tenant brand overrides platform brand in tenant- *and* client-facing surfaces — the tenant's logo replaces the PaigeMark everywhere in the client's view | §6/§7; owner ruling 2026-08-08. Override mechanism EXISTS (`get_client_portal_brand`, `useClientPortalBrand`; research: portal-taxonomy facts) — **verify no residual PaigeMark leaks** before green (owner flagged this as an MVP gap to close) |
| B-Q5 | Customer portal is mobile-first (operator platform desktop-first) | owner ruling 2026-08-08 |
| B-Q6 | Client signup flow works end-to-end: coach onboards client → client signs up → confirmation → agreement signed | owner ruling 2026-08-08; scaffolding EXISTS (`paige_signed_agreements`, `finalize-agreement`, `Step2Agreement`; research: portal-taxonomy facts) — **verify it runs coaching-generic, not funding/BTF-only** (research: notable gap; §2) |
| B-Q7 | Per-tier availability + gating verified for God/Agency/Standalone/Sub-account/Client/Anonymous | §51 six-component railing; sub-account-is-never-agency invariant enforced structurally (research: §51 ABSOLUTE INVARIANT, migration 20260807230000) |
| B-Q8 | Every shipped surface passes the §5 verifier + compliance officer and §25 design-critic passes | §5, §11, §25 |

**COMPLIANCE gate (Beta):**

| # | Criterion | Grounding |
|---|---|---|
| B-C1 | Track S security/hygiene green before real tenants onboard (SSN-key encryption, IDOR, MMA-OS RLS, finance-default gating, de-brand) | research: paige-master-implementation-order L12–20 ("MUST be green before real tenants onboard") |
| B-C2 | §2 clean — no finance/credit content in any platform default; funding is per-tenant opt-in only | §2; research: finance-default gating #104/#90/#52 |
| B-C3 | Beta legal: invite terms + subscriber agreement in place (SOC 2 *not* required at this gate) | §11; owner ruling 2026-08-08 |

### 10.2 PUBLIC-launch gates (general availability)

Everything in 10.1 remains green, plus:

**FEATURES:**
- P-F1 — Full pricing live (Beta 50% discount on Solo + Agency retired): Solo $149/mo, Agency $397/mo, Enterprise custom (owner ruling 2026-08-08).
- P-F2 — Platform-subscription **create** wire live: `paigeagent.ai` signup → Stripe checkout → subscription creation → tenant activation. **[PLANNED — not yet on main]** — this is build slice B-Platform (#56), explicitly *missing* today (research: money-spine Tier 1 "Missing"). This is a hard public-launch blocker: no live self-serve checkout exists yet.
- P-F3 — Revenue tracking reconciled to live Stripe (God MRR/ARR trustworthy). **[PLANNED — not yet on main]** — 3 subscription stores / 2 webhooks currently unreconciled; named go-live blocker (research: inventory §4 gap a, Ranked priority ③).

**QUALITY:**
- P-Q1 — Beta graduation met: first 100 subscribers + their teams onboarded and the cohort's bugs triaged/closed (owner ruling 2026-08-08). Exact exit criteria beyond the count are **OWNER INPUT REQUIRED** (see §13b).
- P-Q2 — General-public quality bar confirmed across landing → signup → app → client portal as one continuous brand system (§6).

**COMPLIANCE:**
- P-C1 — **SOC 2 landed** — required before public launch (owner ruling 2026-08-08). See §11.
- P-C2 — Public monetization terms, privacy policy, and DPA posture in force (§11).

---

## 11. Compliance Roadmap

The compliance sequence is owner-locked (owner ruling 2026-08-08):

```
Beta invites  ──►  SOC 2  ──►  Public launch  ──►  HIPAA + others
(may start         (REQUIRED                        (post-launch)
 BEFORE SOC 2)      before public)
```

### 11.1 Stage 1 — Beta (SOC 2 not yet required)

Beta invites may go out **before** SOC 2 certification (owner ruling 2026-08-08). Because Beta is invite-only and closed, the compliance surface is narrower — but it is not zero:

- Track S security/hygiene must be green before any real tenant onboards (research: paige-master-implementation-order L12–20). This is the true Beta compliance floor: SSN-key encryption, IDOR closure, RLS tenant isolation, finance-default gating, and de-branding.
- §9 tenant/operator isolation and the §51 per-tier gating railing must hold for all six tiers, with the structurally-enforced sub-account-is-never-agency invariant (research: §51 ABSOLUTE INVARIANT).
- §2 finance/credit prohibition in platform defaults verified (§2).
- Beta invite terms + a subscriber agreement in place (owner ruling 2026-08-08).

### 11.2 Stage 2 — SOC 2 (gate to Public launch)

SOC 2 is **required before public launch** (owner ruling 2026-08-08). It is the single named compliance blocker separating Beta from general availability.

> **Sequencing (research: canonical-build-order §196, W9/#136):** SOC 2 (#136) sits at the end of the pre-launch wave chain, after the Beta-launch gate (W8). This is consistent with the owner ruling — Beta opens first, SOC 2 closes before the public doors.

> **OWNER INPUT REQUIRED — SOC 2 type & timeline:** Whether Beta targets SOC 2 **Type I** (point-in-time) with **Type II** (observation-period) to follow, and the audit-firm engagement date, are not specified in any grounded source. These drive the realistic Beta→Public interval and must be confirmed by the owner.

### 11.3 Stage 3 — HIPAA + others (post-launch)

HIPAA and additional certifications land **post-launch** (owner ruling 2026-08-08). They are not launch-blocking for either Beta or Public, but they gate specific vocabulary and verticals (see 11.4).

### 11.4 The "Practice" wording ban — tied to HIPAA + SOC 2

Per owner ruling 2026-08-08 (cited as **§3.b**): the word **"Practice"** in copy is **prohibited until HIPAA + SOC 2 + related certs land.** Until then, shared/platform copy must use the other inclusive alternatives — *business · clients · work · team* (§2) — and avoid "Practice" as a surface noun.

**RULING EXPANDED (owner, 2026-08-08).** The conflict is now resolved in one direction: **§3.b WINS over §2.** Where §2 currently recommends "practice" as an inclusive prose word and §3.b bans it, **§3.b governs.** The **§2 doctrine wording is being edited** to replace its "practice" recommendation with **"business" / "company"** as the preferred inclusive terms. Concretely: **no "practice" in Marketplace copy or any shipped copy until the HIPAA + certification research is complete.** This is a hard copy gate on every public/shared surface (marketing, sign-up, app chrome, Marketplace listings, platform defaults), enforceable by the §5 compliance officer and §25 design critic.

> **Honesty flag (§13) — doctrine grounding, updated:** As of this pass the research **could not locate a §3.b as a numbered section in `CLAUDE.md`**, and §2's text still *recommends* "practice." The owner has now ruled the resolution (§3.b wins; §2 wording to be edited to "business"/"company"), so the contradiction is **owner-resolved in intent** but the doctrine **file edits are owed**:
>
> - The doctrine sections `Practice Blueprints` and "Practice Vault"/vertical naming across shipped strategy docs still use "Practice" liberally (research: money-spine, blueprints docs) — these need the same sweep once the ban lands.
> - **OWNER-OWED (doctrine file edits, not a decision):** (1) paste §3.b into `CLAUDE.md` as canonical; (2) edit §2 to swap its "practice" recommendation for "business"/"company"; (3) the precise cert list beyond HIPAA + SOC 2 ("related certs") is still **OWNER INPUT REQUIRED**. The ruling (§3.b wins) is authoritative for MVP copy now; the compliance officer enforces the ban on shipped/Marketplace copy immediately, ahead of the doctrine-file paste.

The rationale binding the ban to HIPAA + SOC 2 is that "Practice" reads as a clinical/professional-services term carrying regulated-industry expectations (health, legal, financial advisory); the platform should not present that framing before the certifications that back it are in force. (That rationale is the **crew's inference, not owner-stated** — the owner ruled the *rule*, not the *why*, §13.) The specific cert list beyond HIPAA + SOC 2 ("related certs") is **OWNER INPUT REQUIRED**.

---

## 12. Success Metrics

Metrics are proposed against the two named milestones — **launch** (Beta open) and the **100-subscriber graduation** (owner ruling 2026-08-08). Every metric below is a *proposed, measurable* definition; **specific target numbers the owner has not ruled are marked OWNER INPUT REQUIRED** (§13). The instrumentation to compute several of these is itself partly unbuilt (revenue reconciliation, metered usage) — flagged inline.

### 12.1 Activation

The activation thesis is §36 (intuitiveness is the moat: a non-technical user reaches value within 5 minutes) and the Systems Check MVP (owner ruling 2026-08-08: Paige surfaces gaps one-by-one and actively helps close them).

| Metric | Proposed definition | Target |
|---|---|---|
| Time-to-first-value | Median minutes from first login to first completed Paige action (draft sent, appointment set, or client onboarded) | **OWNER INPUT REQUIRED** — §36 implies a <5-minute *discovery* bar; a first-*value* target is unruled |
| Systems Check completion rate | % of new tenants who close ≥ N of the 10 Systems Check checks in week 1 (comms, website, social, automation, company info, CRM, pipeline, revenue tracking, processors, payment options) | **OWNER INPUT REQUIRED** for N and the % target (checks defined: owner ruling 2026-08-08) |
| Comms configured | % of tenants with email sender + SMS sender + business phone registered | check #1 (owner ruling 2026-08-08); phone registration caveat: no per-tenant telephony yet (research: inventory §1 gap b) |
| First client onboarded | % of tenants who onboard ≥ 1 real client (client signup → agreement signed) within week 1 | signup scaffold EXISTS (research: portal-taxonomy) |
| Portal skin adoption | % of tenants who apply their own brand (logo override + colors) before inviting a client | override mechanism EXISTS (research: portal-taxonomy) |

### 12.2 Retention

Retention is the platform's structural thesis — the Agency account as the compounding NRR unit (research: 1B-growth-map Engine 1; §17).

| Metric | Proposed definition | Target |
|---|---|---|
| Logo/subscriber retention | Monthly subscriber churn rate | **OWNER INPUT REQUIRED** |
| Net revenue retention (NRR) | Expansion (added sub-accounts, upgrades, metered usage) net of churn/contraction | Doctrine references NRR 120%+ as a *valuation* posture (research: 1B-growth-map), **not** an MVP target — MVP NRR target is **OWNER INPUT REQUIRED** |
| Sub-account expansion (Agency) | Avg. sub-accounts per Agency tenant, and % of Agency tenants adding ≥ 1 sub-account in first 90 days | Agency is the growth atom (research: §17); target **OWNER INPUT REQUIRED** |
| Weekly active tenants | % of subscribers with ≥ 1 Paige-driven action per week | agent-driven-surface thesis (§36); target **OWNER INPUT REQUIRED** |
| Autonomy adoption | % of tenants who let Paige run ≥ 1 action at `auto` (green) tier vs. `confirm`-only | §16 autonomy lanes; runtime worker **[PLANNED — not yet on main]** (research: inventory §3) |

### 12.3 Revenue

> **Instrumentation honesty (§13):** Live revenue metrics depend on two builds that do **not** yet exist on `main`: the platform-subscription create/checkout wire (B-Platform #56) and metered-usage capture (B-Meter #57), and on reconciling the 3 subscription stores / 2 webhooks (research: money-spine "Missing"; inventory §4 gap a). Until those land, God-dashboard MRR/ARR figures are **untrustworthy and must not be presented as real** (research: inventory notable gap). Revenue metrics are therefore reportable only *after* P-F2/P-F3 in §10.2 are green.

Pricing basis (owner ruling 2026-08-08): Solo $149/mo · Agency $397/mo · Enterprise custom · Beta = 50% off Solo + Agency · Public = full pricing. Paige holds only its own rails as merchant of record; tenant→client money is facilitator-only (§38).

| Metric | Proposed definition | Target |
|---|---|---|
| Beta MRR | Sum of active Beta subscriptions at 50% pricing (Solo $74.50, Agency $198.50); **excludes Promotional (comp) accounts** | derived from owner-ruled pricing 2026-08-08; subscriber-count target **OWNER INPUT REQUIRED** |
| Paid conversion | % of Beta invitees who become paying subscribers | **OWNER INPUT REQUIRED** |
| Blended ARPA | Avg. revenue per account across Solo/Agency/Enterprise mix | mix assumptions **OWNER INPUT REQUIRED** |
| Trial→paid | If a trial exists in the checkout flow, % converting | trial is named in money-spine Tier 1 model (research); existence at MVP tied to unbuilt B-Platform |

### 12.4 The 100-subscriber graduation milestone

The graduation trigger is owner-ruled: **first 100 subscribers + their teams** (owner ruling 2026-08-08). Proposed milestone scorecard:

- **Count:** 100 paying subscribers reached (the ruled number).
- **Cohort health:** Beta-surfaced bugs triaged and the launch-blocking set closed (per §10.1 QUALITY). The precise "bugs closed" bar is **OWNER INPUT REQUIRED** (§13b).
- **Revenue at graduation:** 100 × blended Beta ARPA — computable once §12.3 instrumentation is green.
- **Retention signal:** early churn across the first cohort below the (owner-owed) threshold.
- **Tier distribution:** Solo vs Agency vs Enterprise split among the first 100 — a leading indicator for the NRR thesis (§17). *Promotional (comp) accounts are excluded from the 100-count (owner ruling 2026-08-08).*

> **OWNER INPUT REQUIRED — the milestone's non-count exit criteria:** the owner ruled the *number* (100 subscribers + teams). Whether graduation also requires a revenue floor, a retention floor, a max open-bug count, or a fixed calendar date is unruled (see also §13b). Do not infer these — they set the Beta→Public trigger.

---

## 13. Owner-Owed Sections

Four items are explicitly owner-owed. Per §13, the crew does not fabricate answers — each is presented with everything research *did* establish, so the owner only supplies the missing decision.

### (a) Agent placement across UI surfaces

**WHAT each of the 6 VPs does is defined** (research: `PaigeAttribution.tsx` VP_ROSTER + `vpDepartments.ts`, the shipped source of truth; roster locked in `paige-c-suite-roster.md §42`). **WHERE each VP lives** — which UI surface hosts it — is owner-owed. The owner ruled the team is *distributed across UI surfaces, not bunched in one chat* (owner ruling 2026-08-08), with Paige reachable from chat per §20.

Shipped remits (source of truth = code; a doc-vs-code divergence exists, see below):

- **PAIGE** — Orchestrator / CEO-Chief-of-Staff; routes every task, owns Executive Office + People/Talent + Owner Ops (research: `vpDepartments.ts`).
- **VERA** — Quality & standards; owns Legal/Compliance; default `confirm` on nearly everything (research: VP_ROSTER; roster §42).
- **NEXUS** — Marketing & growth; owns Marketing; `auto` on approved sequences, `confirm` on new drafts.
- **CURA** — Client success; owns Client Experience; `auto` on lifecycle nudges, `confirm` on retention-critical outreach.
- **MENTOR** — Curriculum & delivery; owns Product/Curriculum; `auto` on infrastructure health, `confirm` on config changes.
- **MERIT** — Sales & revenue; owns Sales + Finance; `confirm` on money, `auto` on tracking/reminders.
- **ZION** — Operations & automation; owns Technology/Automation + Operations/PMO; `confirm` on strategic recommendations (never autonomous strategy).

> **OWNER INPUT REQUIRED — VP surface placement:** For each VP above, name the primary UI surface that hosts it (e.g. NEXUS on Growth, CURA on Clients/Portal, MERIT on Sales pipeline + revenue, MENTOR on Setup/Curriculum, ZION on Command Center automations, VERA on a compliance/approvals rail). The universal surfaces available to place them on are Paige, Command Center, Marketplace, Analytics, Setup; context-specific business surfaces are Clients, Team, Growth (§35). Paige remains chat-reachable across all of them (§20).
>
> **Also OWNER INPUT REQUIRED — resolve the remit divergence:** the roster **doc** (`paige-c-suite-roster.md`, 2026-07-27) assigns *different* remits than the shipped **code** (e.g. doc: NEXUS = "VP Growth (Marketing+Sales)", MENTOR = "VP Operations", MERIT = "VP Finance & People", ZION = "VP Strategy"; code: NEXUS = marketing only, MERIT = sales+finance, ZION = ops+automation). This BRD used the **shipped code** as source of truth. Confirm which is authoritative before the placement is locked, so a VP is not surfaced under a label its shipped behavior contradicts.

### (b) Beta duration + timing

> **OWNER INPUT REQUIRED — Beta duration and timing:** The owner ruled the graduation *number* (first 100 subscribers + their teams, owner ruling 2026-08-08) but not the *duration* or *calendar timing* of Beta. No grounded source specifies a Beta length, a target open date, or a Beta→Public target date (research: no such doc exists). Needed decisions: (1) target Beta open date; (2) whether Beta ends on the 100-count alone or also on a time box / revenue floor / bug-closure bar (ties to §12.4); (3) target Public-launch date, which is downstream of SOC 2 timing (§11.2, also owner-owed). Do not infer these — they set the whole launch calendar.

### (c) Specific Marketplace items available at launch

Research established the Marketplace *substrate* is shipped — Slice 0/1/2 (tier/role/publish, agency-curation allowlist, tenant visibility branch; research: PRs #382/#391/#392), the 3-table model + tenant-scoped RPC, and Paige chat tools (research: MARKETPLACE-DATA-MODEL). A card with a NULL `current_version_id` is a "coming soon" shelf item, not installable (research: MARKETPLACE-DATA-MODEL). What research **did not** establish is the *content* — which specific skills/skins/blueprints ship as installable at launch.

> **OWNER INPUT REQUIRED — launch Marketplace catalog:** Name the specific items that must be live and installable at Beta launch (owner ruling 2026-08-08 scopes "everything shipped in Slice 0–2 + Persona L1" as the *platform*, not the *catalog contents*). No grounded source enumerates a launch item list. Related known facts to decide against: Practice Blueprints are scoped **post-launch v2** (after L8 + SOC 2; research: blueprints doc L54), so a launch catalog likely cannot depend on installable Blueprints; funding/credit items must be per-tenant opt-in, never a default shelf item (§2). Also confirm whether any paid installs are live at Beta or the catalog is free-only (Money-Spine B-ii paid-install shipped but **inert**; research: DONE.md #462).

### (d) Customer-portal Owner-Trilogy taxonomy / matrix

The owner states this taxonomy **already exists**; the crew's job is to verify and reference it, not invent it (owner ruling 2026-08-08). Here is what research **did** and **did not** find:

**Research DID find a shipped portal taxonomy — but in code, not in a doc named "taxonomy":**
- A two-layer model: a Playbook module **catalog** + default order (`tenants.features.playbook_config`) and a tenant **presentation overlay** (`tenants.features.portal_config`), merged by one resolver `src/lib/portal/moduleNav.ts` (research: portal-taxonomy facts).
- The concrete tab set `MODULE_ROUTES` (home, learn/resources, approvals, actions, plus funding-vertical keys gated to opt-in tenants only — §2-clean).
- The tenant-facing skinning surface `PortalStudio.tsx` (brand + tab visibility/order + welcome + "View as Client" preview).
- Client signup + agreement-signing scaffolding shipped (`paige_signed_agreements`, `agreement_templates`, `finalize-agreement`; research: portal-taxonomy facts).
- Tenant logo-override for client surfaces shipped (`get_client_portal_brand`, `useClientPortalBrand`; research: portal-taxonomy facts).

**Research did NOT find:**
- Any single `.md` titled "portal taxonomy" or "portal matrix" — the taxonomy lives in code + the CX-2 strategy spec (`docs/strategy/client-experience-workstream-2026-07-21.md §3`), not a doctrine doc (research: portal-taxonomy `missing_sources`).
- The **richer** composable-portal model (unlimited custom tabs with tier caps, a Blocks/widgets canvas, per-Playbook portal templates) is **doctrine/plan, not built** — the shipped overlay is subtractive/reorder only and *cannot add net-new tabs* (research: portal-taxonomy notable gaps). The owner's MVP asks (unlimited custom tabs, custom menu, client document uploads, holding-company/subsidiary structure inside a client's portal — owner ruling 2026-08-08) largely map to this **unbuilt** CX-2 spec.
- No "holding company / subsidiary" portal primitive distinct from the existing agency→sub-account (`parent_tenant_id`) model (research: portal-taxonomy `missing_sources`).
- No named "skins/snapshots" object — the shipped equivalent is `portal_config` overlay + brand override (research: portal-taxonomy `missing_sources`).

> **OWNER INPUT REQUIRED — confirm the canonical customer-portal Owner-Trilogy taxonomy/matrix:** Point the crew to the canonical source you referenced (owner ruling 2026-08-08 says it already exists). Research found the *shipped* taxonomy in `moduleNav.ts` + the *planned* richer taxonomy in the CX-2 strategy spec — but **no doc that maps the Owner-Trilogy's six components (Systems Check · Vault · Twin · Live Comp Analysis · Newswire · Audit-trail) onto the client-facing portal**. If that matrix exists, name the file so the crew references it; if it is the CX-2 spec, confirm — and note that delivering the owner's MVP portal asks (custom tabs, uploads, holding-company structure) requires the **net-new** composable-portal build, not just the shipped overlay. Separately confirm (§2): the client signup/agreement scaffold and `paige_payment_authorizations` currently carry funding/BTF-flavored wording and must be exposed coaching-generically, not as a platform default.## 14. 2026-08-08 Session Addendum

**Status:** Owner-approved 2026-08-08. Companion updates and cross-references reflecting session shipments + Cowork-locked specs. Additive to sections 1-13; nothing above is changed.

### 14.1 Companion product specs (referenced from earlier sections)

Four Cowork-locked spec docs land alongside this BRD as the concrete design commitments for the MUST-HAVE items in §4:

- `docs/product/agent-ui-placement-spec.md` — Paige agent UI surfaces: right-rail on desktop platform surfaces, ⌘K universal launcher (auth-gated), Customer Portal floating avatar → full-screen chat, Super Admin "Paige Operator" persona (distinct agent identity), agency scope switcher, impersonation banner + dual-identity audit logging. **Answers §13(a) "Agent placement across UI surfaces."**
- `docs/product/promo-account-type-spec.md` — Promotional account type: Solo + Agency tiers only (no Enterprise promo), 30/45/60 day configurable term, Super Admin **unified Invites surface** with type toggle (promo / paid-full / paid-discounted). Data model uses `platform_invites` table with `invite_type` discriminator. Deepens §9.1.
- `docs/product/customer-portal-owner-trilogy-taxonomy-matrix.md` — Customer Portal Owner Trilogy Matrix: 7 pillars × 5 stakeholders (Client / Tenant / Sub-account / Agency / God) with ownership taxonomy (OWN/CONFIG/WRITE/READ/—). §38 money boundary load-bearing on pillar 4. **Answers §13(d) "Customer-portal Owner-Trilogy taxonomy / matrix."**
- `docs/product/interactive-analytics-ui-spec.md` — Interactive Analytics UI: best version on Super Admin (Fleet/Revenue/Ops tabs with cinematic hero cutscene), Agency roll-up + drill (per market-research three-depth pattern), Solo/sub-account persona-curated (coach = compliance/performance/at-risk/renewal; consultant = utilization/profitability/retainers/pipeline; agency-sub = campaign perf/creative refresh/report readiness), client Journey pillar as editorial narrative (not dashboard chrome). Motion + 3D on chrome (hero moments, celebratory beats, drill-down FLIP), never on chart pixels. Grounds the MUST-HAVE MVP item "Robust interactive analytics with impressive UI" (§4 line 178).

### 14.2 Session shipments since 2026-08-08 initial filing

- **Bug A — btf-onboarding bucket** (PR #396 merged): signed-agreement PDF uploads persist again. §32.b-proven; §39 peer-gate caught and fixed a replay-only defect (orphaned `program_onboarding_*` storage policies defensively dropped for DR/preview branch parity).
- **Bug B — 3 tenant logo leakers** (PR #397 merged): broker invite / portal chatbot / coach→client email now resolve tenant brand with a non-Paige fallback. Bonus win: tenant brand threaded through the shared `send-transactional-email` renderer for every future opt-in transactional template; platform emails stay Paige-branded (§37-verified backward-compatible spread order).
- **Voice work**: Ivanna voice on ConvAI agent + Direct-TTS separation; `ELEVENLABS_VOICE_ID` env var wired at edge-function scope.
- **Doctrine amendments in CLAUDE.md**: §47 (MCP-migrations commit SQL same-beat — prevents prod↔repo silent forks), §48 (Cowork rate-limit scope discipline — bounded to the specific rate-limited action).
- **Playwright dev-dep + live-drive helper** (PR #398 near merge): systemic §32 tooling. Chromium download skipped in CI, kept for dev machines. External-URL reachability env-dependent (agent proxy blocks; documented honestly). Closes the "owed live-drives" gap for auth-gated surfaces.

### 14.3 Two owner-owed sections now RESOLVED

- **§13(a) Agent placement across UI surfaces** — RESOLVED by `docs/product/agent-ui-placement-spec.md`. Covers all 5 account types with an explicit answered-questions section for the 4 open items the owner ruled on 2026-08-08 (right-rail collapse default, floating avatar on Customer Portal, ⌘K auth-gated only, Studio primitives + cinematic layer).
- **§13(d) Customer-portal Owner-Trilogy taxonomy / matrix** — RESOLVED by `docs/product/customer-portal-owner-trilogy-taxonomy-matrix.md`. Matrix approach maps pillars × stakeholders with explicit ownership rights, avoiding both the "shipped in code but no doc" gap and the "planned CX-2 spec not built" gap.

Still owner-owed: **§13(b) Beta duration + timing** and **§13(c) Specific Marketplace items at launch** — unchanged.

### 14.4 Canonical build order updated

`docs/doctrine/canonical-build-order.md` updated same-day to reflect: Wave 1/2 fully shipped, new Wave 2.5 for launch-blocker cluster + Playwright infrastructure, Wave 4 restructured as MVP integration hub incorporating BRD-promoted items (L8 Memory moved from W5, Interactive Analytics moved from S2, Playwright browsing moved from W7) + the four Cowork-locked specs, Wave 5 reserved. See that doc for the current wave map + change log.
