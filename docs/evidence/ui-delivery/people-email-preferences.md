# UI delivery evidence: people-email-preferences

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow is installed per-account (owner-side; not readable in this environment — noted honestly). The routed paige-ui-design skill was read COMPLETELY instead (SKILL.md + UPSTREAM.md + vendored frontend-design SKILL.md + accessibility-checklist + paige-quality-gates + review-and-testing) and its workflow followed: job stated, states mapped, tokens reused, rendered tests, honest boundaries.
PAIGE_UI_DESIGN: PASS: All five skill references read in full this session before implementation (listed above); component reuses the setup-tab primitive system (Card/ReadState/Outcome) and Radix Switch + Label (native-paired, keyboard-operable).
MATERIAL_FLOW_CHANGE: YES: The retired notifications destination lands — users GAIN persistent control over what Paige emails them (a goal the retirement banner promised and nothing delivered; #1090). New transitions: toggle→optimistic→upsert→saved | error→revert+retry; master switch off→rows lock; unsubscribed-all→locked+resubscribe path.
FLOW_PROTOTYPE: NOT_REQUIRED: A settings surface with immediate-save toggles — no multi-step funnel, no new exits beyond the section itself; the flow is one control with an honest failure path. (If the owner prefers prototype discipline for any settings addition, this can be prototyped retroactively before further settings work.)
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — the owner controls which emails Paige sends them, in the place the work happens (Settings → Setup → People & Email). Audience — tenant owner/admin. Primary action — flip a toggle and have it persist (upsert, first-use safe).
VISUAL_DIRECTION: PASS: No new visual direction invented — the section is a Card in the existing setup-tab primitive system with the standard label+description+switch row the legacy surface already used. §2 ruling applied: coaching-generic rows ONLY (weekly summary / session reminders / getting-started); credit/funding preferences belong to marketplace-installed funding layers (owner ruling 2026-09-11).
AUTOMATED_EVIDENCE: PASS: src/solo/settings-people-email-preferences.test.tsx — 5/5: (1) rows render with paired labels + ZERO finance vocabulary (§2 regex); (2) toggle UPSERTS {user_id, key} — the legacy update-only first-use no-op regression is pinned; (3) failed save → honest outcome + revert to persisted truth (no false toggle); (4) unsubscribed-all → rows locked + note + master-switch resubscribe; (5) load error → honest copy + Retry + NO default-rendered toggles.
STATIC_EVIDENCE: PASS: ci:tsc ratchet 13=13; eslint 0 errors on all changed files (component, test, SoloBusinessContextSetup).
RENDERED_EVIDENCE: PASS: jsdom render (house createRoot idiom) proving structure, label pairing, switch states, and copy in all five states above. Viewport screenshots: UNVERIFIED this session (no browser harness run) — the layout is a single-column Card in the setup tab's existing scroll owner, with no new fixed heights or horizontal chrome; the Solo viewport matrix run is owed with the next authenticated pass.
BEHAVIORAL_EVIDENCE: PASS: toggle→optimistic→persist and toggle→error→revert→retry exercised in automated tests (above); keyboard operation is Radix Switch (native button semantics, Space/Enter, focus-visible) — manual keyboard drive recorded UNVERIFIED below.
AUTHENTICATED_RUNTIME: UNVERIFIED: The real authenticated Settings → Setup → People & Email drive (owner account, persistence across reload, RLS-path upsert on production) was not run in this session — needs the owner's live app pass. Affected claims: production persistence + the viewport matrix. The weekly-summary toggle now being visible to the owner = the #1090 acceptance.
KEYBOARD_FOCUS: PASS (structural): Radix Switch buttons with real ids + Label htmlFor pairing (asserted in tests); visible focus is Radix default on the tab's token system. Manual screen-reader pass: UNVERIFIED.
ZOOM_REFLOW: PASS (structural): single-column flexible layout, no fixed heights; 200% text and reflow expected to wrap label/description rows — full zoom drive UNVERIFIED with the viewport matrix.
REDUCED_MOTION: PASS (structural): no motion added; the section inherits the app's reduced-motion handling vacuously (nothing animates). UNVERIFIED at browser level by the same token — nothing to observe.
STATE_COVERAGE: PASS: loading (ReadState), load-error+retry, populated, saving (controls disabled), save-error+revert+reload, first-use (no row → defaults rendered then upserted on first toggle), unsubscribed-all (locked+note+resubscribe), master-off (rows disabled). Non-applicable: destructive confirmation (a preference toggle is reversible by construction), workspace-switch (the section reads only the caller's own row via RLS).
