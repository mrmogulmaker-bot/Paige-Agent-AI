# UI delivery evidence: legacy-mark-retirement-command-mark

Owner ruling 2026-09-06: RETIRE the legacy orbital PaigeMark (gold orb + orbital ring + companion
spark) — and the older blue "PAi" monogram raster lockups it sat beside — from every active product
and public surface, and make the Command Mark (a slash + an orb) the one Paige identity, with a
regression guard so no agent can reintroduce the old mark. This record covers the platform-wide
retirement + the approved Social-specific variation.

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow read this session (SKILL.md + inlined orchestration/build references); mode Existing-Project cross-cutting refactor, depth Deep, one integrator + an inventory research agent (complete legacy-mark census) + this session as build/verify. The affected "flow" is brand-mark rendering across surfaces; no actor goal, state, or exit changed. Pre-edit frame + full inventory recorded in the PR body and docs/brain/decision-log.md (2026-09-06 BRAND entry).
PAIGE_UI_DESIGN: PASS: Read .agents/skills/paige-ui-design/SKILL.md and its router this session. Per §00 the owner is the direct design authority; he issued the retirement + Command-Mark ruling and green-lit the Social contrasting-colour + bulging-glyph variation explicitly. The shared operator CommandMark geometry (the pack's, exact) is preserved unmodified; the new work ports it, it does not invent a mark.
MATERIAL_FLOW_CHANGE: NO: brand-mark swap only. No goal, choice, step, transition, state, exit, confirmation, or consequence changed — every migrated call site renders the SAME component contract with a different mark; the truth layers, hooks, routes, and RPCs are untouched.
FLOW_PROTOTYPE: NOT_REQUIRED: no material flow change — a logo/identity swap on already-shipped surfaces, issued as a direct owner ruling. The Command Mark variations were rendered and shown to the owner for the colour pick (a review artifact, not a production flow prototype).
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Audience is everyone who sees Paige's brand — visitors (marketing/landing), users signing in (auth), operators and tenants (app chrome), and the Solo owner on Campaigns → Social. Purpose: one consistent, current brand identity (the Command Mark) everywhere; retire two dead marks. No primary action changed.
VISUAL_DIRECTION: PASS: Owner ruling 2026-09-06. Command Mark = current identity; orbital PaigeMark + blue monogram = retired. New keystone src/components/brand/PaigeCommandMark.tsx renders the pack's slash+orb geometry with self-contained, theme-aware colours (works with or without a data-pg shell). Social gets a contrasting variation (indigo plate + gold slash/orb, glyph bulging out) — token-based, an approved local treatment, NOT a new logo. Gold rides the glyph only; plates are neutral (§11, gold-discipline lint clean).
AUTOMATED_EVIDENCE: PASS: `npx vitest run` = 3569 passed / 242 files; `node scripts/ci/tsc-ratchet.mjs` no new errors (baseline 13, current 13); `npx vite build` succeeded (fresh, after asset deletions — proves no source imports a deleted raster); `node scripts/ci/legacy-mark-lint.mjs` PASS + `--self-test` PASS (17 cases); `node scripts/gold-discipline-lint.mjs` clean on the new CSS; `npx eslint` clean on migrated files (no no-restricted-imports violations, only pre-existing warnings).
STATIC_EVIDENCE: PASS: The regression guard (scripts/ci/legacy-mark-lint.mjs, wired as lint:legacy-mark in package.json + ci.yml) scans active source (src/** + public/** + index.html) for the reintroduction paths a static guard can detect: the retired mark's distinctive gradient hexes (#FCE7B6/#FFF4D8), its orbital animation classes (paige-orbit-spin/orb-breathe/spark-drift), any PaigeMark component/import/tag, AND a re-drawn tilted-ring geometry (a rotated stroked <ellipse>) — PASS with zero offenders; self-test PASS (19 cases). It is a belt-and-suspenders backstop, not an absolute proof (a bespoke re-draw using only the general brand-gold palette and non-rotated shapes could still evade it). eslint no-restricted-imports bans the legacy module paths. Social CSS is token-only (contract test's hex guard passes; the star-slash comment bug that had dropped the plate rule is fixed).
RENDERED_EVIDENCE: PASS: Headless Chromium 1194. Committed under docs/evidence/ui-delivery/assets/legacy-mark-retirement/: command-mark-noshell-light.png + command-mark-noshell-dark.png (keystone on a bare page, plated + bare, three sizes) and social-mark-variations-obsidian.png (the three approved Social colour variations + the ×5 bulge close-up + the eclipse) and splash-command-mark.png (the index.html pre-React splash). Plus the post-review theme proof: with `<html class="dark">` AND an emulated OS-LIGHT preference, the plate renders DARK (the app's next-themes `.dark` class now wins over the OS preference — the fix for review BLOCKER 1), and a height-only caller (`height:44px`, no width) measures 44×44 square (the fix for review MAJOR 2). Zero page errors across all renders; both themes clearly distinct (§23).
BEHAVIORAL_EVIDENCE: PASS: The full jsdom suite (3569 tests) mounts the real migrated components — including PaigeAttribution (which now renders PaigeCommandMark), the Solo social-command surface, and the operator/tenant shells — and all pass. This is a harness drive against jsdom (§32/§70.1), not authenticated runtime; the migration changes only which mark renders, adds no interaction.
AUTHENTICATED_RUNTIME: UNVERIFIED: This headless session holds no browser path to the authenticated live app (live prod unreachable from the sandbox). The keystone + Social mark are proven at the component level (real render, both themes, no shell); the authenticated full-page in-shell render across every migrated surface (landing, auth, admin, agency, Studio, tenant chrome, Solo Social) is owed to a browser-capable session / the owner's live look per §32.c.
KEYBOARD_FOCUS: PASS: No focusable element changed anywhere. The migration swaps a mark component/`<img>` for another mark component; marks are decorative (aria-hidden via label={null}) or carry an aria-label, exactly as before. Tab order, focus management, and focus rings (indigo, contract-asserted on Social) are unchanged.
ZOOM_REFLOW: UNVERIFIED: The Command Mark is a small fixed-size element (icon/plate) that adds no new horizontal content, so it cannot introduce new overflow; the Social surface's zero-horizontal-overflow reflow to a 490px content column was proven by geometry read. A dedicated browser 400% / 320px-CSS-px-floor pass across the migrated pages was not run and is owed to a browser-capable session.
REDUCED_MOTION: PASS: PaigeCommandMark's `animated` breathe is no-oped under `@media (prefers-reduced-motion: reduce)` (paige-command-mark.css); the Social orb corona keeps its contract-asserted `social-orb-ring { animation: none`; the index.html splash keyframe is no-oped under reduced-motion; the retired orbital animation keyframes were removed. Verified via the CSS rules; a live OS-preference toggle was not exercised.
STATE_COVERAGE: PASS: The mark renders identically across states (it is chrome, not stateful). Social first-use / populated / eclipse were rendered; the keystone plated + bare + animated variants were rendered; the splash was rendered. The full suite exercises the components that host the mark in their real states.
TRUTHFUL_STATE_LABELS: PASS: No LIVE/PARTIAL/UNAVAILABLE claim changed — no truth layer, figure source, hook, or RPC was touched by the mark swap. No metric or capability was added or fabricated.
SOLO_UI: YES: src/solo/social-command.tsx + .css (the Social Command Mark variation) and the inline orbital Logo in src/solo/_shared.tsx (→ Command Mark geometry) both changed — recognized Solo UI paths.
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: the Social surface's geometry is unchanged by the mark swap (the mark is a 30px header element); prior geometry read confirmed docHOverflow=0, spHOverflow=0, kpi=5, stage=6, glyph=4 at the full 1240px content column. The authenticated Solo shell (real PAIGE panel/tenant/scroll owner) was not driven — owed to a browser-capable session (§32.c).
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: geometry unchanged by the mark swap; prior read clean at the 1126px docked-rail content column. Authenticated shell not driven — owed (§32.c).
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: geometry unchanged; prior read clean at 1240px. Authenticated shell not driven — owed (§32.c).
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: geometry unchanged; prior read clean at the 956px docked-rail column. Authenticated shell not driven — owed (§32.c).
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: geometry unchanged; prior read clean at 1024px. Authenticated shell not driven — owed (§32.c).
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: geometry unchanged; prior read clean at the 614px docked-rail column. Authenticated shell not driven — owed (§32.c).
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: geometry unchanged; prior read clean at 900px. Authenticated shell not driven — owed (§32.c).
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: geometry unchanged; prior read clean at the 490px docked-rail column (the narrowest real case). Authenticated shell not driven — owed (§32.c).
UNVERIFIED: (1) AUTHENTICATED_RUNTIME — the authenticated full-page in-shell render of every migrated surface (owner live-drive owed, §32.c); this is also why the eight SOLO_* rows are UNVERIFIED (geometry unchanged by a header-element swap, but the real shell was not driven). (2) A dedicated browser 400% / 320px-floor zoom pass across the migrated pages. (3) A live OS reduced-motion toggle (per-effect fallback present + asserted, not toggled live). (4) OPS / UNAVAILABLE from a code PR: the Supabase-storage email logo `email-assets/paige-logo-transparent.png` (used by ~11 transactional email templates) still shows the retired monogram until a Command Mark PNG is uploaded to the storage bucket — a code PR cannot rotate a prod storage asset. (5) A second authenticated known-good tenant context for the Solo surface.

## Scope and collisions

- Classification: platform-wide brand-mark retirement + an approved Solo-specific mark variation. Presentation/identity only.
- Affected surfaces: 25 PaigeMark component importers (marketing, auth, admin, agency, Studio, ArtifactPreview, Paige primitives, onboarding), PaigeSymbol's command branch, inline orbital Logos (src/solo/_shared.tsx + src/agency/_shared.tsx), the index.html splash, the landing Header / ResetPassword / AppNav `<img>` logos, and the favicon/pwa/og rasters.
- Neighboring regressions: none found — full suite 3569/3569, tsc + build green.
- §58 anti-regression: the orbital PaigeMark and the blue monogram are removed by explicit owner instruction; the Command Mark replaces them on every surface (no capability lost). The removals are named here, not silent.
- Active-owner/file collisions: none. No migration, RPC, edge function, hook, or truth layer changed. The shared operator CommandMark geometry is untouched (§00 lock respected).
- Explicit exclusions / kept: paige-ai-avatar.png is the Paige persona character illustration (not a brand mark) — intentionally kept. paige-halo-pulse keyframes kept (the Studio cutscene aurora uses them).

## User job and state map

Purpose: present one current Paige identity (the Command Mark) everywhere and retire the two dead
marks. The mark is chrome, not a stateful flow — it renders the same across a surface's states.
Intended scroll owner and layouts are unchanged (the mark is a fixed-size element). The regression
guard makes the retirement durable: active source cannot reintroduce the orbital mark.

## Evidence index

- Commands (repo root): `npx vitest run` (3569 passed / 242 files); `node scripts/ci/tsc-ratchet.mjs` (13→13); `npx vite build` (green); `node scripts/ci/legacy-mark-lint.mjs` + `--self-test` (PASS); `node scripts/gold-discipline-lint.mjs src/solo/social-command.css src/components/brand/paige-command-mark.css` (clean); `npx eslint` on migrated files (clean).
- Rendered: the four committed frames under docs/evidence/ui-delivery/assets/legacy-mark-retirement/ (keystone no-shell light + dark, Social variations, splash), plus the regenerated public/favicon.png, pwa-192/512, og-image.jpg (Command Mark), driven with Playwright/Chromium 1194, both themes, zero page errors.
- Tenant context: representative fixture for Social; the keystone rendered with no tenant/shell to prove surface-agnostic colour. Timestamp: 2026-09-06. No secrets or customer data present.

## Review and limitations

Independent review ran on the real pushed diff and its findings were folded before merge:
- **§39 adversarial peer-gate — NOT SHIP (1 BLOCKER + 2 MAJOR + 2 MINOR), all addressed:** BLOCKER 1 (keystone ignored the app's next-themes `.dark` class, so the plate followed the OS not the toggle) → fixed: theme now keys on `.dark`/`[data-theme]`/`[data-pg]`, not prefers-color-scheme, and baked-dark marketing/auth marks (Auth, PaigeHome, ChooseAccount) are bare (theme-independent gold on their always-dark ground); re-rendered under `.dark`+OS-light to prove it. MAJOR 2 (height-only callers rendered a non-square pill because `:where` set both axes) → fixed: `:where` sets height only, aspect-ratio supplies width; proven 44×44 square. MAJOR 3 (guard missed a re-drawn orbital ring / public/ assets) → added a rotated-<ellipse> rule + public/ scan + self-test cases (now 19), and softened the "prevents return" claim to the paths a guard can actually detect. MINOR 4 (the cutscene mark would freeze under OS-reduce, against the owner-locked 2026-07-19 Studio-motion ruling) → scoped override so it follows the Studio rule. MINOR 5 (stale PaigeMark comments, incl. one pointing at the deleted module) → the misleading ones corrected.
- **§5 compliance — ITERATE (one MEDIUM = the same theme-signal gap as BLOCKER 1, now fixed) — otherwise SHIP-quality:** confirmed §00 (operator CommandMark byte-identical), §58 (removals named), §13 (evidence matches diff, OWED items honest), §BRAIN (glossary/decision-log/master-doc in-commit), guard quality, plated/bare coherence, and full category coverage.
Limitations: every UNVERIFIED item above, chiefly the authenticated full-page live drive (owner, §32.c) and the Supabase-storage email logo (ops upload). This record attests to what was checked; each PASS traces to a named command, artifact, or render above.
