# UI delivery evidence: the Solo shell's Owner/Team label derives from authoritative membership (INT-071 census repair)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: WAIVED: owner-decision=PR #1285 owner ruling 2026-09-20 (INT-083); reason=the Flow-by-Flow skill is not installed in this delivery environment. Source-level grounding kept for the record: the touched surface is the Solo shell's Owner/Team label wiring in SoloApp.tsx plus the new pure derivation src/solo/shell-role.ts; the label's data path was traced end-to-end at the source level (useTenantContext resolves the active tenant and the authenticated subject server-side; the tenant+user-keyed has_tenant_role probe pair resolves the membership owner and admin verdicts; TenantCommandCenterShell renders the label from the userRole prop, and the defective prior wiring read the display-only tenants.owner_user_id pointer — the two census Owner seats 7e700000… and d8a0a880… were shown "Team workspace" because that pointer names someone else).
PAIGE_UI_DESIGN: PASS: the paige-ui-design skill was read in full this session (SKILL.md, UPSTREAM.md, the vendored frontend-design core, the vendored accessibility checklist, references/paige-quality-gates.md, references/review-and-testing.md). Module routing for this presentation-only change: paige-owner-intent-fidelity applies (the owner intent contract is the coordinator instruction itself — membership ownership is the authority for the label); paige-visual-immersive-quality NOT needed (zero visual delta — the same shell <small> label renders from the same tokens; recorded why per the skill's rule); paige-interaction-geometry-accessibility applies at the structural level (no new controls, no new motion; the label is existing text); paige-protected-behavior-regression applies (the CTA halves and the readiness notice are the neighboring protected seams — pinned unchanged by the suite); paige-release-acceptance-evidence is this record.
MATERIAL_FLOW_CHANGE: NO: presentation-only label-source swap per the skill's material-flow test — the user gains, loses, or changes no goal, choice, step, transition, state, exit, or side effect; the rendered label is byte-identical for tenants whose pointer already matched their membership seat, and the two census seats change from a false "Team workspace" to the truthful "Owner workspace"; authorization contracts (solo_setup_access_scope, RLS, the save RPC) are untouched.
FLOW_PROTOTYPE: NOT_REQUIRED: the paige-ui-design skill's presentation-only clause — an adjustment that changes no action, state, exit, or consequence records NOT_REQUIRED with a reason; this change alters only which server-derived fact feeds an existing static label.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — the shell never shows a false "Team workspace" to an authoritative Owner seat, and never shows "Owner workspace" on the pointer's say-so; Audience — the Solo owner/admin/member in the canonical shell; Primary action — use the shell exactly as before, with a truthful workspace label.
VISUAL_DIRECTION: PASS: existing shell tokens; the change swaps the data source of one existing <small> label — no new colors, motion, chrome, or layout.
AUTOMATED_EVIDENCE: PASS: src/__tests__/solo-setup-readiness-notice.test.tsx — 26/26 (19 carried + 7 new, all failing-first: the suite was red on the missing shell-role module before implementation). New coverage: a membership OWNER sees the Owner label ("admin"); membership ADMIN and MEMBER both see Team ("coach"); an unresolved verdict fails to Team; a verdict keyed to a prior tenant OR prior user is dead (account-switch never leaks the previous workspace's label); the derivation's code contains no owner_user_id input (comments may name it; code may not — asserted on comment-stripped source); SoloApp wires shellRole through soloShellRole(roleProbe, activeTenantId, activeUserId) with sabotage-sensitivity (re-wiring to the pointer fails the pin); the probe state carries owner and admin verdicts separately. The updated a1c5cfd0 keying regression pins the renamed roleProbe shape. src/__tests__/setup-gate-journey.test.tsx — 16/16 unchanged. Mutation-proven load-bearing (each fails exactly its pinning test): re-wiring the label to the pointer; dropping the tenant+user keying; conflating membership admin into the owner label; failing OPEN while unresolved.
STATIC_EVIDENCE: PASS: eslint clean on all three changed files; tsc ratchet at baseline (12 = 12); zero supabase/ diff — no migration, no edge function, no server contract touched.
RENDERED_EVIDENCE: PASS: structural — the label is existing text inside the existing TenantCommandCenterShell header; the pure derivation is exercised directly by the suite across the owner/admin/member/unresolved/switch matrix. Per-viewport captures: UNVERIFIED below — no authenticated browser session is held by this workstream and the preview deployment is Vercel-SSO-gated; the mandatory matrix is explicitly owed rather than falsely claimed.
BEHAVIORAL_EVIDENCE: PASS: structural/pure — the account-switch matrix is proven at the derivation level (prior-tenant and prior-user verdicts are dead values). Manual browser drive: UNVERIFIED below.
AUTHENTICATED_RUNTIME: UNVERIFIED: the real authenticated pass (a membership owner of a pointer-mismatched tenant signing in and seeing "Owner workspace"; an admin and a member seeing "Team workspace"; an account switch showing Team until the new identity's verdict resolves) was not driven this session — no browser credentials for owner/admin/member sessions on the census tenants are held; it is owed as the acceptance act after merge+deploy.
KEYBOARD_FOCUS: PASS: structural — no new interactive controls; the label is static text. Manual screen-reader pass: UNVERIFIED with the matrix.
ZOOM_REFLOW: PASS: structural — a static <small> label in the existing shell header; no fixed dimensions introduced. Full zoom drive UNVERIFIED with the matrix.
REDUCED_MOTION: PASS: structural — no motion introduced or removed.
STATE_COVERAGE: PASS: membership owner (Owner label), membership admin (Team), member (Team), unresolved verdict (Team — fail-closed), account switch (Team until the keyed verdict resolves; prior verdict dead), RPC error (both verdicts false — Team; CTA falls back to the isPrimaryOwner half exactly as before), pointer/membership disagreement (membership wins by construction — the derivation has no pointer input).
TRUTHFUL_STATE_LABELS: PASS: the label states exactly the membership-ownership truth for the active tenant+user; the suite pins that the pointer expression is gone from the wiring and absent from the derivation's code.
SOLO_UI: YES: the change alters rendered Solo shell content (the workspace label); per-viewport captures below are UNVERIFIED (owed with the authenticated pass), not NOT_APPLICABLE.
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: the mandatory browser matrix could not be driven — no authenticated owner/admin/member credentials are held and the preview deployment sits behind Vercel deployment protection; owed exactly as: verify the truthful workspace label at this size with PAIGE closed, no outer-page scroll, no clipped navigation, PAIGE reachable, reminder behavior unchanged.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: same boundary as above; the same checklist applies.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: same boundary as above; the same checklist applies.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: same boundary as above; the same checklist applies.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: same boundary as above; the same checklist applies.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: same boundary as above; the same checklist applies.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: same boundary as above; the same checklist applies.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: same boundary as above; the same checklist applies.
UNVERIFIED: the authenticated browser acceptance pass (a membership owner seeing "Owner workspace", an admin and a member seeing "Team workspace", an account switch resolving to Team first), owed after merge+deploy; everything else is proven at the journey, source-contract, pure-derivation, and mutation-proof level this session.

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->

INTERNAL_BUILD_IDENTITY: d4f80150ec142d57533b9b9a5776952f818d0023; deployment=none-pre-merge; environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this-PR-and-src/__tests__/solo-setup-readiness-notice.test.tsx
RELEASE_CHANNEL: development: the identity above names the exact CODE head this record attests — d4f80150, the last commit touching this PR's product/tests; this evidence-and-docs commit rides on top of it (branch build, environment=development); the frontend change deploys via Vercel on merge; no edge bundle or migration changed so neither edge-live nor db-live moves
RELEASE_CLASSIFICATION: internal-only: a truthful-label presentation repair over untouched authorization seams; no owner-decided customer release beyond the correction itself
CUSTOMER_RELEASE_IDENTITY: none: internal truthfulness repair of an existing static label
RELEASE_NOTE_REQUIRED: NO: the visible delta is a truthful workspace label for seats that were mislabeled; no flow, action, or capability changed
RELEASE_TRUTH_BOUNDARY: PARTIAL: the new contract is proven by 26/26 tests with failing-first and four load-bearing mutations; the authenticated browser acceptance pass on the census tenants is owed after deploy
RELEASE_RECOVERY: position=forward-only recovery — the change is a label-source swap over untouched data and authorization seams, so recovery is a forward PR restoring the pointer-derived label if the owner ever reverses the ruling; reference=git revert of the merge commit plus the suite in this PR which pins the membership-derived contract
