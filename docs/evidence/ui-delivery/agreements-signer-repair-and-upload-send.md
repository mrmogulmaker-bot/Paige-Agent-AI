# UI delivery evidence: agreements — signer repair path and the uploaded-document send

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: `anthropic-skills:flow-by-flow` v2.0.1 invoked and read in full this session (SKILL.md plus its inlined orchestration/audit/build/review/verification references); Deep depth selected on the R2/R3 floor (persistence, an outward-facing legally binding act, and a cross-lane contract). Flow packet: actor = workspace owner/admin; goal = get a signed agreement back from a client; the affected states are draft -> sent and the no-signer refusal state; the regression surface is the SignatureSender panel and the two Solo data hooks named below.
PAIGE_UI_DESIGN: PASS: `.agents/skills/paige-release-acceptance-evidence/SKILL.md` read in full (module 5) along with `docs/evidence/ui-delivery/TEMPLATE.md` and `scripts/ci/ui-delivery-evidence.mjs` itself, so the record is written against the enforced contract rather than a remembered one. No new visual direction is introduced: the added banner reuses the panel's existing `so-banner so-banner-warn` treatment and `so-field` inputs, and the one new CSS rule (`.so-signer-add`) only lays those existing primitives out in a row.
MATERIAL_FLOW_CHANGE: YES: a previously TERMINAL state gains an exit. A client with no email address on file yields no signer, so the send refused with "Add at least one signer before sending" and nothing on any surface could add one — the person was told what was wrong and handed no control that would fix it. The refusal is unchanged; the dead end is not. Separately, the uploaded-document send changes what the counterparty receives (their real contract rather than a document byte-identical to an empty one), which is a changed consequence of the same flow.
FLOW_PROTOTYPE: NEEDS_OWNER_DECISION: no prototype artifact was produced and no prototype waiver has been given, so this field is deliberately left unsatisfiable rather than self-answered. The owner ruled the CHANGE (relayed instruction: fix the `AgreementClient` mapper, then wire the `add_agreement_signer` producer), which is the first half of the template's waiver grammar; the second half — waiving the prototype gate itself — is the owner's to give and is not mine to write on their behalf. Resolve as either `PASS: <prototype artifact + approval reference>` or `WAIVED: owner-decision=<reference>; reason=<reason>`.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: audience is the Solo/Sub-account workspace owner or admin on Campaigns -> Sales -> Commercial Terms (`/solo/:account/growth/sales?view=terms`); the primary action is sending a document for signature, and the added control exists only to unblock that action when the selected client has no address the link can reach. It is shown solely in the state that blocks the act (`!signing.signerName && state === "draft"`) and is absent otherwise.
VISUAL_DIRECTION: PASS: no new direction. The banner is the panel's existing warn treatment (`so-banner so-banner-warn` with the shared shield glyph), the two inputs are existing `so-field` controls, and the act reuses `btn btn-s btn-p`. Gold is untouched and stays on the panel's existing primary send act (§11 gold budget). The single added rule `.so-signer-add` is layout only — flex row, wrap, 10px gap.
AUTOMATED_EVIDENCE: PASS: `npx vitest run src/solo/sales-ops.contract.test.tsx src/solo/sales/deriveSalesCommand.test.ts` — exit 0, 2 files, 108 tests passed, on the merged tree. The lane's exact-match label assertions ("Or copy a link to send yourself", "Make the link", "Send it to Acme") still hold, which is the negative control for this change: nothing on the existing send path was renamed or moved.
STATIC_EVIDENCE: PASS: `npx tsc --noEmit -p tsconfig.app.json` — 12 errors, NONE in any agreement file, measured per-file and per-error-code against the pre-edit baseline; three intermediate states of this change added errors and were corrected rather than accepted. `lint:views`, `lint:pack-lineage`, `lint:operator-reach`, `lint:chat-tool-registry`, `lint:definer-fns`, `lint:tier-features`, `capability-kit-lint`, `action-risk-lint` and the PAIGE Spine registry lint all exit 0. `deno check` exit 0 across five function bundles. Earlier tsc figures on this engine came from `npx tsc --noEmit` with no project flag, which typechecks nothing in this repository; those were void, not a baseline.
RENDERED_EVIDENCE: UNVERIFIED: no browser is reachable from this build environment — it is a headless cloud container with no display, no Playwright-driven session against a deployed preview, and no authenticated account to reach `/solo/:account/growth/sales`. Nothing was screenshotted, so no rendered claim is made at any viewport or theme. Affected claims: the banner's appearance, its wrap behaviour beside the existing recap block, and its contrast in either theme.
BEHAVIORAL_EVIDENCE: UNVERIFIED: the browser drive of the repair path — select a client with no address, see the banner, fill both fields, add the signer, watch the refusal clear and the send become possible — was not performed, because the same missing browser and missing authenticated session block it. What IS proven is the contract beneath it: `add_agreement_signer` is draft-only and admin-gated in the database, the hook reports a resolved promise carrying no `signer_id` as a failure rather than a success, and the button stays disabled until a name and an `@`-bearing address are present.
AUTHENTICATED_RUNTIME: UNVERIFIED: this environment holds no tenant credentials and §63 forbids the owner's real business accounts as fixture targets, so the authenticated route was never opened. Affected claims: that a real workspace owner can complete the repair and then send; that the uploaded document now reaching the signer is the operator's own file. Recovery: a session holding a scoped test-tenant login drives `/solo/:account/growth/sales?view=terms` end to end.
KEYBOARD_FOCUS: UNVERIFIED: not driven, for the same absent-browser reason. Structurally the two inputs are native `<input>` elements inside `<label className="so-field">` with explicit `aria-label`s, and the act is a native `<button>`, so they take the document's natural tab order and the panel's existing focus-visible treatment; that is markup inspection, not an observed keyboard route, and is recorded as such.
ZOOM_REFLOW: UNVERIFIED: no zoom drive was performed. The added row is `display:flex; flex-wrap:wrap` with `flex: 1 1 180px` fields and no fixed widths, which is the shape that reflows, but the 200% behaviour is unobserved.
REDUCED_MOTION: NOT_APPLICABLE: this change adds no animation, transition or transform of any kind — one flex layout rule and static form controls — so there is no motion for a reduced-motion preference to alter.
STATE_COVERAGE: PASS: the added control covers idle (both fields empty, act disabled), partially filled (act still disabled until a name and an `@`-bearing address are present), submitting (`Adding…`, panel inert via the existing `busy` gate), server refusal (the panel's existing `notice` line carries the server's own sentence — draft-only, not-an-admin, bad address — rather than a generic failure), and success (fields cleared, list re-read so the server-assigned signing order is shown rather than guessed). The banner itself is absent in every state except an unsent draft with no signer.
TRUTHFUL_STATE_LABELS: PASS: the banner states the condition ("This document has nobody to sign it") and names the cause (the client has no email address on file) rather than showing a disabled control with no explanation. The success path re-reads from the server instead of patching the row locally, so nothing is displayed that the server did not return. On refusal the hook returns `ok:false` and the surface reports it — a resolved promise carrying no `signer_id` is never rendered as an added signer.
SOLO_UI: YES: Campaigns -> Sales -> Commercial Terms, the SignatureSender panel on `/solo/:account/growth/sales?view=terms` (`src/solo/sales-ops.tsx`).
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: no browser in this headless container, so no viewport was rendered or captured.
UNVERIFIED: every rendered, keyboard, zoom and viewport claim above; the authenticated drive of both the repair path and the corrected upload send; and the production apply of migrations `20270409000000` and `20270410000000`. All four stem from one environment fact — this container reaches no browser, no authenticated session and no database — and all are owed to a session that does.
OWNER_INTENT: the owner's relayed instruction that a client with no email on file must stop being a dead end: fix the `AgreementClient` mapper that was discarding the address, then wire `add_agreement_signer` a producer. Target actor is the workspace owner/admin; the user job is getting a signed agreement back from a client the workspace cannot currently email.
MUST_NOT_HAPPEN: no relabelling or relocation of the existing send path, which the UI lane had already made correct (`Send it to {firstName}` gold primary; `Make the link` demoted behind `Or copy a link to send yourself`, where it genuinely still issues a link); no signer added to an agreement that has already been shown to somebody, which would change what they agreed to after they agreed to it; no client record mutated — the address entered here is the agreement's signer, not an edit to the client.
MUST_PRESERVE: the §58 copy-a-link secondary route and its shipped labels; the panel's gold budget, with gold staying on the send act; the server's own refusal sentences, surfaced verbatim rather than replaced with a generic message; the draft-only rule, left to the RPC rather than pre-empted in the client so the surface cannot drift from it.
ACCEPTANCE_CRITERIA: on the real platform, an owner selects a client with no email address, opens Send for signature, sees the banner naming the blocker, enters a signer name and address, adds the signer, sees the blocker clear, and sends — with the counterparty receiving the operator's own uploaded contract rather than an empty document. Not yet completed by a human; that is the authenticated-runtime gap above.
MOTION_PURPOSE: NONE: no motion change.
PROTECTED_SEAMS: AFFECTED+TESTED — the Solo agreements read/write hooks (`useSoloAgreementSignings`, `useSoloAgreements`) and the SignatureSender panel, all three covered by `sales-ops.contract.test.tsx` at 108 passing tests including the untouched-label negative controls. UNAFFECTED+NAMED — the public `/sign/:token` signer page and its §28 frozen styling (not opened by this change); `issue_agreement_signing_link` and the copy-a-link route (unchanged); the Commercial Terms band and `tenant_client_agreements` (this change writes no commercial state); the DocuSign functions (untouched, as the brief required).
INTERNAL_BUILD_IDENTITY: 2c3dc4b99b1bd4a5e9ab1c93dc16661f6c5aad1c (PR #1382 head at authoring; merged main 2f8364c4 rather than rebased); deployment=none-by-this-pr; environment=development; migrations=NOT_APPLICABLE on production — `20270409000000_agreement_learn_autonomy_catalogue.sql` and `20270410000000_agreement_overview_widened.sql` are replayed from zero by the `database-contract` CI job and have never been applied to prod; edge=NOT_APPLICABLE — `agreement-send`, `sign-agreement` and `agreement-document` deploy on merge to main, not from this branch; evidence=this record plus the CI job logs referenced above.
RELEASE_CHANNEL: development: verified in the development environment only — local suites, local lints, and CI's own migration replay. Not deployed by this PR; no staged rollout.
RELEASE_CLASSIFICATION: internal-only: no customer-visible release is claimed. Note that the defect this corrects is live on production, so the FIX is internal-only while the DEFECT is not.
CUSTOMER_RELEASE_IDENTITY: none: no customer release identity is earned or proposed; this is a correctness fix inside an unreleased engine.
RELEASE_NOTE_REQUIRED: no: no customer-visible feature change; the engine has no customers on it yet.
RELEASE_TRUTH_BOUNDARY: PARTIAL: the defect is LIVE and proven by executed smoke (the NULL-body and empty-body renders share a digest, 904 bytes, exit 0); the corrections carry static, type and automated proof only; the rendered, behavioral and authenticated-runtime classes are PROOF OWED, and the production apply of both migrations is PROOF OWED.
RELEASE_RECOVERY: position=revert of this PR restores prior behavior; both migrations are additive apart from the DROP/recreate that `paige_agreement_overview`'s return-type change requires, so a revert must recreate the prior function signature rather than only dropping the new one; reference=this record plus the migration headers, which state the prior shape.

## Scope and collisions

- Classification: correctness fix plus one added repair affordance; Deep depth, R2/R3 floor.
- Affected flows: send an agreement for signature (Solo/Sub-account/Enterprise); the no-signer refusal state within it.
- Neighboring regressions: the copy-a-link secondary route and the existing send act, both covered by the untouched-label assertions.
- Active-owner/file collisions: the UI lane holds `src/solo/sales-ops.tsx` and rewrote it across `dd9d5ef5`, `22cf317e`, `f5d64cbb`, `8d1b7319`; this change inserts at a block boundary rather than inside their hottest hunk, and adopted their `SigningsData` type over an `Omit<>` extension of mine at the merge. PR #1376 is in the same engine on pre-signing security blockers and should be read against this one before either merges.
- Explicit exclusions: the agreement chat tools (unblocked by INT-003's resolution, sequenced separately); Rail attachment (blocked on two recorded preconditions); the production apply of both migrations.

## User job and state map

Recorded in `PURPOSE_AUDIENCE_PRIMARY_ACTION`, `STATE_COVERAGE` and `ACCEPTANCE_CRITERIA` above. The scroll owner is unchanged — the existing `so-editor-body` panel; the banner is inserted at the top of that scroll region, where the blocker it names blocks everything below it.

## Evidence index

- `npx vitest run src/solo/sales-ops.contract.test.tsx src/solo/sales/deriveSalesCommand.test.ts` — exit 0, 108 passed.
- `npx tsc --noEmit -p tsconfig.app.json` — 12 errors, per-file and per-error-code distribution unchanged from baseline, none in agreement files.
- `deno check` — exit 0 across `agreement-send`, `sign-agreement`, `agreement-document`, `_shared/agreements/knowledge.ts`, `_shared/paige-spine/registry.ts`.
- Defect reproduction smoke (Deno, against the real shipped module) — exit 0; NULL-body render byte-identical to empty-body render.
- CI `database-contract` job on `49c1cc1e` — the migration version collision it caught, and the renumber that answered it.

No secrets, tenant data or customer-identifying values appear in this record or in the artifacts it names.

## Review and limitations

The independent review passes on this work ran as delegated reviewers rather than a second human, and are labelled accordingly. Their surviving findings are parked with severities assigned by the reviewing coordinator, not self-assessed: the 10-argument `record_capability_run` overload's suspected default `EXECUTE TO PUBLIC`; `_workspace_event_display`'s `ELSE` branch; `tenant_knowledge_docs.created_by` being NOT NULL against a nullable source; `list_tool_autonomy`'s absent per-key defaults; and the `tenant-agreements` bucket row that no migration creates.

The limitation that matters most: nothing in this change has been seen running. The corrections are proven correct in type, in contract and in test, and the defect they answer is proven real by execution — but no human has completed the job on the real platform, and this record does not claim otherwise.
