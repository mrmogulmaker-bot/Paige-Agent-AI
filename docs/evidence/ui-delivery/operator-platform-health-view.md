# UI delivery evidence: operator-platform-health-view

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: AGENTS.md mandatory routing followed; the account-synced flow-by-flow skill is not installed on this machine (verified: ~/.claude/skills/ has no flow-by-flow; the repo README .claude/skills/README.md documents it as per-account). Its repo-native binding ran instead: second-brain STEP 1 reads (brain README, config registry n/a, decision log, release policy), AGENTS.md capability routing answered in docs/delivery/platform-operator-command-center.md, paige-ui-design SKILL.md read complete + its five modules applied by concern (owner-intent contract recorded in the Phase 0 map § slice ledger; visual fidelity to the owner-locked v3 pack; interaction/geometry via the harness drive; protected seams below; this record is module 5).
PAIGE_UI_DESIGN: PASS: .agents/skills/paige-ui-design/SKILL.md read in full this session; routed references read (operator CLAUDE.md lock, PACK-INVENTORY-v3.md consulted for the Analytics slot, paige-ia.js L268–L294 and L496–L508 opened for THIS exact surface — the pack's block, not memory). Design-quality skills applied per the 2026-09-11 owner rule: structure ports verbatim from the pack; no invented blocks.
MATERIAL_FLOW_CHANGE: NO: a read-only evidence view replaces a stopgap surface at an existing address; no goal, state, transition, exit, or consequence is gained or lost beyond the view's own content. The one flow-shape change — Team Pulse displaced (§58) — is a content relocation recorded in viewSources carries, not a new user flow.
FLOW_PROTOTYPE: NOT_REQUIRED: read-only view, no forms/funnels/multi-state user choices; all states are read states (loading/empty/error/success) covered below.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: audience is the platform operator (super_admin/platform_admin); job is to read the platform's real operating condition in one view (latest sweep outcome, what could not run, alert firings and acknowledgement state, run history); primary actions are the ledger rows' deep links into Fleet systems-check/history and Settings alerts.
VISUAL_DIRECTION: PASS: the owner-locked v3 pack (src/operator/CLAUDE.md THE LOCK) — paige-ia.js ledgerByView['Platform health'], the Analytics slot ledger, and the four chart cards at L496–L508. Labels, titles, meta lines, row names, and the authored slot foot port verbatim; figures are data. Ruling F plate treatment (--pg-raised + rim + lift-1) follows the console's established pattern.
AUTOMATED_EVIDENCE: PASS: src/operator/surfaces/PlatformHealthSurface.test.tsx — 9 tests, all passing: pack structure verbatim, derived figures (5/8 from the controlled run; pack fixture 4/10 asserted absent), rule-3 composed deferral foot, ledger rows resolve from real check keys, honest absences render, Team Pulse absent, no whitespace-before-punctuation; plus pure-derivation tests (null counts stay null; skip+error = could-not-run; findingByKey). Full operator suite: 25 files / 232 tests green.
STATIC_EVIDENCE: PASS: eslint clean on all five touched files; vitest 232/232 in src/operator; ci:tsc ratchet shows exactly ONE error, verified pre-existing on clean main (issue #1180 — paige-capability-gateway.test.ts, introduced by #1175, not by this diff; proven by stash-run-restore on 2026-09-12).
RENDERED_EVIDENCE: PASS: driven per the console's own rule via scripts/live-drive/dev-loop.mjs against the harness mount (real OperatorShell + real dispatch, dev-only unauthenticated entry, fake Supabase by design): dark and light, 1600×1000. Geometry matches CD's compiled reference exactly in both themes (rail 216, spine 416, no document scrollbar, zero page errors). Frames: scripts/live-drive/artifacts/OURS-dark-1600.png, OURS-light-1600.png (gitignored by design — evidence for conversation, not committed artifacts). The drive renders the view's honest fail-closed error state (the mount's fake Supabase cannot answer the read), which is the designed behavior, and proves the surface mounts through the real dispatch (not absence, not Team Pulse).
BEHAVIORAL_EVIDENCE: PASS: loading/empty/error/success states asserted in the static-render tests with controlled reads (loading skeletons, no-run state, read-failure card, derived success figures); ledger deep links resolve to real routes (/operator/fleet/systems-check, /operator/fleet/history, /operator/settings/alerts) per operatorAddress.
AUTHENTICATED_RUNTIME: UNVERIFIED: no operator credentials exist in this environment and §63 forbids the owner's real accounts as fixture targets; the authenticated drive at the real guarded route (real sweep rows, real firings, real ack latency) remains owed to an operator session. This is the same boundary the harness mount itself records ("a local render is not a deployed one").
KEYBOARD_FOCUS: PASS: ledger rows are links with visible focus rings (focus-visible:ring-2 ring-inset); no other interactive controls added; the shell's existing keyboard paths are untouched. Static markup asserts the link semantics.
ZOOM_REFLOW: PASS: grid layout (lg:grid-cols-2 charts, single column below) reflows without fixed widths; tabular-nums figures; truncation with title attributes on long check names. Zoom-specific drive not performed in this environment — UNVERIFIED at 200% specifically.
REDUCED_MOTION: PASS: the only motion is the shared loading pulse (animate-pulse) consistent with the console's existing skeleton treatment; no new parallax/transition added.
STATE_COVERAGE: PASS: loading (per-read skeletons + chart pulses), read-error (fail-closed card naming the failure, no figures inferred), no-run (honest "No sweep has run yet"), success (derived figures), partial reads (series/ack fail independently of the snapshot — each reports its own error, others keep standing), fixture-free everywhere.
TRUTHFUL_STATE_LABELS: PASS: statuses derive from real findings (Pass/Failed/Could not run/No finding); LLM error rate chart renders UNAVAILABLE-by-copy (no metric series substrate) rather than a drawn line; Time to acknowledge renders real median when acknowledged firings exist, honest absence otherwise; no LIVE/PASS claim rests on rendering alone.
SOLO_UI: NO: Platform Operator console — a separate server-enforced authority plane (operator.platform), not a tenant Solo surface; the four-viewport Solo proof bar does not apply. Console geometry proof is the dev-loop match above, both themes.
UNVERIFIED: authenticated-runtime drive at the guarded route with real data (owed to an operator session); 200% zoom reflow specifically; production-deployed render.

OWNER_INTENT: the owner's 2026-09-12 Platform Operator Command Center mandate — Phase 2's first view: a read-only Platform Health command center from real evidence only, each item naming its source, freshness, scope, honest availability; no fabricated metrics; Phase 0 map first.
MUST_NOT_HAPPEN: no fabricated metric, health event, or "all clear"; no fixture presented as real data; no tenant identity/data/access crossing into the operator view; no parallel health database, audit log, or registry; Team Pulse capability not silently dropped (§58 — displaced with record, component preserved, owed to the v3 Settings → Team port).
MUST_PRESERVE: the six-slot IA and 32 view names (untouched); RequireOperator guard and server-side boundary (untouched); Systems Check surface and its snapshot seam (composed, not duplicated — react-query shared); alerting reads for the rules surface (untouched; this view's firing reads are distinct queries); the pack's verbatim copy at the surfaces this view neighbors.
ACCEPTANCE_CRITERIA: an operator at /operator/analytics/platform-health reads the latest sweep's passed/failed/could-not-run from the real run record, sees which checks could not run and why, sees unacknowledged firings and acknowledgement latency from the real firing table, can reach systems check/history/alerts from the rows, and every figure that lacks a substrate says so instead of rendering a number.
MOTION_PURPOSE: NONE: no motion change beyond the shared skeleton pulse.
PROTECTED_SEAMS: AFFECTED+TESTED — analytics/platform-health dispatch (viewSources.test.ts walks the ledger; PlatformHealthSurface.test.tsx pins the rendered content); UNAFFECTED+NAMED — RequireOperator guard contract (RequireOperator.test.tsx green), OperatorShell geometry (OperatorShell.test.tsx 9/9 green, dev-loop rail/spine match), IA parity with the pack (operatorIA.test.ts green), Fleet surfaces (FleetConsole/SystemsCheck/History/AlertRules tests green), TrustCompass/Knowledge dispatch (green).

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->
INTERNAL_BUILD_IDENTITY: to-be-filled-at-merge (exact PR head SHA + merge commit recorded in the shipped-log row per §4.0); deployment=not deployed by this PR (client-only slice, ships with the next platform deploy); environment=internal; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this record + dev-loop frames.
RELEASE_CHANNEL: internal: operator-console workstream under Gate A per the owner mandate; no staged rollout.
RELEASE_CLASSIFICATION: internal capability slice — a read-only operator view; not a customer-facing outcome.
CUSTOMER_RELEASE_IDENTITY: none: internal Platform Operator surface; no tenant-visible change.
RELEASE_NOTE_REQUIRED: no: internal operator tooling; no customer-visible behavior change.
RELEASE_TRUTH_BOUNDARY: the view's figures are LIVE only where their substrate is real and read (sweep run record, findings, firings); LLM error rate is UNAVAILABLE (no series substrate — said so on the surface); authenticated-runtime presentation is PROOF OWED (no operator session drove the guarded route yet); the Command Center as a whole is PARTIAL (Phase 2 first view; Phases 2-remainder–5 owed per the slice ledger).
RELEASE_RECOVERY: position=client-only revert of this PR restores the prior stopgap mount (FleetTeamPulseSurface at the address — one-line viewSources change); reference=this record + the §58 displacement note in viewSources.ts.

## Scope and collisions

- Classification: Platform Operator console UI slice (read-only), plus a two-line platform fix to scripts/live-drive/dev-loop.mjs (Windows-invalid file:// reference URL — verified ERR_INVALID_URL before, loads after).
- Affected flows: Analytics → Platform health view only.
- Neighboring regressions: none observed — full operator suite green; IA/dispatch contract tests green.
- Active-owner/file collisions: none at PR open (checked 2026-09-12: no open PR touches src/operator/** or the seams composed). Owner flagged calendar/harness/orchestration/chat streams as actively landing — this slice composes only stable read seams (systems-check snapshot RPC, run/firing tables) and does not touch their files.
- Explicit exclusions: no new RPC, table, migration, edge function, or registry; no changes to the guard; no Team Pulse re-placement (recorded as owed).

## User job and state map

Operator opens Analytics → Platform health. Title row states what reads here. Latest-run ledger: passed X/N, failed (with composed severity detail), could-not-run, and a composed deferral foot naming the actual unrun checks. Platform-health ledger: five rows (resolver integrity / RLS posture / migration drift / run history / alert firings) each naming its check key or table, figures real or "—", rows deep-linking to the surfaces that own the detail. Charts: sweep outcome (12-run stackbars), checks that could not run (per-check, latest run), LLM error rate (honest absence — no series substrate), time to acknowledge (real median when acknowledged firings exist). Scroll owner: the shell's surface body region (docScroll=no verified); charts grid reflows to one column below lg.

## Evidence index

- Tests: node node_modules/vitest/vitest.mjs run src/operator/ → 25 files / 232 tests passed (2026-09-12, includes the 9 new).
- Lint: npx eslint <five touched files> → clean.
- Types: npm run ci:tsc → 1 error, verified pre-existing on clean main via git stash -u round-trip (issue #1180).
- Drive: node scripts/live-drive/dev-loop.mjs --at "/?at=/operator/analytics/platform-health&theme=dark|light" --theme dark|light, 1600×1000, harness mount (npm run harness:serve, 127.0.0.1:5199) → OURS frames + geometry match vs REF (rail 216 / spine 416 both themes, docScroll=no, errors=none). Frames in scripts/live-drive/artifacts/ (gitignored by design).
- Static render content proof: PlatformHealthSurface.test.tsx (controlled reads, derived figures asserted, fixture-absence asserted).

## Review and limitations

- Independent review: not yet performed (PR review owed); self-review caught and fixed one destructuring bug (snapshot exposes isError, not error) and one test-data inconsistency (aggregate vs findings counts) before any run.
- Remaining limitations: authenticated drive owed (no operator credentials here; §63); 200% zoom untested; the "Latest run" Passed-row status reads "Attention" while any failure exists (derived, honest — the pack's fixture said "Pass" unconditionally); run-aggregate and per-finding counts render independently and could disagree on a crashed run (each names its source; a reconciliation rule is a later slice if the owner wants one).
- Findings outside scope: issue #1180 (main's ratchet red — filed when found, not absorbed).
