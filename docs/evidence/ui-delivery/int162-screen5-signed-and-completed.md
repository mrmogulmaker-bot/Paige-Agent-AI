# UI delivery evidence: INT-162 screen 5 — Signed and completed

The fifth and last of the owner-approved screens for the Agreements lane (Artifact
`S271qc7uGTFXbdNoTz49VC`, §28 APPROVED-FROZEN). Screens 1–4 shipped in #1356, #1359 and #1362;
this record covers screen 5 only. The lane-wide picture stays in
`docs/evidence/ui-delivery/int162-agreements-and-signing.md`, which is updated in the same commit.

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: flow-by-flow v2.0.1 with flow-prototype v2.0.1 paired at the same version (Gate 5 PASS); mode Existing Project, risk R2 (a read-only surface over an existing contract, no new write path) so depth Standard; affected flow = a Solo owner opens a signed agreement and sees what was signed, by whom, and how it got there, then retrieves the sealed copy or returns to their record.
PAIGE_UI_DESIGN: PASS: impeccable read; reference/craft-floor.md read immediately before the first UI edit. The visual direction was NOT invented — screen 5 was ported rule for rule from the owner-approved pack, which itself copies src/solo/solo-tokens.css verbatim, per §00 (Flow-by-Flow then Impeccable, owner approves).
MATERIAL_FLOW_CHANGE: YES: the act on a completed agreement changes from "Download the signed copy" — a file with no account of how it came to be signed — to "View the signed record", which opens a surface carrying the seal, the recorded audit trail, and three exits. A new read of paige_agreement_events is added to the adapter.
FLOW_PROTOTYPE: PASS: owner-approved prototype Artifact S271qc7uGTFXbdNoTz49VC Version 2, screen 5 of 5, approved 2026-09-22 and recorded §28 APPROVED-FROZEN. The pack's own CSS for .seal, .orb (halo/body/ring), .trail and .ev was read from the artifact and ported under the surface's .so- prefix rather than reinterpreted; its markup was read to settle the heading structure and the closing paragraph.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: purpose is to show an owner that an agreement is settled and to account for how it got there; audience is the Solo business owner or tenant admin; the primary action is retrieve the sealed PDF, with opening the client's record and returning to Commercial Terms as the two secondary exits.
VISUAL_DIRECTION: PASS: the committed Solo palette. Green is the settled state and gold is the act that produced it, meeting once on the seal plate — the only place in this flow the two share a surface. Gold is spent on the orb's travelling corona and the seal's hairline, never on a resting control; the sealed-copy act is violet (btn-p), matching the approved pack. The corona SWEEPS rather than pulses, because a pulse on a finished document would read as something still running.
AUTOMATED_EVIDENCE: PASS: src/solo/sales-ops.contract.test.tsx EXIT CODE 0, 83 tests (81 before this change). Two tests added, both proven NON-VACUOUS by restoring the defect they guard: re-introducing the duplicated title fails exactly one of 83, and the trail assertions read DOM that exists only on the new surface (five rows, two settled marks, distinct minute stamps, recorded actor/address/device, the "by owner" fallback where the engine recorded none). One PRE-EXISTING guard was found to be over-broad and tightened rather than deleted — the "evidence chips never spend gold" check spanned lazily from the first chip rule to any later gold declaration anywhere in the stylesheet, so it passed only while no gold rule happened to sit below the chips, and failed on a screen-5 rule that has nothing to do with chips; it now reads the chips' own declaration blocks, and injecting gold into one still fails exactly one test.
STATIC_EVIDENCE: PASS: tsc --noEmit EXIT 0; vite build EXIT 0; npm run lint:tier-features EXIT 0 across 1456 files. HONEST LIMIT, stated rather than implied: src/solo/sales-ops.tsx carries // @ts-nocheck, so tsc gives ZERO signal on the file this change is mostly in — the clean tsc above must not be read as covering it, and the suite plus the browser drive are its only evidence.
RENDERED_EVIDENCE: PASS: rendered in real Chromium through the repo's sales-mount harness, which mounts the real components inside the real shell scroll chain with only the network boundary stubbed. The full surface drive (scripts/live-drive/sales-ops-drive.mjs) passes 536/536 EXIT CODE 0 with HORIZONTAL OVERFLOW 0 across all 48 column states. Screen 5 was additionally shot in BOTH themes at 900x1000 with its assertions read from the live DOM rather than from the image: five trail rows, two carrying the settled mark, exactly one heading, the seal 136px tall, the orb ring 80px wide (which is what proves the inset override actually applies rather than losing a specificity tie), the sealed-copy act present, and no sideways scroll inside the sheet. Frames S5-completed-light.png and S5-completed-dark.png in scripts/live-drive/artifacts/sales-ops (gitignored). LOCAL renders, never deployed or authenticated captures (§32.c).
BEHAVIORAL_EVIDENCE: PASS: the surface is reached the way a person reaches it and not by mounting it directly — the drive clicks the completed row in the rendered table, then "View the signed record", then waits for the trail read to resolve. Driven and asserted: the trail loads and renders the recorded events newest-first; a trail whose READ FAILS says so and renders zero rows while the seal and the sealed-copy act still stand; a completed document with no sealed copy says why instead of offering a control that cannot work; and a reader who cannot write can still retrieve the copy, because reading a signed contract is a read and the storage policy decides it. Harness and double evidence, NOT authenticated runtime; the two are not interchangeable.
AUTHENTICATED_RUNTIME: UNVERIFIED: no authenticated proof exists, for two independent reasons, both named rather than blurred. The backend (INT-163) is not deployed, so paige_agreement_events has no rows to read anywhere; and LIVE_DRIVE_EMAIL / LIVE_DRIVE_PASSWORD are unset in this session, so the authenticated Solo route cannot be signed into — that second blocker is credentials, not capability. The end-to-end drive is OWED to a capable session once the backend is live. No claim of a working production read is made here.
KEYBOARD_FOCUS: PASS: the surface is a real modal dialog and behaves like one. It reuses the shared focus trap and inert treatment; with no head row there is no first control to receive focus, so the panel itself takes it and a screen reader reads the dialog and its name rather than starting mid-surface. Its accessible name comes from the seal's heading. ESCAPE NOW CLOSES IT — the shared hook deliberately leaves dismissal to the caller because only the caller knows whether a save is in flight, and this surface is read-only, so there is no ambiguity. On close, focus returns to the control that opened it.
ZOOM_REFLOW: PASS: the sheet is width-capped at min(760px, 100vw - 28px) and converts to a full-height sheet below 560px, where the seal stacks to a column and the trail's time column drops its fixed width rather than squeezing the description. Measured in the shot: no horizontal overflow inside the sheet in either theme; the full surface drive measures zero horizontal overflow across all 48 column states including the 439px tightest real column.
REDUCED_MOTION: PASS: the orb's entrance and its 22-second corona are both removed under prefers-reduced-motion: reduce. This is covered by the sheet-wide guard already in sales-ops.css (.so-editor * with !important), which is where the orb lives — stated explicitly because a third copy of that rule was written and then deliberately REMOVED rather than left as a duplicate to keep in sync, and "I deleted a reduced-motion block" must not be mistaken for "motion safety is missing".
STATE_COVERAGE: PASS: trail loading, trail read-failed (kept distinct in words from a document with no history, because rendering both as an empty list states the wrong fact), trail empty, trail populated with mixed settled and in-flight events, an event the engine recorded with no address or device (falls back to who did it rather than a blank), an event with no timestamp, a completed document with a sealed copy, one without, a sealed-copy mint that refuses, and a reader who may read but not write.
TRUTHFUL_STATE_LABELS: PASS: the trail is READ from paige_agreement_events ordered by seq, never reconstructed from the row's four timestamps — the distinction the surface exists to carry, since timestamps can say a document was viewed but not by which address, from which device, or in what order. Nothing absent is filled in. Times render at MINUTE resolution because date-only rendering made events nine minutes apart identical and hid the ordering the trail exists to prove; the seal's sentence was carrying the day while the trail below carried the minute, so the surface disagreed with itself about how precisely this is known, and both now state the minute.
INTERNAL_BUILD_IDENTITY: f065199328334c71a174fc7c2a7791558655912a; deployment=none-pre-merge; environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this-PR+src/solo/sales-ops.contract.test.tsx(83-tests-EXIT-0)+scripts/live-drive/sales-ops-drive.mjs(536/536-EXIT-0)+two-theme-screen-5-shot-with-live-DOM-assertions
RELEASE_CHANNEL: development: branch build (environment=development); a frontend-only change that deploys via Vercel on merge. No supabase/migrations file and no supabase/functions bundle changed, so neither db-live nor edge-live moves.
RELEASE_CLASSIFICATION: internal-only: the last of five owner-approved prototype screens ported into the tenant app (§28 APPROVED-FROZEN), plus a harness-fixture repair and a tightened CI guard; no owner-decided customer release, and the backend this surface reads is not deployed.
CUSTOMER_RELEASE_IDENTITY: none: pre-launch platform with no live customers on the surface; the owner reviews on the live site per §4 and no customer release identity has been earned or decided.
RELEASE_NOTE_REQUIRED: NO: no customer holds an account on this surface and the backend contract it reads is undeployed, so there is no customer-visible behaviour change to announce.
RELEASE_TRUTH_BOUNDARY: PARTIAL: the surface, its states and its refusals are fully proven headless — 83 tests EXIT 0 with two new guards proven non-vacuous by defect restoration, 536/536 harness checks EXIT 0 with zero horizontal overflow, and a two-theme render whose claims are read from the live DOM rather than from the image. What is NOT proven is any read against a live database: paige_agreement_events has no rows anywhere yet, so the trail is proven against the contract and the harness, never against real recorded history. tsc is additionally NOT a signal for src/solo/sales-ops.tsx, which carries // @ts-nocheck.
RELEASE_RECOVERY: position=fully reversible by revert — the change is frontend-only and additive, creating no table, column, index or stored object and altering no deployed contract, so reverting the merge commit restores the previous completed-row act ("Download the signed copy") with no data or runtime state to unwind, and the harness fixtures and CI-guard tightening revert with it; reference=git revert of the merge commit
SOLO_UI: YES: Campaigns → Sales → Commercial Terms (src/solo/sales-ops.tsx, src/solo/sales-ops.css, src/solo/useSoloAgreementSignings.ts).
SOLO_1536X770_PAIGE_CLOSED: PASS: rendered via the sales-mount harness at 1536x770 with PAIGE docked-closed, light and dark, frames fit-{light,dark}-1536-paige-closed-terms.png. Measured: zero horizontal overflow, the shell is the single vertical scroll owner, the Commercial Terms column fits. Harness render, NOT a deployed or authenticated capture.
SOLO_1536X770_PAIGE_OPEN: PASS: rendered at 1536x770 with PAIGE open (521px column), light and dark, frames fit-{light,dark}-1536-paige-open-terms.png. Measured: zero horizontal overflow, single shell scroll owner.
SOLO_1366X768_PAIGE_CLOSED: PASS: rendered at 1366x768 with PAIGE docked-closed (685px column), light and dark, frames fit-{light,dark}-1366-paige-closed-terms.png. Measured: zero horizontal overflow, single shell scroll owner.
SOLO_1366X768_PAIGE_OPEN: PASS: rendered at 1366x768 with PAIGE open — the 439px tightest real column, the case a full-window drive would never test. Frames fit-{light,dark}-1366-paige-open-terms.png. Measured: zero horizontal overflow, single shell scroll owner; the column scrolls vertically, which the drive records as an expected scroll rather than a fit.
SOLO_1024X768_PAIGE_CLOSED: PASS: rendered at 1024x768 where PAIGE becomes an overlay rather than a dock, light and dark, frames fit-{light,dark}-1024-overlay-terms.png. Measured: zero horizontal overflow, single shell scroll owner.
SOLO_1024X768_PAIGE_OPEN: PASS: covered by the same 1024x768 overlay frames — at this width PAIGE opens AS an overlay above the surface rather than narrowing it, so the open and closed geometries of the page itself are identical. Named rather than duplicated, because reporting two measurements of one geometry would overstate the evidence.
SOLO_900X1000_PAIGE_CLOSED: PASS: rendered at 900x1000 (overlay), light and dark, frames fit-{light,dark}-900-overlay-terms.png, and this is also the viewport the two dedicated screen-5 frames were shot at. Measured: zero horizontal overflow, single shell scroll owner.
SOLO_900X1000_PAIGE_OPEN: PASS: covered by the same 900x1000 overlay frames, for the reason given in the 1024 open row — PAIGE overlays rather than narrows at this width.
UNVERIFIED: the authenticated runtime drive of this surface against a live database. It is owed to a capable session after INT-163 deploys, and additionally needs credentials this session does not hold. The Vercel preview does not substitute: that deployment sits behind Vercel SSO, a credential gate rather than a capability one.
OWNER_INTENT: an owner opens a signed agreement and can see it is settled, see who signed it and when, account for how it got there, and take the sealed copy — the approved screen 5, delivered rather than described.
MUST_NOT_HAPPEN: never reconstruct the trail from the row's timestamps and present it as recorded history; never render a failed READ as an empty history, because they are different facts; never fill a missing address, device or time with a plausible-looking blank; never claim the commercial engagement is finished because the signature is; never spend gold on a resting control.
MUST_PRESERVE: the sealed-copy retrieval and all four of its tests, which are RE-POINTED to the new surface rather than removed — the act now lives one click deeper, behind "View the signed record", and the tests click through to it instead of asserting it on the row. The read-not-write distinction is preserved: a reader who cannot manage can still retrieve the copy. Nothing shipped was removed (§58).
ACCEPTANCE_CRITERIA: on the real platform, an owner opens a completed agreement and sees the seal naming who signed and at what minute, a trail of the events the engine actually recorded in their real order with the actor and address where it has them, and three exits that work — download the sealed PDF, open the client's record, return to Commercial Terms. NOT YET PROVABLE on the real platform: the backend is undeployed, so this is asserted against the contract and the harness, never against a live read.
MOTION_PURPOSE: motion says this is finished, not running. The orb's corona sweeps once every 22 seconds — a seal turning slowly, not a pulse, because a pulse reads as live traffic on a document that has stopped moving. The entrance is a single spring pop on arrival and nothing else on the surface animates. Both are removed under reduced motion.
PROTECTED_SEAMS: the sales-mount live-drive harness is an impacted seam and is TESTED. Shooting this screen exposed a real gap in it: the completed signing fixture pointed at an agreement id that does not exist, so it joined nothing and THE TERMINAL STATE OF THIS ENTIRE FLOW had never rendered in any frame and could not be reviewed — the same class of gap as the signings modes that had no call site. A matching client and agreement were added, and the stub gained the trail read (a READ, which a local fixture can answer honestly, unlike the writes beside it, which still refuse). The full drive passes 536/536 EXIT 0 after the change. Unaffected and named: the Catalog surface, Revenue, Scenarios, the legacy n8n and Zapier drawers, and every other live-drive harness.
