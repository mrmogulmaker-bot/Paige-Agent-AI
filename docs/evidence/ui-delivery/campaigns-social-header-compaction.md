# UI delivery evidence: campaigns-social-header-compaction

Solo → Campaigns → Growth → Social: two owner-ruled fixes to the Social Command header (owner,
2026-09-06). (1) Retire the Solo-local orbital swirl mark and use the shared brand `PaigeMark` — the
same mark the Paige chat and the menu render — everywhere on this surface: the header lockup and the
full-surface eclipse orb. "Stay to that one and that one only." (2) Compact the command brief so it
stops eating the fold restating why the operator is on the tab: the 25px display headline becomes a
modest 15.5px/600 line, the standalone "PAIGE — your chief of staff" signature line is removed, and
the hero padding/gap tighten. Presentation-only — `src/solo/social-command.tsx` (view) and
`src/solo/social-command.css` (styles) only. The projection layer (`social-truth.ts`), the hook
(`useSocialCommand.ts`), the `growth2.tsx` mount, both migrations, and every figure's source are
byte-identical, so every value still comes from a source a test can call and every capability the
platform holds no seam for still renders its honest absence.

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow read this session (SKILL.md + inlined orchestration/build references); pre-edit frame produced — mode Micro-Task / Existing-Project refinement, depth Quick→Standard, one actor-goal flow unchanged (the Solo owner reading Campaigns → Social). No contract, seam, state, or exit touched; only the header mark and the brief's density changed. Recorded in this PR.
PAIGE_UI_DESIGN: PASS: Read .agents/skills/paige-ui-design/SKILL.md and its router wrapper this session before editing. No Claude Design pack exists for this Solo tenant surface; per §00 the owner is the direct design authority here and issued both changes verbatim (retire the swirl, use the chat/menu PaigeMark and only that; the brief takes up far too much space) — ported faithfully, not invented, substituted, or critiqued. The correct mark is @/components/brand/PaigeMark, already the shared mark on the landing header/footer/intro and the public sign-up flow.
MATERIAL_FLOW_CHANGE: NO: presentation-only. No goal, choice, step, transition, state, exit, confirmation, or consequence changed — the record-accounts write, the next-move routing, the phase gates, and every honest-absence branch are behaviorally identical. Only the header lockup's SVG (a decorative, aria-hidden mark) and the brief's typography/spacing changed, and one decorative signature line was removed.
FLOW_PROTOTYPE: NOT_REQUIRED: no material flow change — a logo swap and a header-density refinement on an already-shipped, owner-approved surface, issued as a direct owner correction. No new journey, state, or interaction to prototype.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Audience is the Solo business owner. Purpose unchanged — answer at a glance what PAIGE is doing to grow the business socially and what she cannot yet see — but the brief now leads compactly so the KPI tiles and the "Do this next" move sit higher in the fold. Primary action unchanged: record the accounts the business posts from (Update accounts), or act on the single next move; Ask PAIGE unchanged.
VISUAL_DIRECTION: PASS: Owner ruling 2026-09-06. The header/eclipse mark is now the shared brand PaigeMark (gold orb + orbital ring + companion spark + halo), rendered bare and small in the lockup (25px, no plate) and at 46px inside the eclipse orb body — matching how the chat and menu render it. The retired Solo swirl (CommandLockupMark + the inline orbital SVG in PaigeOrb) is deleted, not gated. Brief compaction: --sd-* desk vocabulary and Solo tokens unchanged; headline 25px→15.5px/600, signature line removed, hero gap 16→11px and padding 17/20/18→13/18/14. Gold still spent only on the act (contract-asserted); focus rings var(--violet).
AUTOMATED_EVIDENCE: PASS: `npx vitest run src/solo/social-command.contract.test.ts src/solo/social-command.render.test.tsx` = 86 passed; `node scripts/ci/tsc-ratchet.mjs` no new errors (baseline 13, current 13); `node scripts/ci/regression-lint.mjs` clean (origin/main…HEAD); `node scripts/gold-discipline-lint.mjs` clean on both changed files; `npx eslint src/solo/social-command.tsx` exit 0; `npx vite build` succeeded.
STATIC_EVIDENCE: PASS: The ship-blocking contract test's static assertions all pass on the new bytes — tokens-only (zero hex outside mask lines; PaigeMark's gradient hexes live inside the imported shared component, not this stylesheet), no --danger/--success, focus ring var(--violet), the prefers-reduced-motion block still contains `social-orb-ring { animation: none`, the forced-colors block is present (the deleted .social-cmd-mark plate was dropped from its border list since the bare mark carries no box), min-width:0 count still >6 (31), and the .social-kpi/.social-stage/.social-panel-glyph counts are unchanged (5/6/4). Truth layer + hook are contract-locked and untouched.
RENDERED_EVIDENCE: PASS: Real component DOM (the actual PaigeMark SSR'd via react-dom renderToStaticMarkup for the eclipse state; the header/hero fragments carry the same real PaigeMark markup) + the shipped CSS rendered headless (Chromium 1194), both themes. Durable artifacts committed with this record under docs/evidence/ui-delivery/assets/campaigns-social-header/: populated-obsidian.png and populated-mineral.png (the header lockup shows the correct PaigeMark and the compacted brief, dark + light), firstuse-mineral.png (compacted first-use), and eclipse-obsidian.png (the full-surface eclipse orb now rendering the correct PaigeMark inside the token corona). Zero page errors across all rendered states (PAGE ERRORS: []); both themes clearly distinct (light genuinely light, §23).
BEHAVIORAL_EVIDENCE: PASS: The jsdom render suite (social-command.render.test.tsx, 86 assertions with the contract file) mounts the real SocialCommand and asserts each state renders — first-use ("Nothing is on record yet"), populated, the record dialog flow, the honest-absence branches, and the KPI/pipeline/panel-glyph counts. This is a harness drive against an in-memory double (§32/§70.1), not authenticated runtime; the change is presentational (a mark swap + CSS density) and adds no interaction, so no new behavior was introduced. The authenticated interaction drive is owed (see UNVERIFIED).
AUTHENTICATED_RUNTIME: UNVERIFIED: This headless session holds no browser-driving tool and no path to the authenticated live Solo shell (live prod is unreachable from the sandbox — net::ERR_CONNECTION_RESET). Renders are the real component code + the shipped CSS, not the authed production surface with the real PAIGE panel and a real tenant. Per §32.c the authenticated live drive of the deployed Solo → Campaigns → Social — confirming the correct PaigeMark renders in-shell and the header sits higher in the real viewport — is owed to a browser-capable session (Cowork/Chrome) or the owner's live look.
KEYBOARD_FOCUS: PASS: No focusable element changed. The change removed a static, non-focusable <p> (the signature line) and swapped two decorative, aria-hidden SVG marks; the header's actionable controls (Ask PAIGE, Update accounts) and the record dialog are untouched, so tab order and focus management are identical to the shipped surface. The global focus-visible ring is 2px var(--violet) offset 2px on every .social-page and dialog button (contract-asserted).
ZOOM_REFLOW: UNVERIFIED: A dedicated browser 400% / 320px-CSS-px-floor zoom pass was not run, so this is not claimed as PASS. What IS proven: a programmatic geometry read this session confirmed zero horizontal overflow (document and .social-page) and clean @container single-column reflow at every Solo content-column width down to 490px (the narrowest real case) with the compacted header. Since the change only reduces the header's vertical footprint and type size and adds no new horizontal content, it cannot introduce new overflow; the 320px-floor zoom behavior is owed to a browser-capable session.
REDUCED_MOTION: PASS: The orb corona still writes its own fallback — the contract asserts the exact `social-orb-ring { animation: none` within the prefers-reduced-motion block. The mark itself is rendered static: PaigeMark is used without the `animated` prop, and its own keyframes (.paige-orbit-spin/.paige-orb-breathe/.paige-halo-pulse/.paige-spark-drift) are all no-oped under prefers-reduced-motion in index.css per the component's contract. Verified via the CSS rule + contract assertion; a live OS-preference toggle was not exercised.
STATE_COVERAGE: PASS: first-use (compacted brief, honest absences), populated, and the unavailable/eclipse state (the PaigeOrb path with the swapped mark) are all rendered as durable frames; the full state set (resolving, loading, unavailable, error, first-use, populated, record open/type/submit/clear/refusal/read-only, handlesUnknown, notPermitted, authorityUnknown, campaigns-unread, pending-error) is covered by the render suite, which is unchanged and passes.
TRUTHFUL_STATE_LABELS: PASS: The truth vocabulary (LIVE · PARTIAL · PROPOSED · UNAVAILABLE) and every figure come from social-truth.ts (unchanged). Figure still renders an em-dash for null and never a zero-as-count; channel reach is always the absent mark; queue and placements are always UNAVAILABLE. No metric, roll-up, or capability was added or fabricated — the contract's metric-negation and banned-literal guards pass. The compacted brief's copy is the same builder output (buildBrief), only styled smaller.
SOLO_UI: YES: Solo → Campaigns → Growth → Social (src/solo/social-command.tsx), mounted by src/solo/growth2.tsx GrowthHub — the canonical Solo Campaigns surface.
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: static component rendered at the full 1240px content column (capped by the surface max-width) with the compacted header, layout clean — docHOverflow=0, spHOverflow=0, spWidth=1240, kpi=5, stage=6, glyph=4, channels=2 (populated) / 0 (first-use), figures=13/11. This is the harness at that width, not the authenticated Solo shell (real PAIGE panel/tenant/scroll owner), which was not driven — owed to a browser-capable session (§32.c).
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: static component rendered at the 1126px content column the ~410px docked PAIGE rail would leave, layout clean — docHOverflow=0, spHOverflow=0, spWidth=1126, kpi=5, stage=6, glyph=4, channels=2/0, figures=13/11. The real Solo shell with the live PAIGE panel was NOT rendered, so panel-specific stacking, the actual scroll owner, and dock clipping are unverified — owed to a browser-capable session (§32.c).
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: static component at the full 1240px content column, layout clean — docHOverflow=0, spHOverflow=0, spWidth=1240, kpi=5, stage=6, glyph=4, channels=2/0, figures=13/11. Not the authenticated Solo shell (not driven); owed to a browser-capable session (§32.c).
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: static component at the 956px content column the docked PAIGE rail would leave, layout clean — docHOverflow=0, spHOverflow=0, spWidth=956, kpi=5, stage=6, glyph=4, channels=2/0, figures=13/11. The real shell with the live PAIGE panel was NOT rendered — panel-specific stacking, scroll owner, and clipping owed to a browser-capable session (§32.c).
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: static component at the 1024px content column, layout clean — docHOverflow=0, spHOverflow=0, spWidth=1024, kpi=5, stage=6, glyph=4, channels=2/0, figures=13/11. Not the authenticated Solo shell (not driven); owed to a browser-capable session (§32.c).
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: static component at the 614px content column the docked PAIGE rail would leave, layout clean — docHOverflow=0, spHOverflow=0, spWidth=614, two-column grid collapsed to one and pipeline to 3-up, kpi=5, stage=6, glyph=4, channels=2/0, figures=13/11. The real shell with the live PAIGE panel was NOT rendered — panel-specific stacking, scroll owner, and clipping owed to a browser-capable session (§32.c).
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: static component at the 900px content column, layout clean — docHOverflow=0, spHOverflow=0, spWidth=900, kpi=5, stage=6, glyph=4, channels=2/0, figures=13/11. Not the authenticated Solo shell (not driven); owed to a browser-capable session (§32.c).
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: static component at the 490px content column the docked PAIGE rail would leave (the narrowest real Solo case), layout clean — docHOverflow=0, spHOverflow=0, spWidth=490, single-column grid, KPI + pipeline at 2-up, channels stacked, kpi=5, stage=6, glyph=4, channels=2/0, figures=13/11. The real shell with the live PAIGE panel was NOT rendered — panel-specific stacking, scroll owner, and clipping owed to a browser-capable session (§32.c).
UNVERIFIED: (1) AUTHENTICATED_RUNTIME on the deployed Solo surface with the real PAIGE panel and a real tenant — no browser-driving tool in this headless session (live prod unreachable, net::ERR_CONNECTION_RESET); owed to a browser-capable session per §32.c. This is also why all eight SOLO_* rows are marked UNVERIFIED rather than PASS: the harness reproduces only the content-column WIDTH (for PAIGE-open, the width the docked panel would leave), never the authenticated Solo shell's real scroll owner, stacking, or clipping. The per-row geometry numbers are the durable in-record measurements. (2) A dedicated browser 400% / 320px-floor zoom pass — reflow verified to a 490px content column via container queries, not to the 320px WCAG floor. (3) A live OS reduced-motion toggle — the per-effect fallback is present and contract-asserted but was not exercised against a real preference. (4) The rendered/geometry harness shows one console error per page, the sandbox-blocked Google Fonts @import in solo-tokens.css (net::ERR_CONNECTION_RESET); it is an offline-harness artifact, not surface code, and the surface falls back to the system font stack there. (5) A second authenticated known-good Solo tenant context — the populated state was rendered from a representative fixture, not a second authenticated tenant drive.

## Scope and collisions

- Classification: Solo UI, presentation-only refinement of an already-shipped surface (the Social Command header), issued as a direct owner correction (2026-09-06).
- Affected flows: one — the Solo owner reading Campaigns → Social; unchanged in behavior.
- Neighboring regressions: none. Only social-command.tsx + social-command.css changed; the other five Campaigns tabs, the six-tab shell (growth2.tsx), the projection layer, and the hook are untouched.
- §58 anti-regression: two elements are removed — the Solo swirl mark (CommandLockupMark + PaigeOrb's inline swirl SVG) and the decorative "PAIGE — your chief of staff" signature line. Both removals are the owner's explicit instruction ("use that one [PaigeMark] and that one only"; the brief "takes up far too much space"), not a silent drop. No shipped capability is removed — the mark is replaced by the correct one, and the signature line was decorative copy, not a capability.
- Active-owner/file collisions: none. No migration, RPC, edge function, or hook return shape changed; the frozen solo-campaigns.css (§28) and solo-tokens.css were not touched.
- Explicit exclusions: no channel connection, publishing/scheduling, telemetry, or new capability was added — this change is a mark swap and a density refinement only.

## User job and state map

Purpose: a Solo owner opens Campaigns → Social to see what PAIGE is doing to grow the business
socially and what she cannot yet see, then act on the next move or record their accounts —
unchanged. The header now carries the correct brand mark and leads with a compact brief so the work
(KPI tiles, "Do this next", panels) sits higher in the fold. Complete states covered by the render
suite; first-use, populated, and the eclipse (unavailable) state committed as frames. Intended scroll
owner: unchanged — .campaigns-scroll owns the tab's scroll and .paige-solo main is clipped; the
geometry read proves zero horizontal overflow at every Solo content-column width from 1240px down to
490px with the compacted header.

## Evidence index

- Commands (repo root, node_modules installed): `npx vitest run src/solo/social-command.contract.test.ts src/solo/social-command.render.test.tsx` (86 passed); `node scripts/ci/tsc-ratchet.mjs` (13→13); `node scripts/ci/regression-lint.mjs` (clean); `node scripts/gold-discipline-lint.mjs src/solo/social-command.tsx src/solo/social-command.css` (clean); `npx eslint src/solo/social-command.tsx` (exit 0); `npx vite build` (succeeded).
- Rendered/behavioral: the real PaigeMark SSR'd (react-dom renderToStaticMarkup) into the real eclipse-state structure and the real header/hero fragments, served as static HTML with the shipped solo-tokens.css + social-command.css, driven with Playwright/Chromium 1194; both themes; first-use + populated + eclipse; the eight Solo viewport/PAIGE content-column widths above, each with a programmatic geometry read (document + .social-page horizontal overflow, .social-page width, kpi/stage/panel-glyph/channel/figure counts, page/console errors). Four representative frames committed durably under docs/evidence/ui-delivery/assets/campaigns-social-header/.
- Tenant context: representative fixture (2 handles Instagram + LinkedIn, 2 published artifacts, 1 form submission, 2 filed confirm-lane actions, platform-default trust lanes). Theme: light and dark. Timestamp: 2026-09-06. No secrets or customer data present in the harness.

## Review and limitations

Independent review: the production diff is small and presentation-only; the adversarial read of the
real pushed diff (§39) confirms it removes the retired mark and the signature line, imports the shared
PaigeMark, and changes only the header's typography/spacing — no seam, contract, or truth-layer edit.
Limitations: every item under UNVERIFIED above, chiefly the authenticated live drive of the deployed
Solo surface with the real PAIGE panel, owed to a browser-capable session per §32.c. This record
attests to what was checked; each PASS traces to a named command, artifact, or geometry read above.
