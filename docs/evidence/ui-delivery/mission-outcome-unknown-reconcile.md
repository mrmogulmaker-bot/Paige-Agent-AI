# UI delivery evidence: mission outcome-unknown truthfulness + retry idempotency (PR-B)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow is installed per-account (owner-side; unreadable in this environment — noted honestly). The touched flow was re-read end-to-end this session at the source level: owner action → PlanInMotion drawer → payload construction → request-key lifecycle (src/solo/mission-retry.ts) → useBusinessGamePlanMissions.mutate → business-mission-action edge → tenant-brain classification → mutation RPC idempotency/replay → readback → MissionActionResult (now carrying outcomeUnknown) → drawer state rendering.
PAIGE_UI_DESIGN: PASS: the one-home principle is preserved — the mission write remains the single governed Business Game Plan seam; this change only makes its failure/recovery leg truthful (a distinct reconcile state) and its retry contract idempotent (stable request identity). No new surface, component family, or visual direction is introduced; the banner reuses the existing drawer alert chrome (ov-err) plus the existing RefreshCw icon.
MATERIAL_FLOW_CHANGE: NO: the owner-visible happy path (create/revise/transition with verified readback) is unchanged. What changed is the UNHAPPY path's truth: an outcome-unknown result now renders a distinct status banner with a Check again action that reconciles the SAME operation under its ORIGINAL request key, instead of the previous behavior where it looked like an ordinary failure and a retry silently minted a fresh request key — which could apply a possibly-succeeded consequential write a second time. No new goal, state machine, or exit beyond that reconcile affordance.
FLOW_PROTOTYPE: NOT_REQUIRED: a single alert banner + button inside the existing drawer; no new layout, navigation, or multi-step flow to prototype.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — the owner can distinguish success, definite failure, and outcome unknown, and a retry of an ambiguous operation reconciles the original request rather than creating a new one. Audience — the Solo owner working strategic plays. Primary action — Check again (reconcile the same operation) after an outcome-unknown result.
VISUAL_DIRECTION: PASS: existing drawer tokens and alert chrome only (ov-err class + AlertTriangle + RefreshCw); no new colors, spacing system, or motion.
AUTOMATED_EVIDENCE: PASS: src/__tests__/mission-outcome-truthfulness.test.ts — 19/19: keeper lifecycle (unchanged payload reuses the EXACT key; changed payload mints; settle releases; UUID shape; PlanInMotion contains no direct crypto.randomUUID mint), invoke classification (network failure → outcome unknown, never definite; edge body with a code → the edge's own classification; unreadable non-2xx → outcome unknown), backend source pins (receipts unique per tenant+actor+key; replay returns the stored receipt before the revision guard; drift fails closed; the tenant brain separates definite RPC failure from transport ambiguity), and six component drives (distinct reconcile banner with truthful copy and no generic-failure wording; Check again reuses the exact original key; edited content gets a new key; definite failure keeps the ordinary error path with no banner; success settles so a later identical save is a new identity; ACTIVE_ACCOUNT_CHANGED fails closed with no reconcile offer). Both protections proven load-bearing by adversarial mutation: re-introducing a fresh key per call fails two tests; removing the outcome-unknown branch fails two tests (including the truthfulness assertion that the generic could-not-be-verified copy must NOT appear).
STATIC_EVIDENCE: PASS: eslint clean on every changed file; tsc ratchet at baseline; no edge function or migration changed (business-mission-action, business-mission-tenant-brain, and the mission foundation migration are byte-identical to main — the fix rides the existing replay contract).
RENDERED_EVIDENCE: PASS structural — the banner renders inside the existing drawer sheet with role=status and aria-live=polite; the component suite asserts its presence, copy, and action button via the real DOM. Per-viewport captures: UNVERIFIED below.
BEHAVIORAL_EVIDENCE: PASS: the component drives cover the three outcome states (success settles; definite failure errors ordinarily; outcome-unknown presents the reconcile state) and the retry contract (same key on Check again, new key on changed content). Manual browser drive: UNVERIFIED below.
AUTHENTICATED_RUNTIME: UNVERIFIED: the real authenticated pass (owner submits a strategic play, the confirmation is lost, the reconcile banner appears, Check again replays the original operation and the receipt readback resolves) was not driven this session; it is owed with the certification lane's acceptance matrix like the rest of the Solo matrix's runtime drives. The backend half it relies on is already adversarially proven: the mission foundation pgTAP suite (34 assertions incl. replay, idempotency conflict, RLS denial) runs in the paige-spine-contract CI job unchanged.
KEYBOARD_FOCUS: PASS structural — the Check again control is a native button in the drawer's focus order; the existing Escape/focus-trap handling is untouched. Manual screen-reader pass: UNVERIFIED.
ZOOM_REFLOW: PASS structural — the banner is a standard flow element in the drawer body with no fixed dimensions; full zoom drive UNVERIFIED with the matrix.
REDUCED_MOTION: PASS structural — no new motion; the existing icon rendering is static.
STATE_COVERAGE: PASS: success (settles), definite failure (ordinary error + refresh on revision conflict), outcome unknown (distinct reconcile state retaining the original identity), account-switch failure (fails closed, no reconcile). Validation errors unchanged.
TRUTHFUL_STATE_LABELS: PASS: the outcome-unknown copy states exactly what is and is not known — "We couldn't confirm whether that change completed. Nothing new will be sent — the next check reconciles the original request" — and never implies definite failure, definite success, or a fresh retry; the test suite asserts the generic definite-failure wording does not appear in this state.
SOLO_UI: YES: the Business Game Plan drawer is a Solo-owner interface and the change is a rendered state change (a new banner + action), so the per-viewport captures below are UNVERIFIED rather than NOT_APPLICABLE.
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass; the banner is a flow element with no fixed sizing, so reflow risk is structural-only.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
UNVERIFIED: the authenticated browser drive of the three outcome states and the reconcile round-trip on a real tenant (owed with the certification lane); everything else is proven at the unit, component, and source-contract level this session.

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->

INTERNAL_BUILD_IDENTITY: 0cb10706165d0a45fc2b5189631e362668a01ac7; deployment=none-pre-merge; environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this-PR-and-src/__tests__/mission-outcome-truthfulness.test.ts
RELEASE_CHANNEL: development: branch build (environment=development); the frontend change deploys via Vercel on merge; no edge bundle or migration changed so neither edge-live nor db-live moves
RELEASE_CLASSIFICATION: internal-only: a P0 correctness-and-truthfulness remediation from the PR4 Solo completion matrix (mission outcome-unknown rendering + retry idempotency); no owner-decided customer release
CUSTOMER_RELEASE_IDENTITY: none: internal correctness hardening; no customer-decided release
RELEASE_NOTE_REQUIRED: NO: closes a double-write hazard on the owner's own ambiguous retries and makes an ambiguous outcome honest in the UI; no customer-facing feature change beyond the corrected failure state
RELEASE_TRUTH_BOUNDARY: PARTIAL: the client-side lifecycle, classification, and rendering are proven by 19/19 tests with both protections mutation-proven load-bearing; the backend replay contract is cited from source and already pgTAP-proven in CI; the single remaining item is the authenticated runtime drive owed with the certification lane
RELEASE_RECOVERY: position=forward-only recovery — the change is additive client behavior over an untouched backend seam, so recovery is a forward PR restoring the previous rendering if ever needed (a git revert also suffices since no data shape changed); reference=git revert of the merge commit plus the contract test in this PR which would fail against any regression of either protection
