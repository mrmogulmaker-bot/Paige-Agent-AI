# UI delivery evidence: INT-178 agreement read tools

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: skill read in full at session start; pre-edit packet returned before the first edit — mode Existing Project / New Feature, paradigm `api`, depth Standard + independent review, risk R2 (tenant-scoped read, permissions), affected actor-goal flows named as "tenant owner/admin asks PAIGE which agreements are outstanding" and "…asks whether a client has signed yet", both previously IMPOSSIBLE rather than broken; file/contract ownership and collision result, changed-file boundary, state coverage, Regression impact map and applicable gates all stated in that packet.
PAIGE_UI_DESIGN: PASS: router read in full at `.claude/skills/paige-ui-design/SKILL.md` and, as it directs, the canonical project-owned skill at `.agents/skills/paige-ui-design/SKILL.md`. Its material flow test is what corrected the MATERIAL_FLOW_CHANGE line below from NO to YES. Of the five composable modules it routes to, this record carries module 1 (OWNER_INTENT / MUST_NOT_HAPPEN / MUST_PRESERVE / ACCEPTANCE_CRITERIA), module 4 (PROTECTED_SEAMS) and module 5 (evidence separated by class with truthful labels). Module 2 (visual and motion) and module 3 (interaction geometry, accessibility, the four Solo viewports) are recorded as not needed here and why: this diff adds no route, screen, component, token, layout, control or focusable element, so there is no geometry, motion or viewport behaviour to judge. The one rendered artifact it touches is an existing activity-chip string, corrected in this change so it can no longer contradict the tool result it describes.
MATERIAL_FLOW_CHANGE: YES: users GAIN A GOAL. A tenant owner or admin can now ask PAIGE where their agreements stand and whether a client has signed; before this change PAIGE had no agreement capability at all and could only decline. Two side effects change with it — a Rail receipt is written per invocation, and the activity trail gains two new step labels. This corrects my own first classification in this record, which read NO on the ground that a logic-only change has no interaction shape to prototype. The router skill's material flow test decides it the other way and is explicit: a prototype is required when users "gain, lose, or materially change a goal, choice, step, transition, state, confirmation, exit, recovery path, or side effect", and the NOT_REQUIRED escape is reserved for a "token-only correction, exact copy correction, or presentation-only adjustment". This is none of those. The exact-head review raised the same point and was more right than I first credited.
FLOW_PROTOTYPE: PASS: artifact at `docs/evidence/ui-delivery/int178-agreement-read-tools.prototype.html` — the six answers these tools can produce, each rendered with the string the shipped code actually returns and the activity chip that accompanies it: the ordinary populated answer, a genuinely empty book, an agency that holds no client book, a contact that does not exist in the workspace, a refusal for a caller who reached the workspace by delegated access rather than membership, and a refusal for a workspace that changed mid-turn. Owner-intent approval is NOT waited on, per CLAUDE.md §69 pre-launch override and §4 (owner ruling dated 2026-09-05): that ruling directs work to continue through to merge rather than park behind sign-off, with the adjustment loop happening after the ship. It lifts the WAIT, not the BUILD — "build the prototype where it genuinely helps you get the thing right … then keep going" — which is why this is an artifact rather than a waiver. CORRECTS THE PREVIOUS REVISION of this line, which recorded WAIVED against that same ruling; the exact-head review rejected that as substituting an older wait-removal for a current build requirement, and it was right. The wording in the artifact is what the owner should read and correct; nothing in it has been driven on the live platform.

PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: audience is a tenant owner/admin in PAIGE chat; purpose is that PAIGE can see the workspace's e-signature agreements at all; primary action is asking her where they stand, or whether one client has signed. INT-163 shipped the engine and withheld its chat tools, leaving `registry.ts` asserting in a comment that three of them registered when the array held zero.
VISUAL_DIRECTION: NOT_APPLICABLE: no visual surface, token, palette, hierarchy or motion is touched by this diff, so there is no direction to source from the approved pack or to port.
AUTOMATED_EVIDENCE: PASS: `npx vitest run src/__tests__/agreements-chat-reach.test.ts` — 30 assertions, exit 0. Each proven to BITE by restoring its defect: un-registering the capabilities fails 3; making `project()` a delete-list leaks the signing IP `203.0.113.42`; echoing the database error fails the refusal test; restored, 26 pass. Full suite `npx vitest run` — 20 failed / 5273 passed across 5 files, identical to the inherited #1372 baseline recorded for clean `main`, with this file not among the failures.
STATIC_EVIDENCE: PASS: `tsc --noEmit -p tsconfig.app.json` — 12 errors before, 12 after, the identical set, ZERO introduced (the root `npx tsc --noEmit` checks nothing and exits 0; that command is void here, and both new shared modules are confirmed inside the program via `--listFiles`). CI `verify` step 66 Deno ratchet on affected edge functions PASSED with no new diagnostics, which is the `deno check` this environment cannot run. Lints: capability-kit 309/309 · chat-tool-registry 98 none added · paige-spine-registry PASS 69→71 · action-risk 0 unclassified writes · governed-execution PASS · tool-catalogue 9/0.
RENDERED_EVIDENCE: NOT_APPLICABLE: no route, screen, component, layout, token or viewport behaviour is added or altered, so there is no frame to capture. The activity-chip copy change is text inside an existing renderer and is covered by the behavioral class below, which is UNVERIFIED.
BEHAVIORAL_EVIDENCE: UNVERIFIED: this session holds no browser-driving tool and cannot reach the authenticated Solo surface, so no one has driven PAIGE in a real chat and asked her where the agreements stand. Affected claims: that the tools are actually offered to the model in a live turn, that the model selects them, and that the rendered activity chip reads as intended.
AUTHENTICATED_RUNTIME: UNVERIFIED: no authenticated session and no database access in this container. Affected claims: that `paige_agreement_overview` is persisted on prod (migration `20270402000000` apply is unconfirmed), that the Rail receipt lands, and that the three diagnostics resolve as designed for a real agency, sub-account and operator caller. Per §32's capability-conditional rule this is OWED to the next browser-capable session and is not reported as done.
KEYBOARD_FOCUS: NOT_APPLICABLE: no focusable element, control, route or keyboard path is added or altered by this diff.
ZOOM_REFLOW: NOT_APPLICABLE: no layout, breakpoint or reflow behaviour is touched by this diff.
REDUCED_MOTION: NOT_APPLICABLE: no motion is added or changed by this diff, so there is no reduced-motion alternative to specify.
STATE_COVERAGE: PASS: covered in the adapter and asserted in tests — populated read; empty workspace; empty because the account type holds no client book; empty because the named contact does not exist in the workspace; filtered by contact; filtered by status; unknown status refused before the RPC is called; malformed contact id refused before the RPC is called; a one-client read refused rather than silently widened to the whole book; 42501 refusal for a non-member; 42501 refusal for a delegated-access caller, distinguished; any other database error; a thrown transport; no active workspace; and the 200-row cap stated without claiming to know whether more exist.
TRUTHFUL_STATE_LABELS: PASS: both capabilities register `chatBinding: "LIVE"`, which is true in this same change — the tools are spread into the model's tool list and dispatched. Both are `classification: read` / `riskPolicyKey: read_only` / `approvalAuthority: none`, and neither can draft, send, resend, void or remind; the tool descriptions say so explicitly. Tier reality is recorded per-cohort in `docs/doctrine/tier-matrix.md` in this same commit, including the God act-as row corrected from a false `✓`.
SOLO_UI: NO: this change adds no Solo interface. It is an edge-function and Spine-registry change; the eight Solo viewport records do not apply because no Solo surface is added or altered.
UNVERIFIED: The authenticated live drive (§70) — nobody has yet confirmed the owner can ask PAIGE "where do my agreements stand" on the live platform and get a true answer. Prod persistence of migration `20270402000000`. The behaviour of the three diagnostics against real agency, sub-account and operator callers. All three are owed to the next session holding a browser and an authenticated session; none is claimed here.

OWNER_INTENT: PAIGE must be able to SEE a tenant's agreements. The target actor is a tenant owner or admin in PAIGE chat; the user job is "tell me where my agreements stand / has this client signed"; the required outcome is a truthful answer, including a truthful refusal, where before there was no capability at all. A live client is waiting on the signature path, and the read half lands first deliberately so the send is not rushed.
MUST_NOT_HAPPEN: Signer evidence must never become chat-retrievable — no signing IP, user agent, signature image, token hash or signing-link token in a tool result, a summary or an error message. No second aggregation of `paige_agreement_signers` alongside the existing UI hook. No widening of any existing gate. No mutation reachable from this slice. No confident empty answer: an empty read must never be reported as "you have no agreements" when the truth is that the account type holds no client book or the named contact does not exist.
MUST_PRESERVE: `public.paige_agreement_overview` is unchanged — its signature, its §59 in-body guards and its grants are untouched, so its one existing non-code consumer (a psql proof script) is unaffected. The existing UI read path `src/solo/useSoloAgreementSignings.ts` is untouched. The send path `agreement-send` keeps its own admin check and confirm gate. Every other registered Spine capability and every other Chat tool is unchanged; the chat-tool inline baseline stays at 98.
ACCEPTANCE_CRITERIA: On the real platform, an owner or admin of a Solo or sub-account workspace asks PAIGE where their agreements stand and receives the actual list with per-agreement status and who is still outstanding; asks about one client by contact and receives that client's agreements only; receives an honest "that contact was not found" rather than "no agreements" for an id that does not exist; and, on a top-level agency, is told the account type holds no client book rather than that there are none. A platform operator acting-as, and an agency owner switched into a managed sub-account, are told the real membership boundary rather than that they are not a member. None of these has been driven yet — see AUTHENTICATED_RUNTIME.
MOTION_PURPOSE: NONE: no motion change.
PROTECTED_SEAMS: The governed read seam `public.paige_agreement_overview` — IMPACTED as a new consumer, tested: §37 inventory walked all eight caller classes and found the definition, one psql proof script, three doc references and one migration comment; the RPC is unchanged so no producer can break. The Rail receipt seam `record_capability_run` via `_shared/capability-record.ts` — IMPACTED as a new caller, tested: the capability key is the underscore tool name (the dotted Spine key would raise `CAPABILITY_RUN_KEY_INVALID` and silently write nothing), the service-role client is used because the RPC is granted to `service_role` only, and no `detail` payload is passed because `redactDetail` scrubs on key NAME alone and is not a safety net for signer data. The Spine registry `PAIGE_SPINE_CAPABILITIES` — IMPACTED, tested: 69 → 71, full validator clean. NAMED AND UNAFFECTED: the approval/confirm gate (both capabilities are reads and carry no action-risk entry); `agreement-send` and its tier gate; the signing act `sign-agreement`; document retrieval `agreement-document`; the integrity triggers; the tenant UI read hook; and every other Chat tool's dispatch.

INTERNAL_BUILD_IDENTITY: 60f273d14bccb661fbf1fd7ffd27863ed29ba213 is the first head of PR #1388 and the head CI measured, superseded by the review-fix commit that carries this record; deployment=none; environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=PR #1388 plus its three root-cause comments and the INT-178 entry in docs/brain/decision-log.md
RELEASE_CHANNEL: development: branch build, not deployed; production follows on merge to `main` under the pre-launch stance with no live customer data
RELEASE_CLASSIFICATION: internal-only: a capability becomes reachable by PAIGE; no customer-facing surface, pricing, entitlement or contract changes
CUSTOMER_RELEASE_IDENTITY: none: internal reach wiring with no earned customer release identity
RELEASE_NOTE_REQUIRED: no: no customer-visible surface change and no customer release identity
RELEASE_TRUTH_BOUNDARY: PARTIAL: `agreement_list` and `agreement_status` are registered, wired, dispatched, lint- and test-proven, with the authenticated live drive PROOF OWED and prod persistence of migration 20270402000000 unconfirmed. Agreement draft, send, resend and void remain UNAVAILABLE by design in this slice. LIVE is claimed for nothing here.
RELEASE_RECOVERY: position=revert this branch's commits — no schema, data or grant change exists to unwind, and the two Spine entries and the tool spread disappear with them; reference=PR #1388
