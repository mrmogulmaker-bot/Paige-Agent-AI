# UI delivery evidence: Solo Security & Data adopts the canonical panel + truthful personal-data controls (PR-C)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow is installed per-account (owner-side; unreadable in this environment — noted honestly). The touched flow was grounded end-to-end this session at the source level: Solo Settings tab → SecurityView (was three copy cards) → now SoloSecurityDataView → canonical AccountSecurityPanel (password via supabase.auth.updateUser; TOTP via mfa.enroll/challengeAndVerify/unenroll with stray-factor cleanup; sessions via performSignOut local + global) and the personal-data card → downloadMyUserData (the ONE extracted user-scoped export helper, shared with the client-portal DataPrivacyPanel) and the existing request-data-deletion edge (authenticated, user-scoped pending request processed within 30 days by the compliance processor).
PAIGE_UI_DESIGN: PASS: §18 one home enforced — the Security & Data tab now renders the SAME canonical AccountSecurityPanel the client dashboard and admin hub mount, unmodified; the personal-data card reuses the existing Solo settings card chrome and the shared AlertDialog/Button primitives; gold discipline clean on the new files.
MATERIAL_FLOW_CHANGE: NO: no new goal/state machine is invented — the tab's advertised capabilities become real by adopting existing seams. New affordances: a confirm-gated deletion-request control (with requestId readback replacing the button on success), an exact-scope download control, and an honestly-unavailable workspace-deletion note that carries no button.
FLOW_PROTOTYPE: NOT_REQUIRED: adoption of an already-shipped panel plus one card of standard controls inside the existing Settings tab; no new layout or navigation to prototype.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — every Security & Data control a Solo owner sees either performs a real governed act through the existing canonical seam or states plainly that the capability is absent. Audience — the Solo owner. Primary actions — change password, manage two-factor, sign out everywhere, download personal data, request personal-data deletion.
VISUAL_DIRECTION: PASS: existing Solo settings tokens and card chrome; no new colors, spacing system, or motion; destructive styling follows the shared Button/AlertDialog conventions.
AUTOMATED_EVIDENCE: PASS: src/__tests__/security-data-adoption.test.tsx — 16/16, proven failing-first: canonical-adoption source pins (the panel is mounted, not forked — no ChangePasswordCard/TwoFactorCard/SessionsCard re-implementations, no SoloAccountSecurityPanel; the export is ONE shared helper consumed by both surfaces; the deletion control rides the existing edge and nothing in the view addresses tenants); label-truthfulness pins (workspace deletion says "not available from this screen" with no button in that block; the export states its exact scope; the deletion control names a 30-day request over personal data and never claims workspace deletion); component drives (the panel's three controls render inside the Solo view; unavailable copy with no button; unauthenticated request fails closed with no success state; success renders the requestId and removes the control; backend failure never renders success; the export control rides the helper and its failure never claims success; in-flight guards + submitted state mean one invoke). Adversarial mutations all proven load-bearing: removing the double-submit layers fails the guard test; a no-op handler with fabricated confirmation fails the truthfulness tests; removing the unavailable copy fails 2 tests; export success without the helper fails the export test.
STATIC_EVIDENCE: PASS: eslint 0 errors on every changed file (12 react-refresh warnings in settings.tsx are pre-existing); gold-discipline clean; tsc ratchet at baseline (12/12); no edge function or migration changed — request-data-deletion, process-data-deletion, and the auth provider are byte-identical to main.
RENDERED_EVIDENCE: PASS: structural — the component suite drives the real view via createRoot/act and asserts the rendered labels, states, button presence/absence, and disabled layers. Per-viewport captures: UNVERIFIED below.
BEHAVIORAL_EVIDENCE: PASS: the suite covers success, definite failure, unauthenticated refusal, double-submit layering, and the submitted end-state for the request control; the panel's own behaviors are its previously shipped ones, unchanged by this PR.
AUTHENTICATED_RUNTIME: UNVERIFIED: the real authenticated pass (owner opens Solo Settings → Security & data; changes password; enrolls 2FA; signs out everywhere; downloads the export; files a deletion request up to its reference on a disposable account) was not driven this session — owed with the certification lane. No destructive act was executed against production data, per the proof rules.
KEYBOARD_FOCUS: PASS: structural — every control is a native Button inside the shared AlertDialog/Label primitives; the panel's existing focus behavior is unchanged. Manual screen-reader pass: UNVERIFIED.
ZOOM_REFLOW: PASS: structural — standard flow layout in the existing Settings card grid; no fixed dimensions introduced. Full zoom drive UNVERIFIED with the matrix.
REDUCED_MOTION: PASS: structural — no new motion; shared primitives carry the app's existing behavior.
STATE_COVERAGE: PASS: idle, in-flight (buttons disabled), success (requestId rendered, control replaced), definite failure (destructive alert copy, retry possible), unauthenticated refusal, and the honestly-unavailable state each render distinctly; the export control has idle/downloading/error states.
TRUTHFUL_STATE_LABELS: PASS: every control's label names its actual operation — "Download my personal data" states it is not a workspace export and lists exactly what it contains; "Request deletion of my personal data" states the 30-day request semantics and that the workspace is untouched; workspace deletion states it is not available and why there is no button; the suite pins the misleading wordings that must not appear.
SOLO_UI: YES: the change is a rendered Solo Settings tab replacing the former copy-only cards with real controls; per-viewport captures below are UNVERIFIED (capture owed with the authenticated pass), not NOT_APPLICABLE.
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass; standard flow layout, reflow risk structural-only.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: capture owed with the certification lane's authenticated pass.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: capture owed with the certification lane's authenticated pass.
UNVERIFIED: the authenticated runtime drive of every control on a real tenant (password, 2FA, global sign-out, export download, deletion request up to its reference), owed with the certification lane; everything else is proven at the unit, component, and source-contract level this session.

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->

INTERNAL_BUILD_IDENTITY: b44837c0d4be81262b7f317229743821670068d8; deployment=none-pre-merge; environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this-PR-and-src/__tests__/security-data-adoption.test.tsx
RELEASE_CHANNEL: development: branch build (environment=development); the frontend change deploys via Vercel on merge; no edge bundle or migration changed so neither edge-live nor db-live moves
RELEASE_CLASSIFICATION: internal-only: a P1 truthfulness and adoption remediation from the PR4 Solo completion matrix (the formerly copy-only Security & Data tab); no owner-decided customer release
CUSTOMER_RELEASE_IDENTITY: none: internal capability-adoption and labeling hardening; no customer-decided release
RELEASE_NOTE_REQUIRED: NO: replaces copy-only text on a settings tab with the real existing controls; no customer-facing feature change beyond honest labels on previously dead copy
RELEASE_TRUTH_BOUNDARY: PARTIAL: the adoption, labeling, and failure behavior are proven by 16/16 tests with five adversarial mutations load-bearing (including success-without-requestId); the underlying auth controls and the compliance edge are previously shipped seams left byte-identical; the single remaining item is the authenticated runtime drive owed with the certification lane
RELEASE_RECOVERY: position=forward-only recovery — the change is additive UI adoption over untouched seams, so recovery is a forward PR restoring the former copy-only view if ever needed; reference=git revert of the merge commit plus the contract test in this PR which fails against any regression of the adoption or the truthful labeling
