# UI delivery evidence: operator Team Pulse retired-pack strip

This record exists because the change touches three recognized operator UI paths. It renders
nothing: `FleetTeamPulseSurface` was ALREADY unmounted on `main` before this change, and what
remains here is its deletion plus two comment corrections. Every field below says what was
actually done, and `NOT_APPLICABLE` is used where there is no pixel to photograph rather than
dressing a comment edit as a visual delivery.

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: flow trace grounded in the repo — the only address that ever reached this component is `analytics/platform-health`, and `src/operator/ia/viewSources.ts` maps that address to `bespoke: "PlatformHealthSurface"`, with `carries: ["fleet/team-pulse"]` as the drop-nothing ledger. `SlotSurfaceBody.tsx` holds no lazy import for the deleted component. Grepped `FleetTeamPulseSurface` across `src/` — the only remaining hits were two comments, both corrected here. No actor can reach the deleted surface from any live path, before or after.
PAIGE_UI_DESIGN: PASS: read `.claude/skills/paige-ui-design/SKILL.md` (the router, which forbids using itself as a summary) and then the canonical `.agents/skills/paige-ui-design/SKILL.md` in full. Recording which of its five modules this change needed, as the skill itself requires of a presentation-only change: module 1 (owner intent) APPLIED — see OWNER_INTENT / MUST_NOT_HAPPEN / MUST_PRESERVE / ACCEPTANCE_CRITERIA below; module 4 (protected behaviour) APPLIED — see PROTECTED_SEAMS; module 5 (release acceptance evidence) APPLIED — evidence is separated by class below and nothing is labelled LIVE for merely compiling. Module 2 (visual/immersive quality) NOT NEEDED: no visible interface is authored, altered or restyled — the diff deletes an unreachable component and rewrites two comments, so no token, layout, motion, type or colour decision is made and there is no Claude Design pack direction to port. Module 3 (interaction geometry and accessibility) NOT NEEDED: no focusable element, scroll owner, viewport or interaction is added or changed, and this is not a Solo surface.
MATERIAL_FLOW_CHANGE: NO: the surface was already unmounted on `main` (`SlotSurfaceBody.tsx` carried an explicit "deliberately NOT dispatched anymore" note, and `viewSources.ts` already routed the address to `PlatformHealthSurface`). No actor-goal flow, state, transition, exit, or consequence changes — deleting an unreachable file removes nothing a user could reach.
FLOW_PROTOTYPE: NOT_REQUIRED: MATERIAL_FLOW_CHANGE is NO. There is no changed flow to prototype and no appearance decision to approve; a prototype of a deleted unreachable file would show an empty frame.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: purpose is trunk health — `lint:pack-lineage` and `lint:operator-reach` were both red on `main` because the §58 unmount left this retired-pack port unreachable AND dead-pack, the single cell pack-lineage exists to fail and the one with no exemption path by design. Audience is the platform operator (no tenant surface). Primary action unchanged: the operator opens `analytics/platform-health` and reads `PlatformHealthSurface`, exactly as before this change.
VISUAL_DIRECTION: NOT_APPLICABLE: nothing is drawn. The deleted component was a port of the superseded `Super Admin Shell.dc.html` pack, which the v3 pack draws nowhere; the live surface at that address (`PlatformHealthSurface`) is untouched by this diff.
AUTOMATED_EVIDENCE: PASS: exit codes, not pass counts, measured at head. `lint:pack-lineage` 1 → 0 · `lint:operator-reach` 1 → 0 · `ci:tsc` 0 (baseline 12, current 12) · `lint:views` 0 · `lint:chat-tool-registry` 0 · `smoke:secure-browser-security` 0 · `lint:release-governance` 0. Both repaired guards were proven to still bite by restoring the defect: a new unreachable dead-pack surface under `src/operator/surfaces/` → `lint:pack-lineage` EXIT=1; a new orphaned module under `src/operator/` → `lint:operator-reach` EXIT=1.
STATIC_EVIDENCE: PASS: `npx tsc --noEmit -p tsconfig.app.json` EXIT=0 via `npm run ci:tsc` (the bare `npx tsc --noEmit` typechecks NOTHING here — the root config is solution-style with `"files": []` — so any prior "tsc 0" in this area is void). Producer inventory for the delete: `grep -rn "FleetTeamPulseSurface" src/` returned only the two comments corrected in this commit; `grep -rn "useTeamPulse" src/` showed the deleted component as its only importer, which is why the hook is registered in `operator-reachability.mjs`'s EXEMPT map rather than deleted with it.
RENDERED_EVIDENCE: NOT_APPLICABLE: the change renders no pixels. The deleted component had no live mount on `main` to photograph, and the surface at its former address (`PlatformHealthSurface`) is not modified by this diff, so a screenshot would show an unchanged surface and prove nothing about the change.
BEHAVIORAL_EVIDENCE: NOT_APPLICABLE: no behavior is reachable to drive. No live import path led to the deleted component before this change; the remaining two references were comments. There is no state, exit, or outcome introduced or altered to exercise.
AUTHENTICATED_RUNTIME: UNVERIFIED: this session has no browser-driving capability (no Chrome MCP, no in-app browser, and live prod is not reachable headless from this sandbox), and the operator console is auth-gated. Under §32.c the browser-driven live look is OWED to the next capable session. Affected claims: none of the exit codes above depend on it — what remains owed is only the confirmation that `analytics/platform-health` still renders `PlatformHealthSurface` for a real operator, which this diff does not change.
KEYBOARD_FOCUS: NOT_APPLICABLE: no focusable element, tab order, or keyboard route is added, removed, or reordered on any reachable surface.
ZOOM_REFLOW: NOT_APPLICABLE: no layout, breakpoint, or reflow behavior is touched on any reachable surface.
REDUCED_MOTION: NOT_APPLICABLE: no motion is added, removed, or altered; the diff contains no animation, transition, or `useReducedMotion` change.
STATE_COVERAGE: NOT_APPLICABLE: the change introduces no state. The deleted component's own loading/empty/error states left the tree with it and were unreachable beforehand; `PlatformHealthSurface` keeps all of its states unchanged.
TRUTHFUL_STATE_LABELS: PASS: this change exists partly to repair untruthful records. `docs/doctrine/tier-matrix.md` claimed the address was "wired" to `FleetTeamPulseSurface` — stale since the unmount, and corrected here to `PlatformHealthSurface` (§66, same commit). The `RELEASE_RECOVERY` line in `docs/evidence/ui-delivery/operator-platform-health-view.md` recorded a one-line `viewSources` route back to the stopgap; the delete makes that false, so it is marked superseded with the real recovery path. The two in-code comments that cited `OperatorLegacyApp` as the retention precedent are corrected: `src/operator/legacy/` does not exist — verified by `ls` and `find src -iname "*LegacyApp*"` — so the precedent was a file already stripped.
SOLO_UI: NO: this is the platform-operator console (`src/operator/`), not a Solo tenant interface. No Solo canonical surface is touched, so the eight Solo viewport records do not apply.
UNVERIFIED: One item remains, and it is the AUTHENTICATED_RUNTIME row above: no authenticated operator session drove `analytics/platform-health` after this change, because this session holds no browser tool. Proof boundary otherwise complete — static and automated evidence are measured exit codes at head, and both repaired guards were re-run with their defects restored. Nothing else is outstanding.

OWNER_INTENT: Clear the checks that are red on the trunk itself, so that every branch stops inheriting them and a red check means something again. For this file specifically: resolve the conflict between the §58 record (which said keep the component on disk) and `lint:pack-lineage` (which has no exemption for an unreachable dead-pack file), without discarding the capability the §58 record says is owed.
MUST_NOT_HAPPEN: The owed capability must not be lost. The §58 record keeps the platform roster read (`list_platform_staff()`) owed to the v3 Settings → Team port, so the change must not delete that read to satisfy a graph check — which is exactly what `operator-reachability.mjs`'s existing `useOperatorChrome` exemption warns against. The guards must also not be blinded: neither may stop failing for any other file.
MUST_PRESERVE: `PlatformHealthSurface` at `analytics/platform-health`, unchanged. The `carries: ["fleet/team-pulse"]` drop-nothing ledger entry. `src/operator/data/useTeamPulse.ts` and its `list_platform_staff()` read, kept on disk and registered in EXEMPT with its reason. Both guards' ability to fail on every other file.
ACCEPTANCE_CRITERIA: (1) `npm run lint:pack-lineage` EXIT=0 and `npm run lint:operator-reach` EXIT=0 — met. (2) Introducing a new unreachable dead-pack surface still fails pack-lineage, and a new orphaned operator module still fails operator-reach — both measured EXIT=1. (3) `npm run ci:tsc` EXIT=0 — met. (4) An operator opening `analytics/platform-health` still sees `PlatformHealthSurface` — code-level confirmed (`viewSources.ts` bespoke mapping unchanged); the authenticated live look is OWED per AUTHENTICATED_RUNTIME.
MOTION_PURPOSE: NONE: no motion change.
PROTECTED_SEAMS: NONE_AFFECTED: the diff touches no RPC, edge function, migration, RLS policy, tenant-scope resolver, authority path, or entitlement gate. The operator view-source map is edited only where it already pointed at `PlatformHealthSurface`; the address-to-surface mapping for every other operator view is byte-unchanged. The one migration in this PR (`20270408000000_unclassified_inbound_security_invoker.sql`) belongs to the `lint:views` §9 fix, not to this UI change, and touches no operator surface.

INTERNAL_BUILD_IDENTITY: efb5832eb7229c96c39e951f07e10f0fb3072fc3; deployment=none-no-deploy-performed-from-this-session; environment=development; migrations=PROOF_OWED(20270408000000_unclassified_inbound_security_invoker belongs to this PR's separate lint:views section 9 repair and is NOT yet applied to prod - merged is not applied); edge=NOT_APPLICABLE; evidence=the measured exit codes recorded under AUTOMATED_EVIDENCE and STATIC_EVIDENCE, reproducible with npm run lint:pack-lineage, npm run lint:operator-reach and npm run ci:tsc at this SHA
RELEASE_CHANNEL: development: branch `claude/adoring-cerf-c1o2vc`, draft PR #1367, not deployed and not staged
RELEASE_CLASSIFICATION: internal-only: platform-operator console file removal plus CI guard repairs; no tenant-visible surface and no customer-facing behavior
CUSTOMER_RELEASE_IDENTITY: none: internal operator tooling and CI hygiene; nothing reaches a tenant or a client
RELEASE_NOTE_REQUIRED: no: no customer-visible behavior change — the deleted component was already unreachable on `main`, so no user could observe its removal
RELEASE_TRUTH_BOUNDARY: PARTIAL: CI guard behavior is LIVE (every claim is a measured exit code at head, including the restored-defect runs that prove both repaired guards still bite); the operator surface at `analytics/platform-health` is UNCHANGED by this diff and its authenticated presentation is PROOF OWED (no operator session drove the guarded route — this session has no browser tool); the PR's view migration is PROOF OWED separately (merged is not applied, §32.b)
RELEASE_RECOVERY: position=client-only revert of this commit restores the deleted component to disk and re-reds both guards, which is the accepted trade, and it does NOT restore a mount because none existed on main; reference=this record, the section 58 displacement note in `src/operator/ia/viewSources.ts`, and the EXEMPT entry with its reason in `scripts/ci/operator-reachability.mjs`. A forward fix is preferred over a revert if the v3 Settings to Team port later wants the roster, because `src/operator/data/useTeamPulse.ts` and its `list_platform_staff()` read are still present and unmodified.

## Scope and collisions

- Classification: internal operator console; retired-pack strip (§30) plus two CI guard repairs and three record corrections (§37/§58/§66).
- Affected flows: none reachable. `analytics/platform-health` continues to render `PlatformHealthSurface`; every other operator address is untouched.
- Neighboring regressions: none found. `ci:tsc` EXIT=0 with the baseline unchanged at 12; nothing in `src/` imported the deleted component.
- Active-owner/file collisions: none known at head. The branch is a fast-forward of `64c568b` (current `origin/main`) — an earlier measurement in this session against a stale local `origin/main` ref was wrong and has been corrected.
- Explicit exclusions: the `lint:views` §9 migration, the `lint:chat-tool-registry` baseline record, the `smoke:secure-browser-security` assertion repair, and the INT-003 capability-kit change all ship in the same PR but are NOT UI changes and are evidenced in the PR body rather than here.

## User job and state map

The operator's job at `analytics/platform-health` is to read platform health. That job, its surface, its states and its exits are unchanged by this diff — `PlatformHealthSurface` owns them and is not modified. The deleted `FleetTeamPulseSurface` was a stopgap that previously answered a different job (the platform seat roster); that job is not being answered today by anything, is recorded as owed to the v3 Settings → Team port, and its data read survives at `src/operator/data/useTeamPulse.ts`. Scroll owner: unchanged (`PlatformHealthSurface`).

## Evidence index

- `npm run lint:pack-lineage` — EXIT=1 on `64c568b`, EXIT=0 at head.
- `npm run lint:operator-reach` — EXIT=1 on `64c568b`, EXIT=0 at head.
- Bite proof, pack-lineage: added `src/operator/surfaces/__BiteProbe.tsx` citing `Super Admin Shell.dc.html` → EXIT=1, naming the probe; removed.
- Bite proof, operator-reach: added `src/operator/data/__biteOrphan.ts` → EXIT=1, naming the probe; removed.
- `npm run ci:tsc` — EXIT=0, baseline 12, current 12.
- `grep -rn "FleetTeamPulseSurface" src/` — two comment hits only, both corrected in this commit.
- `grep -rn "useTeamPulse" src/` — the deleted component was its only importer.
- `ls -d src/operator/legacy` and `find src -iname "*LegacyApp*"` — both empty, disproving the retention precedent the §58 note cited.
- Routes/roles/tenants: none exercised — no authenticated session was available (see AUTHENTICATED_RUNTIME). No secrets or customer data appear in this record.

## Review and limitations

An independent adversarial pass reviewed the strip-versus-retain decision and the guard repairs. Its findings acted on here: the `OperatorLegacyApp` precedent is false (verified independently before acting); the `RELEASE_RECOVERY` line in `operator-platform-health-view.md` is invalidated by the delete and is corrected in this commit (§37); and the tier-matrix row was stale and is corrected in this commit (§66).

Remaining limitations: (1) AUTHENTICATED_RUNTIME above — the browser-driven live look at `analytics/platform-health` is owed to the next capable session. (2) The master-reference §4/§10 rows and the `docs/brain/` entries are owed and routed, not silently skipped. Neither blocks this change, and both are stated rather than implied.
