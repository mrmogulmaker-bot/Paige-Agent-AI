# UI delivery evidence: Trust Compass delete knob fronts the governed tool (PR-A retirement fallout)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow is installed per-account (owner-side; unreadable in this environment — noted honestly). The change is a one-key identity swap inside the Trust Compass knob registry (src/solo/data/capabilityTools.ts): the delete knob now addresses crm_hard_delete_contact (the governed crm-command executor action) instead of the retired crm_delete_contact, whose edge was deleted by PR-A because it could cross-tenant delete on a global role check. The affected flows (Trust Compass surface, composer autonomy chip) were re-read at their seams this session and their existing suites re-driven on the swapped key.
PAIGE_UI_DESIGN: PASS: the one-home principle is preserved — the delete knob still fronts exactly ONE real, classified, runnable action; only the action's identity changed. No new component, copy, token, or layout is introduced, and the knob's risk cap (confirm) is unchanged because both actions are classified high in the real policy.
MATERIAL_FLOW_CHANGE: NO: the owner-visible Trust Compass shape is unchanged — the CRM domain still shows a delete knob capped at confirm with the same held-back reason copy. What changed is WHICH action the knob addresses, because the old namesake no longer exists at runtime. No new goal, state, transition, exit, or consequence exists.
FLOW_PROTOTYPE: NOT_REQUIRED: a registry-identity swap on an existing knob with unchanged risk semantics (both actions classified high, both capped to confirm); no new interaction of any kind to prototype.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — keep the Trust Compass delete knob truthful by fronting a real classified action after its legacy namesake was retired with its cross-tenant edge. Audience — tenant owner (Trust Compass) and owner/coach (composer autonomy chip). Primary action — unchanged: set the delete tool's autonomy lane, still capped at confirm.
VISUAL_DIRECTION: NOT_APPLICABLE: no visual surface changed; the knob inherits the existing Trust Compass and chip chrome and tokens.
AUTOMATED_EVIDENCE: PASS: useSoloToolGovernance.test.ts and PaigeComposerAutonomyChip.test.tsx re-driven on the governed twin — the high-risk clamp (a stored auto is held to confirm with the risk reason), platform-ceiling attribution, and domain aggregation all pass; capabilityTools.test.ts drift guard passes (every mapped tool's risk copy matches the real policy exactly and none is unclassified); scripts/ci/action-risk-lint.mjs passes with the retired name absent (157 classified actions, 0 unclassified writes — the retired name now resolves unclassified and is refused by default, which is the deliberate fail-closed reintroduction protection).
STATIC_EVIDENCE: PASS: eslint and gold-discipline clean on every changed file; full vitest at the inherited baseline (the nineteen long-known main failures only — resend-receipt 1, connections-actions 3, rendered-copy 1, GamePlan 14); deno diagnostics unchanged at twelve; tsc ratchet at baseline.
RENDERED_EVIDENCE: NOT_APPLICABLE: no rendered structure changed — the knob renders through the existing Trust Compass and chip components; jsdom tests assert the derived lanes against the swapped key.
BEHAVIORAL_EVIDENCE: NOT_APPLICABLE: deriveGovernance and the chip derive effective lanes from the map key; their automated suites cover the held-back reason and ceiling attribution on the swapped key; no other behavior path is touched by a data-map identity swap.
AUTHENTICATED_RUNTIME: UNVERIFIED: the real authenticated pass (owner opens Trust Compass, sees the delete knob addressing the governed action, lane capped at confirm) was not driven this session; it is owed with the certification lane's acceptance matrix alongside the rest of the Solo matrix's runtime drives.
KEYBOARD_FOCUS: NOT_APPLICABLE: no focusable element added, removed, or reordered.
ZOOM_REFLOW: NOT_APPLICABLE: no layout change of any kind.
REDUCED_MOTION: NOT_APPLICABLE: no motion change.
STATE_COVERAGE: PASS: the knob's states are unchanged (stored, effective, maxSettable, held-back), re-asserted by the existing suites against the swapped key; retirement refusals are server-side and out of this surface's scope.
TRUTHFUL_STATE_LABELS: PASS: the knob map's risk copy is proven against the real policy by the drift test — the knob can no longer claim to front an action the runtime no longer classifies, which is the exact drift the guard exists to catch.
SOLO_UI: YES: the Trust Compass surface and composer autonomy chip are Solo-owner interfaces; the change is a data-map key swap beneath them with no rendered-surface edit, so the per-viewport captures below are NOT_APPLICABLE with that reason.
SOLO_1536X770_PAIGE_CLOSED: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
SOLO_1536X770_PAIGE_OPEN: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
SOLO_1366X768_PAIGE_CLOSED: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
SOLO_1366X768_PAIGE_OPEN: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
SOLO_1024X768_PAIGE_CLOSED: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
SOLO_1024X768_PAIGE_OPEN: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
SOLO_900X1000_PAIGE_CLOSED: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
SOLO_900X1000_PAIGE_OPEN: NOT_APPLICABLE: data-map identity swap under existing Solo surfaces — no rendered surface or visual state to capture.
UNVERIFIED: the authenticated Trust Compass drive on a real tenant is owed with the certification lane; everything else is proven at the seam and unit level this session, and the retirement contract test pins that the retired name stays out of the knob map, the handler, the policy, the baseline, and the provider.

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->

INTERNAL_BUILD_IDENTITY: 2c40e24767aa74b40af725fcafc91a172210e3e3; deployment=none-pre-merge; environment=development; migrations=NOT_APPLICABLE; edge=APPLIED(delete-contact retired at the provider by deploy-edge-functions.yml's new retired-function delete on merge); evidence=this-PR-and-src/__tests__/legacy-contact-delete-retirement.test.ts
RELEASE_CHANNEL: development: branch build (environment=development); the map/test change deploys via Vercel on merge; the deleted edge function is removed at the provider by deploy-edge-functions.yml on merge (retired-set computation proven to yield exactly delete-contact), moving edge-live
RELEASE_CLASSIFICATION: internal-only: a P0 tenant-isolation remediation slice (legacy cross-tenant delete retirement) with its knock-on knob-map identity swap; no owner-decided customer release
CUSTOMER_RELEASE_IDENTITY: none: internal security-integrity remediation from the PR4 Solo completion matrix; no customer-decided release
RELEASE_NOTE_REQUIRED: NO: closes a cross-tenant destructive-delete hole that no customer-visible journey exposed (the legacy UI lane was unrouted and the chat tool was already unlisted); the knob swap preserves owner-visible behavior
RELEASE_TRUTH_BOUNDARY: PARTIAL: the retirement is proven in source and CI (contract test, mutation-proven, registry/policy/action-risk lints green) and the provider-side deletion is wired into the deploy workflow with its retired-set computation verified against the real diff; the provider deletion's execution on production is confirmed by the deploy run post-merge
RELEASE_RECOVERY: position=forward-only recovery — re-adding the retired function means re-introducing its source directory and config block (the retirement contract test fails until updated, deliberately) and re-registering its classification, so recovery is a reviewed forward PR, not a revert; reference=git revert of the merge commit restores the source side only, and the deploy workflow's retired-set guard keeps the provider-side state visible in its logs
