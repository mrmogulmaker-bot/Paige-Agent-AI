# UI delivery evidence: trust-compass delete knob fronts the governed tool (PR-A retirement fallout)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow is installed per-account (owner-side; unreadable in this environment — noted honestly). The change is a one-key map swap inside the Trust Compass knob registry; the affected flows (Trust Compass surface, composer autonomy chip) were re-read at their seams this session and their existing tests re-driven.
PAIGE_UI_DESIGN: PASS: §18 one home preserved — the delete knob still fronts exactly ONE real classified action; only the action's identity changed from the retired legacy edge tool to its governed twin. No new visual surface, copy, or component is introduced.
MATERIAL_FLOW_CHANGE: NO: the owner-visible Trust Compass behavior is unchanged in shape — the CRM domain still shows a delete knob capped at confirm. What changed is WHICH action the knob addresses: `crm_hard_delete_contact` (the governed crm-command executor path) instead of the retired `crm_delete_contact` (whose edge was deleted by PR-A because it could cross-tenant delete on a global role). No new goal, state, transition, exit, or consequence exists.
FLOW_PROTOTYPE: NOT_REQUIRED: a registry-identity swap on an existing knob with unchanged risk semantics (both classified high, both capped to confirm); no new interaction of any kind.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — keep the Trust Compass delete knob truthful by fronting a real, runnable, classified action after its legacy namesake was retired. Audience — tenant owner (Trust Compass) and owner/coach (composer autonomy chip). Primary action — unchanged: set the delete tool's autonomy lane, which remains capped at confirm.
VISUAL_DIRECTION: PASS: no visual change; the knob inherits the existing Trust Compass/chip chrome and tokens.
AUTOMATED_EVIDENCE: PASS: useSoloToolGovernance.test.ts and PaigeComposerAutonomyChip.test.tsx re-driven on the governed twin — high-risk clamp (auto held to confirm with the risk reason), ceiling attribution, and domain aggregation all pass; capabilityTools.test.ts drift guard passes (every mapped tool's risk copy matches the policy exactly, none unclassified); action-risk-lint passes with the retired name absent (157 classified, 0 unclassified writes — the retired name now resolves unclassified and is refused by default).
STATIC_EVIDENCE: PASS: eslint clean and gold-discipline clean on every changed file; full vitest at the inherited baseline (the 19 known main failures only); deno diagnostics unchanged at 12.
RENDERED_EVIDENCE: PASS (structural): the knob renders through the existing Trust Compass and chip components with no markup change; jsdom tests assert the derived lanes. Per-viewport captures are NOT_APPLICABLE: no rendered structure changed.
BEHAVIORAL_EVIDENCE: PASS: deriveGovernance and the chip derive effective lanes from the swapped key in automated tests, including the held-back reason copy and the platform-ceiling attribution; no other behavior path touched.
AUTHENTICATED_RUNTIME: UNVERIFIED: the real authenticated pass (owner opens Trust Compass, sees the delete knob labeled for the governed action, lane still capped at confirm) was not driven this session; it is owed with the certification lane's acceptance matrix like the rest of the matrix's runtime drives.
KEYBOARD_FOCUS: PASS (structural): no focusable element added, removed, or reordered.
ZOOM_REFLOW: PASS (structural): no layout change of any kind.
REDUCED_MOTION: PASS (structural): no motion change.
STATE_COVERAGE: PASS: the knob's states are unchanged (stored/effective/maxSettable/held-back), re-asserted by the existing tests against the swapped key; retirement refusals are server-side and out of this surface's scope.
TRUTHFUL_STATE_LABELS: PASS: the knob map's risk copy is proven against the real policy by the drift test — the knob can no longer claim to front an action the runtime no longer classifies, which was the exact drift the guard exists to catch.
SOLO_UI: YES: the Trust Compass surface and composer autonomy chip are Solo-owner interfaces; the change is a data-map key swap beneath them with no rendered-surface edit, so per-viewport captures are NOT_APPLICABLE with that reason.
UNVERIFIED: authenticated Trust Compass drive on a real tenant (with the certification lane); everything else proven at the seam and unit level this session.
