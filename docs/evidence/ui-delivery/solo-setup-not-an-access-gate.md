# UI delivery evidence: Setup is not an access gate for Solo (owner adjudication access repair)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow is installed per-account (owner-side; unreadable in this environment — noted honestly). The touched flow was grounded end-to-end at the source level before editing: login → active tenant resolution (useTenantContext, server-derived) → signup gate (RequireCompleteSignup) → entitlement gate (RequireSoloBetaEntitlement) → the former Setup redirect (RequireSetupComplete) → SoloEntry decideWorkspaceEntry → canonical shell routes. The production-database state behind the adjudication was also traced read-only (both comparison tenants' features/marker shapes, the completion trigger's presence, and the population census of gated vs grandfathered standalone tenants).
PAIGE_UI_DESIGN: PASS: one home preserved — the change deletes an access decision from the routing seam and adds ONE non-blocking readiness banner in the same seam; no new component family, chrome, or visual direction; the banner uses plain semantic markup (role=status, a link, a dismiss button) styled by the existing shell context.
MATERIAL_FLOW_CHANGE: NO per the owner's adjudication this IMPLEMENTS the intended contract rather than inventing one: for the solo tier the redirect is removed and a dismissible readiness banner takes its place; the sub-account redirect, agency/operator no-ops, loading fail-open, and the outer signup/entitlement guards are byte-equivalent in behavior. No new goal/state machine beyond banner visibility + dismissal.
FLOW_PROTOTYPE: NOT_REQUIRED: the routing seam renders children either way; the only new visible element is a standard status banner whose behavior (visibility, dismissal, link) is covered by the journey suite against the real guard order.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — every entitled standalone Solo owner gets the same application contract regardless of setup state; PAIGE Chat is never blocked by missing business context the owner could ask PAIGE to help collect. Audience — the Solo owner. Primary action — use any Solo destination; optionally follow the banner's Finish-setup link or dismiss it.
VISUAL_DIRECTION: PASS: existing shell tokens; no new colors or motion; the banner is a standard flow element.
AUTOMATED_EVIDENCE: PASS: src/__tests__/setup-gate-journey.test.tsx — 16/16 under the new contract, driving the REAL guard order over the REAL route decisions: a fresh zero-save Solo lands on the requested destination with the banner visible; PAIGE Chat renders immediately; Setup itself renders and the owner can navigate away; repeated evaluation (reload/login) is stable on the destination; the banner is dismissible and stays dismissed with children rendering; solo_setup_complete absent or false never blocks; missing business context surfaces as the server-side readiness seam, not a redirect; an MMA-style legacy-playbook tenant (playbook + config + the dead shell flag) resolves through the exact same contract with no banner; playbook presence grants nothing beyond the identical shell (the same five destinations render for both states); the source pins the redirect to sub_account only with NO Navigate in the solo branch and the outer guards intact in App wiring; staff never gated/bannered; loading fails open; no account literal anywhere; agency entry decision unchanged; a sub-account without a playbook is still sent to its own shell Setup (no cross-tier leak). Failing-first: the 8 access proofs fail against the pre-adjudication gate — the same run doubles as the adversarial mutation proof (reintroducing solo blocking fails the suite).
STATIC_EVIDENCE: PASS: eslint clean on the changed files; tsc ratchet at baseline; no backend/edge/migration change — the completion-marker trigger, the save RPC, and every setup data seam are byte-identical to main.
RENDERED_EVIDENCE: PASS: structural — the journey suite drives the real guard component through a memory router and asserts the rendered banner's presence, copy, link, and dismissal. Per-viewport captures: UNVERIFIED below.
BEHAVIORAL_EVIDENCE: PASS: the suite covers both setup states, dismissal, staff/loading no-ops, and the cross-tier boundary. Manual browser drive: UNVERIFIED below.
AUTHENTICATED_RUNTIME: UNVERIFIED: the real authenticated pass (the named comparison accounts signing in and landing in the canonical shell without a setup redirect, banner visible where incomplete, absent where complete) was not driven this session — no browser credentials for those owner sessions are held; it is owed as the acceptance act after merge+deploy. The production-database grounding behind the change WAS verified read-only this session (tenant shapes, marker absence, trigger presence, population census).
KEYBOARD_FOCUS: PASS: structural — the banner's link and dismiss button are native focusable elements in document order; no focus trap introduced or removed. Manual screen-reader pass: UNVERIFIED.
ZOOM_REFLOW: PASS: structural — a standard flow banner with no fixed dimensions. Full zoom drive UNVERIFIED with the matrix.
REDUCED_MOTION: PASS: structural — no motion introduced.
STATE_COVERAGE: PASS: incomplete solo (banner shown, dismissible), complete solo (no banner), loading (nothing), staff (nothing), sub-account (unchanged redirect incl. its chooser-reachability carve-out), agency (entry decision owns routing). No fabricated completion state anywhere — the marker's write path is untouched.
TRUTHFUL_STATE_LABELS: PASS: the banner states exactly the truthful condition — setup unfinished, PAIGE more useful with it, PAIGE can help, finish when ready, everything else works meanwhile; the suite asserts the copy appears only when incomplete and never for complete tenants.
SOLO_UI: YES: the change alters the rendered Solo experience (removes a redirect; adds a banner); per-viewport captures below are UNVERIFIED (owed with the authenticated pass), not NOT_APPLICABLE.
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: capture owed with the authenticated acceptance pass; standard flow banner, reflow risk structural-only.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: capture owed with the authenticated acceptance pass.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: capture owed with the authenticated acceptance pass.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: capture owed with the authenticated acceptance pass.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: capture owed with the authenticated acceptance pass.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: capture owed with the authenticated acceptance pass.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: capture owed with the authenticated acceptance pass.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: capture owed with the authenticated acceptance pass.
UNVERIFIED: the authenticated browser acceptance pass on the named comparison accounts (sign-in → canonical shell, no setup redirect, banner truthful in both directions), owed after merge+deploy; everything else is proven at the journey, source-contract, and production-database grounding level this session.

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->

INTERNAL_BUILD_IDENTITY: 6184233886a44bb161e93cf76c846ce01133deb7; deployment=none-pre-merge; environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this-PR-and-src/__tests__/setup-gate-journey.test.tsx
RELEASE_CHANNEL: development: branch build (environment=development); the frontend change deploys via Vercel on merge; no edge bundle or migration changed so neither edge-live nor db-live moves
RELEASE_CLASSIFICATION: internal-only: an owner-adjudicated canonical access repair (Setup stops gating Solo application access); no owner-decided customer release beyond the contract correction itself
CUSTOMER_RELEASE_IDENTITY: none: internal canonical-parity and access-correctness repair
RELEASE_NOTE_REQUIRED: NO: removes a blocking redirect the owner has ruled was never the intended product behavior; the visible delta is a truthful non-blocking banner
RELEASE_TRUTH_BOUNDARY: PARTIAL: the new contract is proven by 16/16 journey tests with the failing-first/adversarial evidence, and the production grounding was read-only verified; the authenticated browser acceptance pass on the named accounts is owed after deploy
RELEASE_RECOVERY: position=forward-only recovery — the change is a routing-contract correction over untouched data seams, so recovery is a forward PR restoring the prior redirect if the owner ever reverses the adjudication; reference=git revert of the merge commit plus the journey suite in this PR which pins the adjudicated contract either way
