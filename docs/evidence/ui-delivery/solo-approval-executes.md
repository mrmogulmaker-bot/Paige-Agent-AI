# UI delivery evidence: solo-approval-executes

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: 2.0.1 read completely (SKILL.md + inlined orchestration/delivery/audit/build/review/verification). Mode Bug-Repair + Security, paradigm `api`+UI, depth Deep at risk floor R3 (permissions). Pre-edit packet returned in-session before the first edit: affected actor-goal flows (operator approves and it runs; operator learns the truth on a card-less surface; Paige cannot self-approve), head `2e47c69` level with origin/main, changed-file boundary, Regression impact map, failing-first plan. Gate 5 recorded FAIL — `flow-prototype` is NOT installed beside `flow-by-flow` in this environment.
PAIGE_UI_DESIGN: PASS: `.agents/skills/paige-ui-design/SKILL.md` read completely, then `references/paige-quality-gates.md` and `references/review-and-testing.md`. Impeccable 4.3.1 skill read; `impeccable context --target src/components/chat/PaigeConfirmCard.tsx` run once (returned SCOPED_EXISTING_ALLOWED / EXISTING_VISUAL_SYSTEM — narrow refinement on incumbent authority, no PRODUCT.md required); `reference/craft-floor.md` read immediately before the first UI edit. Mode Operate.
MATERIAL_FLOW_CHANGE: YES: an approval on a Solo surface now executes the action instead of ticking a different table; and a NEW refusal state exists — an action with no binding renders no approve control and names where the decision can be completed.
FLOW_PROTOTYPE: WAIVED: owner-decision=PR #1450 + owner ruling in the 2026-09-25 delivery session ("the approval card — we can make improvements on the way it looks. Use impeccable, and let's improve it", then "You got the green light"); reason=The owner ruled the changed flow himself from live production behaviour he had personally hit ("when I press approve, nothing fully executes"), and was shown the REAL component rendered in both themes across all seven states — including the new refusal state — before authorizing. §69's pre-launch override additionally lifts Gate 1 while the platform has no live customers. The rendered frames are the artifact the gate exists to produce, and they preceded the authorization rather than following it.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — let a Solo operator approve a confirmation-required action and have it actually run, and tell them the truth when this surface cannot complete one. Audience — the signed-in Solo owner or sub-account operator in Paige chat. Primary action — press Approve and have the named action execute exactly once.
VISUAL_DIRECTION: PASS: incumbent Paige system, tokens only (`--foreground`/`--muted`/`--border`/`--success`/`--destructive`/`--accent-foreground`), which the Solo console redefines at `src/index.css:355-380` so the shared card themes correctly inside the Solo shell. Gold spent only on Approve (§11); one elevation declaration (hairline border on a tinted surface, no shadow under it); radius 12px; no eyebrow/kicker; icons from lucide-react at one weight.
AUTOMATED_EVIDENCE: PASS: `npx vitest run src/solo/solo-approval-executes.test.tsx` — 11/11, exit 0. Negative controls BOTH bite: reinstating the optional-`fingerprints` shape (`canApprove = true`) fails 2; reinstating the frame drop and removing the body field fails 2. Regression, CORRECTED 2026-09-25 after CI step 83 failed: the first measurement here read "19 across 4 Solo files, proven on a stashed clean tree". That was a PARTIAL run — only `src/solo/` — generalised to a whole-suite claim it did not support, which is the §71.3 failure this record is supposed to prevent. Re-measured properly against a real `origin/main` worktree (`2e47c69bc`), full suite both sides: **origin/main 21 failed / 5581 passed across 6 files; this branch 21 failed / 5593 passed across the IDENTICAL 6 files** (`artifact-receipt-contract`, `TenantCommandCenterShell.ownership`, `SoloGamePlanWorkspace`, `resend-receipt-handler`, `settings.connections-actions`, `settings.rendered-copy`). Zero new failures, +12 passing — the conclusion held, but it had been asserted on evidence that did not reach it.
STATIC_EVIDENCE: PASS: `tsc --noEmit -p tsconfig.app.json` = 12 errors, the ratchet baseline, ZERO in any touched file. `vite build` exit 0. `impeccable@4.1.0 detect` on `PaigeConfirmCard.tsx` and `solo/agent.tsx` exit 0, zero findings. Repo-wide `lint:impeccable` advisory notes are pre-existing CSS files, none touched here.
RENDERED_EVIDENCE: PASS: `docs/evidence/ui-delivery/solo-approval-executes/confirm-card-light.png` and `-dark.png` — the real `PaigeConfirmCard` via `renderToStaticMarkup` with the real COMPILED tokens (`dist/assets/main-*.css` + `SoloEntry-*.css`), 1720x3880 at deviceScaleFactor 2, Chromium 1194 via Playwright. Seven states: ready, batch, running, done, failed-with-reason, no-binding (no approve control), mixed batch. Light is genuinely light against a genuinely dark dark (§23). Harness: `src/solo/__render__/confirm-card.render.test.tsx` + `scripts/render/shoot-confirm-card.mjs`.
BEHAVIORAL_EVIDENCE: UNVERIFIED: no browser drive of the Solo shell was performed, because no running app instance and no signed-in session were reachable from this container. What WAS exercised is component behaviour in jsdom: the card is really mounted and really clicked — Approve hands back the EXACT fingerprint array; a no-binding card exposes zero buttons and `onApprove` is never called; a mixed batch approves only the bound subset. This is component behaviour, NOT a browser drive of the Solo shell, and it is not offered as one.
AUTHENTICATED_RUNTIME: UNVERIFIED: no authenticated Solo session was reachable from this container (no credentials; `LIVE_DRIVE_EMAIL`/`_PASSWORD` unset). Affected claims: that a real operator's Approve press in the deployed Solo shell mints, echoes, claims and executes end-to-end; and that the `paige_confirm` SSE frame arrives with a fingerprint on the deployed edge. Owed to the next capable session (§32.c).
KEYBOARD_FOCUS: UNVERIFIED: no keyboard route was driven in a browser and no screen reader was exercised. Static reading only: controls are real `<button>` elements from the shared `Button` primitive, so tab order is DOM order and `:focus-visible` inherits the console's indigo ring; the card is a labelled `role="group"` and the heading is `aria-live="polite"` so a state change is announced.
ZOOM_REFLOW: UNVERIFIED: no zoom/reflow drive was performed. The card introduces no fixed width, no horizontal scroll and no clipping — it is a flow-layout block with `flex-wrap` on its control row — but that is a static reading, not a measurement.
REDUCED_MOTION: PASS: the only motion is the `Loader2` spin on the working state, guarded per-element by `motion-reduce:animate-none`; the card's colour transition carries `motion-reduce:transition-none`. Under reduced motion the spinner is a static glyph and the state change is still announced via `aria-live`, so no feedback is lost.
STATE_COVERAGE: PASS: first use (a fresh ask), populated single, populated batch, working, success, failure-with-reason, refusal (no binding — the new state), mixed partial-binding, settled (no second approval offered), empty (`actions.length === 0` renders null), and turn/thread/chat switch (outstanding asks cleared, four call sites). Not applicable: loading-skeleton (the card only exists once the server has already answered) and permission-denied (the gate refuses upstream and never emits a frame).
TRUTHFUL_STATE_LABELS: PASS: the card renders only server-authored summaries and server-authored failure notes; it fabricates no outcome, and a settled action offers no second approval. The refusal copy names a real recovery path rather than implying a capability. Capability labels in this record are separated by evidence class and the authenticated runtime is recorded UNVERIFIED rather than inferred from a passing harness.
SOLO_UI: YES: Paige chat in the Solo shell — `src/solo/agent.tsx` `Agent` (full workspace) and `PaigePanel` (floating panel), both via `src/solo/data/useSoloChat.ts`.
UNVERIFIED: authenticated runtime; all eight Solo viewport x PAIGE-state records; keyboard route driven in a browser; zoom/reflow measurement. Reason for all: no authenticated Solo session and no running app instance were reachable from this container. The component was rendered in isolation with the real compiled tokens, which proves appearance and state coverage, not placement inside the Solo shell at a given geometry.

SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: no authenticated Solo render; the card was rendered standalone, not inside the shell at this geometry.

OWNER_INTENT: The owner must be able to press Approve in Solo and have the named action actually happen — "when I press approve, nothing fully executes" — and the fix must be structural, not per-account: "if I bring on 1,000 new users, I need them to not have the same problem... I don't want the fix to be isolated to just single accounts."
MUST_NOT_HAPPEN: The working PaigeAIChat approval path must not regress (§58) — it is the only path with a non-zero success rate on production. No second approval surface may be built in Solo (§18). No approval may execute on the model's own word (§1418's fix must stay removed). No tenant-specific configuration may become a prerequisite for a working approval.
MUST_PRESERVE: `paige_pending_confirmations` as the one approval store; the body-borne fingerprint as the only channel; the stored proposal arguments as what executes; the proposals rail (`paige_pending_approvals`) and `execute-approval`, which are a separate, legitimate system for `propose_action` drafts and are untouched here.
ACCEPTANCE_CRITERIA: On the real platform, signed in to a Solo account with NO tool-autonomy rows configured: ask Paige to create a contact; a Needs-your-OK card appears in the Solo chat; press Approve; the contact exists on re-read; the card reports Done; pressing Approve again does nothing. And on a surface with no binding, no Approve control is offered and the copy names where to complete it.
MOTION_PURPOSE: The working-state spinner communicates that the server holds the action and the card has not been abandoned — the absence of that signal is why a pressed Approve read as "nothing happened". Under `prefers-reduced-motion` the spinner is static and the state change is carried by the `aria-live` heading instead.
PROTECTED_SEAMS: TESTED — the confirm-gate binding contract (fingerprint echoed in the request body; asserted present inside the fetch body, and the paired summary/fingerprint shape asserted on the PaigeAIChat caller so the index-misalignment cannot return). NAMED AND UNAFFECTED — `paige-ai-chat` edge handler (zero edge files changed; empty `deploy-edge-functions` affected set), `execute-approval` and the proposals rail (untouched), `crm-command` door (untouched), the confirm fingerprint primitives in `_shared/confirm-fingerprint.ts` and `_shared/toolConfirmation.ts` (untouched), and RLS/tenant scoping (no query changed).

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->
INTERNAL_BUILD_IDENTITY: 57456a32de0e34d3e4fcdb88310aa1fc18f7175a; deployment=not-yet-deployed; environment=local; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=vitest 11/11 + tsc baseline 12 + vite build exit 0 + impeccable detect exit 0 + rendered frames at 1720x3880 light/dark
RELEASE_CHANNEL: development: nothing is deployed from this branch yet, so the channel is development and the environment is local. On merge under the §4 pre-launch stance the frontend ships to production via Vercel; that deployment identity belongs in the post-merge closeout, not asserted here.
RELEASE_CLASSIFICATION: patch: restores an ability that regressed; no new capability, no schema, no contract widening.
CUSTOMER_RELEASE_IDENTITY: none: pre-launch, no customer announcement authorized.
RELEASE_NOTE_REQUIRED: no: pre-launch with no customers affected.
RELEASE_TRUTH_BOUNDARY: PROOF OWED: authenticated runtime end-to-end in the deployed Solo shell, and the eight Solo viewport records. LIVE — the card's behaviour and refusal (behavioural tests) and its appearance in both themes (rendered frames). PARTIAL — the Solo wiring, which is source-asserted rather than driven.
RELEASE_RECOVERY: position=forward-fix preferred; reference=frontend-only revert of `57456a32d` and `d23a74597` restores prior behaviour exactly (no migration, no edge deploy, no data change); the pre-change state is a non-functional approval, so a revert reinstates a known defect rather than a safe baseline.

## Scope and collisions

- **Classification:** frontend-only repair of a cross-surface approval seam.
- **Affected flows:** operator approves a proposed action in Solo chat (full workspace and floating panel); operator meets an action this surface cannot bind; Paige proposes and reports the outcome.
- **Neighboring regressions:** `PaigeAIChat` confirm card (the one working path) — its call site changed to the paired `actions` prop; covered by an assertion that the filtered parallel-array shape cannot return.
- **Active-owner/file collisions:** none. PR #1371 (crm create-contact governance) touches the CRM door, not this seam; PRs #1398/#1382 touch agreements. No file overlap with any open PR.
- **Explicit exclusions:** Operator chat, Studio chat and the older client chat still send no fingerprints and remain unable to complete an approval. The Conversations drafts surface for Paige-drafted email is absent (tool, persistence and DB trigger exist; UI does not). Calendar has create but no edit or delete. All three are named in the PR, not silently left.

## User job and state map

A Solo operator asks Paige for something consequential. The server proposes, the card states exactly what will happen, the operator decides once, and the action runs exactly once — or the card says plainly why it did not. The scroll owner is unchanged: the existing chat `.pane`; the card is an in-flow block inside it and introduces no scroll container of its own.

## Evidence index

- `npx vitest run src/solo/solo-approval-executes.test.tsx` → 11 passed, exit 0.
- Negative control A: `canApprove = true` → 2 failed (the no-binding refusal assertions).
- Negative control B: restore `if (parsed.paige_confirm?.summary) continue;` and drop `approvedConfirmations` from the body → 2 failed (both wiring assertions).
- Baseline: `git stash` → the same 4 files, 19 failed / 46 passed; restored.
- `npx tsc --noEmit -p tsconfig.app.json` → 12 (baseline), zero in touched files.
- `npx vite build` → exit 0.
- `npx impeccable@4.1.0 detect src/components/chat/PaigeConfirmCard.tsx src/solo/agent.tsx` → exit 0.
- Production measurement behind the defect (read-only SQL, project `xygzykjyynhzqytbqnzu`): `paige_pending_confirmations` over 30 days → 36 proposals, 21 with `consumed_at IS NULL`, including `crm_create_contact` 2026-09-24 22:53, `document_generate` 18:11, `draft_marketing_content` 18:10.
- `resolve_tool_autonomy` definition read from production: `_mode := coalesce(_mode,'confirm')` — a tenant with no rows defaults to asking, so no per-tenant seeding is required.
- Frames: `solo-approval-executes/confirm-card-light.png`, `confirm-card-dark.png`.

## Review and limitations

Independent review: two read-only agents were dispatched before the first edit — one pinned that #1418's self-approval branch was already removed by `cda229c` and deployed (run #363, `✓ deployed paige-ai-chat`, 2026-09-24 03:47:50 UTC), which reframed this work from "remove the branch" to "repair what its removal broke"; the other produced the wiring map confirming the two tables are disjoint (`execute-approval` contains zero references to `paige_pending_confirmations`). A §39 adversarial read of the pushed diff is OWED and is not claimed here.

Limitations, each stated rather than softened: the Solo wiring is source-asserted, not driven; the authenticated runtime is unverified; the eight Solo viewport records are unverified; no keyboard or zoom drive was performed. `flow-prototype` is not installed in this environment, so Gate 5 fails and the prototype gate is recorded as an owner waiver rather than a satisfied gate.
