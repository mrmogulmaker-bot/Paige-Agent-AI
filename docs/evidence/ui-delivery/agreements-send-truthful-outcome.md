# UI delivery evidence: agreements-send-truthful-outcome

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow traced end to end for the send flow before any edit, and recorded in PR #1398 and issue #1395 as a nine-link source trace — sales-ops.tsx:713 (documentSource hardcoded) -> create_agreement_signing (20270404000000:62) -> save_paige_agreement:838 (body nulled) -> agreement-send:100 (document_path absent from the SELECT) -> :165 (first send renders) -> :168 (renders a null body) -> assertDocumentIsRenderable (passes an empty string) -> doc-render.ts:138 (returns []) -> :176-203 (frozen and emailed). Actor: the Solo operator sending an agreement for signature. Goal: the counterparty receives the document the operator uploaded.
PAIGE_UI_DESIGN: PASS: Read .claude/skills/paige-ui-design/SKILL.md and the canonical .agents/skills/paige-ui-design/SKILL.md in full this session before editing the hook. Module routing recorded below under "Review and limitations": modules 1 and 4 applied; modules 2 and 3 recorded as not needed with reasons, per the skill's instruction that a presentation-only change still records which modules it did not need.
MATERIAL_FLOW_CHANGE: NO: No goal, choice, step, transition, confirmation, exit or side effect is gained or lost in the interface. The send flow's shape is unchanged — send resolves to either a success notice or a refusal notice, both of which already existed and both of which already rendered in the same footer region with the same role="alert". What changed is that each now reports the outcome that actually occurred: the refusal carries the server's own sentence instead of a generic fallback, and a successful send reports the people it reached instead of reporting none. Which inputs reach the refusal exit did change (a non-PDF upload is now refused), but that condition is implemented server-side in agreement-send and reaches this surface through the refusal path that already existed.
FLOW_PROTOTYPE: NOT_REQUIRED: Per MATERIAL_FLOW_CHANGE: NO above — the change corrects the text bound to two pre-existing states and adds no state, exit, control or confirmation. Recorded as a reasoned judgement a reviewer can overturn, not as a convenience: if the reviewer holds that the new server-side refusal condition constitutes a new exit, this record should be returned and a prototype produced.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Audience is the Solo operator on the Sales/agreements send panel. Primary action is sending an agreement for signature. Purpose of this change is that the panel's reported outcome matches reality — an operator must be able to tell a delivered agreement from an undelivered one, and must be told what to fix when a send is refused.
VISUAL_DIRECTION: NOT_APPLICABLE: No visual direction is sourced, invented or changed. No token, colour, layout, geometry, typography, spacing, icon or motion is touched. The two notices already exist and are rendered by unchanged markup at sales-ops.tsx:1203-1206 (refusal) and :1275-1279 (success); only the strings and the array they derive from changed.
AUTOMATED_EVIDENCE: PASS: sales-ops.contract.test.tsx 90 passed exit 0; deriveSalesCommand.test.ts included in the same run. Deno agreements suite (token, document, signing-guard) 58 passed exit 0. Guard proven to bite by restoring the defect: deno test exit 0 baseline, exit 1 with planPresentedDocument forced to always render, exit 0 restored.
STATIC_EVIDENCE: PASS: npm run ci:tsc baseline 12 current 12 exit 0 (no new type errors); npx eslint on the changed files exit 0; deno check --allow-import --node-modules-dir=none on sign-agreement, agreement-document and agreement-send exit 0; scripts/agreements/run-integrity-proof.sh exit 0 (every negative refused with its expected SQLSTATE).
RENDERED_EVIDENCE: UNVERIFIED: No browser is available in this session, so no frame of the send panel was captured. The affected markup is unchanged; what is unproven is the rendered result of the corrected strings.
BEHAVIORAL_EVIDENCE: UNVERIFIED: No browser and no authenticated session in this container, so the send panel was not driven. The hook's two corrections are covered only by reading and by the contract suite, which mocks the hook boundary and therefore cannot exercise them — that mocking is precisely why the "Nothing went out" defect survived in shipped code.
AUTHENTICATED_RUNTIME: UNVERIFIED: No database, no browser and no authenticated tenant are reachable from this session. Affected claims: that a real send now displays "Sent." with the address it reached; that a .docx upload is refused with "That document is not a PDF..."; that a protected PDF is refused naming protection; and that the signing link opens the tenant's uploaded document. All four are owed to an authenticated live check and are named as owed in PR #1398 rather than claimed.
KEYBOARD_FOCUS: NOT_APPLICABLE: No control, focus order, tab stop, accessible name or label is added, removed or reordered. The notice region and its role="alert" are unchanged.
ZOOM_REFLOW: NOT_APPLICABLE: No layout, geometry, container, overflow or scroll owner is touched. Text length inside an existing notice region is the only variable, and that region already wraps arbitrary server sentences.
REDUCED_MOTION: NOT_APPLICABLE: No motion, transition or animation is added or changed.
STATE_COVERAGE: PASS: The states this change touches are refusal and success, both covered by reading plus the edge-function tests. Refusal now distinguishes not_pdf, unreadable (protected or damaged), empty, a missing uploaded file, a document_path that does not belong to this workspace (403), and an agreement frozen before this fix whose stored document is not the uploaded file (409). Success reports the addresses reached and those not delivered. Loading, first-use, empty, cancel and workspace-switch behaviour are untouched — the workspace-identity guard at both ends of sendForSignature is unchanged.
TRUTHFUL_STATE_LABELS: PASS: This change exists to make two labels truthful. Previously a fully delivered agreement rendered "Nothing went out. No message reached anybody, so this document is unchanged.", and every deliberate refusal the edge function wrote — thirteen of them — was replaced by a generic "check the record". Both are corrected to report what actually happened, which is the rule this field exists to enforce.
SOLO_UI: YES: The Solo Sales/agreements send panel (src/solo/sales-ops.tsx), via its data hook src/solo/useSoloAgreementSignings.ts.
SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: No browser in this session. No geometry, layout or container changed, so no reflow is expected at this or any viewport; the claim is nonetheless unproven and is owed to a browser-capable session.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: As above — no browser; no geometry change; owed.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: As above — no browser; no geometry change; owed.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: As above — no browser; no geometry change; owed.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: As above — no browser; no geometry change; owed.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: As above — no browser; no geometry change; owed.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: As above — no browser; no geometry change; owed. This is the narrowest recorded viewport and therefore the one where a longer refusal sentence is most likely to reflow, so it is the first a capable session should look at.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: As above — no browser; no geometry change; owed.
UNVERIFIED: Remaining behaviour, all for the same reason — no browser, no authenticated session and no database in this container: (1) the rendered send panel at any viewport; (2) an authenticated send displaying "Sent." with the address reached; (3) a .docx upload refused with the not-a-PDF sentence; (4) a password- or permissions-protected PDF refused naming protection; (5) the signing link opening the tenant's real uploaded document. Separately and not settleable from source: whether the tenant-agreements storage bucket row exists in production — no migration creates it (20260630190349 defines only four storage.objects policies), so a fresh environment has nowhere to put the file. One query settles it: select id from storage.buckets where id = 'tenant-agreements'.

OWNER_INTENT: The owner's objective is that a real client can receive a real agreement, sign it, and the platform hold correct proof of what they signed. Target actor: the Solo operator sending for signature, and the counterparty receiving it. Required outcome for this change: the operator is told truthfully whether the agreement went out and, when it did not, what to do about it.
MUST_NOT_HAPPEN: A send must not report success it did not achieve, and must not report failure it did not suffer. A refusal must not be replaced by a sentence that gives the operator nothing to act on. No existing refusal, workspace-identity guard, or draft-only rule may be weakened to make a message nicer.
MUST_PRESERVE: The workspace-identity check at both ends of sendForSignature (a send must still abandon if the workspace changed mid-flight); the existing notice markup, placement and role="alert"; the generic fallback sentence as the last resort when the server supplied nothing readable; and the shape of SigningSendResult consumed by sales-ops.tsx.
ACCEPTANCE_CRITERIA: On the real platform, authenticated: (1) send an agreement with a PDF upload and see "Sent." naming the address reached, not "Nothing went out"; (2) open the signing link and see the tenant's uploaded document; (3) attempt a send with a .docx and see the not-a-PDF refusal, with nothing delivered; (4) attempt a send with a protected PDF and see the refusal naming protection.
MOTION_PURPOSE: NONE: no motion change.
PROTECTED_SEAMS: Tenant isolation on the uploads bucket is now an impacted seam and is tested: the send path downloads with the service role, which bypasses the bucket's SELECT policy, so the policy's own predicate (the path's first segment is the owning tenant) is restated in isOwnedByTenant and covered by six cases including traversal, absolute, backslash, empty-segment and prefix-collision forms. Previously impacted and tested: Impacted and tested: the agreement-send request/response contract (a producer and consumer inventory was run across all eight caller classes; the single producer is useSoloAgreementSignings.ts:536 and the single consumer is :542-553, both changed together in this PR); the frozen-document seam (content_storage_key / content_sha256 are still written exactly once, by the same code path, and enforce_agreement_seal_immutable is untouched). Named and unaffected: the seal path (sealAgreementPdf and sealAndComplete are unchanged; the change makes their input loadable rather than altering them); issue_agreement_signing_link — CORRECTED 2026-09-23, this record previously listed it as unaffected on the reasoning that its tenant_upload refusal now correctly no-ops once the send has frozen real bytes. That reasoning holds only when the frozen bytes ARE the real ones. For a row frozen by the pre-fix send the digest is non-null and the document is the blank, so the refusal no-opped straight past the defect and the manual link went on issuing a token for it. It is an IMPACTED seam, and migration 20270411000000 applies the same provenance test the send path carries, proven by scripts/agreements/link-provenance-proof.sql; peek_agreement_signing; the signer-facing signing page (unchanged here and filed separately as #1399); RLS and tenant scoping on paige_agreements and paige_agreement_signers.

INTERNAL_BUILD_IDENTITY: f09587ff60c69cefaa2464886b2ae0bbe07a4bf7; deployment=Dqy66MNG6S5iZ2oYMLBH6WHtm1Zo; environment=preview; migrations=NOT_APPLICABLE; edge=PROOF_OWED(agreement-send and _shared/agreements/document.ts deploy on merge via deploy-edge-functions and none is deployed yet); evidence=PR #1398 plus the command exit codes recorded under AUTOMATED_EVIDENCE and STATIC_EVIDENCE in this record
RELEASE_CHANNEL: preview: this change is not deployed. It exists as PR #1398 with the Vercel preview deployment named in INTERNAL_BUILD_IDENTITY. Production is where it is headed under the pre-launch stance in CLAUDE.md §4, but recording production here would assert a deployment that does not exist.
RELEASE_CLASSIFICATION: patch: a defect correction with no new capability and no schema change; urgent because it governs which document a counterparty is asked to sign
CUSTOMER_RELEASE_IDENTITY: none: pre-launch, no customers are on the platform and no customer-facing release identity is earned by a defect fix
RELEASE_NOTE_REQUIRED: no: pre-launch with no customers to notify; the correction is recorded in PR #1398 and issue #1395
RELEASE_TRUTH_BOUNDARY: LIVE: nothing — this is not deployed. PARTIAL: the send path's correctness is established by source trace, unit and contract tests, a bite proof, and the agreements integrity proof, all at the exit codes recorded above. PROOF OWED: every authenticated runtime claim listed under UNVERIFIED, plus the existence of the tenant-agreements bucket row in production. UNAVAILABLE: rendered and browser-driven evidence, for want of a browser in this session.
RELEASE_RECOVERY: position=revert the commit — no migration, no data transformation and no irreversible write is involved, and the frozen-document columns are written only where they were already written; reference=PR #1398

## Scope and collisions

- Classification: defect correction in a Solo data hook plus two edge-function files; no visual or layout change.
- Affected flows: Solo Sales -> send an agreement for signature (operator side). The signer-facing flow is deliberately excluded, see below.
- Neighboring regressions: none found. The only consumer of the changed hook method is sales-ops.tsx:1201, which was read and is unchanged; its two render sites for the success and refusal notices were read and are unchanged.
- Active-owner/file collisions: another lane holds INT-163/INT-178 work on agreements (branch claude/optimistic-meitner-95a0gl). That work touches _shared/agreements/knowledge.ts, seal.ts, storage-path.ts and the Spine registry. This change touches agreement-send/index.ts, _shared/agreements/document.ts and src/solo/useSoloAgreementSignings.ts. document.ts is the one file where a collision is plausible; this change only ADDS three exported functions and does not modify sealAgreementPdf.
- Explicit exclusions: the signing page's failure to display an uploaded document, and its read gate recording "Read 100%" over a document it never showed, are filed as #1399 and excluded here because the remedy carries an owner decision about what "I have read this" means for a document whose scroll depth cannot be measured.

## User job and state map

The operator's job is to get a document in front of a client for signature and know whether it arrived. Primary action: Send. States touched: success (now names the addresses reached) and refusal (now carries the server's own sentence, distinguishing a non-PDF upload, an unreadable or protected PDF, an empty PDF, a missing uploaded file, and every pre-existing refusal such as a missing workspace contact email or an already-sent agreement). Untouched: first use, loading, cancel, workspace switch, and the draft-only rule, which remains the server's to enforce. Side effect: none added. Scroll owner: unchanged.

## Evidence index

- `npm run ci:tsc` — baseline 12, current 12 — exit 0
- `npx eslint src/hooks/useAnalytics.ts src/solo/useSoloAgreementSignings.ts` — exit 0
- `npx vitest run src/solo/sales-ops.contract.test.tsx` — 90 passed — exit 0
- `deno test --allow-import --node-modules-dir=none --deny-net --deny-env --deny-run --deny-ffi --deny-sys` over token.test.ts, document.test.ts, signing-guard.test.ts — 58 passed — exit 0
- `deno check --allow-import --node-modules-dir=none` over sign-agreement, agreement-document, agreement-send — exit 0
- `./scripts/agreements/run-integrity-proof.sh` — exit 0 — "every negative refused with its expected SQLSTATE; positive controls succeeded"
- Bite proof: deno test exit 0 -> exit 1 with `planPresentedDocument` forced to always render -> exit 0 restored
- Tenant contexts, roles, themes, timestamps: none exercised — no authenticated session was available. Recorded as owed rather than omitted.

## Review and limitations

Module routing under the paige-ui-design skill: module 1 (owner intent) applied and recorded above. Module 4 (protected behaviour and regression) applied and recorded above. Module 5 (release acceptance evidence) applied throughout — every claim is separated by class and no claim is labelled LIVE. Module 2 (visual and immersive quality) not needed: no token, colour, geometry, typography or motion is touched. Module 3 (interaction geometry and accessibility) not needed for design purposes — no control, focus order or accessible name changes — but its Solo viewport requirement still applies and is recorded UNVERIFIED above rather than skipped.

Independent review: a producer and consumer inventory of `agreement-send` was run across the eight caller classes. It found the two client defects corrected here, which reading the edge function alone would not have surfaced: `supabase-js` throws `FunctionsHttpError` on any non-2xx before parsing the body, so `data` is null and the server's sentence is discarded; and the function returns recipients as objects while the client mapped them with a string-only helper.

Remaining limitations: everything under `UNVERIFIED` above. The most consequential is that no authenticated send was performed from this session, so the corrected outcome reporting is proven by construction and by test, not by a human completing the job on the real platform.
