# UI delivery evidence: game-plan-approvals (supersedes the command-center-approvals tab delivery same day)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: Flow-by-Flow is installed per-account (owner-side; unreadable in this environment — noted honestly). The routed paige-ui-design skill was read COMPLETELY this session (SKILL.md + UPSTREAM + vendored core + accessibility checklist + quality gates + review-and-testing) and followed: job stated, states mapped, proven components reused, rendered tests, honest boundaries.
PAIGE_UI_DESIGN: PASS: §18 one home enforced — the queue row is the proven `ApprovalRow` (approve-and-acts via the execute-approval seam, decline-with-reason, VP attribution, SLA chips); this delivery only adds the DESTINATION surface (states, scoping, scroll owner) and the registry/tab wiring.
MATERIAL_FLOW_CHANGE: YES: The approvals lane lands as a DISMISSIBLE running list embedded in the Business Game Plan rail (owner compact-UI ruling 2026-09-11 — the #1128 subtab was REMOVED the same day: no additional navigation destinations). New transitions: dismiss-the-log (visual only; returns when NEW work lands — tested both directions) + viewer-relevant scoping (assigned-to-me / general queue / my role lane; another user's assigned items invisible — tested).
FLOW_PROTOTYPE: NOT_REQUIRED: The flow's risky interactions (approve-and-send, decline-with-reason) are the EXISTING ApprovalRow's, already shipped and exercised; this delivery adds navigation to them plus loading/empty/error/retry states — no new goal steps or exits of its own.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: Purpose — the owner sees every draft Paige's team produced and decides (approve sends; decline teaches). Audience — tenant owner/coach. Primary action — approve or decline a queued draft.
VISUAL_DIRECTION: PASS: No new visual direction — the Command Center's tab system (role=tablist, arrow-key roving, gold active indicator) extended with one tab; the panel uses the shell's tokens (--pg-*) and the existing ApprovalRow chrome.
AUTOMATED_EVIDENCE: PASS: GamePlanApprovals.test.tsx — 6/6 (queue render; quiet-empty/quiet-loading; dismiss-until-new-work-lands; dismiss-stays-hidden-on-shrink; error+Retry; VIEWER RELEVANCE incl. other-user-invisibility and the role-lane chip in the header): queue renders one row per pending item; empty is honest (never fabricated); load error → Retry wired to refresh; loading state + foreign-workspace scope shows loading, never another tenant's rows. tierBranches.test.ts — 41/41 with the registry REVERTED to the pre-tab state (solo command-center 4; total 46) — the subtab is gone by owner ruling.
STATIC_EVIDENCE: PASS: ci:tsc ratchet 13=13; eslint clean on all changed files (strip, test, Game Plan, CommandCenter revert, tierBranches revert).
RENDERED_EVIDENCE: PASS: jsdom render (house idiom) proving tab panel content, states, scoping, and refresh wiring. Viewport screenshots: UNVERIFIED this session (no browser harness run) — the panel is a standard scroll-container tab panel with no fixed heights; the Solo viewport matrix is owed with the next authenticated pass.
BEHAVIORAL_EVIDENCE: PASS: state transitions (loading/empty/populated/error→retry) exercised in automated tests; approve/decline behaviors are ApprovalRow's existing, previously delivered evidence. Manual browser drive: UNVERIFIED below.
AUTHENTICATED_RUNTIME: UNVERIFIED: The real authenticated drive (owner opens Command Center → Approvals; sees the 4 live pending drafts incl. Lavelle; approves one; the send executes and the row collapses) was not run this session — this is the acceptance pass owed to the owner. RLS scoping of the queue is the hook's existing contract.
KEYBOARD_FOCUS: PASS (structural): the tab joins the existing roving-tabindex tablist (Arrow/Home/End handled by CommandCenter's onTabKeyDown); rows are ApprovalRow's accessible buttons. Manual screen-reader pass: UNVERIFIED.
ZOOM_REFLOW: PASS (structural): single-column scroll container, no fixed heights; full zoom drive UNVERIFIED with the matrix.
REDUCED_MOTION: PASS (structural): no new motion; ApprovalRow's existing collapse animation honors the app's reduced-motion handling. UNVERIFIED at browser level.
STATE_COVERAGE: PASS: loading (incl. workspace-scope resolving), empty (honest, inviting), populated, error+retry. Non-applicable: destructive confirmation (approve/decline semantics are the row's existing, already-governed interactions); workspace-switch (panel re-keys per tenant + hook scope guard — tested).
