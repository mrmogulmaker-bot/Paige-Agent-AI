# UI delivery evidence: INT-162 — the offer attach list (approved screen 2)

The last outstanding detail of the owner-approved Agreements pack (Artifact `S271qc7uGTFXbdNoTz49VC`,
§28 APPROVED-FROZEN). Screen 2's "Which offer this is for" shipped as a native `<select>` under a
pager; the approved screen is an attach LIST. This record covers that replacement only. The lane's
other records are `int162-agreements-and-signing.md` and `int162-screen5-signed-and-completed.md`.

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: flow-by-flow v2.0.1 with flow-prototype v2.0.1 paired at the same version (Gate 5 PASS); mode Existing Project, risk R1 (one control on one existing surface, no contract change) so depth Standard; affected flow = a Solo owner writing an agreement picks which offer it is for, reading each offer's price at the moment they choose rather than after.
PAIGE_UI_DESIGN: PASS: impeccable read; reference/craft-floor.md read immediately before the edit. The list's rules were ported from the approved pack's own `.results`/`.res` CSS read out of the artifact, not invented.
MATERIAL_FLOW_CHANGE: NO: the same choice, the same state, the same write. What changes is that every offer and its price are visible at once instead of one at a time behind a dropdown, and that the snapshot lock now says why it is locked.
FLOW_PROTOTYPE: PASS: owner-approved prototype Artifact S271qc7uGTFXbdNoTz49VC Version 2, screen 2 of 5, approved 2026-09-22. Its offer list renders each offer with its price and marks the chosen one "Attached"; its no-match state carries Quick offer and Open Catalog. All three are ported here.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: purpose is to attach the right offer to an agreement; audience is the Solo business owner or tenant admin writing it; the primary action is attach one offer, with "no offer — this document carries no price" as an explicit, first-class choice rather than the absence of one.
VISUAL_DIRECTION: PASS: the committed Solo palette. The attached row carries a standing violet ground and violet "Attached" pill — a SELECTION, never an act, so it spends no gold (§11). The "Attach" affordance stays quiet until its row is hovered, because eight shouting buttons down a list is noise rather than an invitation. Prices sit in their own tabular-nums mono column so a reader can run their eye down them.
AUTOMATED_EVIDENCE: PASS: src/solo/sales-ops.contract.test.tsx EXIT CODE 0, 84 tests (83 before). Three existing tests were RE-POINTED and none deleted, because their mechanics changed while what they guard did not: the price/cadence test now reads the row's own price column, the no-match test reads the approved wording and additionally asserts the two acts, and the quote-transition test clicks the attach row instead of assigning a second `<select>`. One test was ADDED for a gap the re-point exposed: the pager test was asserting only its LABEL on a single-page catalog, which proved the label existed and never that paging worked — the new test sets hasMore, asserts the pager appears, clicks Next, and asserts page 2.
STATIC_EVIDENCE: PASS: tsc --noEmit EXIT 0; vite build EXIT 0; npm run lint:tier-features EXIT 0. HONEST LIMIT: src/solo/sales-ops.tsx carries // @ts-nocheck, so tsc gives ZERO signal on the file this change is in; the suite and the browser drive are its only evidence.
RENDERED_EVIDENCE: PASS: rendered in real Chromium through the sales-mount harness, in BOTH themes at 900x1000, with the claims read from the live DOM rather than from the image — three attach rows (the no-offer row plus two offers), exactly one carrying aria-pressed=true, the price column reading "$2,400 · monthly" and "No price recorded", the pill reading "Attached" and the affordance "Attach", a 42px row, no row clipping its own content, and no sideways scroll in the sheet. Exactly ONE native select remains in the editor and it is the client field, which this change does not touch. Frames S2-picker-light.png and S2-picker-dark.png in scripts/live-drive/artifacts/sales-ops (gitignored). The full surface drive passes 536/536 EXIT 0 with horizontal overflow 0 across all 48 column states. LOCAL renders, never deployed or authenticated captures (§32.c).
BEHAVIORAL_EVIDENCE: PASS: driven through the rendered DOM — opening the editor, clicking an attach row and reading aria-pressed back as true, clicking Next and reading page 2, and typing a search that matches nothing and reading the no-match state with its two acts. Harness and double evidence, NOT authenticated runtime.
AUTHENTICATED_RUNTIME: UNVERIFIED: LIVE_DRIVE_EMAIL and LIVE_DRIVE_PASSWORD are unset in this session, so the authenticated Solo route cannot be signed into. That blocker is credentials, not capability. Owed to a capable session. Unlike the rest of this lane the backend here is NOT the blocker — useCatalogOffers reads a live table today — so this one becomes provable the moment credentials exist.
KEYBOARD_FOCUS: PASS: every row is a real `<button>` in document order, reachable by Tab and operable by Enter and Space, with a visible inset violet focus ring. Single-select state is carried on aria-pressed, the same idiom the source chooser above it already uses, so a screen reader announces the offer's name and whether it is pressed. The "Attach" text is aria-hidden because the row IS the button and already carries the offer's name; announcing "Attach" again would read the control twice.
ZOOM_REFLOW: PASS: rows are flex with a flexible spacer, the name truncates with an ellipsis rather than pushing the price out of the row, and the price and state chips are flex:none so they never collapse. Measured in the shot: no row clips its content and the sheet does not scroll sideways in either theme; the full drive measures zero horizontal overflow across all 48 column states including the 439px tightest real column.
REDUCED_MOTION: PASS: the only motion is a 140ms background transition on hover, and the sheet-wide prefers-reduced-motion guard already in sales-ops.css (.so-editor *) removes it. No new animation was added.
STATE_COVERAGE: PASS: no offer chosen (the no-offer row attached), an offer chosen, an offer with no recorded price (reads "No price recorded" rather than a blank or a zero), a catalog read that FAILED (says the catalog could not be read, offers a retry, and does not pretend the list is complete), a search matching nothing kept DISTINCT from an empty catalog, an empty catalog with its Quick offer and Go to Catalog acts, unknown offer authority, more than one page, and the snapshot-locked case.
TRUTHFUL_STATE_LABELS: PASS: an offer with no active price reads "No price recorded" — not blank, not $0. A failed catalog read says the list may be incomplete instead of rendering a short list as if it were the whole catalog. The snapshot lock NOW SAYS WHY: previously the search, the picker and the pager simply went dead with no reason given, which §70.1 refuses to count as delivered; the lock itself is unchanged, what is new is that it explains itself.
SOLO_UI: YES: Campaigns → Sales → Commercial Terms, the agreement editor (src/solo/sales-ops.tsx, src/solo/sales-ops.css).
SOLO_1536X770_PAIGE_CLOSED: PASS: rendered via the sales-mount harness at 1536x770 with PAIGE docked-closed, light and dark, frames fit-{light,dark}-1536-paige-closed-terms.png. Zero horizontal overflow, single shell scroll owner.
SOLO_1536X770_PAIGE_OPEN: PASS: rendered at 1536x770 with PAIGE open (521px column), light and dark, frames fit-{light,dark}-1536-paige-open-terms.png. Zero horizontal overflow, single shell scroll owner.
SOLO_1366X768_PAIGE_CLOSED: PASS: rendered at 1366x768 with PAIGE docked-closed (685px column), light and dark, frames fit-{light,dark}-1366-paige-closed-terms.png. Zero horizontal overflow, single shell scroll owner.
SOLO_1366X768_PAIGE_OPEN: PASS: rendered at 1366x768 with PAIGE open — the 439px tightest real column. Frames fit-{light,dark}-1366-paige-open-terms.png. Zero horizontal overflow, single shell scroll owner; the column scrolls vertically, recorded by the drive as an expected scroll rather than a fit.
SOLO_1024X768_PAIGE_CLOSED: PASS: rendered at 1024x768 where PAIGE becomes an overlay, light and dark, frames fit-{light,dark}-1024-overlay-terms.png. Zero horizontal overflow, single shell scroll owner.
SOLO_1024X768_PAIGE_OPEN: PASS: covered by the same 1024x768 overlay frames — PAIGE opens AS an overlay at this width rather than narrowing the column, so the page geometry is identical. Named rather than duplicated, because two measurements of one geometry would overstate the evidence.
SOLO_900X1000_PAIGE_CLOSED: PASS: rendered at 900x1000 (overlay), light and dark, and this is the viewport the two dedicated picker frames were shot at. Zero horizontal overflow, single shell scroll owner.
SOLO_900X1000_PAIGE_OPEN: PASS: covered by the same 900x1000 overlay frames, for the reason given in the 1024 open row.
UNVERIFIED: the authenticated runtime drive of the picker against a real catalog. Owed to a capable session; it needs credentials this session does not hold, and nothing else.
INTERNAL_BUILD_IDENTITY: f065199328334c71a174fc7c2a7791558655912a; deployment=none-pre-merge; environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this-PR+src/solo/sales-ops.contract.test.tsx(84-tests-EXIT-0)+scripts/live-drive/sales-ops-drive.mjs(536/536-EXIT-0)+two-theme-picker-shot-with-live-DOM-assertions
RELEASE_CHANNEL: development: branch build (environment=development); a frontend-only change that deploys via Vercel on merge. No supabase/migrations file and no supabase/functions bundle changed, so neither db-live nor edge-live moves.
RELEASE_CLASSIFICATION: internal-only: porting the last outstanding detail of an owner-approved prototype (§28) and removing a control §11 bans outright; no owner-decided customer release.
CUSTOMER_RELEASE_IDENTITY: none: pre-launch platform with no live customers on this surface; the owner reviews on the live site per §4 and no customer release identity has been earned or decided.
RELEASE_NOTE_REQUIRED: NO: no customer holds an account on this surface, so there is no customer-visible behaviour change to announce.
RELEASE_TRUTH_BOUNDARY: PARTIAL: the control, its states and its refusals are proven headless — 84 tests EXIT 0, 536/536 harness checks EXIT 0, and a two-theme render whose claims are read from the live DOM. What is NOT proven is the authenticated path against a real catalog, which needs only credentials. tsc is additionally NOT a signal for src/solo/sales-ops.tsx, which carries // @ts-nocheck.
RELEASE_RECOVERY: position=fully reversible by revert — the change is frontend-only, replaces one control with another over the same state and the same write, and creates no table, column, index or stored object, so reverting the merge commit restores the previous dropdown with no data or runtime state to unwind; reference=git revert of the merge commit
OWNER_INTENT: the approved screen 2, delivered as drawn — an owner picks the offer from a list that shows what each one costs, instead of a dropdown that hides every price until it is opened.
MUST_NOT_HAPPEN: never render a price as blank or $0 when none is recorded; never present a partial list from a failed read as the whole catalog; never conflate "nothing matches that search" with "your catalog is empty"; never spend gold on a selection; never leave a disabled control unexplained.
MUST_PRESERVE: the pager, which came off the deleted band with the search and is KEPT rather than dropped with the dropdown it sat under — the approved pack shows three fixture offers and needs none, a real catalog has hundreds, and removing the only way past the first five would be a regression dressed as fidelity. It now renders only when it can do something, and a new test proves it actually pages. Also preserved: the search input, the unknown-offer-authority notice and its retry, the catalog-read-failed retry, the empty-catalog state with Quick offer and Go to Catalog (Quick offer's only call site in the repository), and the snapshot lock.
ACCEPTANCE_CRITERIA: on the real platform an owner opens the agreement editor, sees their offers listed with prices, attaches one in a single click, can attach none deliberately, can page past the first five, and is told plainly when a search matches nothing, when the catalog is empty, when the catalog could not be read, and when the choice is locked by a price snapshot. Proven against the harness and the contract; the authenticated run is owed.
MOTION_PURPOSE: almost none, deliberately. A list of choices should be still: the only motion is a 140ms ground change under the pointer, enough to say "this row is the one you are about to pick" and nothing more. Removed under reduced motion.
PROTECTED_SEAMS: useCatalogOffers is untouched — same hook, same options, same page/pageSize/referenceIds contract, so the terms table, deriveSalesCommand, ScenarioLab and the Revenue view that also read it are unaffected. `offerOptionLabel` had exactly one call site, the `<option>` this change removes, and was DELETED rather than left behind as a second diverging opinion about how an offer is labelled (§18). The sales-mount harness needed no change. The full drive passes 536/536 EXIT 0.
