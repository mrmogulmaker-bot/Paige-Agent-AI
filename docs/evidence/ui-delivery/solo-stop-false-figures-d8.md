# UI delivery evidence: Solo — stop four surfaces asserting things that are not true (D-8)

UI_DELIVERY_EVIDENCE_VERSION: 1
FLOW_BY_FLOW: PASS: flow traced before the first edit — actor: Solo owner reading Command Center, Game Plan and Analytics. Affected flow: "owner reads a figure and forms a belief about their business". Inspection performed against the real call sites, not a description: every render site of won_value_cents / active_clients / at_risk_clients / arpc_cents located by grep, each traced back to its SQL in 20260713152601_tier_dashboard_metrics.sql, and every consumer of the changed strings enumerated (§37) before editing. Collision check: no other lane held these files; the concurrent agreements PR #1382 is on a different branch. Pre-edit packet returned in the session transcript and restated in OWNER_INTENT / MUST_NOT_HAPPEN / MUST_PRESERVE / ACCEPTANCE_CRITERIA below.
PAIGE_UI_DESIGN: PASS: .agents/skills/paige-ui-design/SKILL.md read in full plus all five routed files (UPSTREAM.md, vendor/frontend-design/SKILL.md, vendor/frontend-design/references/accessibility-checklist.md, references/paige-quality-gates.md, references/review-and-testing.md) and four of the five composable modules (1 owner-intent, 3 interaction-geometry, 4 protected-behavior, 5 release-acceptance). Module 2 (visual-immersive-quality) was read as not-applicable and is recorded as such rather than skipped silently: this change alters no visual direction, token, colour, layout or motion.
MATERIAL_FLOW_CHANGE: NO: no goal, choice, step, transition, state, confirmation, exit, recovery path or side effect changes. Four labels are corrected and one unproved stand-in is replaced by an honest empty state. Every control, route and write is untouched.
FLOW_PROTOTYPE: NOT_REQUIRED: copy correction plus suppression of an unproved stand-in, changing no action, state, exit or consequence. Not claimed because a prototype was inconvenient — the change adds nothing for an owner to approve the shape of; it removes assertions.
PURPOSE_AUDIENCE_PRIMARY_ACTION: PASS: purpose — stop the product telling a Solo owner things that are untrue about their own business while the correct numbers are designed in Phase 2. Audience — the Solo owner on their own Command Center, Game Plan and Analytics. Primary action — read a figure and correctly understand what it is and is not.
VISUAL_DIRECTION: PASS: none introduced. Existing tokens and existing components only; the new empty state reuses the .anr-chart-stage--message variant already defined at src/solo/analytics2.css:18 and already used by the sibling error branch in the same function, so it follows the file's established shape rather than adding a parallel pattern (§18). Zero hardcoded colour values added.
AUTOMATED_EVIDENCE: PASS: npx vitest run src/solo/analytics2.render.test.tsx src/solo/SoloSystemsCheckWorkspace.test.tsx src/components/tenant-shell/TenantCommandCenterCore.test.tsx src/solo/data/useSoloGamePlan.test.ts -> 4 files passed, 75 tests passed, exit 0. Negative control included deliberately: analytics2.render.test.tsx previously pinned the invented stage names in place by asserting four of them exist; it now asserts their absence and asserts each of the four invented names is absent by name.
STATIC_EVIDENCE: PASS: npx tsc --noEmit -p tsconfig.app.json -> exit 2, 12 errors, ZERO in any edited file. Pre-existing proven rather than assumed: the edited files were reverted, the identical command re-run, and the two outputs diffed byte-for-byte (identical). npm run lint:doc-citations and lint:doc-citations:test -> PASS.
RENDERED_EVIDENCE: UNVERIFIED: this session has no browser-driving capability (no Playwright/Chrome MCP/in-app browser available), so no screenshot or recording at any viewport or theme was captured. Owed to a capable session. The change alters no layout, token, colour or motion, which argues low risk — it is not a substitute for the proof.
BEHAVIORAL_EVIDENCE: UNVERIFIED: same reason. No control was exercised in a real browser. Statically determined and stated rather than driven: the analytics2 empty branch removes four tabIndex={0} stops and replaces them with static text.
AUTHENTICATED_RUNTIME: UNVERIFIED: no authenticated Solo tenant was driven. This session holds neither browser capability nor database access, so no tenant-scoped route was exercised end to end.
KEYBOARD_FOCUS: UNVERIFIED: not driven. Known statically and recorded because it is a real interaction change: the unproved funnel branch previously exposed four focusable stand-in stages (tabIndex={0}, each announcing an invented stage name); the replacement is static text with zero tab stops. No focusable control was added, removed or reordered anywhere else in the change.
ZOOM_REFLOW: UNVERIFIED: not driven. No fixed height, width or overflow rule was added or changed; the replaced branch moves from a four-item flex row to a single centred message inside the same .anr-chart-stage container, which reflows more freely than what it replaces rather than less.
REDUCED_MOTION: NOT_APPLICABLE: no motion was added, removed or altered. No animation, transition or transform appears anywhere in the change.
STATE_COVERAGE: PASS: the changed state IS an empty/unproved state, and it is the one this change exists to correct. Covered: the unproved funnel state (no issued bundle, UNAVAILABLE truth state, or a bundle resolving to zero stages) now renders an honest absence instead of invented stage names; the proved branch is untouched and still renders server-issued stages; the error branch is untouched. Command Center populated and empty states both render the corrected labels — useCommandCenter.empty is unchanged and still keys on active_clients, so the empty-state threshold did not move.
TRUTHFUL_STATE_LABELS: PASS: this change is entirely an application of this rule. "Revenue this period" asserted money received over a sum of operator-typed deal estimates and is now "Won this period (estimated)". "Active clients" asserted a client relationship over a count of rows whose status column defaults to 'active' and is now "Active contacts". The four "N clients at risk" strings assert the same over the same population and are now "contacts". The funnel no longer names stages the tenant never created. No capability label was inferred from visual treatment, fixtures or a successful unrelated call.
SOLO_UI: YES: src/solo/data/useCommandCenter.ts (Command Center metric tiles and greeting), src/solo/data/useSoloGamePlan.ts (Game Plan chip and evidence lines), src/solo/analytics2.tsx (Analytics sales-funnel unproved state).
UNVERIFIED: The entire rendered, interactive, keyboard, zoom/reflow and Solo-geometry class is unverified — no browser exists in this session. Specifically owed to a capable session: the four Solo viewports PAIGE-open and closed, confirmation that the corrected labels do not wrap or truncate differently from the strings they replace ("Won this period (estimated)" is 9 characters longer than "Revenue this period"), and confirmation that the analytics2 empty state occupies its container gracefully at the narrowest supported width. Nothing else in this change is unverified: the automated and static classes are complete and were measured, not assumed.

SOLO_1536X770_PAIGE_CLOSED: UNVERIFIED: not rendered — this session has no browser-driving capability (no Playwright, Chrome MCP or in-app browser), so no artifact exists at this viewport. Owed to a capable session.
SOLO_1536X770_PAIGE_OPEN: UNVERIFIED: not rendered — same reason. Owed to a capable session.
SOLO_1366X768_PAIGE_CLOSED: UNVERIFIED: not rendered — same reason. Owed to a capable session.
SOLO_1366X768_PAIGE_OPEN: UNVERIFIED: not rendered — same reason. Owed to a capable session.
SOLO_1024X768_PAIGE_CLOSED: UNVERIFIED: not rendered — same reason. Owed to a capable session.
SOLO_1024X768_PAIGE_OPEN: UNVERIFIED: not rendered — same reason. Owed to a capable session.
SOLO_900X1000_PAIGE_CLOSED: UNVERIFIED: not rendered — same reason, and this is the narrowest supported width, where the corrected label lengths and the new centred empty state most need checking. Owed to a capable session.
SOLO_900X1000_PAIGE_OPEN: UNVERIFIED: not rendered — same reason. Owed to a capable session.

OWNER_INTENT: Coordinator ruling D-8, amended and split — computing the right number is Phase 2; stopping the wrong one is now. The surface already implements an honest way to say nothing, so suppressing or relabelling a false figure costs nothing, needs no dictionary entry and no evidence layer, and under §13 does not wait for a phase. Target actor: the Solo owner. Required outcome: no figure on a Solo surface is labelled as something it is not.
MUST_NOT_HAPPEN: No new number, panel, tile, computation or layout. No claim that any replacement figure is correct — none is computed here. No dependency on the lead-versus-client boundary, which is HELD for the owner (D-1), so no label may assert where that boundary sits. No silent removal of a shipped capability (§58). No empty or collapsed container left where content was removed. No surface left internally contradictory by relabelling one string and not its neighbours.
MUST_PRESERVE: The four metrics' underlying SQL, RPC and data contracts — none is touched. useCommandCenter.empty and its threshold. The commandCenterRegistry KPI keys (they key on metric ids, not labels). Tenant scoping and every server-resolved read. The proved funnel branch and the error branch in analytics2. Every other tile, chip and greeting part.
ACCEPTANCE_CRITERIA: A Solo owner opening Command Center sees no figure labelled as something it is not; the Game Plan chip beside the greeting agrees with it rather than contradicting it; a tenant with no pipeline stages sees an honest absence on Analytics rather than four stage names they never created; and no figure that was visible before has silently disappeared without being recorded.
MOTION_PURPOSE: NONE: no motion change. No animation, transition or transform was added, removed or altered, so there is no reduced-motion behaviour to record.
PROTECTED_SEAMS: Declared by an independent adversarial verifier across all fifteen seams. AFFECTED (2): responsive shell geometry — the analytics2 unproved branch changes from a four-item flex row (.anr-cylinder-funnel, width min(690px,94%), staged heights 155/132/109/86px) to a single centred message in the same container; geometry proof at the four Solo viewports is UNVERIFIED and owed, see above. accessibility — the group's accessible name changed to "No proved pipeline stages for this account. No stage, volume, or conversion is implied.", verified accurate against what the branch now renders, and four tabIndex={0} stand-in stops were removed. NOT AFFECTED, each considered explicitly (13): tenant/workspace/client/account isolation — no read, filter or scope touched, labels only; authentication and account choice — no routing or auth path touched; Solo entitlement/signup/paywall/billing/provisioning — untouched; approval/autonomy/authority rules — untouched, no action kind or lane referenced; Spine tool execution — untouched, no capability registered or invoked; canonical writes and readback — no write of any kind is in this change; Rail/receipts/audit/Memory boundaries — untouched, nothing recorded or emitted; chat transcript scroll/stream/message identity/thread change/pop-out/minimize-restore/history hydration — no chat component touched; Live Conversation state — untouched; Secure Browser/Vault and credential boundaries — untouched, no credential or secret referenced; integration/provider status and external side effects — untouched, no provider called; durable job scheduling/retries/idempotency/recovery — untouched, no job path; privacy, secrets and sensitive-data handling — no new data is surfaced; the change strictly reduces what is asserted.

INTERNAL_BUILD_IDENTITY: f495539c67a311ee2a5da62dd218601413b5a946; deployment=not-deployed (draft PR #1403, branch claude/analytics-solo-lane); environment=development; migrations=NOT_APPLICABLE; edge=NOT_APPLICABLE; evidence=this record, plus the automated and static results above
RELEASE_CHANNEL: development: draft pull request on a feature branch; nothing promoted, nothing staged, no rollout in progress
RELEASE_CLASSIFICATION: patch: four user-visible labels corrected and one unproved stand-in replaced; no capability added, no contract changed, no data touched
CUSTOMER_RELEASE_IDENTITY: none: an internal correctness correction on a pre-launch surface; it earns no customer release identity and no version or release name is proposed
RELEASE_NOTE_REQUIRED: no: the platform is pre-launch with no live customers on these surfaces, and the change removes false assertions rather than adding or altering a capability an owner could have relied on
RELEASE_TRUTH_BOUNDARY: PARTIAL: the four label corrections and the empty-state replacement are proven at the automated and static classes and are correct in source; the rendered, interactive, keyboard, zoom and Solo-geometry classes are PROOF OWED because this session has no browser. No claim here rests on a screenshot, a fixture or a mock.
RELEASE_RECOVERY: position=forward-fix or straight revert — the change is six source files, adds no migration, edge function or data write, and reverting restores the prior strings exactly; reference=git revert f495539c6 on branch claude/analytics-solo-lane

## Scope and collisions

- Classification: Solo UI, presentation-only correction (copy plus suppression of an unproved stand-in).
- Affected flows: Solo owner reads a figure on Command Center, Game Plan, or Analytics and forms a belief about their business.
- Neighboring regressions: checked by §37 consumer inventory across the repo for every changed string. Four test files asserted the old strings and were updated in the same change (two of them supplied the retired label as mock data and therefore passed while proving nothing about the real label — that silent drift is what the update ends).
- Active-owner/file collisions: none. No other lane held these files; the concurrent agreements PR #1382 is on a separate branch.
- Explicit exclusions: `src/agency/data/useAgencyMetrics.ts:139,141` emits the identical two false labels on the LIVE agency Command Center from the same RPC values, and was deliberately NOT changed — the agency tier is parked by coordinator ruling while D-8 says a false assertion does not wait for a phase. That tension is recorded in `docs/delivery/analytics-lane-register.md` §B2 F-1 for the coordinator to resolve, not taken unilaterally. `src/pages/admin/PracticeOverview.tsx:117,129` carry the same two labels but the module has zero importers and is classified `orphaned_dead_code` in `docs/delivery/solo-completion-matrix.json`, so it is unreachable and was left.

## User job and state map

Purpose, audience and primary action are recorded above. Visual direction: none introduced; existing tokens and components only. The intended scroll owner is unchanged — no scroll container was added, removed or re-parented.

States: the unproved funnel state is the one that changed, and it now renders an honest absence rather than four invented stage names. The proved and error branches are untouched. Command Center populated and empty states both render the corrected labels; the empty threshold did not move. Exits and side effects: none exist in this change — no control, route or write is involved.

## Evidence index

- `npx vitest run src/solo/analytics2.render.test.tsx src/solo/SoloSystemsCheckWorkspace.test.tsx src/components/tenant-shell/TenantCommandCenterCore.test.tsx src/solo/data/useSoloGamePlan.test.ts` — 4 files passed, 75 tests passed, exit 0.
- `npx tsc --noEmit -p tsconfig.app.json` — exit 2, 12 errors, zero in any edited file; pre-existing proven by revert-and-diff (outputs byte-identical).
- `npm run lint:doc-citations` / `lint:doc-citations:test` — PASS.
- Files changed: `src/solo/data/useCommandCenter.ts`, `src/solo/data/useSoloGamePlan.ts`, `src/solo/analytics2.tsx`, `src/pages/admin/PracticeOverview.tsx`, plus four test files.
- Tenant contexts, roles, themes, timestamps: NOT CAPTURED — no browser and no database access in this session. This is the gap named under UNVERIFIED.

## Review and limitations

An independent adversarial verifier read the real diff (not a description of it) and returned FIX_FIRST with one blocking defect: the change broke `analytics2.render.test.tsx`, confirmed by executing the suite rather than inferring it. That test had pinned the invented stage names in place by counting four of them; it was inverted to assert their absence and now passes.

The verifier also found, and this record carries rather than buries, that the integrator's own crew brief had scoped the at-risk rename to the greeting alone while three further instances render beside it in `useSoloGamePlan.ts` — renaming only the greeting would have left a chip reading "clients at risk" next to a greeting reading "contacts at risk". All four moved together.

Remaining limitations: the entire rendered/interactive/geometry evidence class, named under UNVERIFIED above and owed to a session with a browser. Separately recorded in the lane register and not fixed here: the relabelled won figure is still formatted with a hardcoded `$` (`usd()` pins `currency: "USD"`) over a sum that ignores `deals.currency`, which coordinator ruling D-3 has now established is a live wrong number; closing it needs the per-currency grouping that is Phase 2 compute work, and "(estimated)" does not cover it.
