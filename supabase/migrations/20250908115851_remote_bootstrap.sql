-- =============================================================================
-- Migration 20250908115851 — REPLAY STUB (deliberate no-op)
-- =============================================================================
-- This file was a RECONSTRUCTION of the SQL recorded in prod
-- supabase_migrations.schema_migrations.statements for version 20250908115851
-- (ref xygzykjyynhzqytbqnzu). It was generated by
-- scripts/reconcile-missing-migrations.mjs (task #421) for one purpose only:
-- to give `supabase db push` a local file for a version prod already records,
-- so its "remote migration versions not found in local" history guard passes.
--
-- WHY IT IS NOW A NO-OP
-- Prod already records this version, so `db push` never re-runs it. The body
-- therefore only ever executed on a FRESH replay (`db reset`, and every
-- Supabase Preview branch) — and there it COLLIDED with migration
-- 20251009234919_3d7566f7-15b7-462f-90a3-b74b713298b6.sql,
-- which recreates the entire base schema. The collision aborted that migration
-- at its first duplicate CREATE TABLE, so `public.has_role()` and the rest of
-- the base schema were never created and 342 later migrations failed in
-- cascade. Fresh replay had been impossible since 2025-10-09.
--
-- WHY THE OTHER MIGRATION WINS, NOT THIS ONE
-- Prod's live column types match the superseding migration on every
-- discriminating column, and NOT this reconstruction:
--   businesses.entity_type   = entity_type (enum)   [bootstrap said TEXT]
--   credit_accounts.type     = account_type (enum)  [bootstrap said TEXT]
--   tasks.status             = task_status (enum)   [bootstrap said TEXT]
--   orders.status            = order_status (enum)  [bootstrap said TEXT]
--   funding_offers.requirements = text              [bootstrap said JSONB]
-- Later migrations depend on the enum shapes (e.g. get_business_hierarchy is
-- declared to return entity_type). The superseding migration is also a strict
-- superset: it creates every table, function and trigger these bootstraps
-- create, plus public.user_roles and public.has_role().
--
-- WHAT THIS FILE USED TO CREATE
--   2 tables: dispute_letters, orders (+ RLS on both)
--   6 policies
-- Both tables are recreated by the superseding migration (and prod's
-- orders.status is order_status, that migration's shape, not this one's TEXT).
-- public.dispute_letters is later DROPped by 20260702003624. None of the 6
-- policy names exist on prod today — verified by direct pg_policies query — so
-- omitting them here matches deployed reality rather than diverging from it.
--
-- NOTHING IS LOST ON PROD: this version is already applied there and is not
-- re-run. Nothing is lost on a fresh replay either — every object above is
-- created by the superseding migration or by a later migration (see the PR
-- for the object-by-object accounting). The original reconstructed SQL remains
-- in git history and in prod's own schema_migrations.statements.
--
-- Precedent: 20250908112911_remote_bootstrap_funding_seed_quarantined.sql is
-- the same pattern (a deliberate no-op replacement for a recorded version).
-- =============================================================================

DO $$
BEGIN
  -- Intentional no-op: records the version without applying DDL. The base
  -- schema is applied by 20251009234919_3d7566f7-15b7-462f-90a3-b74b713298b6.sql.
  NULL;
END $$;
