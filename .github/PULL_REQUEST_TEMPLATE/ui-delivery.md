## UI delivery summary

User job, audience, primary action, and visual direction:

## Mandatory routing

- Flow-by-Flow packet:
- Paige UI Design skill read before design/implementation:
- Material flow change: yes/no and reason:
- Flow Prototype artifact/approval, or justified not-required reason:

## Scope and collisions

- Affected flows:
- Regression flows:
- Active-owner/file collision result:
- Explicit exclusions:

## Evidence record

Link the non-template record added under `docs/evidence/ui-delivery/`:

- Automated:
- Static:
- Rendered:
- Behavioral:
- Authenticated runtime:
- `UNVERIFIED`:

For Solo UI, link all required viewports with PAIGE closed/open and identify the affected plus known-good tenant contexts.

## States and accessibility

Describe applicable first-use, loading, empty, validation, success, refusal, error, retry, cancellation, close/Back, permission, destructive, and workspace-switch behavior. Record keyboard/focus, zoom/reflow, reduced motion, labels/errors/status, actual scroll owner, clipping, and reachability.

## Truth check

List every `LIVE`, `PARTIAL`, `UNAVAILABLE`, and `UNVERIFIED` claim and the contract/evidence supporting it. Do not treat this template or its checkboxes as proof.

## Release

<!-- RELEASE_GOVERNANCE_POLICY — read docs/doctrine/release-governance-and-customer-update-policy.md -->

- Internal build identity: exact head SHA; deployment ID/environment when known; migrations; edge status; evidence:
- Release channel and classification:
- Customer release identity: none, or proposed version + name + owner-decision reference
- Release-note requirement, audience/tier, benefits, limitations, owner action, rollback/recovery:
- Exact `LIVE` / `PARTIAL` / `UNAVAILABLE` / `PROOF OWED` claims:
- Second Brain + Master Reference updated, or exact collision-safe handoff:
